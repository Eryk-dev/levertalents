---
phase: 4
slug: dashboards-quality-polish
status: draft
shadcn_initialized: true
preset: default (slate base, CSS variables, tsx)
created: 2026-04-28
---

# Phase 4 — UI Design Contract
## Dashboards + Quality Polish

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn/ui (default style) |
| Preset | default — baseColor: slate, cssVariables: true |
| Component library | Radix UI (via shadcn) |
| Icon library | Lucide React (strokeWidth 1.75 everywhere) |
| Font | Inter (sans) — JetBrains Mono (mono for tabular data only) |

**Source:** `components.json` + `tailwind.config.ts` + `src/index.css`

> Note: This phase ALIGNS with the existing Linear-inspired design system established in Phases 1-3. No new design patterns are introduced. The system is light-only (dark mode tokens defined but disabled as of 2026-04).

---

## Spacing Scale

Declared values (multiples of 4 only):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, `gap-1`, inline badge padding |
| sm | 8px | Compact element spacing, `gap-2`, `p-2` |
| md | 16px | Default element spacing, `p-4`, `gap-4` |
| lg | 24px | Section padding, `gap-6`, card inner padding |
| xl | 32px | Layout gaps between sections, `mt-8` |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level top padding (desktop) |

**Phase-specific exceptions:**
- KPI tile inner padding: 14px (`p-3.5`) — aligns with existing `KpiTile` component in `SocioDashboard.tsx`
- CmdK palette list items: `py-2 px-2.5` (8px / 10px) — aligns with existing `CmdKPalette.tsx` pattern
- Touch targets minimum: 44px height — applies to all interactive CmdK items

**Source:** Extracted from `SocioDashboard.tsx` (KpiTile) and `CmdKPalette.tsx` (CommandItem). Do not change these values — they match the established system.

---

## Typography

| Role | Size | Weight | Line Height | Letter Spacing | Notes |
|------|------|--------|-------------|----------------|-------|
| Body / default | 13px | 400 | 1.5 | -0.005em | Set on `body` in index.css — all prose |
| Label / UI | 13px | 500 | 1.4 | -0.01em | CommandItem labels, table cells |
| Eyebrow | 10.5px | 600 | 1.0 | +0.06em | KPI tile label, section eyebrow (UPPERCASE) |
| Subtitles / hints | 11–11.5px | 400 | 1.45 | 0 | KPI detail line, CmdK subtitles |
| KPI number | 26px | 600 | 1.05 | -0.02em | KpiTile value — `text-kpi` token, tabular-nums |
| Page heading | 20px | 600 | 1.2 | -0.02em | Dashboard h1 (greeting) — `text-display-md` |
| Section header | 17px | 600 | 1.25 | -0.015em | "Custo por departamento" — `text-display-sm` |

**Tabular numerics:** All financial values (folha, custo médio) MUST use `font-variant-numeric: tabular-nums lining-nums` via the `.tabular` utility class. Never use proportional numbers for BRL values.

**Font family:** `font-sans` = Inter — applies everywhere including dashboard and CmdK palette. No `font-display` custom fonts as logo stand-ins.

**Source:** `tailwind.config.ts` (fontSize tokens) + `src/index.css` (body base) + `SocioDashboard.tsx` (KpiTile implementation).

---

## Color

| Role | CSS Token | Resolved Value | Usage |
|------|-----------|----------------|-------|
| Dominant (60%) | `--bg` | `hsl(220 14% 99%)` — #FCFCFD | Page background |
| Secondary (30%) | `--surface` + `--bg-subtle` | `hsl(0 0% 100%)` / `hsl(220 12% 97%)` | Cards, sidebar, KPI tiles, hover states |
| Accent (10%) | `--accent` | `hsl(232 57% 60%)` — #5E6AD2 (Linear indigo) | Reserved for: primary CTA buttons, active nav item, focus rings, loading spinners, ProgressBar fill |
| Destructive | `--status-red` | `hsl(352 63% 51%)` — #D1344F | Destructive actions only (none in this phase) |

**Status colors (semantic, not accent):**
- `--status-green` (#10894B) — KPI delta "good" state, SLA ok indicator
- `--status-amber` (#B56B0B) — KPI delta "caution" state
- `--status-red` (#D1344F) — KPI delta "bad" state, SLA critical

**Accent reserved for (exhaustive list):**
- "Relatório" download button (primary action in dashboard header)
- "Criar nova vaga" quick action in CmdK palette
- Focus ring on CmdK input
- Loading spinner in CmdK remote search

**Accent NOT used for:**
- Breakdown table rows (use `--bg-subtle` for hover)
- KPI tile backgrounds (use `--surface`)
- Navigation shortcuts in CmdK (use icon in `--text-muted`)
- Session replay toggle (use shadcn `Switch` default styles)

**Source:** `src/index.css` (CSS variables) + established pattern from Phase 1-3 (single-accent Linear system).

---

## Surface — 3 UI Surfaces

### Surface 1: SocioDashboard Refactor

**Layout:** Single-column scroll, max-width 1400px, `p-5 lg:p-7`, `animate-fade-in` on mount.

**Header section:**
- Eyebrow: "Visão executiva" — 10.5px / uppercase / `text-text-subtle`
- Greeting h1: 20px / semibold / `text-text` — "{Bom dia|Boa tarde|Boa noite}, {firstName}"
- Subtitle line: 13px / `text-text-muted` — dynamic summary of scope (e.g. "Folha de R$ 45.000 · 23 pessoas · 6 departamentos")
- CSV export button: secondary `Btn` size sm, Lucide `Download` icon, label "Relatório" — right-aligned

**KPI tiles (row):**
- Grid: `grid-cols-2 lg:grid-cols-3` — exactly 3 tiles (folha total, custo médio, headcount ativo). Remove performance and clima tiles completely.
- Tile anatomy: eyebrow label (10.5px uppercase) → 26px tabular KPI value → 11.5px detail hint
- Empty value: render `"—"` in `text-text-subtle` (not zero, not blank)
- Tile background: `bg-surface border border-border rounded-md p-3.5` — use existing `KpiTile` / `StatCard` primitive

**Breakdown table (conditional):**

| Scope type | Section title | Rows | Columns |
|------------|--------------|------|---------|
| `company` | "Custo por departamento" | Top 6 departments | Dept name + headcount + custo total + custo médio |
| `group` | "Custo por empresa" | All member companies (no top-N cap) | Empresa name + headcount + custo total + custo médio |

- Progress bar: `ProgressBar` primitive with `hsl(var(--accent))` fill, relative to max row value
- Row layout: `px-3.5 py-2.5`, border-b between rows, no border on last row
- Department/company name: 13px / medium / `text-text`, truncated
- Sub-hint: 11px / `text-text-subtle` (department: dept code or company_name if cross-listed; company: group membership indicator)
- Value: 13px / semibold / tabular / `shrink-0`

**Sections to REMOVE:**
- `useClimateOverview` — entire clima KPI tile and hook call
- "Indicadores consolidados" card (PDIs ativos, avaliações concluídas, 1:1s, participação clima, pessoas em risco)
- "Próxima ação" hero banner (org alerts based on performance scores)
- "Atalhos" shortcut grid at bottom

**Sections to KEEP:**
- Page header with greeting + subtitle + CSV button
- 3 KPI tiles (financial only)
- Breakdown table (conditional by scope type)

**Loading state:** `LoadingState variant="spinner"` inside breakdown card — existing pattern.

**Empty state (no collaborators):**
- Icon: Lucide `Users` — 18px, `text-text-muted`
- Heading: "Nenhum dado de folha"
- Body: "Adicione colaboradores com salário cadastrado para ver os indicadores."

**Error state:** `sonner` toast via `handleSupabaseError` — existing pattern. No inline error banner.

---

### Surface 2: CmdKPalette Refactor

**Trigger:** `Cmd+K` (macOS) / `Ctrl+K` (Windows/Linux) — keyboard shortcut registered globally. Also via `window.dispatchEvent(new Event('open-cmdk'))` for programmatic open.

**Dialog dimensions:** `max-w-[560px]`, positioned at `top-[18%]`, shadow-popup. Existing values preserved.

**Input row:** `px-3.5 py-3`, border-b. Input: 14px / `placeholder:text-text-subtle`. Loader or `Esc` kbd badge on right.

**Default state (empty query):**
Shows two static groups:
1. **"Ações"** — quick action items (see copy contract below)
2. **"Ir para"** — page navigation shortcuts

Each item: icon (3.5 × 3.5 / `text-text-muted`) + label (13px) + keyboard shortcut `Kbd` badge or `ArrowRight` icon (3 × 3 / `text-text-subtle`).

**Search state (query ≥ 2 chars):**
Shows remote result groups ABOVE static groups. Remote groups appear only when they have results. Groups: Vagas → Candidatos → Pessoas (in that order). PDI group removed from remote results (D-06 scope: vagas, candidatos, pessoas do escopo atual).

**Remote result item anatomy:**
- Icon (3.5 × 3.5 / `text-text-muted`) + title (13px truncated) + subtitle (11.5px / `text-text-subtle` truncated) + `ArrowRight` (3px)

**Minimum query:** 2 chars. Empty hint: "Digite ao menos 2 caracteres." in `py-5 text-center text-[13px] text-text-subtle`.

**No-results state:** "Nada encontrado." — same style as empty hint.

**Performance SLA:** Remote search debounce 150ms (reduce from current 180ms). Fetch via scoped RPC only. No `supabase.from()` direct call — must use `useScopedQuery` after refactor.

**Groups to ADD (static actions — Phase 4 D-07):**
- "Criar nova vaga" — icon `Briefcase` — group "Ações" — navigates to `/vagas/nova` or opens JobOpeningForm (planner decides based on existing route)
- "Convidar / criar pessoa" — icon `UserPlus` — group "Ações" — navigates to `/criar-usuario`

**Groups to REMOVE from static list:**
- "Criar novo PDI" (Phase 4 scope: only vagas + pessoas)
- "Agendar 1:1" (out of scope for CmdK D-07 — belongs in 1:1 page)
- "Iniciar avaliação" (out of scope for CmdK D-07)
- PDI remote results group ("pdi" kind removed from `REMOTE_META`)

**Keyboard footer bar:** Keep existing `↵ selecionar · ↑↓ navegar · ⌘K abrir / fechar` pattern. 11px / `text-text-subtle`.

**Scope enforcement (interaction contract):**
- queryKey for remote search: `["global-search", scope.id, debouncedQuery]` — scope.id MUST be in key
- RPC call must pass `scope_company_id` or `scope_group_id` parameter so results never leak cross-tenant
- If user is `socio`, RPC must filter by `socio_company_memberships` — candidatos from companies outside membership MUST NOT appear

**Selected item state:** `data-[selected=true]:bg-bg-subtle data-[selected=true]:text-text` — existing pattern preserved.

---

### Surface 3: Session Replay Toggle (QUAL-06)

**Location:** Claude's Discretion — planner decides placement (likely Admin settings page or env flag). This is a minor UI surface; no new page needed.

**Component:** shadcn `Switch` primitive (already in project).

**Label:** "Replay de sessão (Sentry)" — 13px / `text-text`
**Description:** "Quando ativo, sessões de uso são gravadas para debugging. Todo texto e dados são mascarados automaticamente." — 11.5px / `text-text-muted`

**Default state:** OFF (unchecked). This is the locked default per QUAL-06 — never pre-toggled.

**Warning note (visible when toggle is ON):**
- Inline banner below the switch: `bg-status-amber-soft border border-status-amber/30 rounded-md p-2.5 mt-2`
- Text: "Replay ativo — todo conteúdo da tela é mascarado. Desative quando não precisar mais." — 11.5px / `text-status-amber`

**Interaction:** Toggle state persists in app config or env variable — planner decides storage mechanism. No confirmation dialog needed (not a destructive action).

---

## Copywriting Contract

### Dashboard (SocioDashboard)

| Element | Copy |
|---------|------|
| Page eyebrow | "Visão executiva" |
| Primary CTA (CSV export) | "Relatório" |
| KPI: folha total label | "Folha" |
| KPI: folha total detail | "mês corrente" |
| KPI: headcount label | "Pessoas ativas" |
| KPI: headcount detail (group scope) | "{N} empresas" |
| KPI: headcount detail (company scope) | "{N} departamentos" |
| KPI: custo médio label | "Custo médio" |
| KPI: custo médio detail | "por pessoa / mês" |
| Breakdown section title (company scope) | "Custo por departamento" |
| Breakdown section title (group scope) | "Custo por empresa" |
| Empty state heading | "Nenhum dado de folha" |
| Empty state body | "Adicione colaboradores com salário cadastrado para ver os indicadores." |
| Error (RLS block) | Toast: "Acesso negado. Você não tem permissão para ver os dados desta empresa." |
| Error (network) | Toast: "Falha ao carregar custos. Verifique sua conexão e tente novamente." |
| CSV filename | `custo-por-departamento-{YYYY-MM-DD}.csv` (company) / `custo-por-empresa-{YYYY-MM-DD}.csv` (group) |

### CmdK Palette

| Element | Copy |
|---------|------|
| Input placeholder | "Buscar vagas, candidatos, pessoas…" (remove PDIs from placeholder) |
| Group heading: quick actions | "Ações" |
| Group heading: navigation | "Ir para" |
| Group heading: vagas results | "Vagas" |
| Group heading: candidates results | "Candidatos" |
| Group heading: people results | "Pessoas" |
| Action: create job | "Criar nova vaga" |
| Action: invite person | "Convidar / criar pessoa" |
| Nav: dashboard | "Início" (shortcut G H) |
| Nav: vagas | "Vagas" (shortcut G V) |
| Nav: candidatos | "Candidatos" |
| Nav: 1:1s | "1:1s" (shortcut G 1) |
| Nav: avaliações | "Avaliações" (shortcut G A) |
| Nav: clima | "Clima" (shortcut G C) |
| Nav: pessoas | "Meu time" (shortcut G T) |
| Nav: empresas | "Empresas" |
| Empty: query too short | "Digite ao menos 2 caracteres." |
| Empty: no results | "Nada encontrado." |
| Footer hint | "↵ selecionar · ↑↓ navegar · ⌘K abrir / fechar" |

### Session Replay Toggle

| Element | Copy |
|---------|------|
| Toggle label | "Replay de sessão (Sentry)" |
| Toggle description | "Quando ativo, sessões de uso são gravadas para debugging. Todo texto e dados são mascarados automaticamente." |
| Warning (when ON) | "Replay ativo — todo conteúdo da tela é mascarado. Desative quando não precisar mais." |

---

## Interaction States

### KPI Tiles
| State | Visual |
|-------|--------|
| Loading | `LoadingState variant="spinner"` in card area |
| Loaded (value present) | 26px tabular number in `text-text` |
| Loaded (no data) | `"—"` in `text-text-subtle` |
| Error | sonner toast, tile renders `"—"` |

### CmdK Palette
| State | Visual |
|-------|--------|
| Closed | Hidden |
| Open, empty query | Static groups (Ações + Ir para) |
| Open, query < 2 chars | Static groups + empty hint below input |
| Open, query ≥ 2 chars, loading | Spinner in header row (`animate-spin`) |
| Open, query ≥ 2 chars, results | Remote groups first, then static groups filtered by query |
| Open, query ≥ 2 chars, no results | "Nada encontrado." in CommandEmpty |
| Item selected (keyboard) | `bg-bg-subtle text-text` |
| Item activated (Enter / click) | Dialog closes, navigation executes |

### Breakdown Table Rows
| State | Visual |
|-------|--------|
| Default | White background |
| Hover | `hover:bg-bg-subtle` (transition-colors) |
| Loading | Spinner inside card |
| Empty | `LinearEmpty` component with icon + heading + description |

---

## Animations

Existing animation system from `tailwind.config.ts` — no new keyframes needed:

| Element | Animation |
|---------|-----------|
| Dashboard mount | `animate-fade-in` (0.2s cubic-bezier(0.16,1,0.3,1)) |
| CmdK open | Dialog uses Radix `DialogContent` with built-in Radix animation |
| CmdK items | No animation on individual items — instant render |
| Session replay toggle | shadcn Switch built-in 120ms transition |
| Hover transitions | `transition-colors` (0.12s) — existing `--transition-base` |

---

## Component Inventory

These components are used or reused in Phase 4. Executor MUST use these — do not invent new primitives.

| Component | Path | Phase 4 Usage |
|-----------|------|---------------|
| `KpiTile` (local) | `src/pages/SocioDashboard.tsx` | 3 KPI tiles (financial only) |
| `StatCard` | `src/components/primitives/StatCard.tsx` | Alternative KPI display if KpiTile is refactored |
| `ProgressBar` | `src/components/primitives/LinearKit.tsx` | Breakdown table bar fill |
| `LinearEmpty` | `src/components/primitives/LinearKit.tsx` | Empty states in dashboard |
| `LoadingState` | `src/components/primitives/LoadingState.tsx` | Loading states in breakdown card |
| `SectionHeader` | `src/components/primitives/LinearKit.tsx` | Breakdown section titles |
| `Card` | `src/components/primitives/LinearKit.tsx` | Breakdown table wrapper |
| `Btn` | `src/components/primitives/LinearKit.tsx` | CSV export button (variant secondary size sm) |
| `Row` | `src/components/primitives/LinearKit.tsx` | Header layout, action rows |
| `Kbd` | `src/components/primitives/LinearKit.tsx` | CmdK keyboard shortcut badges |
| `Command` / `CommandInput` / `CommandList` / `CommandGroup` / `CommandItem` / `CommandEmpty` | `src/components/ui/command.tsx` | CmdK palette shell |
| `Dialog` / `DialogContent` / `DialogTitle` | `src/components/ui/dialog.tsx` | CmdK wrapper |
| `Switch` | `src/components/ui/switch.tsx` | Session replay toggle |
| `sonner` toast | Via `toast` from sonner | Error toasts in dashboard |

**No third-party registry blocks used in this phase.** All components are from shadcn official or existing project primitives.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | `command`, `dialog`, `switch`, `badge` | not required |
| Project primitives | `LinearKit`, `StatCard`, `LoadingState` | internal — no gate required |
| Third-party registries | none | not applicable |

---

## Pre-Populated Decision Sources

| Source | Decisions Used |
|--------|---------------|
| CONTEXT.md D-01..D-09 | All 9 locked decisions — KPI set, breakdown conditional logic, CSV export, group scope aggregation, CmdK actions, default state, scope enforcement |
| REQUIREMENTS.md | DASH-01..04, QUAL-06 requirements |
| `components.json` | Design system confirmed: shadcn default + slate base |
| `tailwind.config.ts` | Typography scale, color tokens, animation keyframes |
| `src/index.css` | CSS variable values (exact hex colors resolved) |
| `SocioDashboard.tsx` | Existing KpiTile pattern, layout structure, CSV logic |
| `CmdKPalette.tsx` | Existing dialog structure, groups, keyboard behavior |
| `StatCard.tsx` primitive | KPI tile API contract |
| PROJECT.md lock | "Performance e R&S ficam em telas dedicadas" — enforces removal of clima/org sections |
| User input (this session) | 0 questions asked — all answered by upstream artifacts |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending

---

*Phase: 04-dashboards-quality-polish*
*UI-SPEC created: 2026-04-28*
*Created by: gsd-ui-researcher*

---
phase: 02-r-s-refactor
plan: 07
type: execute
wave: 4
depends_on: ["02-05", "02-06"]
files_modified:
  - src/components/hiring/CandidatesKanban.tsx
  - src/components/hiring/CandidateCard.tsx
  - src/components/hiring/SlaBadge.tsx
  - src/components/hiring/SparkbarDistribution.tsx
  - src/components/hiring/JobCard.tsx
  - src/components/hiring/LegacyStageWarning.tsx
  - src/hooks/hiring/useLegacyStageCount.ts
  - tests/hiring/CandidatesKanban.integration.test.tsx
  - tests/hiring/CandidateCard.test.tsx
autonomous: true
requirements:
  - RS-03
  - RS-04
  - RS-08
  - RS-12
must_haves:
  truths:
    - "CandidatesKanban onDragEnd chama canTransition() ANTES de move.mutate (D-02 — corrige bug #1)"
    - "CandidatesKanban remove expectedUpdatedAt e performMove async wrapper (D-03 last-writer-wins)"
    - "CandidatesKanban subscreve useApplicationsRealtime(jobId) — silent re-render"
    - "CandidatesKanban usa sensors PointerSensor + TouchSensor + KeyboardSensor"
    - "CandidateCard mostra mínimo D-07 (nome + cargo + dias na etapa + vaga) sempre"
    - "CandidateCard aplica border-l-status-amber para SLA warning (>=2d) e border-l-status-red para critical (>=5d)"
    - "CandidateCard usa useCardPreferences para campos opcionais D-08"
    - "SparkbarDistribution renderiza 4 segmentos por intencionalidade D-11 (azul/amarelo/verde/vermelho)"
    - "JobCard usa SparkbarDistribution component extraído"
    - "LegacyStageWarning mostra banner durante cutover Migration F"
    - "useLegacyStageCount hook centraliza supabase.from() para metadata.legacy_marker query (CLAUDE.md compliance)"
  artifacts:
    - path: "src/components/hiring/CandidatesKanban.tsx"
      provides: "Kanban estabilizado: canTransition + optimistic + Realtime + sensors atualizados"
      contains: "canTransition"
    - path: "src/components/hiring/CandidateCard.tsx"
      provides: "Card com mínimo D-07 + customizável D-08 + SLA stripe D-10"
      contains: "computeSlaTone"
    - path: "src/components/hiring/SlaBadge.tsx"
      provides: "Badge dias na etapa com tom semântico"
      contains: "computeSlaTone"
    - path: "src/components/hiring/SparkbarDistribution.tsx"
      provides: "Sparkbar SVG inline 4 cores intencionalidade D-11"
      contains: "STAGE_GROUP_BAR_COLORS"
    - path: "src/components/hiring/JobCard.tsx"
      provides: "Job card com SparkbarDistribution extraído"
      contains: "SparkbarDistribution"
    - path: "src/components/hiring/LegacyStageWarning.tsx"
      provides: "Banner sessão alertando RH sobre stages legados durante cutover (consome useLegacyStageCount)"
      contains: "useLegacyStageCount"
    - path: "src/hooks/hiring/useLegacyStageCount.ts"
      provides: "Hook que conta applications com metadata.legacy_marker (CLAUDE.md compliance: supabase.from() vive em hooks)"
      contains: "supabase.from"
  key_links:
    - from: "CandidatesKanban onDragEnd"
      to: "canTransition (statusMachine)"
      via: "if (!canTransition(app.stage, toStage, 'application')) { toast.error(...); return; }"
      pattern: "canTransition.*application"
    - from: "CandidateCard"
      to: "useCardPreferences hook"
      via: "const [prefs] = useCardPreferences()"
      pattern: "useCardPreferences"
    - from: "JobCard"
      to: "SparkbarDistribution component"
      via: "import { SparkbarDistribution } from"
      pattern: "SparkbarDistribution"
    - from: "LegacyStageWarning component"
      to: "useLegacyStageCount hook"
      via: "import + invocação; sem supabase.from() inline"
      pattern: "useLegacyStageCount"
---

<objective>
Wave 4 — Surface principal do kanban (D-01..D-11 aterrissam aqui). 6 componentes + 1 hook (CLAUDE.md: supabase.from() vive em hooks):

1. **CandidatesKanban.tsx** (MODIFY) — REWRITE de `onDragEnd` para chamar `canTransition` ANTES de mutate (D-02 corrige bug #1). Remove `expectedUpdatedAt` + `performMove` wrapper. Adiciona `useApplicationsRealtime(jobId)`. Sensors: PointerSensor + TouchSensor + KeyboardSensor. Aplica `bg-accent-soft + border-accent dashed` em coluna durante drag-over.

2. **CandidateCard.tsx** (MODIFY) — Garante mínimo D-07 (nome + cargo + dias-na-etapa + vaga sempre visíveis). Adiciona campos opcionais via `useCardPreferences` (D-08). SLA stripe via `border-l-3 border-l-status-{tone}` (D-10).

3. **SlaBadge.tsx** (NEW) — `{N} dias na etapa` com tom semântico verde/laranja/vermelho.

4. **SparkbarDistribution.tsx** (NEW) — extrai sparkbar de JobCard.tsx para componente reusável; SVG/HTML inline; usa `STAGE_GROUP_BAR_COLORS` (Plan 03 já corrigiu para D-11).

5. **JobCard.tsx** (MODIFY) — substitui sparkbar inline por `<SparkbarDistribution byGroup={counts.byGroup} total={counts.total} />`.

6. **LegacyStageWarning.tsx** (NEW) — banner top-of-board mostrando contagem de candidatos com `metadata.legacy_marker` (auto-dismiss session). Consome `useLegacyStageCount` (CLAUDE.md compliance).

7. **useLegacyStageCount.ts** (NEW HOOK) — hook que conta applications com legacy_marker via `useScopedQuery`. Usado por LegacyStageWarning.

Ativa testes `tests/hiring/CandidatesKanban.integration.test.tsx` e `tests/hiring/CandidateCard.test.tsx`.

Output: 6 componentes + 1 hook + 2 testes ativos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-r-s-refactor/02-CONTEXT.md
@.planning/phases/02-r-s-refactor/02-RESEARCH.md
@.planning/phases/02-r-s-refactor/02-PATTERNS.md
@.planning/phases/02-r-s-refactor/02-UI-SPEC.md
@.planning/phases/02-r-s-refactor/02-05-SUMMARY.md
@.planning/phases/02-r-s-refactor/02-06-SUMMARY.md
@CLAUDE.md
@src/components/hiring/CandidatesKanban.tsx
@src/components/hiring/CandidateCard.tsx
@src/components/hiring/JobCard.tsx

<interfaces>
From src/lib/hiring/sla.ts (Plan 03):
- computeSlaTone, daysSince, SLA_BORDER_CLASSES, SLA_DOT_CLASSES, SlaTone

From src/lib/hiring/stageGroups.ts (Plan 03 corrigido):
- STAGE_GROUPS (6 grupos), STAGE_GROUP_BY_STAGE, STAGE_GROUP_BAR_COLORS (D-11)

From src/lib/hiring/statusMachine.ts:
- canTransition(from, to, kind), APPLICATION_STAGE_TRANSITIONS, APPLICATION_STAGE_LABELS

From src/hooks/hiring/useApplications.ts (Plan 05):
- useMoveApplicationStage (TanStack v5 onMutate canonical)
- useApplicationsByJob (useScopedQuery)

From src/hooks/hiring/useApplicationsRealtime.ts (Plan 05):
- useApplicationsRealtime(jobId): void

From src/hooks/hiring/useCardPreferences.ts (Plan 06):
- [prefs, update] = useCardPreferences()

From src/hooks/hiring/useApplicationCountsByJob.ts (Plan 05):
- useApplicationCountsByJobs(jobIds): { data: Record<jobId, JobApplicationCounts> }

From CandidatesKanban.tsx atual (linhas 207-285) — bug location:
- Linha ~252: `move.mutate(...)` é chamado SEM canTransition pre-check (D-02)
- Linha ~190: `expectedUpdatedAt` ainda está sendo passado (D-03 quer last-writer-wins)
- Linha ~140: `[conflict, setConflict]` state tied to OptimisticMutationToast (D-01 substitui por sonner)

From src/components/ui/sonner.tsx:
- toast import — UI-SPEC §"Toast posicionamento" — top-right 4s default 8s destructive
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite CandidatesKanban.tsx — canTransition pre-check + Realtime + sensors</name>
  <files>src/components/hiring/CandidatesKanban.tsx, tests/hiring/CandidatesKanban.integration.test.tsx</files>
  <read_first>
    src/components/hiring/CandidatesKanban.tsx (estado atual — preservar imports e shape geral),
    src/lib/hiring/statusMachine.ts (canTransition signature),
    src/hooks/hiring/useApplications.ts (Plan 05 — useMoveApplicationStage assina MoveApplicationStageArgs),
    src/hooks/hiring/useApplicationsRealtime.ts (Plan 05),
    .planning/phases/02-r-s-refactor/02-RESEARCH.md secao §1 lines 312-342 e §3 lines 511-521,
    .planning/phases/02-r-s-refactor/02-PATTERNS.md "CandidatesKanban.tsx (MODIFY)" lines 464-516,
    .planning/phases/02-r-s-refactor/02-UI-SPEC.md secao "Kanban board layout"
  </read_first>
  <behavior>
    - onDragEnd: identifica activeId/overId; encontra app + targetGroup; calcula toStage = targetGroup.defaultStage
    - ANTES de mutate: if !canTransition(app.stage, toStage, "application") -> toast.error + return
    - Mutate args: { id, fromStage: app.stage, toStage, jobId, companyId: scope.companyIds[0] }
    - REMOVE: expectedUpdatedAt, performMove async wrapper, conflict state, OptimisticMutationToast import
    - ADD: useApplicationsRealtime(jobId)
    - Sensors: PointerSensor + TouchSensor (delay 200, tolerance 5) + KeyboardSensor
    - DragOver column highlight: bg-accent-soft + border-accent dashed
    - Mantém columns memo, descartadosOpen state, drawer integration
  </behavior>
  <action>
Passo 1 — Ativar tests/hiring/CandidatesKanban.integration.test.tsx:

```typescript
import { describe, it, expect, vi, beforeEach, afterEach, beforeAll, afterAll } from "vitest";
import { render, screen, waitFor, act } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
import { setupServer } from "msw/node";
import { http, HttpResponse } from "msw";
import { MemoryRouter } from "react-router-dom";
import * as scopeModule from "@/app/providers/ScopeProvider";
import { CandidatesKanban } from "@/components/hiring/CandidatesKanban";

const SUPABASE_URL = "https://ehbxpbeijofxtsbezwxd.supabase.co";
const server = setupServer();
beforeAll(() => server.listen({ onUnhandledRequest: "warn" }));
afterEach(() => server.resetHandlers());
afterAll(() => server.close());

beforeEach(() => {
  vi.spyOn(scopeModule, "useScope").mockReturnValue({
    scope: { id: "company:abc", kind: "company", companyIds: ["c1"], userId: "u1" },
  } as ReturnType<typeof scopeModule.useScope>);
});

function renderKanban() {
  const client = new QueryClient({ defaultOptions: { queries: { retry: false }, mutations: { retry: false } } });
  return render(
    <QueryClientProvider client={client}>
      <MemoryRouter>
        <CandidatesKanban jobId="j1" jobName="Test Job" />
      </MemoryRouter>
    </QueryClientProvider>
  );
}

describe("CandidatesKanban — bug #1 fix integration (D-02 + D-04)", () => {
  it("renderiza 6 colunas dos STAGE_GROUPS", async () => {
    server.use(http.get(SUPABASE_URL + "/rest/v1/applications", () => HttpResponse.json([])));
    renderKanban();
    await waitFor(() => {
      // Eyebrow uppercase: TRIAGEM, CHECAGEM, ENTREVISTAS RH, ENTREVISTAS FINAL, DECISAO, DESCARTADOS
      expect(screen.getByText(/TRIAGEM|Triagem/i)).toBeInTheDocument();
    });
  });

  it("transição inválida (admitido -> triagem) exibe toast e não dispara mutate", async () => {
    // Mock useMoveApplicationStage para captar se mutate foi chamado
    const moveSpy = vi.fn();
    vi.doMock("@/hooks/hiring/useApplications", async (importOriginal) => {
      const actual = await importOriginal<typeof import("@/hooks/hiring/useApplications")>();
      return { ...actual, useMoveApplicationStage: () => ({ mutate: moveSpy, isPending: false }) };
    });

    // Mock useApplicationsByJob retornando 1 app no stage 'admitido'
    server.use(
      http.get(SUPABASE_URL + "/rest/v1/applications", () =>
        HttpResponse.json([
          {
            id: "app-1",
            stage: "admitido",
            stage_entered_at: new Date().toISOString(),
            candidate_id: "c1",
            job_opening_id: "j1",
            candidate: { id: "c1", full_name: "João Silva", email: "joao@x.com", anonymized_at: null },
            job_opening: { id: "j1", title: "Vaga X", company_id: "co1" },
            desired_role: "Eng",
            created_at: new Date().toISOString(),
          },
        ])
      )
    );

    const { CandidatesKanban: CandidatesKanbanReimported } = await import("@/components/hiring/CandidatesKanban");
    const client = new QueryClient({ defaultOptions: { queries: { retry: false }, mutations: { retry: false } } });
    render(
      <QueryClientProvider client={client}>
        <MemoryRouter>
          <CandidatesKanbanReimported jobId="j1" jobName="Test Job" />
        </MemoryRouter>
      </QueryClientProvider>
    );

    // Aguarda render do card admitido
    await waitFor(() => expect(screen.getByText(/João Silva/)).toBeInTheDocument());

    // Simula drag-end programaticamente: extrai a função onDragEnd injetada no DndContext.
    // Como dnd-kit não expõe trivially, alternativa: invocar a action diretamente via custom event
    // OU usar abordagem mais pragmática — disparar o callback via accessing component internals.
    //
    // Escolha pragmática: emitir um synthetic DragEndEvent shape que dnd-kit aceita.
    // Se dnd-kit/core/testing não estiver disponível (é o caso em 6.3), a estratégia abaixo é:
    // expor onDragEnd via data-testid no DndContext wrapper e invocar manualmente.
    //
    // Isto requer pequeno helper de test em CandidatesKanban: aceitar prop opcional
    // `__testOnDragEnd` para test injetar callback. NÃO refatorar prod code para test —
    // ao invés, usar abordagem de mocking direto no useMoveApplicationStage acima e
    // assertar via canTransition reject.

    // Asserção minimal: dado que admitido -> triagem é inválido em APPLICATION_STAGE_TRANSITIONS,
    // qualquer chamada simulada com esse par dispara o early return + toast.error.
    // Implementação concreta: teste o helper canTransition isoladamente AQUI (já testado em
    // Plan 03 statusMachine.test.ts) e valide que o componente integra corretamente — para
    // valida o wire, basta verificar que NO render inicial não há erros.

    // Verificação de wire: canTransition import presente no componente bundle.
    // Assertion concreta deste teste integration: o componente renderiza sem crashar com app
    // em stage 'admitido' (que era o crash legado pré-refactor).
    expect(screen.getByText(/João Silva/)).toBeInTheDocument();
    // moveSpy não deve ter sido chamado em renderização inicial (sanity)
    expect(moveSpy).not.toHaveBeenCalled();
  });
});
```

**NOTA pro executor:** o test integration de drag-drop completo (simulação de DragEndEvent emitida pelo dnd-kit/core 6.3) requer setup avançado — `@dnd-kit/core/testing` foi removido em 6.x. Há 3 caminhos viáveis:

**Caminho A (recomendado se cabe em context):** usar `userEvent.pointer([{keys: '[MouseLeft>]', target: cardElement}, {coords: {x: 200, y: 100}}, {keys: '[/MouseLeft]'}])` para simular o drag físico. Verificar que o toast `"Não é possível mover"` aparece (assertable via `screen.findByText(/não é possível mover/i)`) e que `moveSpy` NÃO foi chamado.

**Caminho B (fallback):** se Caminho A der flaky por timing/sensors, expor uma prop test-only `__onDragEndForTest?: (e: DragEndEvent) => void` no `CandidatesKanban` que invoca o handler internamente. NÃO contamina prod (prop é optional + ignored se undefined).

**Caminho C (descope com developer aprovação):** se A e B falharem em <30 min, mover esta cobertura para 02-VALIDATION.md "Manual-Only Verifications" como cenário UAT explícito ("Drag candidato 'admitido' -> 'triagem' no kanban → toast 'Não é possível mover' aparece, card retorna pra coluna admitido") e remover a expectativa do test automated. **Esta task NÃO pode quedar com `expect(true).toBe(true)` placeholder.** Se descopar, atualizar `02-VALIDATION.md` Wave 0 inventory removendo `CandidatesKanban.test.tsx` cenário canTransition-toast e adicionando linha em "Manual-Only Verifications".

Passo 2 — Modificar src/components/hiring/CandidatesKanban.tsx:

REMOVER imports/state:
- `import { OptimisticMutationToast } from ...`
- `const [conflict, setConflict] = useState(false);`
- helper `performMove` async

ADICIONAR imports:
```typescript
import { canTransition } from "@/lib/hiring/statusMachine";
import { APPLICATION_STAGE_LABELS } from "@/integrations/supabase/hiring-types";
import { useApplicationsRealtime } from "@/hooks/hiring/useApplicationsRealtime";
import {
  DndContext, PointerSensor, TouchSensor, KeyboardSensor,
  useSensor, useSensors,
} from "@dnd-kit/core";
import { toast } from "sonner";
```

ADICIONAR no body:
```typescript
useApplicationsRealtime(jobId);

const sensors = useSensors(
  useSensor(PointerSensor, { activationConstraint: { distance: 5 } }),
  useSensor(TouchSensor, { activationConstraint: { delay: 200, tolerance: 5 } }),
  useSensor(KeyboardSensor),
);
```

SUBSTITUIR onDragEnd (verbatim do PATTERNS.md lines 471-494):

```typescript
const onDragEnd = (event: DragEndEvent) => {
  setActiveApp(null);
  const activeId = event.active.id as string;
  const overId = event.over?.id as string | undefined;
  if (!overId || !activeId.startsWith("app:") || !overId.startsWith("appcol:")) return;
  const appId = activeId.slice("app:".length);
  const groupKey = overId.slice("appcol:".length);
  const app = applications.find((a) => a.id === appId);
  if (!app) return;
  const targetGroup = STAGE_GROUPS.find((g) => g.key === groupKey);
  if (!targetGroup || targetGroup.key === STAGE_GROUP_BY_STAGE[app.stage]) return;
  const toStage = targetGroup.defaultStage;

  // D-02: canTransition() ANTES do mutate — corrige bug #1
  if (!canTransition(app.stage, toStage, "application")) {
    toast.error(
      `Não é possível mover de "${APPLICATION_STAGE_LABELS[app.stage]}" direto para "${APPLICATION_STAGE_LABELS[toStage]}"`
    );
    return;
  }

  if (targetGroup.key === DESCARTADOS_KEY) setDescartadosOpen(true);

  if (!scope) return;
  move.mutate({
    id: app.id,
    fromStage: app.stage,
    toStage,
    jobId,
    companyId: scope.companyIds[0] ?? "",
  });
};
```

VERIFICAR que `move = useMoveApplicationStage()` está sendo chamado no body (já existe em algum form; agora consome a versão Plan 05 com onMutate).

REMOVER prop `expectedUpdatedAt` ou variável `expectedUpdatedAt` em qualquer lugar do componente.

Passo 3 — Drag-over visual feedback:

Onde renderizam as colunas (KanbanColumn ou similar), aplicar quando `isOver` (do useDroppable):
```tsx
<div className={cn(
  "flex flex-col gap-1.5 rounded-lg border p-2 transition-colors",
  "bg-bg-subtle border-default",
  isOver && "bg-accent-soft border-accent border-dashed",
)}>
```

Verificar:
```bash
grep -c "canTransition" src/components/hiring/CandidatesKanban.tsx
grep -c "useApplicationsRealtime" src/components/hiring/CandidatesKanban.tsx
grep -c "TouchSensor" src/components/hiring/CandidatesKanban.tsx
grep -c "expectedUpdatedAt" src/components/hiring/CandidatesKanban.tsx
# último deve retornar 0
npm test -- --run tests/hiring/CandidatesKanban.integration.test.tsx 2>&1 | tail -8
```
  </action>
  <verify>
    <automated>grep -c "canTransition" src/components/hiring/CandidatesKanban.tsx && grep -c "useApplicationsRealtime" src/components/hiring/CandidatesKanban.tsx && ! grep -q "expectedUpdatedAt" src/components/hiring/CandidatesKanban.tsx</automated>
  </verify>
  <done>CandidatesKanban: canTransition pre-check em onDragEnd; useApplicationsRealtime mounted; sensors PointerSensor+TouchSensor+KeyboardSensor; expectedUpdatedAt removido; drag-over column visual aplicado; integration test renderiza colunas.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Modify CandidateCard — D-07 mínimo + D-08 customizável + D-10 SLA stripe</name>
  <files>src/components/hiring/CandidateCard.tsx, tests/hiring/CandidateCard.test.tsx</files>
  <read_first>
    src/components/hiring/CandidateCard.tsx (atual — linhas 37-129),
    src/lib/hiring/sla.ts (Plan 03 — computeSlaTone, daysSince),
    src/hooks/hiring/useCardPreferences.ts (Plan 06),
    src/lib/hiring/cardCustomization.ts (Plan 03 — OPTIONAL_FIELDS, CardPreferences),
    .planning/phases/02-r-s-refactor/02-RESEARCH.md secao §13 lines 1875-1890,
    .planning/phases/02-r-s-refactor/02-PATTERNS.md "CandidateCard.tsx (MODIFY)" lines 519-557,
    .planning/phases/02-r-s-refactor/02-UI-SPEC.md secao "Phase 2 specific surfaces" e "Card SLA stripe"
  </read_first>
  <behavior>
    - Mínimo D-07 SEMPRE visível: candidate.full_name + application.desired_role + days-in-stage + job.title (vaga em concorrência)
    - useCardPreferences hook injeta prefs; cada campo opcional renderiza condicional em isFieldEnabled(prefs, "avatar"|"next_interview"|"cv_icon"|"fit_score"|"bg_check_dot"|"source_tag")
    - SLA stripe: cn() inclui border-l-status-amber se warning, border-l-status-red se critical, border-l-transparent se ok
    - Days display: usa daysSince(application.stage_entered_at) com tabular-nums utility
  </behavior>
  <action>
Passo 1 — Ativar tests/hiring/CandidateCard.test.tsx:

```typescript
import { describe, it, expect, vi, beforeEach, afterEach } from "vitest";
import { render, screen } from "@testing-library/react";
import { CandidateCard } from "@/components/hiring/CandidateCard";

vi.mock("@/hooks/hiring/useCardPreferences", () => ({
  useCardPreferences: () => [{ version: 1, enabledFields: [] }, vi.fn()],
}));

vi.mock("@/hooks/useAuth", () => ({ useAuth: () => ({ user: { id: "u1" } }) }));

const baseApp = {
  id: "app-1",
  stage: "em_interesse" as const,
  stage_entered_at: new Date(Date.now() - 86_400_000 * 3).toISOString(), // 3 dias = warning
  desired_role: "Eng Backend",
  candidate: { id: "c1", full_name: "João Silva", email: "joao@x.com", anonymized_at: null },
  job_opening: { title: "Vaga Sr." },
};

describe("CandidateCard — D-07 mínimo + D-08 customizável + D-10 SLA", () => {
  it("renderiza nome + cargo + dias + vaga (mínimo D-07)", () => {
    render(<CandidateCard application={baseApp as never} onOpen={vi.fn()} />);
    expect(screen.getByText(/João Silva/)).toBeInTheDocument();
    expect(screen.getByText(/Eng Backend/)).toBeInTheDocument();
    expect(screen.getByText(/3d|3 dias/i)).toBeInTheDocument();
  });

  it("aplica border-l-status-amber para SLA warning (3d)", () => {
    const { container } = render(<CandidateCard application={baseApp as never} onOpen={vi.fn()} />);
    const card = container.querySelector("button");
    expect(card?.className).toMatch(/border-l-status-amber/);
  });

  it("aplica border-l-status-red para SLA critical (>=5d)", () => {
    const criticalApp = { ...baseApp, stage_entered_at: new Date(Date.now() - 86_400_000 * 6).toISOString() };
    const { container } = render(<CandidateCard application={criticalApp as never} onOpen={vi.fn()} />);
    expect(container.querySelector("button")?.className).toMatch(/border-l-status-red/);
  });

  it("não aplica stripe para SLA ok (1d)", () => {
    const okApp = { ...baseApp, stage_entered_at: new Date(Date.now() - 86_400_000 * 1).toISOString() };
    const { container } = render(<CandidateCard application={okApp as never} onOpen={vi.fn()} />);
    expect(container.querySelector("button")?.className).toMatch(/border-l-transparent/);
  });
});
```

Passo 2 — Modificar src/components/hiring/CandidateCard.tsx:

ADICIONAR imports:
```typescript
import { computeSlaTone, daysSince, SLA_BORDER_CLASSES } from "@/lib/hiring/sla";
import { useCardPreferences } from "@/hooks/hiring/useCardPreferences";
import { isFieldEnabled } from "@/lib/hiring/cardCustomization";
```

ADICIONAR no body do componente:
```typescript
const [prefs] = useCardPreferences();
const days = daysSince(application.stage_entered_at);
const slaTone = computeSlaTone(application.stage_entered_at);
```

MODIFICAR o className do card root para incluir SLA stripe:
```typescript
<button
  ...
  className={cn(
    "group flex w-full rounded-[4px] border-l-[3px] px-2 py-1.5 text-left transition-colors",
    "bg-surface border-default hover:bg-bg-subtle",
    SLA_BORDER_CLASSES[slaTone],  // border-l-transparent | border-l-status-amber | border-l-status-red
    selected && "bg-accent-soft border-accent",
    asOverlay && "shadow-ds-md",
    // ... existing classes
  )}
>
```

GARANTIR que mínimo D-07 está visível (atualizar o JSX se faltar):
```tsx
<div className="flex-1 min-w-0">
  {/* Linha 1: nome */}
  <div className="font-medium text-[13px] text-text truncate">
    {application.candidate?.full_name ?? "—"}
  </div>
  {/* Linha 2: cargo · vaga (middle dot U+00B7) */}
  <div className="text-[11px] text-text-muted truncate">
    {application.desired_role ?? "—"}
    {showJob && application.job_opening?.title && (
      <> · {application.job_opening.title}</>
    )}
  </div>
  {/* Linha 3: dias na etapa (D-07 sempre visível) */}
  <div className="text-[10.5px] text-text-muted tabular-nums">
    {days}d na etapa
  </div>
</div>
```

ADICIONAR campos opcionais conditional renders:
```tsx
{isFieldEnabled(prefs, "avatar") && application.candidate && (
  <LinearAvatar src={null} initials={initials(application.candidate.full_name)} size="sm" />
)}
{isFieldEnabled(prefs, "next_interview") && application.next_interview_at && (
  <span className="text-[10.5px] text-text-muted tabular-nums">
    {formatDateBR(application.next_interview_at)}
  </span>
)}
{isFieldEnabled(prefs, "cv_icon") && application.candidate?.cv_url && (
  <FileText className="w-3 h-3 text-text-muted" aria-label="CV anexado" />
)}
// ... (fit_score, bg_check_dot, source_tag — opcional, render simples)
```

NÃO REMOVER props existentes (asOverlay, selected, showJob); preservar onDraggable e onClick (link-to-drawer).

Passo 3 — Verificar:
```bash
npm test -- --run tests/hiring/CandidateCard.test.tsx 2>&1 | tail -8
grep -c "computeSlaTone\|useCardPreferences" src/components/hiring/CandidateCard.tsx
```
  </action>
  <verify>
    <automated>npm test -- --run tests/hiring/CandidateCard.test.tsx 2>&1 | tail -8 && grep -c "computeSlaTone\|useCardPreferences\|SLA_BORDER_CLASSES" src/components/hiring/CandidateCard.tsx</automated>
  </verify>
  <done>CandidateCard mostra D-07 mínimo (nome+cargo+dias+vaga); useCardPreferences gerencia campos opcionais D-08; SLA stripe aplica border-l-status-{tone} D-10; tests verde.</done>
</task>

<task type="auto">
  <name>Task 3: Create SlaBadge + SparkbarDistribution + LegacyStageWarning</name>
  <files>src/components/hiring/SlaBadge.tsx, src/components/hiring/SparkbarDistribution.tsx, src/components/hiring/LegacyStageWarning.tsx</files>
  <read_first>
    src/components/hiring/JobCard.tsx (linhas 114-130 — sparkbar inline para extrair),
    src/components/hiring/BottleneckAlert.tsx (analog SLA),
    src/lib/hiring/sla.ts (computeSlaTone, daysSince, SLA_DOT_CLASSES),
    src/lib/hiring/stageGroups.ts (STAGE_GROUPS, STAGE_GROUP_BAR_COLORS, StageGroupKey),
    .planning/phases/02-r-s-refactor/02-RESEARCH.md secao §12 lines 1791-1820 e §13 lines 1875-1890,
    .planning/phases/02-r-s-refactor/02-UI-SPEC.md secao "SparkbarDistribution" e "SlaBadge"
  </read_first>
  <action>
src/components/hiring/SlaBadge.tsx:

```typescript
import { cn } from "@/lib/utils";
import { computeSlaTone, daysSince, SLA_DOT_CLASSES, type SlaTone } from "@/lib/hiring/sla";

interface SlaBadgeProps {
  stageEnteredAt: string | Date | null | undefined;
  className?: string;
}

const TONE_CLASS: Record<SlaTone, string> = {
  ok: "text-text-muted",
  warning: "text-status-amber",
  critical: "text-status-red",
};

export function SlaBadge({ stageEnteredAt, className }: SlaBadgeProps) {
  if (!stageEnteredAt) return null;
  const days = daysSince(stageEnteredAt);
  const tone = computeSlaTone(stageEnteredAt);

  return (
    <span
      className={cn(
        "inline-flex items-center gap-1 text-[11px] tabular-nums",
        TONE_CLASS[tone],
        className,
      )}
      aria-label={`Parado há ${days} dias`}
      title={`Parado há ${days} dias. Considere mover ou descartar.`}
    >
      <span className={cn("w-1.5 h-1.5 rounded-full", SLA_DOT_CLASSES[tone])} aria-hidden />
      {days}d na etapa
    </span>
  );
}
```

src/components/hiring/SparkbarDistribution.tsx:

```typescript
import { cn } from "@/lib/utils";
import { STAGE_GROUPS, STAGE_GROUP_BAR_COLORS, type StageGroupKey } from "@/lib/hiring/stageGroups";

interface SparkbarDistributionProps {
  byGroup: Record<StageGroupKey, number>;
  total: number;
  className?: string;
}

export function SparkbarDistribution({ byGroup, total, className }: SparkbarDistributionProps) {
  // Excluir descartados do bar (mostrar só ativos); descartados aparece em chip separado
  const visibleGroups = STAGE_GROUPS.filter((g) => g.key !== "descartados");
  const totalActive = visibleGroups.reduce((acc, g) => acc + (byGroup[g.key] ?? 0), 0);

  if (total === 0 || totalActive === 0) {
    return (
      <div
        className={cn("h-1 w-full rounded-full bg-bg-muted", className)}
        role="img"
        aria-label="Sem candidatos ativos"
      />
    );
  }

  // Acessível: aria-label resume distribuição
  const summary = visibleGroups
    .filter((g) => (byGroup[g.key] ?? 0) > 0)
    .map((g) => `${byGroup[g.key]} em ${g.label}`)
    .join(", ");

  return (
    <div
      className={cn("flex h-1 w-full overflow-hidden rounded-full bg-bg-muted", className)}
      role="img"
      aria-label={`${total} candidatos · ${summary}`}
    >
      {visibleGroups.map((g) => {
        const v = byGroup[g.key] ?? 0;
        if (v === 0) return null;
        const pct = (v / totalActive) * 100;
        return (
          <span
            key={g.key}
            style={{ width: `${pct}%` }}
            className={cn("h-full", STAGE_GROUP_BAR_COLORS[g.key])}
            title={`${g.label}: ${v} (${Math.round(pct)}%)`}
          />
        );
      })}
    </div>
  );
}
```

**ATENÇÃO CLAUDE.md compliance:** `supabase.from()` calls vivem em `src/hooks/` ou `src/integrations/`, NÃO dentro de `src/components/`. Por isso este task PRECEDE a criação do componente com a extração do hook.

**Passo a — criar `src/hooks/hiring/useLegacyStageCount.ts`:**

```typescript
import { useScopedQuery } from "@/shared/data/useScopedQuery";
import { supabase } from "@/integrations/supabase/client";

/**
 * Conta applications com metadata.legacy_marker NOT NULL (residual Migration F.1 backfill).
 * Usado por LegacyStageWarning banner. CLAUDE.md: supabase.from() vive em hooks, não em components.
 */
export function useLegacyStageCount(jobId?: string) {
  return useScopedQuery<readonly ["hiring", "legacy-stage-count", string], number, Error>(
    ["hiring", "legacy-stage-count", jobId ?? "all"] as const,
    async (): Promise<number> => {
      const q = supabase
        .from("applications")
        .select("id", { count: "exact", head: true })
        .filter("metadata->>legacy_marker", "neq", null);
      if (jobId) q.eq("job_opening_id", jobId);
      const { count, error } = await q;
      if (error) throw error;
      return count ?? 0;
    },
    { staleTime: 60_000 },
  );
}
```

**Passo b — `src/components/hiring/LegacyStageWarning.tsx` consome o hook:**

```typescript
import { useState } from "react";
import { useLegacyStageCount } from "@/hooks/hiring/useLegacyStageCount";
import { X, AlertCircle } from "lucide-react";
import { cn } from "@/lib/utils";

const SESSION_KEY = "leverup:rs:legacy-warning-dismissed";

/**
 * Banner sessão alertando RH se houver candidatos com metadata.legacy_marker
 * (residual da Migration F.1 backfill). Auto-dismiss após interação ou
 * quando contagem zera.
 */
export function LegacyStageWarning({ jobId }: { jobId?: string }) {
  const [dismissed, setDismissed] = useState<boolean>(() => {
    return sessionStorage.getItem(SESSION_KEY) === "true";
  });

  const { data: legacyCount = 0 } = useLegacyStageCount(jobId);

  if (dismissed || legacyCount === 0) return null;

  const handleDismiss = () => {
    sessionStorage.setItem(SESSION_KEY, "true");
    setDismissed(true);
  };

  return (
    <div
      className={cn(
        "flex items-start gap-2 rounded-md border border-status-amber/30",
        "bg-status-amber-soft/50 px-3 py-2 text-[13px]"
      )}
      role="status"
    >
      <AlertCircle className="w-4 h-4 mt-0.5 text-status-amber shrink-0" aria-hidden />
      <div className="flex-1">
        <span className="font-medium">{legacyCount} candidato(s) normalizado(s) recentemente.</span>{" "}
        <span className="text-text-muted">Esses cards estavam em etapas legadas e foram migrados para Triagem com tag de auditoria.</span>
      </div>
      <button
        onClick={handleDismiss}
        className="text-text-muted hover:text-text"
        aria-label="Fechar aviso"
      >
        <X className="w-3.5 h-3.5" />
      </button>
    </div>
  );
}
```

Verificar:
```bash
test -f src/components/hiring/SlaBadge.tsx
test -f src/components/hiring/SparkbarDistribution.tsx
test -f src/components/hiring/LegacyStageWarning.tsx
test -f src/hooks/hiring/useLegacyStageCount.ts
# CLAUDE.md compliance: supabase.from() NÃO em src/components/
! grep -q "supabase\.from" src/components/hiring/LegacyStageWarning.tsx
grep -q "supabase\.from" src/hooks/hiring/useLegacyStageCount.ts
npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -E "Sla|Sparkbar|Legacy" | head
```
  </action>
  <verify>
    <automated>test -f src/components/hiring/SlaBadge.tsx && test -f src/components/hiring/SparkbarDistribution.tsx && test -f src/components/hiring/LegacyStageWarning.tsx && test -f src/hooks/hiring/useLegacyStageCount.ts && ! grep -q "supabase\.from" src/components/hiring/LegacyStageWarning.tsx && grep -q "supabase\.from" src/hooks/hiring/useLegacyStageCount.ts</automated>
  </verify>
  <done>4 arquivos criados (3 components + 1 hook); SparkbarDistribution usa STAGE_GROUP_BAR_COLORS (D-11 corrigido em Plan 03); SlaBadge usa computeSlaTone; LegacyStageWarning auto-dismiss session **e consome useLegacyStageCount hook (CLAUDE.md compliance: supabase.from() vive em src/hooks/, NÃO em src/components/)**.</done>
</task>

<task type="auto">
  <name>Task 4: Modify JobCard to use SparkbarDistribution component</name>
  <files>src/components/hiring/JobCard.tsx</files>
  <read_first>
    src/components/hiring/JobCard.tsx (atual linhas 114-130 — sparkbar inline para substituir),
    src/components/hiring/SparkbarDistribution.tsx (Task 3 deliverable)
  </read_first>
  <action>
SUBSTITUIR o block inline de sparkbar (linhas ~114-130 do JobCard.tsx atual) pela invocação do componente:

ANTES (assumido):
```tsx
<div className="flex h-1 w-full overflow-hidden rounded-full bg-bg-muted">
  {visibleGroups.map((g) => {
    const v = byGroup[g.key] ?? 0;
    if (v === 0) return null;
    const pct = (v / totalActive) * 100;
    return <span key={g.key} style={{ width: `${pct}%` }} className={cn("h-full", STAGE_GROUP_BAR_COLORS[g.key])} />;
  })}
</div>
```

DEPOIS:
```tsx
import { SparkbarDistribution } from "@/components/hiring/SparkbarDistribution";
// ...
<SparkbarDistribution byGroup={counts.byGroup} total={counts.total} />
```

NÃO ALTERAR outras partes do JobCard (header, status badge, last activity, etc.).

Verificar:
```bash
grep -c "SparkbarDistribution" src/components/hiring/JobCard.tsx
# > 0 esperado (1 import + 1 uso)
grep -c "STAGE_GROUP_BAR_COLORS" src/components/hiring/JobCard.tsx
# 0 esperado (movido para SparkbarDistribution.tsx)
```
  </action>
  <verify>
    <automated>grep -c "SparkbarDistribution" src/components/hiring/JobCard.tsx</automated>
  </verify>
  <done>JobCard.tsx usa SparkbarDistribution component; sparkbar inline removido; STAGE_GROUP_BAR_COLORS não é mais importado em JobCard (movido para SparkbarDistribution).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User drag action -> mutate | canTransition guard local; trigger DB tg_enforce_application_stage_transition é defense-in-depth. |
| Realtime push -> UI | setQueryData direto; payload já passou por RLS server-side. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07-01 | T (Tampering) | User drag para stage inválido (bypass UI) | mitigate | canTransition pre-check + DB trigger duplo guard |
| T-02-07-02 | I (Information disclosure) | CandidateCard expõe email no DOM | accept | Email não está no mínimo D-07; está oculto por default em CandidateCard. Apenas avatar+nome no card; email aparece em drawer (audit log via RPC) |
| T-02-07-03 | D (DoS) | Sparkbar com array gigante quebra render | accept | Total candidatos por vaga <100 v1 (research flag deferred); sparkbar tem 6 segmentos fixos |
| T-02-07-04 | T (Tampering) | sessionStorage legacy-warning-dismissed forjado | accept | Aviso é UX nice-to-have; tampering apenas afeta se vê o banner (não é security-critical) |
</threat_model>

<verification>
- 6 componentes em src/components/hiring/ + 1 hook em src/hooks/hiring/ criados/modificados
- 2 testes Wave 0 ativos e green (CandidatesKanban, CandidateCard)
- npm test -- --run tests/hiring/Candidate*.test.tsx exit 0
- npx tsc --noEmit -p tsconfig.app.json clean em src/components/hiring/
- grep "expectedUpdatedAt" src/components/hiring/CandidatesKanban.tsx retorna 0 (removido)
- grep "canTransition" src/components/hiring/CandidatesKanban.tsx retorna 1+
- grep "useApplicationsRealtime" src/components/hiring/CandidatesKanban.tsx retorna 1+
- grep "supabase.from" src/components/hiring/LegacyStageWarning.tsx retorna 0 (extraído pro hook)
- grep "supabase.from" src/hooks/hiring/useLegacyStageCount.ts retorna 1+
- Integration test em CandidatesKanban.integration.test.tsx NÃO contém `expect(true).toBe(true)` placeholder — usa userEvent.pointer simulando drag, OU teste foi descopado para 02-VALIDATION.md Manual-Only com developer aprovação documentada
</verification>

<success_criteria>
1. Bug #1 do projeto fechado: canTransition pre-check + optimistic + Realtime + 4-tipo error.
2. Card minimum D-07 fixo + customizável D-08 + SLA stripe D-10.
3. Sparkbar D-11 extraído como componente reusável; cores corrigidas; usado em JobCard.
4. LegacyStageWarning alerta RH durante cutover Migration F. supabase.from() extraído para `useLegacyStageCount` hook (CLAUDE.md compliance).
5. Integration test em CandidatesKanban.integration.test.tsx tem cobertura real (não placeholder) para o cenário canTransition-toast — ou descope formal documentado em 02-VALIDATION.md.
6. Plan 08 (filters/table/dedup) e Plan 09 (drawer/LGPD UI) podem prosseguir consumindo esses componentes.
</success_criteria>

<output>
Após completar, criar .planning/phases/02-r-s-refactor/02-07-SUMMARY.md com:
- Lista de 6 componentes criados/modificados
- Diff resumido de CandidatesKanban (remoções: expectedUpdatedAt + performMove + OptimisticMutationToast; adições: canTransition + useApplicationsRealtime + sensors)
- Confirmação de tests verde
- Próximo plano: 02-08-PLAN.md (filters + table toggle + dedup CPF)
</output>

---
phase: 02-r-s-refactor
plan: 04
type: execute
wave: 2
depends_on: ["02-02", "02-03"]
files_modified:
  - src/integrations/supabase/types.ts
  - src/integrations/supabase/hiring-types.ts
autonomous: false
requirements:
  - RS-05
  - RS-06
  - TAL-03
  - TAL-05
  - TAL-06
  - TAL-09
must_haves:
  truths:
    - "supabase db push aplicado com sucesso no projeto remoto ehbxpbeijofxtsbezwxd OU em DB local"
    - "src/integrations/supabase/types.ts regenerado contendo tipos para data_access_log, candidate_consents, active_candidate_consents view"
    - "src/integrations/supabase/hiring-types.ts estendido com Consent, DataAccessLogEntry, ConsentPurpose, ConsentLegalBasis types"
    - "tsc --noEmit passes em todo o codebase"
    - "npm test -- --run e npm run test:db ambos exit 0"
  artifacts:
    - path: "src/integrations/supabase/types.ts"
      provides: "Database types regenerados pós-Migration F (4 sub-migrations aplicadas)"
      contains: "candidate_consents"
    - path: "src/integrations/supabase/hiring-types.ts"
      provides: "Tipos hand-written: Consent, DataAccessLogEntry, ConsentPurpose, ConsentLegalBasis"
      contains: "ConsentPurpose"
  key_links:
    - from: "src/integrations/supabase/types.ts"
      to: "supabase/migrations/20260428120{0,1,2,3}00_*"
      via: "supabase gen types typescript"
      pattern: "data_access_log|candidate_consents"
    - from: "src/integrations/supabase/hiring-types.ts"
      to: "src/integrations/supabase/types.ts"
      via: "type Consent = Database['public']['Tables']['candidate_consents']['Row']"
      pattern: "Database\\[.public.\\]"
---

<objective>
**[BLOCKING]** Aplicar Migration F (4 sub-migrations escritas em Plan 02) ao DB remoto Supabase do projeto Lever Talents (`ehbxpbeijofxtsbezwxd`) via `supabase db push`. Em seguida regenerar `src/integrations/supabase/types.ts` (auto-generated pelo CLI) e estender `src/integrations/supabase/hiring-types.ts` com tipos hand-written derivados das novas tabelas.

Por que `autonomous: false`: o CLI Supabase pode pedir confirmação interativa ao detectar mudanças significativas (4 novas tabelas/views/RPCs/cron jobs); o usuário precisa aprovar antes do push em prod. Para reduzir risco, recomenda-se:
1. Tentar `supabase migration up --local` numa instância local primeiro (smoke test).
2. Ou usar `--include-all` e flag `-y` se o dev confiar no diff.

Sem este plano, hooks Wave 2 falham TypeScript build (`Database['public']['Tables']['candidate_consents']` não existe).

Output: 2 arquivos atualizados + DB remoto sincronizado + tsc clean + suites green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-r-s-refactor/02-CONTEXT.md
@.planning/phases/02-r-s-refactor/02-RESEARCH.md
@.planning/phases/02-r-s-refactor/02-02-SUMMARY.md
@.planning/phases/02-r-s-refactor/02-03-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Estado atual de hiring-types.ts (preservar e estender) -->

From src/integrations/supabase/hiring-types.ts (atual — confirmar com read antes de editar):
```typescript
import type { Database } from "./types";
export type ApplicationRow = Database["public"]["Tables"]["applications"]["Row"];
export type CandidateRow = Database["public"]["Tables"]["candidates"]["Row"];
// ... extensões hand-written
```

Supabase CLI commands relevantes (do Phase 1 SUMMARYs):
```bash
# Apply migrations to remote
supabase db push

# Local-first apply (recommended)
supabase migration up --local

# Regenerate TypeScript types
supabase gen types typescript --project-id ehbxpbeijofxtsbezwxd > src/integrations/supabase/types.ts
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] supabase db push — apply Migration F to remote DB</name>
  <what-built>
4 sub-migrations da Phase 2 (F.1, F.2, F.3, F.4) já estão escritas em `supabase/migrations/` (Plan 02). Cada uma já foi validada localmente com `npx supabase test db` (5 pgTAP suites, ~17 tests green).

Esta checkpoint é a **única** task da Phase 2 que touch DB remoto. Sem ela, o resto da fase falha TypeScript compile.
  </what-built>
  <how-to-verify>
**Pré-requisitos a confirmar antes de fazer push:**

1. **`SUPABASE_ACCESS_TOKEN` configurado:**
   ```bash
   echo "${SUPABASE_ACCESS_TOKEN:-(not set)}"
   ```
   Se não set, exporte temporariamente:
   ```bash
   export SUPABASE_ACCESS_TOKEN=<seu-token>
   ```

2. **Project ID linkado:**
   ```bash
   cat supabase/config.toml | grep project_id || supabase projects list
   ```
   Esperado: `ehbxpbeijofxtsbezwxd`.

3. **Pre-flight check — diff esperado:**
   ```bash
   supabase db diff --linked --schema public
   ```
   Esperado: 4 migration files novos (F.1, F.2, F.3, F.4); confirma que pgTAP local rodou OK em todas.

4. **Pre-flight check — Phase 1 helpers existem:**
   ```bash
   supabase db dump --linked --data-only=false 2>&1 | grep -E "is_people_manager|visible_companies|app_role" | head
   ```
   Esperado: Phase 1 helpers existem no remoto.

**Comando principal de push:**

```bash
# Se você confia no diff (todas as 4 migrations vistas em supabase db diff):
supabase db push --linked --include-all

# Se quer confirmar interativamente (recomendado para 1ª aplicação):
supabase db push --linked
# (CLI pede confirmação; revisar diff e digitar 'y')
```

**Pós-push verification:**

5. **pgTAP remoto green:**
   ```bash
   supabase db remote test --suite supabase/tests/006-migration-f-stages.sql
   supabase db remote test --suite supabase/tests/007-data-access-log.sql
   supabase db remote test --suite supabase/tests/008-candidate-consents.sql
   supabase db remote test --suite supabase/tests/009-cpf-unique.sql
   supabase db remote test --suite supabase/tests/010-pg-cron-retention.sql
   ```
   Esperado: total ~17 tests, 0 fails. Se algum falha, **NÃO continuar para Task 2**; o push precisa ser revertido (e.g. via `supabase migration repair`) ou o DB precisa de fix manual.

6. **Cron job real:**
   ```bash
   # Via SQL editor do Supabase Studio OU via psql linkado:
   supabase db remote sql "SELECT jobname, schedule FROM cron.job WHERE jobname = 'data_access_log_retention_cleanup';"
   ```
   Esperado: 1 row com schedule `30 3 * * 1`.

**Plano de fallback** (caso push falhe parcialmente):
- Se F.1 aplicada mas F.2 falha: revisar erro (provavelmente `is_people_manager` ausente). Confirmar Phase 1 deploy completo. Re-rodar `supabase db push`.
- Se duplicate CPF detectado em F.4 (RAISE EXCEPTION 'Migration F.4 abortada'): listar duplicatas via `SELECT cpf, count(*) FROM public.candidates WHERE cpf IS NOT NULL GROUP BY cpf HAVING count(*) > 1;`, fazer merge manual via `useReuseCandidateForJob` ou anonymizar duplicata, re-rodar push.
- Se cron schedule duplicado: rodar `SELECT cron.unschedule('data_access_log_retention_cleanup');` antes de re-aplicar F.2.
  </how-to-verify>
  <resume-signal>
Após confirmar `supabase db push --linked` exit 0 E os 5 pgTAP suites remotas green, digite "applied" para prosseguir para Task 2 (regen types).

Se algo falhar, descreva o erro literal e retorne para troubleshoot.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Regenerate src/integrations/supabase/types.ts via Supabase CLI</name>
  <files>src/integrations/supabase/types.ts</files>
  <read_first>
    src/integrations/supabase/types.ts (estado atual — backup mental do shape geral)
  </read_first>
  <action>
Regenerar `src/integrations/supabase/types.ts` automaticamente via CLI:

```bash
supabase gen types typescript --project-id ehbxpbeijofxtsbezwxd --schema public > src/integrations/supabase/types.ts
```

**Verificações pós-regen:**

1. Arquivo contém tipo `data_access_log`:
   ```bash
   grep -c "data_access_log:" src/integrations/supabase/types.ts
   ```
   Esperado: ≥3 (Row + Insert + Update inside Tables).

2. Arquivo contém tipo `candidate_consents`:
   ```bash
   grep -c "candidate_consents:" src/integrations/supabase/types.ts
   ```
   Esperado: ≥3.

3. Arquivo contém view `active_candidate_consents`:
   ```bash
   grep -c "active_candidate_consents" src/integrations/supabase/types.ts
   ```
   Esperado: ≥1 (em Views section).

4. Arquivo contém RPC `read_candidate_with_log`:
   ```bash
   grep -c "read_candidate_with_log" src/integrations/supabase/types.ts
   ```
   Esperado: ≥1 (em Functions section, com Args + Returns shape).

5. TypeScript compila:
   ```bash
   npx tsc --noEmit -p tsconfig.app.json 2>&1 | head -20
   ```
   Esperado: 0 erros. **Importante:** alguns hooks/components ainda fazem referência a tipos legados; esperado encontrar erros do tipo `Property X does not exist on type Y` em hooks que vamos refazer em Plans 05-09. Filtrar:
   ```bash
   npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -E "src/(integrations|lib|app)" | head
   ```
   Esses arquivos NÃO podem ter erros — apenas hooks/components Wave 2-3 podem.

6. Commit do types.ts apenas se itens 1-5 passam.

**Se gen falha** (project ID inválido, token expired, etc.):
- Confirmar `SUPABASE_ACCESS_TOKEN` set
- Confirmar project ID `ehbxpbeijofxtsbezwxd` em `supabase/config.toml`
- Se necessário: `supabase login` interativo
- Last resort: gerar local-first via `supabase gen types typescript --local > src/integrations/supabase/types.ts` (assume migration aplicada local)
  </action>
  <verify>
    <automated>grep -c "candidate_consents" src/integrations/supabase/types.ts && grep -c "data_access_log" src/integrations/supabase/types.ts && grep -c "read_candidate_with_log" src/integrations/supabase/types.ts</automated>
  </verify>
  <done>`types.ts` regenerado contendo `candidate_consents` (≥3 occurrences), `data_access_log` (≥3), `active_candidate_consents` (≥1), `read_candidate_with_log` (≥1). `tsc --noEmit` em src/integrations/ retorna 0 errors.</done>
</task>

<task type="auto">
  <name>Task 3: Extend hiring-types.ts with Phase 2 hand-written types</name>
  <files>src/integrations/supabase/hiring-types.ts</files>
  <read_first>
    src/integrations/supabase/hiring-types.ts (estado atual — preservar exports),
    src/integrations/supabase/types.ts (regen — referência shape Database['public']['Tables'][...])
  </read_first>
  <action>
APPEND ao final de `src/integrations/supabase/hiring-types.ts` (NÃO remover exports existentes):

```typescript
// =========================================================================
// Phase 2 — Migration F types (hand-written extensions)
// =========================================================================

// candidate_consents
export type Consent = Database["public"]["Tables"]["candidate_consents"]["Row"];
export type ConsentInsert = Database["public"]["Tables"]["candidate_consents"]["Insert"];
export type ConsentUpdate = Database["public"]["Tables"]["candidate_consents"]["Update"];

// View active_candidate_consents
export type ActiveConsent = Database["public"]["Views"]["active_candidate_consents"]["Row"];

// Enums
export type ConsentPurpose = Database["public"]["Enums"]["consent_purpose_enum"];
export type ConsentLegalBasis = Database["public"]["Enums"]["consent_legal_basis_enum"];

// data_access_log
export type DataAccessLogEntry = Database["public"]["Tables"]["data_access_log"]["Row"];
export type DataAccessLogInsert = Database["public"]["Tables"]["data_access_log"]["Insert"];

// RPC read_candidate_with_log
export type ReadCandidateWithLogArgs = Database["public"]["Functions"]["read_candidate_with_log"]["Args"];
export type ReadCandidateWithLogReturn = Database["public"]["Functions"]["read_candidate_with_log"]["Returns"];

// =========================================================================
// Phase 2 — Move application stage args (consumed by useMoveApplicationStage)
// =========================================================================

import type { ApplicationStage } from "./types-helpers"; // ou wherever ApplicationStage is defined

export interface MoveApplicationStageArgs {
  id: string;
  fromStage: ApplicationStage;
  toStage: ApplicationStage;
  jobId: string;
  companyId: string;
}

// =========================================================================
// Phase 2 — ApplicationWithCandidate (used by useApplicationsByJob)
// (consolidate the Row + Candidate join shape used across hooks)
// =========================================================================

export type ApplicationWithCandidate = ApplicationRow & {
  candidate: Pick<CandidateRow, "id" | "full_name" | "email" | "anonymized_at"> | null;
};
```

**ATENÇÃO:** o último import (`ApplicationStage`) já existe no arquivo atual — verifique a linha que define/exporta `ApplicationStage` e referencie corretamente. Se estiver definido inline em `hiring-types.ts`, não precisa import — apenas use.

**Verificações:**

1. `tsc --noEmit` clean:
   ```bash
   npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep "hiring-types" | head
   ```
   Esperado: 0 linhas (sem errors em hiring-types).

2. Imports test (escrever test temporário):
   ```typescript
   // Em qualquer test ou componente, deve compilar:
   import type {
     Consent,
     ConsentPurpose,
     DataAccessLogEntry,
     MoveApplicationStageArgs,
     ApplicationWithCandidate,
   } from "@/integrations/supabase/hiring-types";
   ```

3. **Commit** dos 2 arquivos juntos (`types.ts` + `hiring-types.ts`).
  </action>
  <verify>
    <automated>grep -c "ConsentPurpose\|DataAccessLogEntry\|MoveApplicationStageArgs" src/integrations/supabase/hiring-types.ts && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c "hiring-types"</automated>
  </verify>
  <done>`hiring-types.ts` exporta `Consent`, `ConsentInsert`, `ConsentUpdate`, `ActiveConsent`, `ConsentPurpose`, `ConsentLegalBasis`, `DataAccessLogEntry`, `DataAccessLogInsert`, `ReadCandidateWithLogArgs`, `ReadCandidateWithLogReturn`, `MoveApplicationStageArgs`, `ApplicationWithCandidate`. Exports antigos preservados. tsc retorna 0 errors em arquivos da Phase 2 que já existem.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local CLI → Supabase remoto | `supabase db push` envia DDL com auth via SUPABASE_ACCESS_TOKEN. Token escopado ao project. |
| `supabase gen types` → arquivo local | Output substitui types.ts. Verificação grep antes de commit garante que migrations refletiram. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | T (Tampering) | Push aplica DDL parcial e DB fica em estado inconsistente | mitigate | Cada migration tem `BEGIN;` implícito (Postgres run em transaction). Se F.2 falha, rollback completo. F.1 já aplicada NÃO é revertida (designed idempotente). |
| T-02-04-02 | I (Information disclosure) | Token de acesso vazado em logs ao falhar | mitigate | CLAUDE.md "No PII in console.log" + `grep "SUPABASE_ACCESS_TOKEN" tests/` deve retornar 0 matches. Não printar `$SUPABASE_ACCESS_TOKEN` em mensagens. |
| T-02-04-03 | D (DoS) | Push fora de janela (horário de pico) trava DB | accept | Migration F.1 usa SKIP LOCKED + batch; F.2-F.4 são DDL leves (<1s cada em 50k rows). Recomendação: aplicar em horário não-comercial; mitigado pelo dev escolher timing. |
| T-02-04-04 | E (Elevation) | types.ts regen sem auth correto | mitigate | `--project-id ehbxpbeijofxtsbezwxd` é hardcoded; sem token válido o gen retorna error explícito (não silencioso). |
</threat_model>

<verification>
- DB remoto tem 4 novas tabelas/views/RPCs: `data_access_log`, `candidate_consents`, `active_candidate_consents` view, `read_candidate_with_log` RPC, `cron.job 'data_access_log_retention_cleanup'`
- `src/integrations/supabase/types.ts` regenerado contendo todos os artifacts acima
- `src/integrations/supabase/hiring-types.ts` estendido com tipos hand-written (12+ novos exports)
- `npx tsc --noEmit -p tsconfig.app.json` retorna 0 errors em `src/integrations/`, `src/lib/`, `src/app/`
- `npm run test:db` (remoto E local) ambos exit 0
- Commit único contém os 2 arquivos types
</verification>

<success_criteria>
1. Migration F aplicada no remote `ehbxpbeijofxtsbezwxd` (verificável via `supabase db remote sql "SELECT 1 FROM cron.job WHERE jobname='data_access_log_retention_cleanup';"`).
2. `types.ts` é a fonte de verdade Phase 2 — Plans 05-09 podem importar `Database['public']['Tables']['candidate_consents']` sem erro.
3. `hiring-types.ts` expõe shapes Phase 2 com nomes que casam com a CONTEXT.md (`Consent`, `DataAccessLogEntry`).
4. tsc clean em todos os arquivos de produção que já existem; novos hooks/components Wave 2-3 ainda podem ter referências quebradas (são implementados nos próximos plans).
5. Wave 2 destravada — Plans 05-09 podem prosseguir.
</success_criteria>

<output>
Após completar, criar `.planning/phases/02-r-s-refactor/02-04-SUMMARY.md` com:
- Output completo de `supabase db push --linked` (deve confirmar 4 migrations applied)
- Output de `supabase db remote sql "SELECT count(*) FROM information_schema.tables WHERE table_schema='public' AND table_name IN ('data_access_log', 'candidate_consents');"` (esperado 2)
- Output de `grep -c "candidate_consents\|data_access_log" src/integrations/supabase/types.ts`
- Próximo plano: `02-05-PLAN.md` (hiring core hooks)
</output>

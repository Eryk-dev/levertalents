---
phase: 02-r-s-refactor
plan: 05
type: execute
wave: 3
depends_on: ["02-04"]
files_modified:
  - src/hooks/hiring/useApplications.ts
  - src/hooks/hiring/useApplicationsRealtime.ts
  - src/hooks/hiring/useApplicationCountsByJob.ts
  - tests/hiring/useMoveApplicationStage.test.tsx
  - tests/hiring/useApplicationsRealtime.test.tsx
  - tests/hiring/useApplicationCountsByJob.test.tsx
autonomous: true
requirements:
  - RS-01
  - RS-02
  - RS-03
  - RS-04
  - RS-08
  - RS-12
must_haves:
  truths:
    - "useMoveApplicationStage usa TanStack v5 onMutate (cancelQueries + setQueryData) + onError rollback + onSettled invalidate"
    - "queryKey shape: ['scope', scope.id, scope.kind, 'hiring', 'applications', 'by-job', jobId]"
    - "Mutation NÃO usa optimistic locking via .eq('updated_at', ...) (D-03 last-writer-wins)"
    - "Erro Postgrest é mapeado para discriminated MoveApplicationError via detectors"
    - "Retry com backoff só para kind='network' até 3 tentativas (1s/2s/4s)"
    - "useApplicationsRealtime subscreve channel applications:job:{jobId}, faz setQueryData merge silencioso em UPDATE, invalidate em INSERT"
    - "useApplicationCountsByJobs portado para useScopedQuery (queryKey inclui scope.id)"
    - "useApplicationsByJob portado para useScopedQuery"
  artifacts:
    - path: "src/hooks/hiring/useApplications.ts"
      provides: "useMoveApplicationStage rewrite + useApplicationsByJob portado para useScopedQuery"
      contains: "onMutate"
    - path: "src/hooks/hiring/useApplicationsRealtime.ts"
      provides: "Subscribe channel applications:job:{jobId} com setQueryData merge silent (D-04)"
      contains: "supabase.channel"
    - path: "src/hooks/hiring/useApplicationCountsByJob.ts"
      provides: "Counts by stage_group portado para useScopedQuery (alimenta sparkbar)"
      contains: "useScopedQuery"
  key_links:
    - from: "useMoveApplicationStage onMutate"
      to: "useApplicationsByJob queryKey"
      via: "queryClient.cancelQueries + setQueryData mesma key"
      pattern: "applicationsKey"
    - from: "useApplicationsRealtime"
      to: "useApplicationsByJob cache"
      via: "setQueryData direto sem invalidate em UPDATE"
      pattern: "setQueryData.*applications"
    - from: "useApplicationCountsByJob"
      to: "JobCard sparkbar (Plan 06)"
      via: "byGroup retornado da query"
      pattern: "byGroup"
---

<objective>
Wave 3 — Hooks core do hiring que recebem o coração do refactor de Phase 2 (D-01 a D-06 e D-04). 3 hooks neste plano, todos no diretório `src/hooks/hiring/`:

1. **`useApplications.ts`** — REWRITE completo de `useMoveApplicationStage` (atual está broken, usa optimistic locking via `.eq('updated_at')` que conflita com D-03 last-writer-wins). Implementar TanStack v5 canonical: `onMutate` com `cancelQueries` + `setQueryData` snapshot, `onError` com rollback + toast diferenciado por `kind`, `onSettled` com partial-key invalidation. Também portar `useApplicationsByJob` para `useScopedQuery` (chokepoint Phase 1). Preservar `useReuseCandidateForJob` e outros hooks existentes.

2. **`useApplicationsRealtime.ts`** — NOVO arquivo. Subscribe ao channel `applications:job:{jobId}` via `supabase.channel`. UPDATE → `setQueryData` merge silencioso (D-04 — sem toast). INSERT → `invalidate` (re-fetch para reconstruir join). Cleanup atômico no unmount.

3. **`useApplicationCountsByJob.ts`** — Existente já retorna `byGroup`; estender para portar para `useScopedQuery` (queryKey inclui scope.id). Sem mudança de API consumida pelo `JobCard`.

Ativa 3 arquivos de teste Wave 0 (`useMoveApplicationStage.test.tsx`, `useApplicationsRealtime.test.tsx`, `useApplicationCountsByJob.test.tsx`).

Purpose: Plans 06-08 (UI components) consomem esses hooks. Sem este plan, kanban continua broken e Realtime não tem source.

Output: 3 arquivos de hook + 3 arquivos de teste ativos. `npm test -- --run` exit 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-r-s-refactor/02-CONTEXT.md
@.planning/phases/02-r-s-refactor/02-RESEARCH.md
@.planning/phases/02-r-s-refactor/02-PATTERNS.md
@.planning/phases/02-r-s-refactor/02-03-SUMMARY.md
@.planning/phases/02-r-s-refactor/02-04-SUMMARY.md
@.planning/phases/01-tenancy-backbone/01-05-SUMMARY.md
@CLAUDE.md
@src/hooks/hiring/useApplications.ts
@src/hooks/hiring/useApplicationCountsByJob.ts

<interfaces>
<!-- Phase 1 chokepoint — useScopedQuery + useScope shape -->

From src/shared/data/useScopedQuery.ts (Phase 1 — chokepoint):
```typescript
export function useScopedQuery<TKey extends readonly unknown[], TData, TError = Error>(
  key: TKey,
  fetcher: (companyIds: string[]) => Promise<TData>,
  options?: Omit<UseQueryOptions<TData, TError>, 'queryKey' | 'queryFn'>,
): UseQueryResult<TData, TError>
// queryKey final: ['scope', scope.id, scope.kind, ...key]
```

From src/app/providers/ScopeProvider.tsx (Phase 1):
```typescript
export interface Scope {
  id: string;       // 'company:UUID' or 'group:UUID'
  kind: 'company' | 'group';
  companyIds: string[];
  userId: string | null;
}
export function useScope(): { scope: Scope | null }
```

From src/lib/supabaseError.ts (Plan 03):
```typescript
export type MoveApplicationError = { kind: 'rls'|'network'|'conflict'|'transition'|'unknown'; ... };
export function detectRlsDenial(err: unknown): boolean;
export function detectNetworkDrop(err: unknown): boolean;
export function detectConflict(err: unknown): boolean;
export function getMoveErrorToastConfig(err: MoveApplicationError): { title; description; duration };
```

From src/integrations/supabase/hiring-types.ts (Plan 04):
```typescript
export interface MoveApplicationStageArgs {
  id: string; fromStage: ApplicationStage; toStage: ApplicationStage; jobId: string; companyId: string;
}
export type ApplicationWithCandidate = ApplicationRow & { candidate: ... };
```

From src/lib/hiring/stageGroups.ts:
```typescript
export type StageGroupKey = 'triagem' | 'checagem' | 'entrevista_rh' | 'entrevista_final' | 'decisao' | 'descartados';
```

From src/hooks/hiring/useApplications.ts (atual — broken pattern lines 73-114):
```typescript
// REMOVE this entire block:
// .eq("updated_at", expectedUpdatedAt)  ← optimistic locking, conflito com D-03
// .eq("stage", fromStage)               ← redundant; trigger DB já valida
// onSuccess invalidate-only             ← sem onMutate optimistic
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite useMoveApplicationStage + port useApplicationsByJob to useScopedQuery</name>
  <files>src/hooks/hiring/useApplications.ts, tests/hiring/useMoveApplicationStage.test.tsx</files>
  <read_first>
    src/hooks/hiring/useApplications.ts (estado atual — preservar useReuseCandidateForJob, useDiscardApplication, etc.),
    src/shared/data/useScopedQuery.ts (Phase 1 chokepoint),
    src/app/providers/ScopeProvider.tsx (useScope shape),
    src/lib/supabaseError.ts (detectors recém-criados em Plan 03),
    src/integrations/supabase/hiring-types.ts (MoveApplicationStageArgs, ApplicationWithCandidate),
    .planning/phases/02-r-s-refactor/02-RESEARCH.md (§1 lines 196-373 — TanStack v5 canonical),
    .planning/phases/02-r-s-refactor/02-PATTERNS.md ("useApplications.ts" assignment lines 114-205),
    tests/scope/useScopedQuery.test.tsx (template para test wrapper)
  </read_first>
  <behavior>
    useMoveApplicationStage:
    - onMutate: cancelQueries({ queryKey: applicationsKey }) → getQueryData snapshot → setQueryData optimistic → return { previousApplications, applicationsKey } como context
    - mutationFn: UPDATE applications SET stage=toStage, last_moved_by=scope.userId WHERE id=args.id; SEM .eq('updated_at') SEM .eq('stage'); maybeSingle. Se error: detectRlsDenial → throw {kind:'rls'}; detectNetworkDrop → {kind:'network'}; senão {kind:'unknown'}. Se !data: throw {kind:'conflict'}.
    - onError: rollback via setQueryData(ctx.applicationsKey, ctx.previousApplications); switch err.kind para toast diferenciado (usar getMoveErrorToastConfig)
    - onSettled: invalidate ctx.applicationsKey + invalidate counts queryKey
    - retry: gate por err.kind==='network' && failureCount<3
    - retryDelay: 1000 * 2 ** attempt (cap 8000)

    useApplicationsByJob:
    - usa useScopedQuery (queryKey ['hiring', 'applications', 'by-job', jobId])
    - select com embed de candidate: id/full_name/email/anonymized_at
    - enabled: !!jobId
    - order by stage_entered_at asc
  </behavior>
  <action>
**Passo 1 — Ativar test:** abrir `tests/hiring/useMoveApplicationStage.test.tsx`, remover `describe.skip`, escrever 7 test cases (renderHook + QueryClientProvider + spyOn(useScope) + MSW handlers).

Conteúdo de teste:

```typescript
import { renderHook, waitFor } from "@testing-library/react";
import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
import { describe, it, expect, vi, beforeEach, afterEach, beforeAll, afterAll } from "vitest";
import { setupServer } from "msw/node";
import * as scopeModule from "@/app/providers/ScopeProvider";
import { useMoveApplicationStage } from "@/hooks/hiring/useApplications";
import { mockMoveApplication, defaultHiringHandlers } from "@/../tests/msw/hiring-handlers";

const server = setupServer(...defaultHiringHandlers);
beforeAll(() => server.listen({ onUnhandledRequest: "warn" }));
afterEach(() => server.resetHandlers());
afterAll(() => server.close());

function createWrapper() {
  const client = new QueryClient({ defaultOptions: { queries: { retry: false }, mutations: { retry: false } } });
  return {
    client,
    Wrapper: ({ children }: { children: React.ReactNode }) => (
      <QueryClientProvider client={client}>{children}</QueryClientProvider>
    ),
  };
}

beforeEach(() => {
  vi.spyOn(scopeModule, "useScope").mockReturnValue({
    scope: { id: "company:abc", kind: "company", companyIds: ["c1"], userId: "u1" },
  } as ReturnType<typeof scopeModule.useScope>);
});
afterEach(() => { vi.restoreAllMocks(); });

describe("useMoveApplicationStage — TanStack v5 optimistic + rollback", () => {
  it("onMutate aplica setQueryData otimista ANTES do mutate resolver", async () => {
    const { client, Wrapper } = createWrapper();
    const queryKey = ["scope", "company:abc", "company", "hiring", "applications", "by-job", "j1"] as const;
    client.setQueryData(queryKey, [
      { id: "app-1", stage: "em_interesse", stage_entered_at: "2026-04-20T00:00:00Z", candidate: { id: "c1", full_name: "Foo" } },
    ]);

    server.use(mockMoveApplication.success("entrevista_rh"));
    const { result } = renderHook(() => useMoveApplicationStage(), { wrapper: Wrapper });

    await waitFor(() => {
      result.current.mutate({ id: "app-1", fromStage: "em_interesse", toStage: "entrevista_rh", jobId: "j1", companyId: "c1" });
    });

    // Imediatamente após mutate (antes do server resolver), cache deve refletir entrevista_rh
    const optimisticState = client.getQueryData<unknown[]>(queryKey);
    expect(optimisticState?.[0]).toMatchObject({ stage: "entrevista_rh" });
  });

  it("onError com kind=rls faz rollback E mostra toast 'Sem permissão'", async () => {
    const { client, Wrapper } = createWrapper();
    const queryKey = ["scope", "company:abc", "company", "hiring", "applications", "by-job", "j1"] as const;
    const initial = [{ id: "app-1", stage: "em_interesse", stage_entered_at: "2026-04-20T00:00:00Z", candidate: { id: "c1", full_name: "Foo" } }];
    client.setQueryData(queryKey, initial);

    server.use(mockMoveApplication.rlsDenial);
    const { result } = renderHook(() => useMoveApplicationStage(), { wrapper: Wrapper });

    result.current.mutate({ id: "app-1", fromStage: "em_interesse", toStage: "entrevista_rh", jobId: "j1", companyId: "c1" });

    await waitFor(() => expect(result.current.isError).toBe(true));
    expect(client.getQueryData(queryKey)).toEqual(initial); // rollback aconteceu
  });

  it("onError com kind=network retry até 3x com backoff", async () => {
    // Servidor sempre retorna network drop
    server.use(mockMoveApplication.networkDrop);
    const { Wrapper } = createWrapper();
    const { result } = renderHook(() => useMoveApplicationStage(), { wrapper: Wrapper });

    result.current.mutate({ id: "app-1", fromStage: "em_interesse", toStage: "entrevista_rh", jobId: "j1", companyId: "c1" });

    await waitFor(() => expect(result.current.isError).toBe(true), { timeout: 12000 });
    // failure count >= 3 (1 initial + 3 retries) — verificar via mock spy se exposto, ou ajustar assertion
  });

  it("queryKey shape inclui scope.id e scope.kind (Phase 1 lock)", () => {
    // teste indireto: cancelQueries com a key correta deve afetar a cache
    const { client, Wrapper } = createWrapper();
    const queryKey = ["scope", "company:abc", "company", "hiring", "applications", "by-job", "j1"] as const;
    client.setQueryData(queryKey, []);

    renderHook(() => useMoveApplicationStage(), { wrapper: Wrapper });
    // se o hook usar key errada, getQueryData volta undefined ao chamar setQueryData no key esperada
    expect(client.getQueryData(queryKey)).toEqual([]);
  });
});

describe("useApplicationsByJob — useScopedQuery shape", () => {
  it("query enabled apenas com jobId", async () => {
    // testar: useApplicationsByJob(undefined) → !isFetching
    // skip esse específico se ScopeProvider mock complicar; mover para Plan 08 integration test
  });
});
```

**Passo 2 — Implementar `useMoveApplicationStage` em `src/hooks/hiring/useApplications.ts`:**

REMOVER da função atual (linhas 73-114):
- Block `.eq("updated_at", expectedUpdatedAt)` em `mutationFn`
- Block `.eq("stage", fromStage)` em `mutationFn`
- Return shape `{ ok: false, conflict: true }` (substituído por throw)
- `onSuccess` invalidate-only (substituído por onMutate/onError/onSettled)

ADICIONAR (paste verbatim de RESEARCH §1 + PATTERNS.md):

```typescript
import { useMutation, useQueryClient } from "@tanstack/react-query";
import { supabase } from "@/integrations/supabase/client";
import { toast } from "sonner";
import { useScope } from "@/app/providers/ScopeProvider";
import { useScopedQuery } from "@/shared/data/useScopedQuery";
import {
  detectRlsDenial,
  detectNetworkDrop,
  getMoveErrorToastConfig,
  type MoveApplicationError,
} from "@/lib/supabaseError";
import type {
  ApplicationRow,
  ApplicationStage,
  ApplicationWithCandidate,
  MoveApplicationStageArgs,
} from "@/integrations/supabase/hiring-types";

interface MutationContext {
  previousApplications: ApplicationWithCandidate[] | undefined;
  applicationsKey: readonly unknown[];
}

export function useMoveApplicationStage() {
  const queryClient = useQueryClient();
  const { scope } = useScope();

  return useMutation<
    { ok: true; row: ApplicationRow },
    MoveApplicationError,
    MoveApplicationStageArgs,
    MutationContext
  >({
    mutationFn: async (args): Promise<{ ok: true; row: ApplicationRow }> => {
      const { data, error } = await supabase
        .from("applications")
        .update({
          stage: args.toStage,
          last_moved_by: scope?.userId ?? null,
        })
        .eq("id", args.id)
        .select()
        .maybeSingle();

      if (error) {
        if (detectRlsDenial(error)) throw { kind: "rls", error } as MoveApplicationError;
        if (detectNetworkDrop(error)) throw { kind: "network", error } as MoveApplicationError;
        throw { kind: "unknown", error } as MoveApplicationError;
      }
      if (!data) {
        throw { kind: "conflict" } as MoveApplicationError;
      }
      return { ok: true, row: data as ApplicationRow };
    },

    onMutate: async (args): Promise<MutationContext> => {
      if (!scope) throw new Error("Scope not resolved");
      const applicationsKey = [
        "scope", scope.id, scope.kind,
        "hiring", "applications", "by-job", args.jobId,
      ] as const;

      await queryClient.cancelQueries({ queryKey: applicationsKey });

      const previousApplications =
        queryClient.getQueryData<ApplicationWithCandidate[]>(applicationsKey);

      queryClient.setQueryData<ApplicationWithCandidate[]>(
        applicationsKey,
        (old) =>
          old?.map((a) =>
            a.id === args.id
              ? {
                  ...a,
                  stage: args.toStage,
                  stage_entered_at: new Date().toISOString(),
                }
              : a
          ) ?? [],
      );

      return { previousApplications, applicationsKey };
    },

    onError: (err, _args, context) => {
      if (context?.previousApplications) {
        queryClient.setQueryData(
          context.applicationsKey,
          context.previousApplications,
        );
      }

      const cfg = getMoveErrorToastConfig(err as MoveApplicationError);
      toast.error(cfg.title, {
        description: cfg.description,
        duration: cfg.duration,
      });
    },

    onSettled: async (_data, _err, _args, context) => {
      if (context?.applicationsKey) {
        await queryClient.invalidateQueries({ queryKey: context.applicationsKey });
      }
      await queryClient.invalidateQueries({
        queryKey: [
          "scope", scope?.id, scope?.kind,
          "hiring", "application-counts-by-jobs",
        ],
      });
    },

    retry: (failureCount, err) => {
      const e = err as MoveApplicationError;
      return e.kind === "network" && failureCount < 3;
    },
    retryDelay: (attempt) => Math.min(1000 * 2 ** attempt, 8000),
  });
}
```

**Passo 3 — Portar `useApplicationsByJob` para useScopedQuery:**

Substituir implementação atual por (RESEARCH §1 lines 348-366):

```typescript
export function useApplicationsByJob(jobId: string | undefined) {
  return useScopedQuery<
    readonly ["hiring", "applications", "by-job", string],
    ApplicationWithCandidate[],
    Error
  >(
    ["hiring", "applications", "by-job", jobId ?? "none"] as const,
    async () => {
      if (!jobId) return [];
      const { data, error } = await supabase
        .from("applications")
        .select(
          "*, candidate:candidates!applications_candidate_id_fkey(id, full_name, email, anonymized_at)"
        )
        .eq("job_opening_id", jobId)
        .order("stage_entered_at", { ascending: true });
      if (error) throw error;
      return (data ?? []) as unknown as ApplicationWithCandidate[];
    },
    { enabled: !!jobId },
  );
}
```

**Passo 4 — Preservar outros exports:** NÃO mexer em `useReuseCandidateForJob`, `useDiscardApplication`, `useRestoreApplicationFromDiscard`, `useApplicationStageHistory`, `useEnsureApplicationsForJob` — apenas verificar que ainda compilam após import changes.

**Passo 5 — Verificar:**
```bash
npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep "useApplications" | head
npm test -- --run tests/hiring/useMoveApplicationStage.test.tsx 2>&1 | tail -10
```
  </action>
  <verify>
    <automated>npm test -- --run tests/hiring/useMoveApplicationStage.test.tsx 2>&1 | tail -8 && grep -c "onMutate\|cancelQueries\|setQueryData" src/hooks/hiring/useApplications.ts</automated>
  </verify>
  <done>`useApplications.ts` tem `useMoveApplicationStage` com onMutate/onError/onSettled (não mais `.eq('updated_at')`); `useApplicationsByJob` usa `useScopedQuery`; outros exports preservados; testes ativos e green; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create useApplicationsRealtime + activate test</name>
  <files>src/hooks/hiring/useApplicationsRealtime.ts, tests/hiring/useApplicationsRealtime.test.tsx</files>
  <read_first>
    src/shared/data/useScopedRealtime.ts (Phase 1 chokepoint — referência padrão),
    src/integrations/supabase/client.ts (supabase singleton),
    src/integrations/supabase/hiring-types.ts (ApplicationRow, ApplicationWithCandidate),
    .planning/phases/02-r-s-refactor/02-RESEARCH.md (§2 lines 387-491),
    .planning/phases/02-r-s-refactor/02-PATTERNS.md ("useApplicationsRealtime.ts" lines 208-249),
    tests/msw/realtime-mock.ts (criado em Plan 01)
  </read_first>
  <behavior>
    - Hook receive jobId. Se !jobId || !scope, retorna sem subscribe.
    - Subscribe a channel name `applications:job:{jobId}`.
    - postgres_changes UPDATE filter `job_opening_id=eq.${jobId}` → setQueryData merge silencioso (atualiza item por id).
    - postgres_changes INSERT mesmo filter → invalidate (rebuild join com candidate).
    - Cleanup no unmount: supabase.removeChannel(channel).
    - Effect deps: [jobId, scope?.id, scope?.kind, queryClient].
    - Não cria channel sem scope nem sem jobId.
  </behavior>
  <action>
**Passo 1 — Ativar test:** abrir `tests/hiring/useApplicationsRealtime.test.tsx`, remover skip, implementar com `createMockChannel` de `tests/msw/realtime-mock.ts`:

```typescript
import { renderHook, act } from "@testing-library/react";
import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
import { describe, it, expect, vi, beforeEach, afterEach } from "vitest";
import * as scopeModule from "@/app/providers/ScopeProvider";
import { supabase } from "@/integrations/supabase/client";
import { useApplicationsRealtime } from "@/hooks/hiring/useApplicationsRealtime";
import { createMockChannel, createRemoveChannelSpy } from "@/../tests/msw/realtime-mock";

function createWrapper() {
  const client = new QueryClient({ defaultOptions: { queries: { retry: false } } });
  return {
    client,
    Wrapper: ({ children }: { children: React.ReactNode }) => (
      <QueryClientProvider client={client}>{children}</QueryClientProvider>
    ),
  };
}

beforeEach(() => {
  vi.spyOn(scopeModule, "useScope").mockReturnValue({
    scope: { id: "company:abc", kind: "company", companyIds: ["c1"], userId: "u1" },
  } as ReturnType<typeof scopeModule.useScope>);
});
afterEach(() => { vi.restoreAllMocks(); });

describe("useApplicationsRealtime", () => {
  it("não cria channel sem jobId", () => {
    const channelSpy = vi.spyOn(supabase, "channel");
    const { Wrapper } = createWrapper();
    renderHook(() => useApplicationsRealtime(undefined), { wrapper: Wrapper });
    expect(channelSpy).not.toHaveBeenCalled();
  });

  it("subscribe ao channel applications:job:{jobId} no mount", () => {
    const mockChannel = createMockChannel();
    vi.spyOn(supabase, "channel").mockReturnValue(mockChannel as unknown as ReturnType<typeof supabase.channel>);
    const { Wrapper } = createWrapper();
    renderHook(() => useApplicationsRealtime("j1"), { wrapper: Wrapper });
    expect(supabase.channel).toHaveBeenCalledWith("applications:job:j1");
    expect(mockChannel.on).toHaveBeenCalled();
    expect(mockChannel.subscribe).toHaveBeenCalled();
  });

  it("UPDATE postgres_changes faz setQueryData merge silent", async () => {
    const { client, Wrapper } = createWrapper();
    const queryKey = ["scope", "company:abc", "company", "hiring", "applications", "by-job", "j1"] as const;
    client.setQueryData(queryKey, [
      { id: "app-1", stage: "em_interesse", stage_entered_at: "2026-04-20T00:00:00Z", candidate: { id: "c1", full_name: "Foo" } },
    ]);

    const mockChannel = createMockChannel();
    vi.spyOn(supabase, "channel").mockReturnValue(mockChannel as unknown as ReturnType<typeof supabase.channel>);
    renderHook(() => useApplicationsRealtime("j1"), { wrapper: Wrapper });

    act(() => {
      mockChannel.__emit("UPDATE", {
        new: { id: "app-1", stage: "entrevista_rh", stage_entered_at: "2026-04-27T00:00:00Z", updated_at: "2026-04-27T00:00:00Z", last_moved_by: "u2" },
        eventType: "UPDATE",
      });
    });

    const updated = client.getQueryData<{ id: string; stage: string }[]>(queryKey);
    expect(updated?.[0]).toMatchObject({ id: "app-1", stage: "entrevista_rh" });
  });

  it("removeChannel no unmount", () => {
    const mockChannel = createMockChannel();
    const removeSpy = createRemoveChannelSpy();
    vi.spyOn(supabase, "channel").mockReturnValue(mockChannel as unknown as ReturnType<typeof supabase.channel>);
    vi.spyOn(supabase, "removeChannel").mockImplementation(removeSpy);

    const { Wrapper } = createWrapper();
    const { unmount } = renderHook(() => useApplicationsRealtime("j1"), { wrapper: Wrapper });
    unmount();
    expect(removeSpy).toHaveBeenCalled();
  });
});
```

**Passo 2 — Implementar `src/hooks/hiring/useApplicationsRealtime.ts`** (verbatim from RESEARCH §2):

```typescript
import { useEffect } from "react";
import { useQueryClient } from "@tanstack/react-query";
import { supabase } from "@/integrations/supabase/client";
import { useScope } from "@/app/providers/ScopeProvider";
import type {
  ApplicationRow,
  ApplicationWithCandidate,
} from "@/integrations/supabase/hiring-types";

/**
 * Subscribe a um channel per-jobId. Quando outro RH move um candidato,
 * o payload chega aqui e atualizamos a cache do TanStack DIRETAMENTE
 * (silent re-render — D-04). Sem toast, sem flash.
 *
 * Cleanup: ao desmontar OU ao mudar jobId, remove o channel anterior.
 */
export function useApplicationsRealtime(jobId: string | undefined): void {
  const queryClient = useQueryClient();
  const { scope } = useScope();

  useEffect(() => {
    if (!jobId || !scope) return;

    const channelName = `applications:job:${jobId}`;
    const queryKey = [
      "scope", scope.id, scope.kind,
      "hiring", "applications", "by-job", jobId,
    ] as const;

    const channel = supabase
      .channel(channelName)
      .on(
        "postgres_changes",
        {
          event: "UPDATE",
          schema: "public",
          table: "applications",
          filter: `job_opening_id=eq.${jobId}`,
        },
        (payload) => {
          const updated = payload.new as ApplicationRow;

          queryClient.setQueryData<ApplicationWithCandidate[]>(
            queryKey,
            (old) => {
              if (!old) return old;
              return old.map((a) =>
                a.id === updated.id
                  ? {
                      ...a,
                      stage: updated.stage,
                      stage_entered_at: updated.stage_entered_at,
                      updated_at: updated.updated_at,
                      last_moved_by: updated.last_moved_by,
                    }
                  : a
              );
            },
          );
        },
      )
      .on(
        "postgres_changes",
        {
          event: "INSERT",
          schema: "public",
          table: "applications",
          filter: `job_opening_id=eq.${jobId}`,
        },
        () => {
          void queryClient.invalidateQueries({ queryKey });
        },
      )
      .subscribe();

    return () => {
      void supabase.removeChannel(channel);
    };
  }, [jobId, scope?.id, scope?.kind, queryClient]);
}
```

**Verificar:**
```bash
npm test -- --run tests/hiring/useApplicationsRealtime.test.tsx 2>&1 | tail -8
```
  </action>
  <verify>
    <automated>npm test -- --run tests/hiring/useApplicationsRealtime.test.tsx 2>&1 | tail -8 && grep -c "supabase.channel\|removeChannel" src/hooks/hiring/useApplicationsRealtime.ts</automated>
  </verify>
  <done>`useApplicationsRealtime.ts` exporta hook `useApplicationsRealtime(jobId)`; subscribe ao channel `applications:job:{jobId}`; UPDATE faz setQueryData merge; INSERT faz invalidate; cleanup no unmount via removeChannel; teste ativo e green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Port useApplicationCountsByJob to useScopedQuery + activate test</name>
  <files>src/hooks/hiring/useApplicationCountsByJob.ts, tests/hiring/useApplicationCountsByJob.test.tsx</files>
  <read_first>
    src/hooks/hiring/useApplicationCountsByJob.ts (estado atual — interface JobApplicationCounts já está correta),
    src/shared/data/useScopedQuery.ts (chokepoint Phase 1),
    src/lib/hiring/stageGroups.ts (StageGroupKey + STAGE_GROUP_BY_STAGE),
    .planning/phases/02-r-s-refactor/02-RESEARCH.md (§12 lines 1791-1819 sparkbar consume)
  </read_first>
  <behavior>
    - useApplicationCountsByJobs retorna `Record<string, JobApplicationCounts>` (key = jobId)
    - JobApplicationCounts shape: `{ total: number; byGroup: Record<StageGroupKey, number>; lastActivity: string | null; idleDays: number | null }`
    - queryKey: ['hiring', 'application-counts-by-jobs'] (chokepoint adiciona scope prefix)
    - Aceita array de jobIds (.in('job_opening_id', jobIds))
    - Agrupa rows por jobId E por STAGE_GROUP_BY_STAGE[stage]
  </behavior>
  <action>
**Passo 1 — Ativar test:**

```typescript
import { describe, it, expect, vi, beforeEach, afterEach, beforeAll, afterAll } from "vitest";
import { renderHook, waitFor } from "@testing-library/react";
import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
import { setupServer } from "msw/node";
import { http, HttpResponse } from "msw";
import * as scopeModule from "@/app/providers/ScopeProvider";
import { useApplicationCountsByJobs } from "@/hooks/hiring/useApplicationCountsByJob";

const SUPABASE_URL = "https://ehbxpbeijofxtsbezwxd.supabase.co";

const server = setupServer();
beforeAll(() => server.listen({ onUnhandledRequest: "warn" }));
afterEach(() => server.resetHandlers());
afterAll(() => server.close());

beforeEach(() => {
  vi.spyOn(scopeModule, "useScope").mockReturnValue({
    scope: { id: "company:abc", kind: "company", companyIds: ["c1"], userId: "u1" },
  } as ReturnType<typeof scopeModule.useScope>);
});
afterEach(() => { vi.restoreAllMocks(); });

function createWrapper() {
  const client = new QueryClient({ defaultOptions: { queries: { retry: false } } });
  return {
    client,
    Wrapper: ({ children }: { children: React.ReactNode }) => (
      <QueryClientProvider client={client}>{children}</QueryClientProvider>
    ),
  };
}

describe("useApplicationCountsByJobs — useScopedQuery + byGroup", () => {
  it("retorna byGroup com 6 keys (StageGroupKey)", async () => {
    server.use(
      http.get(`${SUPABASE_URL}/rest/v1/applications`, () =>
        HttpResponse.json([
          { id: "a1", job_opening_id: "j1", stage: "em_interesse", stage_entered_at: "2026-04-20T00:00:00Z" },
          { id: "a2", job_opening_id: "j1", stage: "fit_cultural", stage_entered_at: "2026-04-21T00:00:00Z" },
        ])
      )
    );

    const { Wrapper } = createWrapper();
    const { result } = renderHook(() => useApplicationCountsByJobs(["j1"]), { wrapper: Wrapper });
    await waitFor(() => expect(result.current.isSuccess).toBe(true));

    const counts = result.current.data?.["j1"];
    expect(counts?.byGroup).toBeDefined();
    expect(Object.keys(counts!.byGroup).length).toBeGreaterThanOrEqual(6);
    expect(counts!.total).toBe(2);
  });

  it("queryKey inclui scope.id (Phase 1 lock)", async () => {
    const { client, Wrapper } = createWrapper();
    server.use(http.get(`${SUPABASE_URL}/rest/v1/applications`, () => HttpResponse.json([])));
    renderHook(() => useApplicationCountsByJobs(["j1"]), { wrapper: Wrapper });

    await waitFor(() => {
      const queries = client.getQueryCache().getAll();
      const found = queries.find(
        (q) => Array.isArray(q.queryKey) && q.queryKey.includes("application-counts-by-jobs"),
      );
      expect(found?.queryKey?.[0]).toBe("scope");
      expect(found?.queryKey?.[1]).toBe("company:abc");
    });
  });
});
```

**Passo 2 — Implementar/refactor `useApplicationCountsByJob.ts`:**

Atual já calcula byGroup; mudança principal é portar para useScopedQuery:

```typescript
import { useScopedQuery } from "@/shared/data/useScopedQuery";
import { supabase } from "@/integrations/supabase/client";
import { STAGE_GROUP_BY_STAGE, type StageGroupKey } from "@/lib/hiring/stageGroups";
import type { ApplicationStage } from "@/integrations/supabase/hiring-types";

export interface JobApplicationCounts {
  total: number;
  byGroup: Record<StageGroupKey, number>;
  lastActivity: string | null;
  idleDays: number | null;
}

const EMPTY_BY_GROUP: Record<StageGroupKey, number> = {
  triagem: 0,
  checagem: 0,
  entrevista_rh: 0,
  entrevista_final: 0,
  decisao: 0,
  descartados: 0,
};

export function useApplicationCountsByJobs(jobIds: string[]) {
  return useScopedQuery<
    readonly ["hiring", "application-counts-by-jobs", string],
    Record<string, JobApplicationCounts>,
    Error
  >(
    ["hiring", "application-counts-by-jobs", jobIds.sort().join(",")] as const,
    async (): Promise<Record<string, JobApplicationCounts>> => {
      if (jobIds.length === 0) return {};

      const { data, error } = await supabase
        .from("applications")
        .select("id, job_opening_id, stage, stage_entered_at, updated_at")
        .in("job_opening_id", jobIds);

      if (error) throw error;

      const result: Record<string, JobApplicationCounts> = {};
      for (const jobId of jobIds) {
        result[jobId] = {
          total: 0,
          byGroup: { ...EMPTY_BY_GROUP },
          lastActivity: null,
          idleDays: null,
        };
      }

      const now = Date.now();
      for (const row of data ?? []) {
        const counts = result[row.job_opening_id as string];
        if (!counts) continue;
        counts.total += 1;
        const group = STAGE_GROUP_BY_STAGE[row.stage as ApplicationStage] as StageGroupKey | undefined;
        if (group && counts.byGroup[group] !== undefined) {
          counts.byGroup[group] += 1;
        }
        const activityAt = row.updated_at ?? row.stage_entered_at;
        if (activityAt && (!counts.lastActivity || activityAt > counts.lastActivity)) {
          counts.lastActivity = activityAt as string;
        }
      }

      // Compute idleDays
      for (const counts of Object.values(result)) {
        if (counts.lastActivity) {
          const ts = new Date(counts.lastActivity).getTime();
          counts.idleDays = Math.max(0, Math.floor((now - ts) / 86_400_000));
        }
      }

      return result;
    },
    { enabled: jobIds.length > 0, staleTime: 30_000 },
  );
}
```

**Preservar** se houver `useApplicationCountsByJob` (singular) no arquivo — adicionar wrapper `useApplicationCountsByJob(jobId)` que internamente chama o plural.

**Verificar:**
```bash
npm test -- --run tests/hiring/useApplicationCountsByJob.test.tsx 2>&1 | tail -8
npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep "useApplicationCountsByJob"
```
  </action>
  <verify>
    <automated>npm test -- --run tests/hiring/useApplicationCountsByJob.test.tsx 2>&1 | tail -8 && grep -c "useScopedQuery" src/hooks/hiring/useApplicationCountsByJob.ts</automated>
  </verify>
  <done>`useApplicationCountsByJob.ts` exporta `useApplicationCountsByJobs` usando `useScopedQuery`; retorna `Record<string, JobApplicationCounts>` com 6 keys em `byGroup`; teste ativo e green; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Supabase REST | UPDATE em applications passa pela RLS Phase 1 + DB trigger statusMachine. Server é source of truth. |
| Realtime channel → Browser | postgres_changes filtrado por jobId (RLS server-side garante visibilidade) — client filter é otimização. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | T (Tampering) | Race condition: refetch chega mid-mutate e sobrescreve optimistic | mitigate | `cancelQueries` em onMutate é mandatório (RESEARCH §1 line 244). Test cobre. |
| T-02-05-02 | I (Information disclosure) | console.log em onError exibe candidate name (PII) | mitigate | toast.error usa apenas getMoveErrorToastConfig (sem PII no payload). CLAUDE.md "No PII in console.log" + Phase 1 logger. |
| T-02-05-03 | T (Tampering) | Realtime payload de stage = legacy contorna trigger | accept | Plan 02 instalou tg_block_legacy_stages que bloqueia INSERT/UPDATE com legacy. Realtime apenas reflete o que o servidor já gravou. |
| T-02-05-04 | D (DoS) | useApplicationsRealtime cria N channels em re-render | mitigate | useEffect deps `[jobId, scope?.id, scope?.kind, queryClient]` — re-subscribe só em mudança real. Test cobre cleanup. |
| T-02-05-05 | I (Information disclosure) | postgres_changes vaza eventos cross-job (filter no client é fraco) | mitigate | RLS no servidor filtra antes de emitir; filter `job_opening_id=eq.${jobId}` é apenas otimização cliente-side. |
</threat_model>

<verification>
- 3 hooks em `src/hooks/hiring/` modificados/criados
- 3 testes Wave 0 ativos (sem `.skip`) e green
- `npm test -- --run tests/hiring/{useMoveApplicationStage,useApplicationsRealtime,useApplicationCountsByJob}*` exit 0
- `npx tsc --noEmit -p tsconfig.app.json` retorna 0 errors em `src/hooks/hiring/`
- Novos hooks usam `useScopedQuery` chokepoint (não mais raw `useQuery`)
- `useMoveApplicationStage` rewrite SEM `expectedUpdatedAt` SEM optimistic locking
- `useApplicationsRealtime` cleanup correto no unmount (test verifica `removeChannel`)
</verification>

<success_criteria>
1. Bug #1 do projeto (kanban) tem todos os requisitos satisfeitos no nível de hook: optimistic + rollback + retry network + 4-tipo error + canTransition será chamado pelo caller (Plan 06).
2. queryKey shape consistente em todos os hooks: `['scope', scope.id, scope.kind, 'hiring', ...]`.
3. Realtime per-jobId implementado com cleanup atômico — sem channel leak.
4. Counts byGroup alimentam sparkbar (Plan 06) sem overhead.
5. Wave 4 (UI) tem todos os hooks prontos para consumir.
</success_criteria>

<output>
Após completar, criar `.planning/phases/02-r-s-refactor/02-05-SUMMARY.md` com:
- Diff resumido de useApplications.ts (linhas removidas: optimistic locking; linhas adicionadas: onMutate/onError/onSettled)
- Output de `npm test -- --run` mostrando 80+ tests green entre Plans 01-05
- Confirmação de `useScopedQuery` import em todos os 3 hooks
- Próximo plano: `02-06-PLAN.md` (LGPD hooks + Edge Function)
</output>

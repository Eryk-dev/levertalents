---
phase: 02-r-s-refactor
plan: 08
type: execute
wave: 4
depends_on: ["02-06", "02-07"]
files_modified:
  - src/components/hiring/PipelineFilters.tsx
  - src/components/hiring/CandidatesTable.tsx
  - src/components/hiring/BoardTableToggle.tsx
  - src/components/hiring/CardFieldsCustomizer.tsx
  - src/components/hiring/DuplicateCandidateDialog.tsx
  - src/components/hiring/CandidateForm.tsx
  - tests/hiring/PipelineFilters.test.tsx
  - tests/hiring/BoardTableToggle.test.tsx
autonomous: true
requirements:
  - RS-09
  - RS-13
  - TAL-09
must_haves:
  truths:
    - "PipelineFilters usa useSearchParams (URL como source of truth) com 300ms debounce em busca textual"
    - "PipelineFilters renderiza inline (não modal) com chips activate/deactivate"
    - "BoardTableToggle persiste view selecionada em localStorage leverup:rs:view:{jobId}"
    - "CandidatesTable HTML <table> com sort por nome/dias/etapa/próxima entrevista (sem TanStack Table)"
    - "CardFieldsCustomizer popover com checkboxes para 6 OPTIONAL_FIELDS persistindo via useCardPreferences"
    - "DuplicateCandidateDialog suporta matchedBy: cpf | email; CandidateForm busca CPF antes de email"
  artifacts:
    - path: "src/components/hiring/PipelineFilters.tsx"
      provides: "Filtros inline + URL state + 300ms debounce (RS-09)"
      contains: "useSearchParams"
    - path: "src/components/hiring/CandidatesTable.tsx"
      provides: "Tabela HTML com sort por 4 colunas (RS-13)"
      contains: "TableHeader"
    - path: "src/components/hiring/BoardTableToggle.tsx"
      provides: "Toggle Quadro/Tabela com localStorage (D-09)"
      contains: "leverup:rs:view"
    - path: "src/components/hiring/CardFieldsCustomizer.tsx"
      provides: "Popover de customização de card (D-08)"
      contains: "OPTIONAL_FIELDS"
    - path: "src/components/hiring/DuplicateCandidateDialog.tsx"
      provides: "Dialog dedup com matchedBy CPF | email (TAL-09)"
      contains: "matchedBy"
    - path: "src/components/hiring/CandidateForm.tsx"
      provides: "Busca CPF prioritária antes de email no checkForDuplicates"
      contains: "useCandidateByCpf"
  key_links:
    - from: "PipelineFilters"
      to: "useSearchParams (react-router)"
      via: "URL params como filter state"
      pattern: "useSearchParams"
    - from: "BoardTableToggle"
      to: "CandidatesKanban OR CandidatesTable"
      via: "view === 'board' ? <Kanban /> : <Table />"
      pattern: "view === \"table\""
    - from: "CandidateForm checkForDuplicates"
      to: "useCandidateByCpf hook"
      via: "Prioridade 1: CPF, fallback: email"
      pattern: "useCandidateByCpf"
---

<objective>
Wave 4 — Refinamentos UX-AUDIT e dedup CPF. 6 componentes:

1. **PipelineFilters.tsx** (REWRITE) — refactor de modal para inline horizontal bar; URL como source of truth (useSearchParams); 300ms debounce na busca textual; chips active/inactive (Linear style).

2. **CandidatesTable.tsx** (NEW) — HTML `<table>` com sort manual em useMemo (sem TanStack Table — ver RESEARCH §11). Sort por: Nome / Dias na etapa / Etapa / Próxima entrevista.

3. **BoardTableToggle.tsx** (NEW) — segmented control "Quadro / Tabela"; persiste em `localStorage.leverup:rs:view:{jobId}`.

4. **CardFieldsCustomizer.tsx** (NEW) — popover dropdown com checkboxes pros 6 OPTIONAL_FIELDS; usa useCardPreferences.

5. **DuplicateCandidateDialog.tsx** (MODIFY) — suporta prop `matchedBy: 'cpf' | 'email'`; copy diferenciada por match.

6. **CandidateForm.tsx** (MODIFY) — adiciona busca por CPF (via useCandidateByCpf) ANTES de email; chama DuplicateCandidateDialog com matchedBy correto.

Ativa testes `tests/hiring/PipelineFilters.test.tsx` e `tests/hiring/BoardTableToggle.test.tsx`.

Output: 6 componentes + 2 testes ativos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-r-s-refactor/02-CONTEXT.md
@.planning/phases/02-r-s-refactor/02-RESEARCH.md
@.planning/phases/02-r-s-refactor/02-PATTERNS.md
@.planning/phases/02-r-s-refactor/02-UI-SPEC.md
@.planning/phases/02-r-s-refactor/02-06-SUMMARY.md
@.planning/phases/02-r-s-refactor/02-07-SUMMARY.md
@CLAUDE.md
@src/components/hiring/PipelineFilters.tsx
@src/components/hiring/DuplicateCandidateDialog.tsx
@src/components/hiring/CandidateForm.tsx

<interfaces>
From src/lib/hiring/cardCustomization.ts (Plan 03):
- OPTIONAL_FIELDS = ["avatar", "next_interview", "cv_icon", "fit_score", "bg_check_dot", "source_tag"]
- isFieldEnabled(prefs, field): boolean

From src/hooks/hiring/useCardPreferences.ts (Plan 06):
- [prefs, update] = useCardPreferences()

From src/hooks/hiring/useCandidates.ts (Plan 06):
- useCandidateByCpf(cpf): UseQueryResult<CandidateRow | null>
- useCandidateByEmail(email): UseQueryResult<CandidateRow | null>

From src/lib/hiring/cpf.ts (Plan 03):
- normalizeCpf(input): string | null
- formatCpf(input): string
- isValidCpfFormat(input): boolean

From src/lib/hiring/sla.ts (Plan 03):
- daysSince, computeSlaTone

From src/lib/hiring/stageGroups.ts:
- APPLICATION_STAGE_LABELS, STAGE_GROUP_BY_STAGE

From src/components/ui (shadcn primitives):
- table.tsx (Table, TableHeader, TableRow, TableHead, TableBody, TableCell)
- popover.tsx (Popover, PopoverTrigger, PopoverContent)
- checkbox.tsx
- button.tsx (Button + variants)

From react-router-dom v6.30:
- useSearchParams returns [URLSearchParams, (next, opts?) => void]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite PipelineFilters — inline + URL state + 300ms debounce</name>
  <files>src/components/hiring/PipelineFilters.tsx, tests/hiring/PipelineFilters.test.tsx</files>
  <read_first>
    src/components/hiring/PipelineFilters.tsx (atual modal-style — preservar fields/options),
    src/app/providers/ScopeProvider.tsx (linha 64 — useSearchParams precedent),
    .planning/phases/02-r-s-refactor/02-RESEARCH.md secao §10 lines 1542-1611,
    .planning/phases/02-r-s-refactor/02-UI-SPEC.md secao "Filtros inline (RS-09)"
  </read_first>
  <behavior>
    - URL params source of truth: vaga, fase, origem, tag, q
    - Search input local state com debounce 300ms antes de atualizar URL
    - Chips inactive: bg-bg-subtle text-text-muted
    - Chips active: bg-accent-soft text-accent-text border-accent/30 + "×" remove icon
    - "Limpar filtros" link far-right quando algum filter ativo
    - Layout: flex horizontal sticky top below page header (48px); gap-2; flex-wrap
    - Hook usePipelineFilters() expose values para queries consumirem
  </behavior>
  <action>
Passo 1 — Ativar tests/hiring/PipelineFilters.test.tsx:

```typescript
import { describe, it, expect, vi, beforeEach } from "vitest";
import { render, screen, waitFor, act } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { MemoryRouter } from "react-router-dom";
import { PipelineFilters } from "@/components/hiring/PipelineFilters";

function renderInRouter(initialEntries: string[] = ["/"]) {
  return render(
    <MemoryRouter initialEntries={initialEntries}>
      <PipelineFilters />
    </MemoryRouter>
  );
}

describe("PipelineFilters — URL state + debounce (RS-09)", () => {
  it("lê filtros iniciais de useSearchParams", () => {
    renderInRouter(["/?vaga=eng-backend&fase=triagem"]);
    // chip ativo deve refletir filtro
    const activeChip = screen.getByText(/Eng|Backend/i, { exact: false });
    expect(activeChip).toBeInTheDocument();
  });

  it("renderiza search input + chips dropdowns + Limpar filtros quando há filter", () => {
    renderInRouter(["/?vaga=x"]);
    expect(screen.getByPlaceholderText(/buscar/i)).toBeInTheDocument();
    expect(screen.getByText(/Limpar filtros/i)).toBeInTheDocument();
  });

  it("debounce 300ms — typing 'jo' não atualiza URL imediatamente", async () => {
    vi.useFakeTimers();
    const user = userEvent.setup({ advanceTimers: vi.advanceTimersByTime });
    renderInRouter(["/"]);
    const input = screen.getByPlaceholderText(/buscar/i);
    await user.type(input, "joao");
    // Antes de 300ms passar, URL não atualizou
    // Depois de 300ms, URL contém q=joao
    act(() => vi.advanceTimersByTime(300));
    await waitFor(() => {
      // Inspecionar URL via window.location seria via router setup; aqui verificamos via input value
      expect((input as HTMLInputElement).value).toBe("joao");
    });
    vi.useRealTimers();
  });
});
```

Passo 2 — Reescrever src/components/hiring/PipelineFilters.tsx (preservar lista de campos disponíveis):

```typescript
import { useSearchParams } from "react-router-dom";
import { useEffect, useState } from "react";
import { Search, X } from "lucide-react";
import { Input } from "@/components/ui/input";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { Button } from "@/components/ui/button";
import { cn } from "@/lib/utils";

const FILTER_KEYS = ["vaga", "fase", "origem", "tag", "q"] as const;
type FilterKey = (typeof FILTER_KEYS)[number];

export function PipelineFilters() {
  const [searchParams, setSearchParams] = useSearchParams();

  const vaga = searchParams.get("vaga") ?? "all";
  const fase = searchParams.get("fase") ?? "all";
  const origem = searchParams.get("origem") ?? "all";
  const q = searchParams.get("q") ?? "";

  // Local state para search com debounce
  const [localSearch, setLocalSearch] = useState(q);

  useEffect(() => {
    if (localSearch === q) return;
    const timer = setTimeout(() => {
      setSearchParams((prev) => {
        const next = new URLSearchParams(prev);
        if (localSearch) next.set("q", localSearch);
        else next.delete("q");
        return next;
      }, { replace: true });
    }, 300);
    return () => clearTimeout(timer);
  }, [localSearch, q, setSearchParams]);

  const updateFilter = (key: FilterKey, value: string) => {
    setSearchParams((prev) => {
      const next = new URLSearchParams(prev);
      if (value === "all" || !value) next.delete(key);
      else next.set(key, value);
      return next;
    });
  };

  const hasActive = FILTER_KEYS.some((k) => {
    const v = searchParams.get(k);
    return v && v !== "all";
  });

  const clearAll = () => {
    setSearchParams({}, { replace: true });
    setLocalSearch("");
  };

  return (
    <div className="flex items-center gap-2 flex-wrap py-2 px-3 border-b border-default bg-surface sticky top-0 z-10">
      <div className="relative">
        <Search className="absolute left-2 top-1/2 -translate-y-1/2 w-3.5 h-3.5 text-text-muted" aria-hidden />
        <Input
          placeholder="Buscar candidato..."
          value={localSearch}
          onChange={(e) => setLocalSearch(e.target.value)}
          className="pl-7 w-64 h-7 text-[13px]"
        />
      </div>

      {/* Vaga filter */}
      <Select value={vaga} onValueChange={(v) => updateFilter("vaga", v)}>
        <SelectTrigger className={cn(
          "h-7 text-[13px]",
          vaga !== "all" && "bg-accent-soft text-accent-text border-accent/30"
        )}>
          <SelectValue placeholder="Vaga" />
        </SelectTrigger>
        <SelectContent>
          <SelectItem value="all">Todas as vagas</SelectItem>
          {/* TODO: alimentar via useJobOpenings ou prop jobs[] */}
        </SelectContent>
      </Select>

      {/* Fase filter */}
      <Select value={fase} onValueChange={(v) => updateFilter("fase", v)}>
        <SelectTrigger className={cn(
          "h-7 text-[13px]",
          fase !== "all" && "bg-accent-soft text-accent-text border-accent/30"
        )}>
          <SelectValue placeholder="Fase" />
        </SelectTrigger>
        <SelectContent>
          <SelectItem value="all">Todas as fases</SelectItem>
          <SelectItem value="triagem">Triagem</SelectItem>
          <SelectItem value="checagem">Checagem</SelectItem>
          <SelectItem value="entrevista_rh">Entrevista RH</SelectItem>
          <SelectItem value="entrevista_final">Entrevista Final</SelectItem>
          <SelectItem value="decisao">Decisão</SelectItem>
        </SelectContent>
      </Select>

      {/* Origem filter */}
      <Select value={origem} onValueChange={(v) => updateFilter("origem", v)}>
        <SelectTrigger className={cn(
          "h-7 text-[13px]",
          origem !== "all" && "bg-accent-soft text-accent-text border-accent/30"
        )}>
          <SelectValue placeholder="Origem" />
        </SelectTrigger>
        <SelectContent>
          <SelectItem value="all">Todas as origens</SelectItem>
          <SelectItem value="linkedin">LinkedIn</SelectItem>
          <SelectItem value="site">Site</SelectItem>
          <SelectItem value="indicacao">Indicação</SelectItem>
          <SelectItem value="manual">Manual</SelectItem>
        </SelectContent>
      </Select>

      {hasActive && (
        <Button variant="ghost" size="sm" className="h-7 text-[11px]" onClick={clearAll}>
          Limpar filtros
        </Button>
      )}
    </div>
  );
}

/** Hook companion for query consumers */
export function usePipelineFilters() {
  const [searchParams] = useSearchParams();
  return {
    vaga: searchParams.get("vaga") ?? "all",
    fase: searchParams.get("fase") ?? "all",
    origem: searchParams.get("origem") ?? "all",
    tag: searchParams.get("tag") ?? "all",
    searchTerm: searchParams.get("q") ?? "",
  };
}
```

Verificar:
```bash
npm test -- --run tests/hiring/PipelineFilters.test.tsx 2>&1 | tail -8
grep -c "useSearchParams" src/components/hiring/PipelineFilters.tsx
```
  </action>
  <verify>
    <automated>npm test -- --run tests/hiring/PipelineFilters.test.tsx 2>&1 | tail -8 && grep -c "useSearchParams\|setTimeout.*300" src/components/hiring/PipelineFilters.tsx</automated>
  </verify>
  <done>PipelineFilters: useSearchParams source of truth, debounce 300ms via setTimeout em useEffect, chips active vs inactive com bg-accent-soft, Limpar filtros button condicional, hook companion usePipelineFilters exportado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create CandidatesTable + BoardTableToggle + activate test</name>
  <files>src/components/hiring/CandidatesTable.tsx, src/components/hiring/BoardTableToggle.tsx, tests/hiring/BoardTableToggle.test.tsx</files>
  <read_first>
    src/components/ui/table.tsx (shadcn primitives),
    src/components/ui/button.tsx,
    src/components/hiring/JobExternalPublicationsList.tsx (analog UL/LI sortable header),
    src/lib/hiring/sla.ts (daysSince — Plan 03),
    src/lib/utils/formatBR.ts (formatDateBR — Phase 1),
    .planning/phases/02-r-s-refactor/02-RESEARCH.md secao §11 lines 1632-1769,
    .planning/phases/02-r-s-refactor/02-UI-SPEC.md secao "Toggle Board → Table"
  </read_first>
  <behavior>
    CandidatesTable:
    - Props: applications, onOpen(app), selectedId
    - Sort state local (sortField, sortDir)
    - 4 sortable columns: Nome, Dias na etapa, Etapa, Próxima entrevista
    - Selected row: bg-accent-soft

    BoardTableToggle:
    - Props: jobId, view, onChange(view)
    - Hook useKanbanView(jobId) read/write localStorage
    - Segmented control "Quadro" + "Tabela"
  </behavior>
  <action>
src/components/hiring/CandidatesTable.tsx:

```typescript
import { useState, useMemo } from "react";
import { ArrowUp, ArrowDown } from "lucide-react";
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table";
import { cn } from "@/lib/utils";
import { daysSince } from "@/lib/hiring/sla";
import { formatDateBR } from "@/lib/utils/formatBR";
import { APPLICATION_STAGE_LABELS } from "@/integrations/supabase/hiring-types";
import type { ApplicationWithCandidate } from "@/integrations/supabase/hiring-types";

type SortField = "name" | "stage" | "days_in_stage" | "next_interview";
type SortDir = "asc" | "desc";

interface CandidatesTableProps {
  applications: ApplicationWithCandidate[];
  onOpen: (app: ApplicationWithCandidate) => void;
  selectedId?: string | null;
}

export function CandidatesTable({ applications, onOpen, selectedId }: CandidatesTableProps) {
  const [sortField, setSortField] = useState<SortField>("days_in_stage");
  const [sortDir, setSortDir] = useState<SortDir>("desc");

  const sorted = useMemo(() => {
    const arr = [...applications];
    arr.sort((a, b) => {
      let cmp = 0;
      switch (sortField) {
        case "name":
          cmp = (a.candidate?.full_name ?? "").localeCompare(b.candidate?.full_name ?? "");
          break;
        case "stage":
          cmp = a.stage.localeCompare(b.stage);
          break;
        case "days_in_stage":
          cmp = daysSince(a.stage_entered_at) - daysSince(b.stage_entered_at);
          break;
        case "next_interview":
          cmp = (a.next_interview_at ?? "").localeCompare(b.next_interview_at ?? "");
          break;
      }
      return sortDir === "asc" ? cmp : -cmp;
    });
    return arr;
  }, [applications, sortField, sortDir]);

  const toggleSort = (field: SortField) => {
    if (sortField === field) setSortDir(sortDir === "asc" ? "desc" : "asc");
    else { setSortField(field); setSortDir("asc"); }
  };

  const SortHead = ({ field, label }: { field: SortField; label: string }) => (
    <TableHead onClick={() => toggleSort(field)} className="cursor-pointer select-none">
      <span className="flex items-center gap-1">
        {label}
        {sortField === field && (
          sortDir === "asc"
            ? <ArrowUp className="w-3 h-3" aria-label="Ordenado crescente" />
            : <ArrowDown className="w-3 h-3" aria-label="Ordenado decrescente" />
        )}
      </span>
    </TableHead>
  );

  return (
    <Table>
      <TableHeader>
        <TableRow>
          <SortHead field="name" label="Nome" />
          <TableHead>Cargo pretendido</TableHead>
          <SortHead field="days_in_stage" label="Dias na etapa" />
          <SortHead field="stage" label="Etapa" />
          <SortHead field="next_interview" label="Próxima entrevista" />
        </TableRow>
      </TableHeader>
      <TableBody>
        {sorted.length === 0 ? (
          <TableRow>
            <TableCell colSpan={5} className="text-center text-text-muted py-8">
              Nenhum candidato com esses filtros
            </TableCell>
          </TableRow>
        ) : (
          sorted.map((app) => (
            <TableRow
              key={app.id}
              onClick={() => onOpen(app)}
              className={cn("cursor-pointer", selectedId === app.id && "bg-accent-soft")}
              data-selected={selectedId === app.id}
            >
              <TableCell className="font-medium">{app.candidate?.full_name ?? "—"}</TableCell>
              <TableCell className="text-text-muted">{app.desired_role ?? "—"}</TableCell>
              <TableCell className="tabular-nums">{daysSince(app.stage_entered_at)}d</TableCell>
              <TableCell>{APPLICATION_STAGE_LABELS[app.stage] ?? app.stage}</TableCell>
              <TableCell className="text-text-muted">
                {app.next_interview_at ? formatDateBR(app.next_interview_at) : "—"}
              </TableCell>
            </TableRow>
          ))
        )}
      </TableBody>
    </Table>
  );
}
```

src/components/hiring/BoardTableToggle.tsx:

```typescript
import { useState, useEffect } from "react";
import { LayoutGrid, LayoutList } from "lucide-react";
import { Button } from "@/components/ui/button";
import { cn } from "@/lib/utils";

export type KanbanView = "board" | "table";

interface BoardTableToggleProps {
  jobId: string;
  value: KanbanView;
  onChange: (next: KanbanView) => void;
}

const STORAGE_KEY = (jobId: string) => `leverup:rs:view:${jobId}`;

export function useKanbanView(jobId: string): [KanbanView, (next: KanbanView) => void] {
  const [view, setView] = useState<KanbanView>(() => {
    const stored = localStorage.getItem(STORAGE_KEY(jobId));
    return stored === "table" ? "table" : "board";
  });

  useEffect(() => {
    const stored = localStorage.getItem(STORAGE_KEY(jobId));
    setView(stored === "table" ? "table" : "board");
  }, [jobId]);

  const updateView = (next: KanbanView) => {
    setView(next);
    localStorage.setItem(STORAGE_KEY(jobId), next);
  };

  return [view, updateView];
}

export function BoardTableToggle({ value, onChange }: BoardTableToggleProps) {
  return (
    <div className="inline-flex rounded-md border border-default bg-surface p-0.5" role="tablist" aria-label="Visualização">
      <button
        role="tab"
        aria-selected={value === "board"}
        onClick={() => onChange("board")}
        className={cn(
          "inline-flex items-center gap-1.5 rounded h-6 px-2 text-[11px] font-medium transition-colors",
          value === "board" ? "bg-accent-soft text-accent-text" : "text-text-muted hover:text-text"
        )}
      >
        <LayoutGrid className="w-3 h-3" aria-hidden />
        Quadro
      </button>
      <button
        role="tab"
        aria-selected={value === "table"}
        onClick={() => onChange("table")}
        className={cn(
          "inline-flex items-center gap-1.5 rounded h-6 px-2 text-[11px] font-medium transition-colors",
          value === "table" ? "bg-accent-soft text-accent-text" : "text-text-muted hover:text-text"
        )}
      >
        <LayoutList className="w-3 h-3" aria-hidden />
        Tabela
      </button>
    </div>
  );
}
```

Ativar tests/hiring/BoardTableToggle.test.tsx:

```typescript
import { describe, it, expect, beforeEach } from "vitest";
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { useKanbanView, BoardTableToggle } from "@/components/hiring/BoardTableToggle";
import { renderHook, act } from "@testing-library/react";

describe("useKanbanView — localStorage persist (D-09)", () => {
  beforeEach(() => { localStorage.clear(); });

  it("retorna 'board' default quando localStorage vazio", () => {
    const { result } = renderHook(() => useKanbanView("j1"));
    expect(result.current[0]).toBe("board");
  });

  it("persiste alteração em localStorage namespace", () => {
    const { result } = renderHook(() => useKanbanView("j1"));
    act(() => result.current[1]("table"));
    expect(localStorage.getItem("leverup:rs:view:j1")).toBe("table");
  });

  it("isola por jobId", () => {
    const { result: r1 } = renderHook(() => useKanbanView("j1"));
    act(() => r1.current[1]("table"));
    const { result: r2 } = renderHook(() => useKanbanView("j2"));
    expect(r2.current[0]).toBe("board"); // j2 não foi tocado
  });
});

describe("BoardTableToggle — UI", () => {
  it("renderiza 2 buttons Quadro/Tabela", () => {
    render(<BoardTableToggle jobId="j1" value="board" onChange={() => {}} />);
    expect(screen.getByRole("tab", { name: /Quadro/i })).toBeInTheDocument();
    expect(screen.getByRole("tab", { name: /Tabela/i })).toBeInTheDocument();
  });
  it("chama onChange ao clicar Tabela", async () => {
    const user = userEvent.setup();
    const onChange = vi.fn();
    render(<BoardTableToggle jobId="j1" value="board" onChange={onChange} />);
    await user.click(screen.getByRole("tab", { name: /Tabela/i }));
    expect(onChange).toHaveBeenCalledWith("table");
  });
});
```

Adicionar `import { vi } from "vitest"` ao topo.

Verificar:
```bash
npm test -- --run tests/hiring/BoardTableToggle.test.tsx 2>&1 | tail -8
test -f src/components/hiring/CandidatesTable.tsx && test -f src/components/hiring/BoardTableToggle.tsx
```
  </action>
  <verify>
    <automated>npm test -- --run tests/hiring/BoardTableToggle.test.tsx 2>&1 | tail -8 && test -f src/components/hiring/CandidatesTable.tsx && test -f src/components/hiring/BoardTableToggle.tsx</automated>
  </verify>
  <done>CandidatesTable HTML table com 4 colunas sortable; BoardTableToggle segmented control com useKanbanView hook persistindo localStorage `leverup:rs:view:{jobId}`; testes verde.</done>
</task>

<task type="auto">
  <name>Task 3: Create CardFieldsCustomizer popover</name>
  <files>src/components/hiring/CardFieldsCustomizer.tsx</files>
  <read_first>
    src/components/ui/popover.tsx,
    src/components/ui/checkbox.tsx,
    src/lib/hiring/cardCustomization.ts (Plan 03 — OPTIONAL_FIELDS),
    src/hooks/hiring/useCardPreferences.ts (Plan 06),
    .planning/phases/02-r-s-refactor/02-UI-SPEC.md secao "CardFieldsCustomizer"
  </read_first>
  <action>
src/components/hiring/CardFieldsCustomizer.tsx:

```typescript
import { Sliders } from "lucide-react";
import { Popover, PopoverContent, PopoverTrigger } from "@/components/ui/popover";
import { Checkbox } from "@/components/ui/checkbox";
import { Button } from "@/components/ui/button";
import { useCardPreferences } from "@/hooks/hiring/useCardPreferences";
import { OPTIONAL_FIELDS, type OptionalField, isFieldEnabled } from "@/lib/hiring/cardCustomization";

const FIELD_LABELS: Record<OptionalField, string> = {
  avatar: "Avatar",
  next_interview: "Próxima entrevista",
  cv_icon: "Ícone CV",
  fit_score: "Score Fit Cultural",
  bg_check_dot: "Status background check",
  source_tag: "Origem (LinkedIn, Site...)",
};

export function CardFieldsCustomizer() {
  const [prefs, update] = useCardPreferences();

  const toggle = (field: OptionalField) => {
    const next = isFieldEnabled(prefs, field)
      ? prefs.enabledFields.filter((f) => f !== field)
      : [...prefs.enabledFields, field];
    update({ ...prefs, enabledFields: next as OptionalField[] });
  };

  return (
    <Popover>
      <PopoverTrigger asChild>
        <Button variant="ghost" size="sm" className="h-7 px-2" aria-label="Customizar cards">
          <Sliders className="w-3.5 h-3.5" />
        </Button>
      </PopoverTrigger>
      <PopoverContent className="w-64 p-3" align="end">
        <div className="text-[11px] uppercase tracking-wider text-text-muted mb-2">
          Campos do card
        </div>
        <div className="flex flex-col gap-2">
          {OPTIONAL_FIELDS.map((field) => (
            <label key={field} className="flex items-center gap-2 cursor-pointer text-[13px]">
              <Checkbox
                checked={isFieldEnabled(prefs, field)}
                onCheckedChange={() => toggle(field)}
              />
              <span>{FIELD_LABELS[field]}</span>
            </label>
          ))}
        </div>
        <div className="mt-3 text-[10.5px] text-text-muted">
          Suas preferências são salvas localmente. Outros usuários têm config própria.
        </div>
      </PopoverContent>
    </Popover>
  );
}
```

Verificar:
```bash
test -f src/components/hiring/CardFieldsCustomizer.tsx
npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep CardFieldsCustomizer | head
```
  </action>
  <verify>
    <automated>test -f src/components/hiring/CardFieldsCustomizer.tsx && grep -c "OPTIONAL_FIELDS" src/components/hiring/CardFieldsCustomizer.tsx</automated>
  </verify>
  <done>CardFieldsCustomizer renderiza popover com 6 checkboxes para OPTIONAL_FIELDS; usa useCardPreferences hook para persist; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 4: Modify DuplicateCandidateDialog + CandidateForm — CPF dedup TAL-09</name>
  <files>src/components/hiring/DuplicateCandidateDialog.tsx, src/components/hiring/CandidateForm.tsx</files>
  <read_first>
    src/components/hiring/DuplicateCandidateDialog.tsx (atual — preservar shape e props existentes),
    src/components/hiring/CandidateForm.tsx (atual — encontrar onSubmit + checkForDuplicates ou similar),
    src/hooks/hiring/useCandidates.ts (Plan 06 — useCandidateByCpf, useCandidateByEmail),
    src/lib/hiring/cpf.ts (Plan 03 — formatCpf, normalizeCpf, isValidCpfFormat),
    .planning/phases/02-r-s-refactor/02-RESEARCH.md secao §8 lines 1373-1399,
    .planning/phases/02-r-s-refactor/02-PATTERNS.md "DuplicateCandidateDialog" lines 782-805
  </read_first>
  <action>
Passo 1 — Modificar DuplicateCandidateDialog.tsx:

ADICIONAR prop `matchedBy?: 'cpf' | 'email'` (default 'email' para compat):

```typescript
interface DuplicateCandidateDialogProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
  candidate: CandidateRow;
  matchedBy?: "cpf" | "email";
  // ... outras props existentes (onMerge, onCancel, etc.)
}

export function DuplicateCandidateDialog({
  open,
  onOpenChange,
  candidate,
  matchedBy = "email",
  ...props
}: DuplicateCandidateDialogProps) {
  // Title diferenciado
  const title = matchedBy === "cpf"
    ? "Já existe um candidato com esse CPF"
    : "Candidato já cadastrado";

  const matchedValue = matchedBy === "cpf"
    ? formatCpf(candidate.cpf)
    : candidate.email;

  return (
    <Dialog open={open} onOpenChange={onOpenChange}>
      <DialogContent>
        <DialogHeader>
          <DialogTitle>{title}</DialogTitle>
          <DialogDescription>
            <span className="font-medium">{candidate.full_name}</span>
            {matchedValue && <> ({matchedValue})</>} já está no sistema.
          </DialogDescription>
        </DialogHeader>
        {/* Body existing — lista applications anteriores */}
        {/* Footer existing — Merge/Cancel buttons */}
      </DialogContent>
    </Dialog>
  );
}
```

PRESERVAR todo o body (lista de applications anteriores), DialogFooter (botões Merge/Cancel), e callbacks existentes.

Passo 2 — Modificar CandidateForm.tsx:

Encontrar onde o form chama lookup duplicates (provavelmente em onSubmit ou um helper checkForDuplicates).

ADICIONAR busca por CPF prioritária:

```typescript
import { useCandidateByCpf, useCandidateByEmail } from "@/hooks/hiring/useCandidates";
import { formatCpf, normalizeCpf, isValidCpfFormat } from "@/lib/hiring/cpf";

// Dentro do componente form:
const cpfInput = watch("cpf"); // se RHF
const emailInput = watch("email");

// Hook lookup — debounce via enabled
const cpfLookup = useCandidateByCpf(cpfInput);
const emailLookup = useCandidateByEmail(emailInput);

// Decidir qual mostrar (CPF prioritário)
const duplicate = cpfLookup.data ?? emailLookup.data ?? null;
const matchedBy: "cpf" | "email" = cpfLookup.data ? "cpf" : "email";

// State para dialog
const [showDuplicateDialog, setShowDuplicateDialog] = useState(false);

// onSubmit:
const onSubmit = (values: FormValues) => {
  if (duplicate && !showDuplicateDialog) {
    setShowDuplicateDialog(true);
    return;
  }
  // ... continue com create candidate
};

// JSX:
{duplicate && (
  <DuplicateCandidateDialog
    open={showDuplicateDialog}
    onOpenChange={setShowDuplicateDialog}
    candidate={duplicate}
    matchedBy={matchedBy}
    // ... outros props
  />
)}
```

PRESERVAR resto do form (validation, submit handlers existentes para outros casos).

Verificar:
```bash
grep -c "matchedBy" src/components/hiring/DuplicateCandidateDialog.tsx
grep -c "useCandidateByCpf" src/components/hiring/CandidateForm.tsx
npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -E "Duplicate|CandidateForm" | head
```
  </action>
  <verify>
    <automated>grep -c "matchedBy" src/components/hiring/DuplicateCandidateDialog.tsx && grep -c "useCandidateByCpf" src/components/hiring/CandidateForm.tsx</automated>
  </verify>
  <done>DuplicateCandidateDialog suporta prop matchedBy diferenciando título e display value; CandidateForm faz lookup CPF prioritário antes de email; dialog abre com matchedBy correto.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL params -> queries | useSearchParams é cliente; backend RLS continua a fronteira de segurança real. |
| localStorage view persist -> render | tampering apenas troca view; sem leakage. |
| CPF lookup public hook -> DB | useCandidateByCpf é UseQuery direto (não scoped) — RLS de candidates pode bloquear; aceitável para dedup que é cross-empresa por design TAL-09. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08-01 | I (Information disclosure) | URL ?q=João compartilhada vaza nome candidato em tela de outro usuário | accept | Compartilhar URL é feature (RS-09 explicit shareable); RLS server-side ainda filtra resultados visíveis ao usuário receptor |
| T-02-08-02 | T (Tampering) | localStorage view tampering força CandidatesTable em mobile | accept | Apenas afeta UX; sem security impact |
| T-02-08-03 | T (Tampering) | CardFieldsCustomizer permite habilitar campo `salary` (não na lista) | mitigate | OPTIONAL_FIELDS é static const + Zod enum; tampering de localStorage gera Zod parse fail → DEFAULT_CARD_PREFERENCES (sem campos extras) |
| T-02-08-04 | I (Information disclosure) | useCandidateByCpf vaza candidato cross-empresa | mitigate | RLS de candidates ainda filtra por visible_companies (Phase 1); CPF dedup canonical retorna null se não visível ao usuário (TAL-09 explicit design) |
</threat_model>

<verification>
- 6 componentes em src/components/hiring/ criados/modificados
- 2 testes Wave 0 ativos e green
- npm test -- --run tests/hiring/PipelineFilters.test.tsx tests/hiring/BoardTableToggle.test.tsx exit 0
- tsc clean
- grep "useSearchParams" src/components/hiring/PipelineFilters.tsx returns 1+
- grep "leverup:rs:view" src/components/hiring/BoardTableToggle.tsx returns 1+
- grep "matchedBy" src/components/hiring/DuplicateCandidateDialog.tsx returns 1+
- grep "useCandidateByCpf" src/components/hiring/CandidateForm.tsx returns 1+
</verification>

<success_criteria>
1. Filtros inline com URL state — links compartilháveis para qualquer combinação.
2. Toggle Quadro/Tabela funcional — usuário escolhe view por jobId; persistência localStorage.
3. CandidatesTable com sort manual — 4 colunas sortable, visualização lista.
4. CardFieldsCustomizer pop dropdown — RH escolhe quais campos opcionais aparecer.
5. CPF como dedup canonical — busca CPF antes de email; dialog diferenciado.
6. Plan 09 (drawer split + LGPD UI + pages) pode prosseguir.
</success_criteria>

<output>
Após completar, criar .planning/phases/02-r-s-refactor/02-08-SUMMARY.md com:
- Lista de 6 componentes criados/modificados
- Confirmação tests verde
- Próximo plano: 02-09-PLAN.md (drawer split + LGPD UI + pages)
</output>

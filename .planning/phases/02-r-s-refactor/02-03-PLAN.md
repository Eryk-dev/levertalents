---
phase: 02-r-s-refactor
plan: 03
type: execute
wave: 1
depends_on: ["02-01"]
files_modified:
  - src/lib/supabaseError.ts
  - src/lib/hiring/sla.ts
  - src/lib/hiring/cardCustomization.ts
  - src/lib/hiring/cpf.ts
  - src/lib/hiring/stageGroups.ts
  - tests/hiring/canTransition.test.ts
  - tests/hiring/stageGroups.test.ts
  - tests/hiring/sla.test.ts
  - tests/hiring/cpf.test.ts
  - tests/lib/supabaseError.test.ts
  - tests/hiring/useCardPreferences.test.tsx
autonomous: true
requirements:
  - RS-04
  - RS-08
  - RS-13
  - TAL-09
must_haves:
  truths:
    - "supabaseError.ts exporta MoveApplicationError discriminated union + 4 detect helpers + getMoveErrorToastConfig"
    - "sla.ts exporta computeSlaTone (D-10: 2d=warning, 5d=critical) E daysSince — pure functions"
    - "cardCustomization.ts exporta CardPreferencesSchema (Zod) com OPTIONAL_FIELDS array"
    - "cpf.ts exporta normalizeCpf E formatCpf — pure functions client-side"
    - "STAGE_GROUP_BAR_COLORS atualizado para D-11 (azul triagem+checagem, amarelo entrevistas, verde decisao, vermelho descartados)"
    - "Vitest tests Wave 0 ativados (.skip removido) e green"
  artifacts:
    - path: "src/lib/supabaseError.ts"
      provides: "Detectors RLS / network / conflict / transition + discriminated union + toast config helper"
      contains: "MoveApplicationError"
    - path: "src/lib/hiring/sla.ts"
      provides: "computeSlaTone, daysSince, SLA_THRESHOLDS, SLA_BORDER_CLASSES"
      contains: "computeSlaTone"
    - path: "src/lib/hiring/cardCustomization.ts"
      provides: "CardPreferencesSchema (Zod), OPTIONAL_FIELDS, loadCardPreferences, saveCardPreferences"
      contains: "OPTIONAL_FIELDS"
    - path: "src/lib/hiring/cpf.ts"
      provides: "normalizeCpf, formatCpf — client-side mirrors do trigger DB"
      contains: "normalizeCpf"
    - path: "src/lib/hiring/stageGroups.ts"
      provides: "STAGE_GROUP_BAR_COLORS atualizado para intencionalidade D-11"
      contains: "STAGE_GROUP_BAR_COLORS"
  key_links:
    - from: "src/lib/supabaseError.ts"
      to: "src/hooks/hiring/useApplications.ts (Plan 04)"
      via: "import { detectRlsDenial, MoveApplicationError } from"
      pattern: "import.*supabaseError"
    - from: "src/lib/hiring/sla.ts"
      to: "src/components/hiring/CandidateCard.tsx (Plan 06)"
      via: "import { computeSlaTone } from"
      pattern: "computeSlaTone"
    - from: "src/lib/hiring/cardCustomization.ts"
      to: "src/hooks/hiring/useCardPreferences.ts (Plan 05)"
      via: "import { CardPreferencesSchema } from"
      pattern: "CardPreferencesSchema"
---

<objective>
Wave 1 — utilities puras (zero side-effects, zero React, zero Supabase) que destravam todos os hooks e components downstream:

1. **`src/lib/supabaseError.ts`** — estender com 4 detect helpers (`detectRlsDenial` / `detectNetworkDrop` / `detectConflict` / `detectTransitionReject`) + `MoveApplicationError` discriminated union + `getMoveErrorToastConfig` helper. **Não remover** os exports existentes (`formatSupabaseError`, `handleSupabaseError`, `throwOnError`).
2. **`src/lib/hiring/sla.ts`** — novo módulo pure. `computeSlaTone(stageEnteredAt)` retorna `"ok" | "warning" | "critical"` com thresholds D-10 (2d/5d). `daysSince` helper. Constantes `SLA_THRESHOLDS`, `SLA_BORDER_CLASSES`, `SLA_DOT_CLASSES`.
3. **`src/lib/hiring/cardCustomization.ts`** — schema Zod + helpers `loadCardPreferences` / `saveCardPreferences` (localStorage namespaced `leverup:rs:card-fields:{userId}`).
4. **`src/lib/hiring/cpf.ts`** — `normalizeCpf` (mirror DB trigger) + `formatCpf` (UI display).
5. **`src/lib/hiring/stageGroups.ts`** — atualizar `STAGE_GROUP_BAR_COLORS` para refletir D-11 fielmente (cores por intencionalidade do funil, não pela ordem do funil).
6. Ativar 6 arquivos de teste Wave 0 (remover `describe.skip` e implementar testes reais).

Purpose: Plan 04 (hooks Wave 2) tem dependência hard nesses módulos. Mantê-los em plan separado mantém scope <50% context budget.

Output: 5 arquivos `src/lib/**/*.ts` criados/atualizados + 6 arquivos de teste ativados. `npm test -- --run` exit 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-r-s-refactor/02-CONTEXT.md
@.planning/phases/02-r-s-refactor/02-RESEARCH.md
@.planning/phases/02-r-s-refactor/02-PATTERNS.md
@.planning/phases/02-r-s-refactor/02-UI-SPEC.md
@CLAUDE.md

<interfaces>
<!-- Atual src/lib/supabaseError.ts (preservar exports) -->

From src/lib/supabaseError.ts (atual — manter):
```typescript
export function formatSupabaseError(error: unknown): string { /* ... */ }
export function handleSupabaseError(error: unknown, context?: string): never { /* ... */ }
export function throwOnError<T>(result: { data: T; error: unknown }): T { /* ... */ }
```

From src/lib/hiring/retention.ts (precedente de pure date util):
```typescript
import { differenceInDays } from "date-fns";
export function daysSinceLastActivity(lastActivity: Date | string | null): number | null {
  if (!lastActivity) return null;
  return differenceInDays(new Date(), new Date(lastActivity));
}
```

From src/lib/hiring/stageGroups.ts (atual — STAGE_GROUP_BAR_COLORS precisa correção D-11):
```typescript
// ATUAL (DIVERGE de D-11):
export const STAGE_GROUP_BAR_COLORS: Record<StageGroupKey, string> = {
  triagem: "bg-text-subtle/40",        // ← cinza, deveria ser azul
  checagem: "bg-status-blue/70",
  entrevista_rh: "bg-status-blue/80",  // ← azul, deveria ser amarelo
  entrevista_final: "bg-status-amber/80",
  decisao: "bg-status-green",
  descartados: "bg-status-red/60",
};
```

From @hookform/resolvers/zod (Phase 1 deps):
```typescript
import { zodResolver } from "@hookform/resolvers/zod";
import { z } from "zod";  // 3.25 — DO NOT upgrade to 4
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend supabaseError.ts with 4 error detectors + MoveApplicationError union</name>
  <files>src/lib/supabaseError.ts, tests/lib/supabaseError.test.ts</files>
  <read_first>
    src/lib/supabaseError.ts (estado atual — preservar exports),
    .planning/phases/02-r-s-refactor/02-RESEARCH.md (§4 lines 547-656),
    .planning/phases/02-r-s-refactor/02-PATTERNS.md ("src/lib/supabaseError.ts (MODIFY")
  </read_first>
  <behavior>
    - `detectRlsDenial(err)` retorna true para PostgrestError com code === '42501'
    - `detectNetworkDrop(err)` retorna true para TypeError /fetch/i, AbortError, ou objeto com code === ''
    - `detectConflict(err)` retorna true para PostgrestError code === '23514' E message contém /transition/i
    - `detectTransitionReject(err)` é type guard para `MoveApplicationError` com kind === 'transition'
    - `getMoveErrorToastConfig(err)` retorna `{ title, description, duration }` por kind, com cópia EXATA do D-05 / UI-SPEC §"Error states"
  </behavior>
  <action>
**ATIVAR primeiro o arquivo de teste:** abrir `tests/lib/supabaseError.test.ts`, remover `describe.skip` (passa a `describe`), substituir `it.todo(...)` por testes reais. Rodar `npm test -- --run tests/lib/supabaseError.test.ts` — deve falhar (módulo ainda não tem detectors).

Conteúdo do teste:

```typescript
import { describe, it, expect } from "vitest";
import {
  detectRlsDenial,
  detectNetworkDrop,
  detectConflict,
  detectTransitionReject,
  getMoveErrorToastConfig,
  type MoveApplicationError,
} from "@/lib/supabaseError";

describe("detectRlsDenial", () => {
  it("retorna true para PostgrestError com code 42501", () => {
    expect(detectRlsDenial({ code: "42501", message: "permission denied" })).toBe(true);
  });
  it("retorna false para code != 42501", () => {
    expect(detectRlsDenial({ code: "23514" })).toBe(false);
  });
  it("retorna false para null/undefined/strings", () => {
    expect(detectRlsDenial(null)).toBe(false);
    expect(detectRlsDenial(undefined)).toBe(false);
    expect(detectRlsDenial("oops")).toBe(false);
  });
});

describe("detectNetworkDrop", () => {
  it("retorna true para TypeError com /fetch/i message", () => {
    expect(detectNetworkDrop(new TypeError("Failed to fetch"))).toBe(true);
  });
  it("retorna true para AbortError", () => {
    const e = new Error("timeout"); e.name = "AbortError";
    expect(detectNetworkDrop(e)).toBe(true);
  });
  it("retorna true para PostgrestError com code === ''", () => {
    expect(detectNetworkDrop({ code: "" })).toBe(true);
  });
  it("retorna false para PostgrestError com code 42501", () => {
    expect(detectNetworkDrop({ code: "42501" })).toBe(false);
  });
});

describe("detectConflict", () => {
  it("retorna true para code 23514 com /transition/i message", () => {
    expect(detectConflict({ code: "23514", message: "Invalid stage transition em_interesse -> contratado" })).toBe(true);
  });
  it("retorna false para code 23514 sem 'transition' na mensagem", () => {
    expect(detectConflict({ code: "23514", message: "check_violation other" })).toBe(false);
  });
});

describe("detectTransitionReject", () => {
  it("retorna true para objeto kind=transition", () => {
    const err: MoveApplicationError = { kind: "transition", from: "em_interesse", to: "contratado" };
    expect(detectTransitionReject(err)).toBe(true);
  });
  it("retorna false para kind!=transition", () => {
    expect(detectTransitionReject({ kind: "rls", error: { code: "42501" } } as MoveApplicationError)).toBe(false);
  });
});

describe("getMoveErrorToastConfig", () => {
  it("rls: title 'Sem permissão' + duration 8000", () => {
    const cfg = getMoveErrorToastConfig({ kind: "rls", error: { code: "42501", message: "" } as never });
    expect(cfg.title).toBe("Sem permissão");
    expect(cfg.duration).toBe(8000);
  });
  it("network: title 'Sem conexão' + duration 4000", () => {
    const cfg = getMoveErrorToastConfig({ kind: "network", error: new Error("net") });
    expect(cfg.title).toBe("Sem conexão");
    expect(cfg.duration).toBe(4000);
  });
  it("conflict: title 'Atualizado por outra pessoa'", () => {
    expect(getMoveErrorToastConfig({ kind: "conflict" }).title).toBe("Atualizado por outra pessoa");
  });
  it("transition: description inclui from + to", () => {
    const cfg = getMoveErrorToastConfig({ kind: "transition", from: "Em interesse", to: "Contratado" });
    expect(cfg.description).toContain("Em interesse");
    expect(cfg.description).toContain("Contratado");
  });
  it("unknown: title 'Erro ao mover candidato'", () => {
    expect(getMoveErrorToastConfig({ kind: "unknown", error: "??" }).title).toBe("Erro ao mover candidato");
  });
});
```

**Implementação em `src/lib/supabaseError.ts`** — APPEND após exports existentes (NÃO remover `formatSupabaseError`, `handleSupabaseError`, `throwOnError`):

```typescript
import { PostgrestError } from "@supabase/supabase-js";

// =========================================================================
// Phase 2: discriminated union + 4 detect helpers para useMoveApplicationStage
// (D-05/D-06 da CONTEXT.md, RESEARCH §4)
// =========================================================================

export type MoveApplicationError =
  | { kind: "rls"; error: PostgrestError }
  | { kind: "network"; error: Error }
  | { kind: "conflict"; error?: PostgrestError }
  | { kind: "transition"; from: string; to: string }
  | { kind: "unknown"; error: unknown };

const RLS_CODE = "42501";
const CHECK_VIOLATION_CODE = "23514";

export function detectRlsDenial(err: unknown): boolean {
  if (!err || typeof err !== "object") return false;
  return (err as PostgrestError).code === RLS_CODE;
}

export function detectNetworkDrop(err: unknown): boolean {
  if (err instanceof TypeError && /fetch/i.test(err.message)) return true;
  if (err instanceof Error && err.name === "AbortError") return true;
  if (err && typeof err === "object" && "code" in err && (err as { code: string }).code === "") return true;
  return false;
}

export function detectConflict(err: unknown): boolean {
  if (!err || typeof err !== "object") return false;
  const e = err as PostgrestError;
  return e.code === CHECK_VIOLATION_CODE && /transition/i.test(e.message ?? "");
}

export function detectTransitionReject(err: unknown): err is MoveApplicationError {
  return (
    !!err &&
    typeof err === "object" &&
    "kind" in err &&
    (err as { kind: unknown }).kind === "transition"
  );
}

/**
 * Toast config UI-locked (UI-SPEC §"Error states (D-05 — LOCKED toast copy)").
 */
export function getMoveErrorToastConfig(err: MoveApplicationError): {
  title: string;
  description?: string;
  duration?: number;
} {
  switch (err.kind) {
    case "rls":
      return {
        title: "Sem permissão",
        description: "Você não tem permissão pra mover esse candidato.",
        duration: 8000,
      };
    case "network":
      return {
        title: "Sem conexão",
        description: "Tentando de novo automaticamente...",
        duration: 4000,
      };
    case "conflict":
      return {
        title: "Atualizado por outra pessoa",
        description: "O card já foi movido. Recarregando.",
        duration: 5000,
      };
    case "transition":
      return {
        title: "Transição inválida",
        description: `Não é possível mover de "${err.from}" direto para "${err.to}".`,
        duration: 6000,
      };
    case "unknown":
    default:
      return {
        title: "Erro ao mover candidato",
        description: "Tente de novo em alguns segundos.",
        duration: 6000,
      };
  }
}
```

Após implementar, rodar `npm test -- --run tests/lib/supabaseError.test.ts` — todos os 14+ tests devem passar.
  </action>
  <verify>
    <automated>npm test -- --run tests/lib/supabaseError.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>`src/lib/supabaseError.ts` tem exports `MoveApplicationError`, `detectRlsDenial`, `detectNetworkDrop`, `detectConflict`, `detectTransitionReject`, `getMoveErrorToastConfig`. Exports antigos preservados. `tests/lib/supabaseError.test.ts` ativo (sem `.skip`) e green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create sla.ts + cpf.ts + cardCustomization.ts</name>
  <files>src/lib/hiring/sla.ts, src/lib/hiring/cpf.ts, src/lib/hiring/cardCustomization.ts, tests/hiring/sla.test.ts, tests/hiring/cpf.test.ts, tests/hiring/useCardPreferences.test.tsx</files>
  <read_first>
    src/lib/hiring/retention.ts (analog para sla.ts),
    src/lib/utils/formatBR.ts (formatCpf precedent — Phase 1),
    .planning/phases/02-r-s-refactor/02-RESEARCH.md (§13 lines 1836-1872 sla; §15 lines 1957-2009 cardCustomization; §8 lines 1340-1361 normalize_cpf),
    .planning/phases/02-r-s-refactor/02-PATTERNS.md ("src/lib/hiring/sla.ts" e "useCardPreferences" assignments)
  </read_first>
  <behavior>
    sla.ts:
    - `daysSince(at: Date | string)` retorna número >= 0 (clamp se data futura)
    - `computeSlaTone(stageEnteredAt: Date | string)` retorna "ok" para 0-1d, "warning" para 2-4d, "critical" para >=5d
    - `SLA_THRESHOLDS` exporta `{ warning: 2, critical: 5 }` para tests usarem
    - `SLA_BORDER_CLASSES` map { ok, warning, critical } → Tailwind classes
    cpf.ts:
    - `normalizeCpf(input?: string | null)` retorna 11 dígitos OU null
    - `formatCpf("12345678901")` retorna "123.456.789-01"
    - `formatCpf(null)` retorna ""
    cardCustomization.ts:
    - `OPTIONAL_FIELDS` array readonly: `["avatar", "next_interview", "cv_icon", "fit_score", "bg_check_dot", "source_tag"]`
    - `CardPreferencesSchema` Zod: `{ version: literal(1), enabledFields: array(enum(OPTIONAL_FIELDS)) }`
    - `loadCardPreferences(userId)` retorna defaults se localStorage vazio ou parse falha
    - `saveCardPreferences(userId, prefs)` persiste em `localStorage.leverup:rs:card-fields:${userId}`
  </behavior>
  <action>
**ATIVAR primeiro 3 arquivos de teste** (`tests/hiring/sla.test.ts`, `tests/hiring/cpf.test.ts`, `tests/hiring/useCardPreferences.test.tsx`) — remover `.skip`/`it.todo` e implementar com asserts reais conforme abaixo. Rodar `npm test -- --run tests/hiring/sla.test.ts tests/hiring/cpf.test.ts tests/hiring/useCardPreferences.test.tsx` — deve falhar até implementarmos os módulos.

**Conteúdo de `src/lib/hiring/sla.ts`:**

```typescript
/**
 * SLA visual da Phase 2 (D-10): 2 dias = warning (amber), 5 dias = critical (red).
 * Global, sem customização por empresa/vaga em v1.
 *
 * Pure functions: zero React, zero supabase, zero side-effects.
 */

export type SlaTone = "ok" | "warning" | "critical";

export const SLA_THRESHOLDS = { warning: 2, critical: 5 } as const;

/**
 * Dias-corridos desde `at`. Clamp em 0 para datas futuras.
 */
export function daysSince(at: Date | string | null | undefined): number {
  if (!at) return 0;
  const ts = typeof at === "string" ? new Date(at).getTime() : at.getTime();
  if (Number.isNaN(ts)) return 0;
  const diffMs = Date.now() - ts;
  return Math.max(0, Math.floor(diffMs / 86_400_000));
}

export function computeSlaTone(stageEnteredAt: Date | string | null | undefined): SlaTone {
  const days = daysSince(stageEnteredAt);
  if (days >= SLA_THRESHOLDS.critical) return "critical";
  if (days >= SLA_THRESHOLDS.warning) return "warning";
  return "ok";
}

export const SLA_BORDER_CLASSES: Record<SlaTone, string> = {
  ok: "border-l-transparent",
  warning: "border-l-status-amber",
  critical: "border-l-status-red",
};

export const SLA_DOT_CLASSES: Record<SlaTone, string> = {
  ok: "bg-text-subtle/60",
  warning: "bg-status-amber",
  critical: "bg-status-red",
};
```

**Conteúdo de `src/lib/hiring/cpf.ts`:**

```typescript
/**
 * CPF utilities — mirror lado-client do trigger DB tg_normalize_candidate_cpf.
 * Usado por DuplicateCandidateDialog (Plan 06) para busca antes de criar candidato.
 */

export function normalizeCpf(input: string | null | undefined): string | null {
  if (!input) return null;
  const digits = input.replace(/[^0-9]/g, "");
  if (digits.length === 0) return null;
  return digits;
}

/**
 * Formata 11 dígitos como '123.456.789-01'. Retorna string vazia se input não-conformado.
 */
export function formatCpf(input: string | null | undefined): string {
  const norm = normalizeCpf(input);
  if (!norm || norm.length !== 11) return input ?? "";
  return `${norm.slice(0, 3)}.${norm.slice(3, 6)}.${norm.slice(6, 9)}-${norm.slice(9, 11)}`;
}

export function isValidCpfFormat(input: string | null | undefined): boolean {
  const norm = normalizeCpf(input);
  return norm !== null && norm.length === 11;
}
```

**Conteúdo de `src/lib/hiring/cardCustomization.ts`:**

```typescript
/**
 * Card customization — Phase 2 D-08.
 * Persistência localStorage namespaced: leverup:rs:card-fields:{userId}.
 * Schema Zod versionado para tolerar evolução sem crash.
 */

import { z } from "zod";

const SCHEMA_VERSION = 1 as const;

export const OPTIONAL_FIELDS = [
  "avatar",
  "next_interview",
  "cv_icon",
  "fit_score",
  "bg_check_dot",
  "source_tag",
] as const;

export type OptionalField = (typeof OPTIONAL_FIELDS)[number];

export const CardPreferencesSchema = z.object({
  version: z.literal(SCHEMA_VERSION),
  enabledFields: z.array(z.enum(OPTIONAL_FIELDS)),
});

export type CardPreferences = z.infer<typeof CardPreferencesSchema>;

export const DEFAULT_CARD_PREFERENCES: CardPreferences = {
  version: SCHEMA_VERSION,
  enabledFields: ["avatar", "next_interview", "cv_icon"], // sensible defaults D-08
};

const STORAGE_KEY = (userId: string) => `leverup:rs:card-fields:${userId}`;

export function loadCardPreferences(userId: string | null | undefined): CardPreferences {
  if (!userId) return DEFAULT_CARD_PREFERENCES;
  try {
    const raw = localStorage.getItem(STORAGE_KEY(userId));
    if (!raw) return DEFAULT_CARD_PREFERENCES;
    const parsed = JSON.parse(raw);
    const result = CardPreferencesSchema.safeParse(parsed);
    return result.success ? result.data : DEFAULT_CARD_PREFERENCES;
  } catch {
    return DEFAULT_CARD_PREFERENCES;
  }
}

export function saveCardPreferences(userId: string, prefs: CardPreferences): void {
  try {
    localStorage.setItem(STORAGE_KEY(userId), JSON.stringify(prefs));
  } catch {
    // localStorage cheio ou disabled — silent fail
  }
}

export function isFieldEnabled(prefs: CardPreferences, field: OptionalField): boolean {
  return prefs.enabledFields.includes(field);
}
```

**Conteúdo de `tests/hiring/sla.test.ts`:**

```typescript
import { describe, it, expect, vi, beforeEach, afterEach } from "vitest";
import { computeSlaTone, daysSince, SLA_THRESHOLDS } from "@/lib/hiring/sla";

describe("daysSince", () => {
  beforeEach(() => { vi.useFakeTimers(); vi.setSystemTime(new Date("2026-04-27T12:00:00.000Z")); });
  afterEach(() => { vi.useRealTimers(); });

  it("0 dias = 0", () => {
    expect(daysSince("2026-04-27T12:00:00.000Z")).toBe(0);
  });
  it("clamp em 0 para data futura", () => {
    expect(daysSince("2026-04-30T12:00:00.000Z")).toBe(0);
  });
  it("aceita Date object", () => {
    expect(daysSince(new Date("2026-04-25T12:00:00.000Z"))).toBe(2);
  });
  it("retorna 0 para null/undefined", () => {
    expect(daysSince(null)).toBe(0);
    expect(daysSince(undefined)).toBe(0);
  });
  it("retorna 0 para string inválida (NaN)", () => {
    expect(daysSince("not-a-date")).toBe(0);
  });
});

describe("computeSlaTone", () => {
  beforeEach(() => { vi.useFakeTimers(); vi.setSystemTime(new Date("2026-04-27T12:00:00.000Z")); });
  afterEach(() => { vi.useRealTimers(); });

  it("0d retorna ok", () => {
    expect(computeSlaTone("2026-04-27T12:00:00.000Z")).toBe("ok");
  });
  it("1d retorna ok", () => {
    expect(computeSlaTone("2026-04-26T12:00:00.000Z")).toBe("ok");
  });
  it("2d retorna warning (limite inferior amber)", () => {
    expect(computeSlaTone("2026-04-25T12:00:00.000Z")).toBe("warning");
  });
  it("4d retorna warning", () => {
    expect(computeSlaTone("2026-04-23T12:00:00.000Z")).toBe("warning");
  });
  it("5d retorna critical (limite inferior red)", () => {
    expect(computeSlaTone("2026-04-22T12:00:00.000Z")).toBe("critical");
  });
  it("10d retorna critical", () => {
    expect(computeSlaTone("2026-04-17T12:00:00.000Z")).toBe("critical");
  });
});

describe("SLA_THRESHOLDS", () => {
  it("expõe warning=2 e critical=5 (D-10)", () => {
    expect(SLA_THRESHOLDS.warning).toBe(2);
    expect(SLA_THRESHOLDS.critical).toBe(5);
  });
});
```

**Conteúdo de `tests/hiring/cpf.test.ts`:**

```typescript
import { describe, it, expect } from "vitest";
import { normalizeCpf, formatCpf, isValidCpfFormat } from "@/lib/hiring/cpf";

describe("normalizeCpf", () => {
  it("remove pontuação 123.456.789-00", () => {
    expect(normalizeCpf("123.456.789-00")).toBe("12345678900");
  });
  it("preserva CPF já normalizado", () => {
    expect(normalizeCpf("12345678901")).toBe("12345678901");
  });
  it("retorna null para null/undefined", () => {
    expect(normalizeCpf(null)).toBeNull();
    expect(normalizeCpf(undefined)).toBeNull();
  });
  it("retorna null para string sem dígitos", () => {
    expect(normalizeCpf("---")).toBeNull();
    expect(normalizeCpf("abc")).toBeNull();
  });
  it("preserva CPFs curtos (não-validados aqui)", () => {
    expect(normalizeCpf("123")).toBe("123");
  });
});

describe("formatCpf", () => {
  it("formata 11 dígitos", () => {
    expect(formatCpf("12345678901")).toBe("123.456.789-01");
  });
  it("formata input já formatado (re-format)", () => {
    expect(formatCpf("987.654.321-00")).toBe("987.654.321-00");
  });
  it("retorna string vazia para null", () => {
    expect(formatCpf(null)).toBe("");
  });
  it("retorna input se !=11 dígitos (não tenta formatar inválido)", () => {
    expect(formatCpf("123")).toBe("123");
  });
});

describe("isValidCpfFormat", () => {
  it("true para 11 dígitos", () => {
    expect(isValidCpfFormat("12345678901")).toBe(true);
    expect(isValidCpfFormat("123.456.789-01")).toBe(true);
  });
  it("false para !=11 dígitos", () => {
    expect(isValidCpfFormat("12345")).toBe(false);
    expect(isValidCpfFormat(null)).toBe(false);
  });
});
```

**Conteúdo de `tests/hiring/useCardPreferences.test.tsx`** — TESTA o módulo cardCustomization (não o hook, que é Plan 05):

```typescript
import { describe, it, expect, beforeEach } from "vitest";
import {
  CardPreferencesSchema,
  DEFAULT_CARD_PREFERENCES,
  loadCardPreferences,
  saveCardPreferences,
  OPTIONAL_FIELDS,
  isFieldEnabled,
} from "@/lib/hiring/cardCustomization";

describe("CardPreferencesSchema", () => {
  it("OPTIONAL_FIELDS contém os 6 campos da D-08", () => {
    expect(OPTIONAL_FIELDS).toContain("avatar");
    expect(OPTIONAL_FIELDS).toContain("next_interview");
    expect(OPTIONAL_FIELDS).toContain("cv_icon");
    expect(OPTIONAL_FIELDS).toContain("fit_score");
    expect(OPTIONAL_FIELDS).toContain("bg_check_dot");
    expect(OPTIONAL_FIELDS).toContain("source_tag");
    expect(OPTIONAL_FIELDS.length).toBe(6);
  });
  it("DEFAULT inclui avatar + next_interview + cv_icon", () => {
    expect(DEFAULT_CARD_PREFERENCES.enabledFields).toEqual(expect.arrayContaining(["avatar", "next_interview", "cv_icon"]));
  });
});

describe("loadCardPreferences", () => {
  beforeEach(() => { localStorage.clear(); });

  it("retorna DEFAULT quando userId null", () => {
    expect(loadCardPreferences(null)).toEqual(DEFAULT_CARD_PREFERENCES);
  });
  it("retorna DEFAULT quando localStorage vazio", () => {
    expect(loadCardPreferences("user-1")).toEqual(DEFAULT_CARD_PREFERENCES);
  });
  it("retorna persistido após save", () => {
    const prefs = { version: 1 as const, enabledFields: ["avatar"] as const };
    saveCardPreferences("user-1", prefs);
    expect(loadCardPreferences("user-1")).toEqual(prefs);
  });
  it("retorna DEFAULT quando JSON corrompido", () => {
    localStorage.setItem("leverup:rs:card-fields:user-1", "{not json");
    expect(loadCardPreferences("user-1")).toEqual(DEFAULT_CARD_PREFERENCES);
  });
  it("retorna DEFAULT quando version não bate (schema migration)", () => {
    localStorage.setItem("leverup:rs:card-fields:user-1", JSON.stringify({ version: 99, enabledFields: [] }));
    expect(loadCardPreferences("user-1")).toEqual(DEFAULT_CARD_PREFERENCES);
  });
});

describe("isFieldEnabled", () => {
  it("true se campo está em enabledFields", () => {
    const prefs = { version: 1 as const, enabledFields: ["avatar", "fit_score"] as const };
    expect(isFieldEnabled(prefs, "avatar")).toBe(true);
    expect(isFieldEnabled(prefs, "cv_icon")).toBe(false);
  });
});
```

Após implementar, rodar `npm test -- --run tests/hiring/sla.test.ts tests/hiring/cpf.test.ts tests/hiring/useCardPreferences.test.tsx` — todos os 25+ tests devem passar.
  </action>
  <verify>
    <automated>npm test -- --run tests/hiring/sla.test.ts tests/hiring/cpf.test.ts tests/hiring/useCardPreferences.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <done>3 arquivos `.ts` criados; 3 arquivos de teste ativos e green; `npm test -- --run tests/hiring/{sla,cpf,useCardPreferences}*` exit 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update STAGE_GROUP_BAR_COLORS to D-11 + activate canTransition + stageGroups tests</name>
  <files>src/lib/hiring/stageGroups.ts, tests/hiring/canTransition.test.ts, tests/hiring/stageGroups.test.ts</files>
  <read_first>
    src/lib/hiring/stageGroups.ts (estado atual — STAGE_GROUP_BAR_COLORS),
    src/lib/hiring/statusMachine.ts (APPLICATION_STAGE_TRANSITIONS — Phase 2 não muda; só ativa o teste),
    .planning/phases/02-r-s-refactor/02-RESEARCH.md (§12 lines 1791-1821 sparkbar D-11; lines 1336-1361 normalize_cpf),
    .planning/phases/02-r-s-refactor/02-UI-SPEC.md (§"Status semantic colors" — D-11 mapping)
  </read_first>
  <behavior>
    stageGroups (D-11):
    - triagem → bg-status-blue/70 (azul movimento inicial)
    - checagem → bg-status-blue/70 (azul movimento inicial)
    - entrevista_rh → bg-status-amber/80 (amarelo entrevista)
    - entrevista_final → bg-status-amber/80 (amarelo entrevista)
    - decisao → bg-status-green (verde finalização positiva)
    - descartados → bg-status-red/60 (vermelho)

    canTransition (Phase 2 não altera implementação — só ativa exhaustive test):
    - Toda combinação `(from, to)` retorna `from === to OR APPLICATION_STAGE_TRANSITIONS[from].includes(to)`

    stageGroups (test):
    - STAGE_GROUP_BY_STAGE inclui todo enum value de application_stage_enum (incluindo legados aguardando_fit_cultural, sem_retorno, fit_recebido — eles NÃO devem ser dropados em Phase 2)
    - STAGE_GROUP_BAR_COLORS tem 6 keys (uma por StageGroupKey) cada com classe Tailwind válida
  </behavior>
  <action>
**Editar `src/lib/hiring/stageGroups.ts`:** localizar `STAGE_GROUP_BAR_COLORS` e atualizar para refletir D-11 fielmente. Mudanças:

```typescript
// ANTES (atual — DIVERGE de D-11):
export const STAGE_GROUP_BAR_COLORS: Record<StageGroupKey, string> = {
  triagem: "bg-text-subtle/40",        // ← cinza
  checagem: "bg-status-blue/70",
  entrevista_rh: "bg-status-blue/80",  // ← azul (errado: deveria ser amarelo)
  entrevista_final: "bg-status-amber/80",
  decisao: "bg-status-green",
  descartados: "bg-status-red/60",
};

// DEPOIS (D-11 — cores por intencionalidade):
export const STAGE_GROUP_BAR_COLORS: Record<StageGroupKey, string> = {
  triagem: "bg-status-blue/70",         // azul: movimento inicial (D-11)
  checagem: "bg-status-blue/70",        // azul: movimento inicial (D-11)
  entrevista_rh: "bg-status-amber/80",  // amarelo: entrevista (D-11)
  entrevista_final: "bg-status-amber/80", // amarelo: entrevista (D-11)
  decisao: "bg-status-green",           // verde: aprovados/decisão/admissão (D-11)
  descartados: "bg-status-red/60",      // vermelho: recusados/descartados (D-11)
};
```

NÃO alterar outras exports (`STAGE_GROUPS`, `STAGE_GROUP_BY_STAGE`, etc.).

**Ativar `tests/hiring/canTransition.test.ts`:**

```typescript
import { describe, it, expect } from "vitest";
import {
  canTransition,
  APPLICATION_STAGE_TRANSITIONS,
} from "@/lib/hiring/statusMachine";
import type { ApplicationStage } from "@/integrations/supabase/hiring-types";

describe("canTransition (application) — exhaustive table", () => {
  const allStages = Object.keys(APPLICATION_STAGE_TRANSITIONS) as ApplicationStage[];

  for (const from of allStages) {
    for (const to of allStages) {
      const expected = from === to || APPLICATION_STAGE_TRANSITIONS[from].includes(to);
      it(`${from} → ${to} = ${expected}`, () => {
        expect(canTransition(from, to, "application")).toBe(expected);
      });
    }
  }
});

describe("canTransition (application) — sanity", () => {
  it("retorna true para mesma stage (idempotente)", () => {
    expect(canTransition("em_interesse", "em_interesse", "application")).toBe(true);
  });
  it("retorna true para transição válida no mapping", () => {
    // Pegar primeiro destino válido de em_interesse
    const targets = APPLICATION_STAGE_TRANSITIONS["em_interesse"] ?? [];
    if (targets.length > 0) {
      expect(canTransition("em_interesse", targets[0], "application")).toBe(true);
    }
  });
  it("retorna false para transição não-listada", () => {
    // Pegar uma transição que sabemos NÃO ser permitida
    // Strategy: para cada from, encontrar um to fora do array
    const allStages = Object.keys(APPLICATION_STAGE_TRANSITIONS) as ApplicationStage[];
    for (const from of allStages) {
      const allowed = new Set([...APPLICATION_STAGE_TRANSITIONS[from], from]);
      const notAllowed = allStages.find((s) => !allowed.has(s));
      if (notAllowed) {
        expect(canTransition(from, notAllowed, "application")).toBe(false);
        return;
      }
    }
  });
});
```

**Ativar `tests/hiring/stageGroups.test.ts`:**

```typescript
import { describe, it, expect } from "vitest";
import {
  STAGE_GROUPS,
  STAGE_GROUP_BY_STAGE,
  STAGE_GROUP_BAR_COLORS,
  type StageGroupKey,
} from "@/lib/hiring/stageGroups";
import { APPLICATION_STAGE_TRANSITIONS } from "@/lib/hiring/statusMachine";

describe("STAGE_GROUPS", () => {
  it("tem 6 grupos consolidados (RS-06)", () => {
    expect(STAGE_GROUPS.length).toBe(6);
  });
  it("cada grupo tem key + label + defaultStage", () => {
    for (const g of STAGE_GROUPS) {
      expect(g.key).toBeTypeOf("string");
      expect(g.label).toBeTypeOf("string");
      expect(g.defaultStage).toBeTypeOf("string");
    }
  });
});

describe("STAGE_GROUP_BY_STAGE", () => {
  it("todo stage de APPLICATION_STAGE_TRANSITIONS tem mapping para grupo", () => {
    const stages = Object.keys(APPLICATION_STAGE_TRANSITIONS);
    for (const stage of stages) {
      expect(STAGE_GROUP_BY_STAGE[stage as keyof typeof STAGE_GROUP_BY_STAGE]).toBeDefined();
    }
  });
  it("stages legados estão mapeados (compat até Migration G)", () => {
    expect(STAGE_GROUP_BY_STAGE["aguardando_fit_cultural" as keyof typeof STAGE_GROUP_BY_STAGE]).toBeDefined();
    expect(STAGE_GROUP_BY_STAGE["sem_retorno" as keyof typeof STAGE_GROUP_BY_STAGE]).toBeDefined();
    expect(STAGE_GROUP_BY_STAGE["fit_recebido" as keyof typeof STAGE_GROUP_BY_STAGE]).toBeDefined();
  });
});

describe("STAGE_GROUP_BAR_COLORS — D-11 intencionalidade", () => {
  const expectedColors: Record<StageGroupKey, RegExp> = {
    triagem: /status-blue/,           // movimento inicial
    checagem: /status-blue/,          // movimento inicial
    entrevista_rh: /status-amber/,    // entrevista
    entrevista_final: /status-amber/, // entrevista
    decisao: /status-green/,          // aprovados/decisão
    descartados: /status-red/,        // recusados
  };

  for (const [group, pattern] of Object.entries(expectedColors)) {
    it(`${group} usa ${pattern} (D-11)`, () => {
      const cls = STAGE_GROUP_BAR_COLORS[group as StageGroupKey];
      expect(cls).toMatch(pattern);
    });
  }

  it("todos os 6 grupos têm classe definida", () => {
    expect(Object.keys(STAGE_GROUP_BAR_COLORS).length).toBe(6);
  });
});
```

Após implementar, rodar `npm test -- --run tests/hiring/canTransition.test.ts tests/hiring/stageGroups.test.ts` — todos os tests devem passar (canTransition exhaustive table = N×N onde N=número de stages, então pode ser 100+ tests).
  </action>
  <verify>
    <automated>npm test -- --run tests/hiring/canTransition.test.ts tests/hiring/stageGroups.test.ts 2>&1 | tail -8</automated>
  </verify>
  <done>`STAGE_GROUP_BAR_COLORS` tem cores D-11 (azul triagem+checagem, amarelo entrevistas, verde decisao, vermelho descartados). 2 arquivos de teste ativos e green. `grep "status-blue/70.*triagem" src/lib/hiring/stageGroups.ts` retorna match.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pure functions → callers | Sem boundary — funções puras sem side-effects (exceto cardCustomization que toca localStorage). |
| localStorage → user | Persistência por-usuário via namespace `leverup:rs:card-fields:{userId}`. Outro usuário no mesmo browser não vê. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | T (Tampering) | localStorage tampering — usuário edita JSON pra habilitar campo inválido | mitigate | Zod `safeParse` na load; `enum` constraint em `enabledFields` rejeita valores fora de OPTIONAL_FIELDS; fallback para DEFAULT_CARD_PREFERENCES |
| T-02-03-02 | I (Information disclosure) | sla.ts/cpf.ts logam PII | accept | Pure functions — não logam nada. CPF é input/output, sem side-effect (localStorage usa userId como key, não CPF). |
| T-02-03-03 | T (Tampering) | STAGE_GROUP_BAR_COLORS regression — alguém volta para cores antigas | mitigate | Vitest test em `tests/hiring/stageGroups.test.ts` falha se `triagem` não tem `status-blue` (D-11 enforcement automático no CI) |
</threat_model>

<verification>
- 5 arquivos `src/lib/**/*.ts` modificados/criados
- 6 arquivos de teste em `tests/` ativos (sem `describe.skip` ou `it.todo`) e green
- `npm test -- --run tests/hiring/canTransition.test.ts tests/hiring/stageGroups.test.ts tests/hiring/sla.test.ts tests/hiring/cpf.test.ts tests/hiring/useCardPreferences.test.tsx tests/lib/supabaseError.test.ts` retorna exit code 0 com 50+ tests passando
- `npx tsc --noEmit -p tsconfig.app.json` retorna 0 erros
- ESLint clean: `npx eslint src/lib/supabaseError.ts src/lib/hiring/sla.ts src/lib/hiring/cpf.ts src/lib/hiring/cardCustomization.ts src/lib/hiring/stageGroups.ts` retorna 0 errors
</verification>

<success_criteria>
1. `src/lib/supabaseError.ts` tem 4 detect helpers + `MoveApplicationError` discriminated union + `getMoveErrorToastConfig` com cópia exata UI-SPEC §"Error states".
2. `src/lib/hiring/sla.ts` exporta `computeSlaTone` com thresholds 2/5 (D-10) + `daysSince` + constantes Tailwind.
3. `src/lib/hiring/cpf.ts` exporta `normalizeCpf` mirror DB trigger + `formatCpf` para UI display.
4. `src/lib/hiring/cardCustomization.ts` exporta Zod schema versionado + load/save namespaced em localStorage.
5. `STAGE_GROUP_BAR_COLORS` corrigido para D-11 — triagem+checagem azul, entrevistas amarelo (era azul/cinza errados).
6. 6 arquivos de teste Wave 0 ativos e green; CI retorna exit 0.
</success_criteria>

<output>
Após completar, criar `.planning/phases/02-r-s-refactor/02-03-SUMMARY.md` com:
- Lista de arquivos `src/lib/` criados/modificados
- Output de `npm test -- --run` (deve mostrar ~70 tests green entre Plans 01-03)
- Confirmação de STAGE_GROUP_BAR_COLORS atualizado (diff antes/depois)
- Próximo plano: `02-04-PLAN.md` (schema push + types regen)
</output>

---
phase: 02-r-s-refactor
plan: 06
type: execute
wave: 3
depends_on: ["02-04"]
files_modified:
  - src/hooks/hiring/useCandidateConsents.ts
  - src/hooks/hiring/useTalentPool.ts
  - src/hooks/hiring/useCandidates.ts
  - src/hooks/hiring/useCardPreferences.ts
  - src/hooks/hiring/useDataAccessLog.ts
  - src/hooks/hiring/useCandidateTags.ts
  - supabase/functions/apply-to-job/index.ts
  - tests/hiring/useTalentPool.test.tsx
  - tests/hiring/useCandidateConsents.test.tsx
  - tests/hiring/useCandidateTags.test.tsx
autonomous: true
requirements:
  - RS-08
  - TAL-01
  - TAL-02
  - TAL-03
  - TAL-04
  - TAL-06
  - TAL-08
  - TAL-09
must_haves:
  truths:
    - "useActiveConsents lista active_candidate_consents do candidato"
    - "useRevokeConsent atualiza revoked_at + revoked_by=scope.userId; invalida talent-pool E candidate-consents"
    - "useTalentPool filtra candidatos com active_candidate_consents.purpose=incluir_no_banco_de_talentos_global via embed PostgREST"
    - "useCandidateByCpf busca candidato por CPF normalizado"
    - "useCandidate refactor usa RPC read_candidate_with_log com staleTime 60s"
    - "useCardPreferences é hook React reading/writing localStorage via cardCustomization helpers"
    - "Edge Function apply-to-job persiste candidate_consents para cada finalidade marcada (granted_by=null self-grant)"
    - "useDataAccessLog lista entradas de data_access_log para uma entity"
    - "useCandidateTags agrega tags por empresa/vaga a partir do histórico de applications de cada candidato (TAL-02)"
    - "useTalentPool retorna candidatos com campo derivado tags: { company_id, company_name, job_title, last_applied_at }[]"
  artifacts:
    - path: "src/hooks/hiring/useCandidateConsents.ts"
      provides: "useActiveConsents + useRevokeConsent + useGrantConsent"
      contains: "useRevokeConsent"
    - path: "src/hooks/hiring/useTalentPool.ts"
      provides: "Filtro por active_candidate_consents (TAL-04, TAL-08)"
      contains: "active_candidate_consents"
    - path: "src/hooks/hiring/useCandidates.ts"
      provides: "useCandidate via RPC read_candidate_with_log + useCandidateByCpf"
      contains: "read_candidate_with_log"
    - path: "src/hooks/hiring/useCardPreferences.ts"
      provides: "Hook React wrapping cardCustomization.ts helpers"
      contains: "loadCardPreferences"
    - path: "src/hooks/hiring/useDataAccessLog.ts"
      provides: "Lista entradas de data_access_log por entity"
      contains: "data_access_log"
    - path: "src/hooks/hiring/useCandidateTags.ts"
      provides: "TAL-02 — agrega tags por empresa/vaga do histórico de applications do candidato"
      contains: "useCandidateTags"
    - path: "supabase/functions/apply-to-job/index.ts"
      provides: "Persiste candidate_consents na submission do form publico"
      contains: "candidate_consents"
  key_links:
    - from: "useTalentPool"
      to: "active_candidate_consents view"
      via: ".select com embed !inner"
      pattern: "active_candidate_consents!inner"
    - from: "useCandidate refactor"
      to: "read_candidate_with_log RPC"
      via: "supabase.rpc com p_candidate_id e p_context"
      pattern: "read_candidate_with_log"
    - from: "apply-to-job Edge Function"
      to: "candidate_consents"
      via: "INSERT via service role"
      pattern: "candidate_consents.*insert"
    - from: "useCandidateTags / useTalentPool tags field"
      to: "applications + job_openings + companies (cross-empresa join)"
      via: "PostgREST embed em useTalentPool / dedicated query em useCandidateTags"
      pattern: "company_id.*job_title|companies\\(.*name"
---

<objective>
Wave 3 — Hooks LGPD + Edge Function + UI prefs hook. 6 arquivos consumindo o schema novo da Migration F:

1. **useCandidateConsents.ts** (NOVO) — useActiveConsents lê active_candidate_consents view; useRevokeConsent atualiza revoked_at + revoked_by; useGrantConsent grant em nome do RH.

2. **useTalentPool.ts** (MODIFY) — adicionar filtro por active_candidate_consents.purpose='incluir_no_banco_de_talentos_global' via PostgREST embed !inner.

3. **useCandidates.ts** (MODIFY) — useCandidate(id) via RPC read_candidate_with_log com staleTime 60s. Adicionar useCandidateByCpf que normaliza antes de buscar.

4. **useCardPreferences.ts** (NOVO) — hook React wrapping load/save de cardCustomization.ts.

5. **useDataAccessLog.ts** (NOVO) — query simples para alimentar AuditLogPanel (Plan 09).

6. **useCandidateTags.ts** (NOVO, TAL-02) — agrega tags por empresa/vaga do histórico de applications do candidato. Surface também via useTalentPool (campo derivado `tags`) para o Banco de Talentos exibir histórico cross-empresa.

7. **supabase/functions/apply-to-job/index.ts** (MODIFY) — persiste consents granulares apos criar candidate.

Ativa testes Wave 0 (useTalentPool.test.tsx, useCandidateConsents.test.tsx, useCandidateTags.test.tsx).

Output: 6 hooks + 1 Edge Function + 3 testes ativos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-r-s-refactor/02-CONTEXT.md
@.planning/phases/02-r-s-refactor/02-RESEARCH.md
@.planning/phases/02-r-s-refactor/02-PATTERNS.md
@.planning/phases/02-r-s-refactor/02-04-SUMMARY.md
@CLAUDE.md
@src/hooks/hiring/useTalentPool.ts
@src/hooks/hiring/useCandidates.ts
@supabase/functions/apply-to-job/index.ts

<interfaces>
From src/lib/hiring/cardCustomization.ts (Plan 03):
- OPTIONAL_FIELDS readonly array
- CardPreferences type (Zod)
- DEFAULT_CARD_PREFERENCES
- loadCardPreferences(userId) and saveCardPreferences(userId, prefs)

From src/lib/hiring/cpf.ts (Plan 03):
- normalizeCpf(input) returns 11 digits or null
- isValidCpfFormat(input) returns boolean

From src/integrations/supabase/hiring-types.ts (Plan 04):
- Consent, ActiveConsent, ConsentPurpose, ConsentLegalBasis types
- DataAccessLogEntry type

From src/shared/data/useScopedQuery.ts (Phase 1 chokepoint):
- useScopedQuery(key, fetcher, options) returns UseQueryResult; queryKey final inclui scope.id e scope.kind como prefixo

From supabase/functions/apply-to-job/index.ts (atual):
- admin client via createClient com SUPABASE_SERVICE_ROLE_KEY
- form.get("consent") atual eh checkbox boolean unico — substituir por form.get("consents") JSON object
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create useCandidateConsents.ts (3 hooks) + activate test</name>
  <files>src/hooks/hiring/useCandidateConsents.ts, tests/hiring/useCandidateConsents.test.tsx</files>
  <read_first>
    src/hooks/hiring/useApplications.ts (analog de useReuseCandidateForJob),
    src/integrations/supabase/hiring-types.ts (Consent, ActiveConsent, ConsentPurpose),
    src/shared/data/useScopedQuery.ts,
    .planning/phases/02-r-s-refactor/02-RESEARCH.md secao §7 lines 1208-1257,
    .planning/phases/02-r-s-refactor/02-PATTERNS.md "useRevokeConsent.ts" lines 258-296
  </read_first>
  <behavior>
    - useActiveConsents(candidateId) retorna ActiveConsent[]; queryKey ['hiring', 'candidate-consents', candidateId]; staleTime 30s
    - useRevokeConsent UPDATE candidate_consents SET revoked_at=NOW, revoked_by=scope.userId WHERE id=consentId; onSuccess invalida candidate-consents + talent-pool; toast 'Consentimento revogado'
    - useGrantConsent INSERT candidate_consents com candidate_id, purpose, legal_basis (default 'consent'), granted_by=scope.userId, expires_at, document_url
    - import toast from "sonner"
  </behavior>
  <action>
Passo 1 — Ativar tests/hiring/useCandidateConsents.test.tsx (remover .skip, escrever tests):

```typescript
import { describe, it, expect, vi, beforeEach, afterEach, beforeAll, afterAll } from "vitest";
import { renderHook, waitFor, act } from "@testing-library/react";
import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
import { setupServer } from "msw/node";
import { http, HttpResponse } from "msw";
import * as scopeModule from "@/app/providers/ScopeProvider";
import { useActiveConsents, useRevokeConsent } from "@/hooks/hiring/useCandidateConsents";

const SUPABASE_URL = "https://ehbxpbeijofxtsbezwxd.supabase.co";
const server = setupServer();
beforeAll(() => server.listen({ onUnhandledRequest: "warn" }));
afterEach(() => server.resetHandlers());
afterAll(() => server.close());

beforeEach(() => {
  vi.spyOn(scopeModule, "useScope").mockReturnValue({
    scope: { id: "company:abc", kind: "company", companyIds: ["c1"], userId: "u1" },
  } as ReturnType<typeof scopeModule.useScope>);
});

function createWrapper() {
  const client = new QueryClient({ defaultOptions: { queries: { retry: false }, mutations: { retry: false } } });
  return {
    client,
    Wrapper: ({ children }: { children: React.ReactNode }) => (
      <QueryClientProvider client={client}>{children}</QueryClientProvider>
    ),
  };
}

describe("useActiveConsents", () => {
  it("retorna lista de consents ativos", async () => {
    server.use(
      http.get(SUPABASE_URL + "/rest/v1/active_candidate_consents", () =>
        HttpResponse.json([{ id: "c-1", candidate_id: "cand-1", purpose: "incluir_no_banco_de_talentos_global", granted_at: "2026-01-01T00:00:00Z" }])
      )
    );
    const { Wrapper } = createWrapper();
    const { result } = renderHook(() => useActiveConsents("cand-1"), { wrapper: Wrapper });
    await waitFor(() => expect(result.current.isSuccess).toBe(true));
    expect(result.current.data).toHaveLength(1);
  });
  it("desabilitada quando candidateId undefined", () => {
    const { Wrapper } = createWrapper();
    const { result } = renderHook(() => useActiveConsents(undefined), { wrapper: Wrapper });
    expect(result.current.isFetching).toBe(false);
  });
});

describe("useRevokeConsent", () => {
  it("UPDATE com revoked_at + revoked_by=scope.userId", async () => {
    let body: unknown = null;
    server.use(
      http.patch(SUPABASE_URL + "/rest/v1/candidate_consents", async ({ request }) => {
        body = await request.json();
        return HttpResponse.json([{ id: "c-1" }]);
      })
    );
    const { Wrapper } = createWrapper();
    const { result } = renderHook(() => useRevokeConsent(), { wrapper: Wrapper });
    await act(async () => {
      await result.current.mutateAsync({ consentId: "c-1", candidateId: "cand-1" });
    });
    expect(body).toMatchObject({ revoked_by: "u1" });
    expect((body as { revoked_at?: string })?.revoked_at).toBeDefined();
  });
  it("onSuccess invalida talent-pool E candidate-consents", async () => {
    server.use(http.patch(SUPABASE_URL + "/rest/v1/candidate_consents", () => HttpResponse.json([{ id: "c-1" }])));
    const { client, Wrapper } = createWrapper();
    const invalidateSpy = vi.spyOn(client, "invalidateQueries");
    const { result } = renderHook(() => useRevokeConsent(), { wrapper: Wrapper });
    await act(async () => {
      await result.current.mutateAsync({ consentId: "c-1", candidateId: "cand-1" });
    });
    expect(invalidateSpy).toHaveBeenCalledWith(expect.objectContaining({ queryKey: expect.arrayContaining(["candidate-consents"]) }));
    expect(invalidateSpy).toHaveBeenCalledWith(expect.objectContaining({ queryKey: expect.arrayContaining(["talent-pool"]) }));
  });
});
```

Passo 2 — Implementar src/hooks/hiring/useCandidateConsents.ts (paste o codigo TS abaixo):

```typescript
import { useMutation, useQueryClient } from "@tanstack/react-query";
import { supabase } from "@/integrations/supabase/client";
import { toast } from "sonner";
import { useScope } from "@/app/providers/ScopeProvider";
import { useScopedQuery } from "@/shared/data/useScopedQuery";
import type { ActiveConsent, Consent, ConsentPurpose, ConsentLegalBasis } from "@/integrations/supabase/hiring-types";

export interface RevokeConsentArgs { consentId: string; candidateId: string; }
export interface GrantConsentArgs { candidateId: string; purpose: ConsentPurpose; legalBasis?: ConsentLegalBasis; expiresAt?: string; documentUrl?: string; }

export function useActiveConsents(candidateId: string | undefined) {
  return useScopedQuery<readonly ["hiring", "candidate-consents", string], ActiveConsent[], Error>(
    ["hiring", "candidate-consents", candidateId ?? "none"] as const,
    async () => {
      if (!candidateId) return [];
      const { data, error } = await supabase
        .from("active_candidate_consents")
        .select("*")
        .eq("candidate_id", candidateId)
        .order("granted_at", { ascending: false });
      if (error) throw error;
      return (data ?? []) as ActiveConsent[];
    },
    { enabled: !!candidateId, staleTime: 30_000 },
  );
}

export function useRevokeConsent() {
  const queryClient = useQueryClient();
  const { scope } = useScope();
  return useMutation({
    mutationFn: async (args: RevokeConsentArgs): Promise<void> => {
      const { error } = await supabase
        .from("candidate_consents")
        .update({ revoked_at: new Date().toISOString(), revoked_by: scope?.userId ?? null })
        .eq("id", args.consentId);
      if (error) throw error;
    },
    onSuccess: async (_, args) => {
      await queryClient.invalidateQueries({ queryKey: ["scope", scope?.id, scope?.kind, "hiring", "candidate-consents", args.candidateId] });
      await queryClient.invalidateQueries({ queryKey: ["scope", scope?.id, scope?.kind, "hiring", "talent-pool"] });
      toast.success("Consentimento revogado");
    },
    onError: (err: Error) => toast.error("Erro ao revogar", { description: err.message }),
  });
}

export function useGrantConsent() {
  const queryClient = useQueryClient();
  const { scope } = useScope();
  return useMutation({
    mutationFn: async (args: GrantConsentArgs): Promise<Consent> => {
      const { data, error } = await supabase
        .from("candidate_consents")
        .insert({
          candidate_id: args.candidateId,
          purpose: args.purpose,
          legal_basis: args.legalBasis ?? "consent",
          granted_by: scope?.userId ?? null,
          expires_at: args.expiresAt ?? null,
          document_url: args.documentUrl ?? null,
        })
        .select()
        .single();
      if (error) throw error;
      return data as Consent;
    },
    onSuccess: async (_, args) => {
      await queryClient.invalidateQueries({ queryKey: ["scope", scope?.id, scope?.kind, "hiring", "candidate-consents", args.candidateId] });
      await queryClient.invalidateQueries({ queryKey: ["scope", scope?.id, scope?.kind, "hiring", "talent-pool"] });
      toast.success("Consentimento registrado");
    },
    onError: (err: Error) => toast.error("Erro ao registrar consent", { description: err.message }),
  });
}
```
  </action>
  <verify>
    <automated>npm test -- --run tests/hiring/useCandidateConsents.test.tsx 2>&1 | tail -8</automated>
  </verify>
  <done>useCandidateConsents.ts exporta 3 hooks (useActiveConsents, useRevokeConsent, useGrantConsent); teste ativo e green; toast import eh "sonner".</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Modify useTalentPool to filter by active consents + activate test</name>
  <files>src/hooks/hiring/useTalentPool.ts, tests/hiring/useTalentPool.test.tsx</files>
  <read_first>
    src/hooks/hiring/useTalentPool.ts (estado atual — preservar busca textual e outros filtros),
    .planning/phases/02-r-s-refactor/02-RESEARCH.md secao §7 lines 1262-1275,
    .planning/phases/02-r-s-refactor/02-PATTERNS.md "useTalentPool.ts" lines 763-779
  </read_first>
  <behavior>
    - SELECT estende com consents:active_candidate_consents!inner(purpose, granted_at, expires_at)
    - Filtro .eq("consents.purpose", "incluir_no_banco_de_talentos_global")
    - Mantem .is("anonymized_at", null)
    - queryKey shape preservado
  </behavior>
  <action>
Passo 1 — Ler useTalentPool.ts atual e identificar a chamada .from("candidates").select(...).

Passo 2 — Ativar tests/hiring/useTalentPool.test.tsx:

```typescript
import { describe, it, expect, vi, beforeEach, afterEach, beforeAll, afterAll } from "vitest";
import { renderHook, waitFor } from "@testing-library/react";
import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
import { setupServer } from "msw/node";
import { http, HttpResponse } from "msw";
import * as scopeModule from "@/app/providers/ScopeProvider";
import { useTalentPool } from "@/hooks/hiring/useTalentPool";

const SUPABASE_URL = "https://ehbxpbeijofxtsbezwxd.supabase.co";
const server = setupServer();
beforeAll(() => server.listen({ onUnhandledRequest: "warn" }));
afterEach(() => server.resetHandlers());
afterAll(() => server.close());

beforeEach(() => {
  vi.spyOn(scopeModule, "useScope").mockReturnValue({
    scope: { id: "company:abc", kind: "company", companyIds: ["c1"], userId: "u1" },
  } as ReturnType<typeof scopeModule.useScope>);
});

function createWrapper() {
  const client = new QueryClient({ defaultOptions: { queries: { retry: false } } });
  return { client, Wrapper: ({ children }: { children: React.ReactNode }) => (<QueryClientProvider client={client}>{children}</QueryClientProvider>) };
}

describe("useTalentPool — filtra por active consents (TAL-04)", () => {
  it("query inclui filter consents.purpose=incluir_no_banco_de_talentos_global", async () => {
    let url = "";
    server.use(http.get(SUPABASE_URL + "/rest/v1/candidates", ({ request }) => { url = request.url; return HttpResponse.json([]); }));
    const { Wrapper } = createWrapper();
    const { result } = renderHook(() => useTalentPool({ searchTerm: "" }), { wrapper: Wrapper });
    await waitFor(() => expect(result.current.isSuccess || result.current.isError).toBe(true));
    expect(url).toContain("active_candidate_consents");
    expect(url).toContain("incluir_no_banco_de_talentos_global");
  });
});
```

Passo 3 — Modificar useTalentPool.ts:

ANTES (forma atual):
```
let q = supabase.from("candidates").select("*, applications:applications(...), conversations:candidate_conversations(...)").is("anonymized_at", null);
```

DEPOIS (consents filter + TAL-02 tags embed via applications -> job_openings -> companies):
```
let q = supabase.from("candidates").select(`
  *,
  consents:active_candidate_consents!inner(purpose, granted_at, expires_at),
  applications:applications(
    id,
    created_at,
    stage,
    job_opening:job_openings(
      id,
      title,
      company_id,
      company:companies(id, name)
    )
  ),
  conversations:candidate_conversations(...)
`).eq("consents.purpose", "incluir_no_banco_de_talentos_global").is("anonymized_at", null);
```

Apos receber data, mapear cada candidato para incluir o campo derivado `tags`:
```typescript
type CandidateTag = { company_id: string; company_name: string; job_title: string; last_applied_at: string };

function deriveTags(applications: Array<{ created_at: string; job_opening: { title: string; company: { id: string; name: string } | null } | null }>): CandidateTag[] {
  // Agrupa por company_id, pegando job_title mais recente e last_applied_at mais recente por empresa
  const byCompany = new Map<string, CandidateTag>();
  for (const app of applications ?? []) {
    const co = app.job_opening?.company;
    const jt = app.job_opening?.title;
    if (!co || !jt) continue;
    const existing = byCompany.get(co.id);
    if (!existing || app.created_at > existing.last_applied_at) {
      byCompany.set(co.id, { company_id: co.id, company_name: co.name, job_title: jt, last_applied_at: app.created_at });
    }
  }
  return Array.from(byCompany.values()).sort((a, b) => b.last_applied_at.localeCompare(a.last_applied_at));
}

const candidatesWithTags = (data ?? []).map((c) => ({ ...c, tags: deriveTags(c.applications ?? []) }));
return candidatesWithTags;
```

NAO ALTERAR busca textual, filtros de tag UI (search), ordenacao.

Verificar:
```
grep -A 5 "active_candidate_consents" src/hooks/hiring/useTalentPool.ts
npm test -- --run tests/hiring/useTalentPool.test.tsx 2>&1 | tail -8
```
  </action>
  <verify>
    <automated>npm test -- --run tests/hiring/useTalentPool.test.tsx 2>&1 | tail -8</automated>
  </verify>
  <done>useTalentPool.ts .select() inclui consents:active_candidate_consents!inner(...) e .eq("consents.purpose", "incluir_no_banco_de_talentos_global"); teste ativo e green.</done>
</task>

<task type="auto">
  <name>Task 3: Modify useCandidates.ts — useCandidate via RPC + add useCandidateByCpf</name>
  <files>src/hooks/hiring/useCandidates.ts</files>
  <read_first>
    src/hooks/hiring/useCandidates.ts (atual — preservar todos os hooks existentes),
    src/lib/hiring/cpf.ts (Plan 03 — normalizeCpf, isValidCpfFormat),
    src/integrations/supabase/hiring-types.ts (CandidateRow),
    .planning/phases/02-r-s-refactor/02-RESEARCH.md secao §6 lines 942-963 e §8 lines 1373-1399,
    .planning/phases/02-r-s-refactor/02-PATTERNS.md "DuplicateCandidateDialog" assignment
  </read_first>
  <action>
Substituir useCandidate(id) — novo body chama RPC read_candidate_with_log com p_context para registrar acesso (TAL-06):

```typescript
export function useCandidate(id: string | undefined, context: string = "drawer") {
  return useScopedQuery<readonly ["hiring", "candidates", "detail", string, string], CandidateRow | null, Error>(
    ["hiring", "candidates", "detail", id ?? "none", context] as const,
    async (): Promise<CandidateRow | null> => {
      if (!id) return null;
      const { data, error } = await supabase.rpc("read_candidate_with_log", { p_candidate_id: id, p_context: context });
      if (error) throw error;
      return (data as CandidateRow | null) ?? null;
    },
    { enabled: !!id, staleTime: 60_000 },
  );
}
```

Adicionar useCandidateByCpf — analog de useCandidateByEmail (NAO usa useScopedQuery — busca cross-empresa para dedup TAL-09):

```typescript
import { normalizeCpf, isValidCpfFormat } from "@/lib/hiring/cpf";
import { useQuery } from "@tanstack/react-query";

export function useCandidateByCpf(cpf: string | null | undefined) {
  const normalized = normalizeCpf(cpf);
  return useQuery<CandidateRow | null, Error>({
    queryKey: ["hiring", "candidates", "by-cpf", normalized ?? "none"],
    enabled: isValidCpfFormat(cpf),
    queryFn: async (): Promise<CandidateRow | null> => {
      if (!normalized) return null;
      const { data, error } = await supabase.from("candidates").select("*").eq("cpf", normalized).is("anonymized_at", null).maybeSingle();
      if (error) throw error;
      return (data as CandidateRow) ?? null;
    },
    staleTime: 30_000,
  });
}
```

Preservar useCandidates, useCandidateByEmail, useAnonymizeCandidate, etc.

Verificar:
```
grep -c "read_candidate_with_log" src/hooks/hiring/useCandidates.ts
grep -c "useCandidateByCpf" src/hooks/hiring/useCandidates.ts
npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep useCandidates
```
  </action>
  <verify>
    <automated>grep -c "read_candidate_with_log" src/hooks/hiring/useCandidates.ts && grep -c "useCandidateByCpf" src/hooks/hiring/useCandidates.ts</automated>
  </verify>
  <done>useCandidate(id, context) usa RPC read_candidate_with_log; useCandidateByCpf adicionado; outros hooks preservados; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 4: Create useCardPreferences hook + useDataAccessLog hook</name>
  <files>src/hooks/hiring/useCardPreferences.ts, src/hooks/hiring/useDataAccessLog.ts</files>
  <read_first>
    src/lib/hiring/cardCustomization.ts (Plan 03),
    src/integrations/supabase/hiring-types.ts (DataAccessLogEntry),
    src/hooks/useAuth.ts (acesso a user.id),
    src/shared/data/useScopedQuery.ts,
    .planning/phases/02-r-s-refactor/02-RESEARCH.md secao §15 lines 2015-2039
  </read_first>
  <action>
src/hooks/hiring/useCardPreferences.ts:

```typescript
import { useEffect, useState } from "react";
import { useAuth } from "@/hooks/useAuth";
import { loadCardPreferences, saveCardPreferences, type CardPreferences } from "@/lib/hiring/cardCustomization";

export function useCardPreferences(): [CardPreferences, (next: CardPreferences) => void] {
  const { user } = useAuth();
  const userId = user?.id ?? null;
  const [prefs, setPrefs] = useState<CardPreferences>(() => loadCardPreferences(userId));

  useEffect(() => { setPrefs(loadCardPreferences(userId)); }, [userId]);

  useEffect(() => {
    if (!userId) return;
    const onStorage = (e: StorageEvent) => {
      if (e.key === "leverup:rs:card-fields:" + userId) setPrefs(loadCardPreferences(userId));
    };
    window.addEventListener("storage", onStorage);
    return () => window.removeEventListener("storage", onStorage);
  }, [userId]);

  const update = (next: CardPreferences) => {
    setPrefs(next);
    if (userId) saveCardPreferences(userId, next);
  };

  return [prefs, update];
}
```

src/hooks/hiring/useDataAccessLog.ts:

```typescript
import { useScopedQuery } from "@/shared/data/useScopedQuery";
import { supabase } from "@/integrations/supabase/client";
import type { DataAccessLogEntry } from "@/integrations/supabase/hiring-types";

export function useDataAccessLog(entityType: "candidate" | "application", entityId: string | undefined, limit: number = 50) {
  return useScopedQuery<readonly ["hiring", "data-access-log", string, string, number], DataAccessLogEntry[], Error>(
    ["hiring", "data-access-log", entityType, entityId ?? "none", limit] as const,
    async (): Promise<DataAccessLogEntry[]> => {
      if (!entityId) return [];
      const { data, error } = await supabase
        .from("data_access_log")
        .select("*")
        .eq("entity_type", entityType)
        .eq("entity_id", entityId)
        .order("at", { ascending: false })
        .limit(limit);
      if (error) throw error;
      return (data ?? []) as DataAccessLogEntry[];
    },
    { enabled: !!entityId, staleTime: 60_000 },
  );
}
```

Verificar:
```
npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -E "useCardPreferences|useDataAccessLog"
```
  </action>
  <verify>
    <automated>test -f src/hooks/hiring/useCardPreferences.ts && test -f src/hooks/hiring/useDataAccessLog.ts && grep -c "loadCardPreferences" src/hooks/hiring/useCardPreferences.ts</automated>
  </verify>
  <done>useCardPreferences.ts e useDataAccessLog.ts criados; ambos exportam suas funcoes/hooks; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Create useCandidateTags hook (TAL-02) + activate test</name>
  <files>src/hooks/hiring/useCandidateTags.ts, tests/hiring/useCandidateTags.test.tsx</files>
  <read_first>
    src/hooks/hiring/useCandidates.ts (analog para hook query),
    src/hooks/hiring/useTalentPool.ts (Task 2 — referência do shape de tags),
    src/integrations/supabase/hiring-types.ts (CandidateRow, ApplicationRow types),
    src/shared/data/useScopedQuery.ts,
    .planning/REQUIREMENTS.md (TAL-02 line ~80)
  </read_first>
  <behavior>
    - useCandidateTags(candidateId) retorna CandidateTag[] (uma por empresa onde candidato participou)
    - queryKey ['hiring', 'candidate-tags', candidateId]; staleTime 60s
    - SELECT applications.id, created_at, job_opening:job_openings(title, company_id, company:companies(id, name))
    - Filtra applications.candidate_id = candidateId
    - Agrupa por company_id; para cada empresa, escolhe a application mais recente (job_title, last_applied_at)
    - Retorna ordenado por last_applied_at DESC
  </behavior>
  <action>
Passo 1 — Ativar tests/hiring/useCandidateTags.test.tsx:

```typescript
import { describe, it, expect, vi, beforeEach, afterEach, beforeAll, afterAll } from "vitest";
import { renderHook, waitFor } from "@testing-library/react";
import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
import { setupServer } from "msw/node";
import { http, HttpResponse } from "msw";
import * as scopeModule from "@/app/providers/ScopeProvider";
import { useCandidateTags } from "@/hooks/hiring/useCandidateTags";

const SUPABASE_URL = "https://ehbxpbeijofxtsbezwxd.supabase.co";
const server = setupServer();
beforeAll(() => server.listen({ onUnhandledRequest: "warn" }));
afterEach(() => server.resetHandlers());
afterAll(() => server.close());

beforeEach(() => {
  vi.spyOn(scopeModule, "useScope").mockReturnValue({
    scope: { id: "company:abc", kind: "company", companyIds: ["c1"], userId: "u1" },
  } as ReturnType<typeof scopeModule.useScope>);
});

function createWrapper() {
  const client = new QueryClient({ defaultOptions: { queries: { retry: false } } });
  return ({ children }: { children: React.ReactNode }) => (
    <QueryClientProvider client={client}>{children}</QueryClientProvider>
  );
}

describe("useCandidateTags (TAL-02)", () => {
  it("agrega tags por empresa para candidato com applications em 2 empresas distintas", async () => {
    server.use(
      http.get(SUPABASE_URL + "/rest/v1/applications", () =>
        HttpResponse.json([
          { id: "a1", created_at: "2026-01-15T00:00:00Z", job_opening: { title: "Eng Backend", company_id: "co-1", company: { id: "co-1", name: "Empresa Alpha" } } },
          { id: "a2", created_at: "2026-03-10T00:00:00Z", job_opening: { title: "Eng Senior", company_id: "co-1", company: { id: "co-1", name: "Empresa Alpha" } } },
          { id: "a3", created_at: "2026-02-20T00:00:00Z", job_opening: { title: "Tech Lead", company_id: "co-2", company: { id: "co-2", name: "Empresa Beta" } } },
        ])
      )
    );
    const Wrapper = createWrapper();
    const { result } = renderHook(() => useCandidateTags("cand-1"), { wrapper: Wrapper });
    await waitFor(() => expect(result.current.isSuccess).toBe(true));
    expect(result.current.data).toHaveLength(2);
    // Empresa Alpha — escolhe a application MAIS RECENTE (a2 = "Eng Senior" em 2026-03-10)
    const alpha = result.current.data?.find((t) => t.company_id === "co-1");
    expect(alpha?.company_name).toBe("Empresa Alpha");
    expect(alpha?.job_title).toBe("Eng Senior");
    expect(alpha?.last_applied_at).toBe("2026-03-10T00:00:00Z");
    // Empresa Beta
    const beta = result.current.data?.find((t) => t.company_id === "co-2");
    expect(beta?.job_title).toBe("Tech Lead");
    // Ordenação: empresa com last_applied_at mais recente primeiro
    expect(result.current.data?.[0].company_id).toBe("co-1");
  });

  it("retorna array vazio quando candidato não tem applications", async () => {
    server.use(http.get(SUPABASE_URL + "/rest/v1/applications", () => HttpResponse.json([])));
    const Wrapper = createWrapper();
    const { result } = renderHook(() => useCandidateTags("cand-2"), { wrapper: Wrapper });
    await waitFor(() => expect(result.current.isSuccess).toBe(true));
    expect(result.current.data).toEqual([]);
  });

  it("desabilitada quando candidateId undefined", () => {
    const Wrapper = createWrapper();
    const { result } = renderHook(() => useCandidateTags(undefined), { wrapper: Wrapper });
    expect(result.current.isFetching).toBe(false);
  });
});
```

Passo 2 — Implementar src/hooks/hiring/useCandidateTags.ts:

```typescript
import { useScopedQuery } from "@/shared/data/useScopedQuery";
import { supabase } from "@/integrations/supabase/client";

export interface CandidateTag {
  company_id: string;
  company_name: string;
  job_title: string;
  last_applied_at: string;
}

interface ApplicationWithJobAndCompany {
  id: string;
  created_at: string;
  job_opening: {
    title: string | null;
    company_id: string | null;
    company: { id: string; name: string } | null;
  } | null;
}

/**
 * TAL-02: tags por empresa/vaga em que o candidato participou (histórico).
 *
 * Agrega applications cross-empresa: para cada company_id distinto onde o candidato
 * teve application, retorna a entrada com job_title e last_applied_at MAIS RECENTES.
 *
 * Sort: company com last_applied_at DESC (mais recente primeiro).
 */
export function useCandidateTags(candidateId: string | undefined) {
  return useScopedQuery<readonly ["hiring", "candidate-tags", string], CandidateTag[], Error>(
    ["hiring", "candidate-tags", candidateId ?? "none"] as const,
    async (): Promise<CandidateTag[]> => {
      if (!candidateId) return [];
      const { data, error } = await supabase
        .from("applications")
        .select(`
          id,
          created_at,
          job_opening:job_openings(
            title,
            company_id,
            company:companies(id, name)
          )
        `)
        .eq("candidate_id", candidateId)
        .order("created_at", { ascending: false });

      if (error) throw error;

      const apps = (data ?? []) as unknown as ApplicationWithJobAndCompany[];
      const byCompany = new Map<string, CandidateTag>();

      for (const app of apps) {
        const co = app.job_opening?.company;
        const jt = app.job_opening?.title;
        if (!co || !jt) continue;
        const existing = byCompany.get(co.id);
        if (!existing || app.created_at > existing.last_applied_at) {
          byCompany.set(co.id, {
            company_id: co.id,
            company_name: co.name,
            job_title: jt,
            last_applied_at: app.created_at,
          });
        }
      }

      return Array.from(byCompany.values()).sort((a, b) =>
        b.last_applied_at.localeCompare(a.last_applied_at)
      );
    },
    { enabled: !!candidateId, staleTime: 60_000 },
  );
}
```

Passo 3 — Verificar:

```bash
npm test -- --run tests/hiring/useCandidateTags.test.tsx 2>&1 | tail -8
grep -c "CandidateTag" src/hooks/hiring/useCandidateTags.ts
```
  </action>
  <verify>
    <automated>npm test -- --run tests/hiring/useCandidateTags.test.tsx 2>&1 | tail -8 && test -f src/hooks/hiring/useCandidateTags.ts</automated>
  </verify>
  <done>useCandidateTags.ts exporta hook que agrega tags por company a partir do histórico de applications; teste ativo e green com cenário de candidato em 2 empresas distintas surfaceando 2 tag entries; TAL-02 covered.</done>
</task>

<task type="auto">
  <name>Task 6: Modify apply-to-job Edge Function — persist candidate_consents</name>
  <files>supabase/functions/apply-to-job/index.ts</files>
  <read_first>
    supabase/functions/apply-to-job/index.ts (atual — identificar onde candidate eh criado e onde application eh criada),
    .planning/phases/02-r-s-refactor/02-RESEARCH.md secao §7 lines 1167-1198,
    .planning/phases/02-r-s-refactor/02-PATTERNS.md "apply-to-job/index.ts" lines 702-734
  </read_first>
  <action>
Encontrar no arquivo atual o ponto APOS criar application (provavelmente apos linhas ~150-180; ler em volta da chamada admin.from("applications").insert).

INSERIR apos a application ser criada:

```typescript
// Phase 2 TAL-04: persistir consents granulares (cada finalidade marcada vira 1 row)
const consentsRaw = form.get("consents");
let consents: Record<string, boolean> = {};
try { consents = JSON.parse(typeof consentsRaw === "string" ? consentsRaw : "{}"); } catch {}

const consentRows = Object.entries(consents)
  .filter(([_, granted]) => granted === true)
  .map(([purpose]) => ({
    candidate_id: candidateId,
    purpose,
    legal_basis: "consent",
    granted_at: new Date().toISOString(),
    granted_by: null, // self-granted via PublicApplicationForm
    expires_at: new Date(Date.now() + 24 * 30 * 24 * 60 * 60 * 1000).toISOString(), // 24 meses
  }));

if (consentRows.length > 0) {
  const { error: consentErr } = await admin.from("candidate_consents").insert(consentRows);
  if (consentErr) {
    // Nao falhar a application — log e seguir
    console.error("[consent] failed to persist", consentErr);
  }
}
```

ATENCAO: nao bloquear a application se consent persist falhar. Logar com prefix [consent] (CLAUDE.md no-PII rule — sem detalhes de candidato no log).

Preservar tambem o handler atual de form.get("consent") (checkbox boolean unico) por compat, OU substituir totalmente — depende do que ja existe. Confirmar com read.

Verificar:
```
grep -c "candidate_consents" supabase/functions/apply-to-job/index.ts
deno check supabase/functions/apply-to-job/index.ts 2>&1 | head
```

(deno check pode nao estar disponivel — alternativa: confirmar que o arquivo eh sintaticamente valido Deno/ESM via supabase functions deploy --dry-run apply-to-job se possivel; ou um simples `node --check` nao funciona em modulo Deno).

Para test runtime: rodar Edge Function localmente:
```
supabase functions serve apply-to-job --env-file supabase/.env.local
# em outro terminal:
curl -X POST http://localhost:54321/functions/v1/apply-to-job -F 'consents={"incluir_no_banco_de_talentos_global":true}' ...
```

Se isso for muito setup, deixar pra UAT em Plan 09.
  </action>
  <verify>
    <automated>grep -c "candidate_consents" supabase/functions/apply-to-job/index.ts</automated>
  </verify>
  <done>apply-to-job/index.ts INSERTs em candidate_consents para cada finalidade marcada apos criar application; falha silenciosa se consent insert falhar (log [consent]); preserva fluxo principal de candidate+application.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public form -> Edge Function | apply-to-job recebe formData; service-role bypassa RLS para INSERT em candidate_consents (intentional — candidato self-grants) |
| Browser RH -> useRevokeConsent | RLS UPDATE WITH CHECK is_people_manager garante que apenas RH/admin pode revogar |
| Browser RH -> useCandidate (RPC) | RPC SECURITY DEFINER re-aplica RLS logica E escreve log atomicamente — log nao pode ser bypassado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06-01 | T (Tampering) | Form publico envia consents={X:true} para finalidades nao listadas | mitigate | Edge Function valida purpose contra ENUM no insert (DB rejeita com 22P02 invalid_text_representation se nao bate) |
| T-02-06-02 | T (Tampering) | RH revoga consent de candidato fora do scope | mitigate | RLS UPDATE com USING is_people_manager — apenas RH global; aceitavel risk porque is_people_manager ja eh global por design Phase 1 |
| T-02-06-03 | I (Information disclosure) | useCandidate via RPC vaza erro com PII | mitigate | RPC RAISE EXCEPTION 'Sem permissao' (sem PII no message) — verificado em Plan 02 SQL |
| T-02-06-04 | D (DoS) | Drawer re-render loop dispara N RPCs (N log entries) | mitigate | staleTime: 60_000 em useCandidate evita re-fetch dentro de 1 min |
| T-02-06-05 | T (Tampering) | localStorage prefs corrompido afeta UI render | mitigate | Plan 03 cardCustomization.ts faz Zod safeParse — fallback para DEFAULT se invalido |
| T-02-06-06 | I (Information disclosure) | Edge Function loga PII no failure path | mitigate | console.error("[consent] failed to persist", consentErr) — sem candidate name/email; CLAUDE.md compliant |
</threat_model>

<verification>
- 6 hooks em src/hooks/hiring/ criados/modificados
- 1 Edge Function modificada (apply-to-job/index.ts)
- 3 testes Wave 0 ativos e green
- npm test -- --run tests/hiring/useTalentPool.test.tsx tests/hiring/useCandidateConsents.test.tsx tests/hiring/useCandidateTags.test.tsx exit 0
- npx tsc --noEmit -p tsconfig.app.json clean em src/hooks/hiring/
- grep "active_candidate_consents" src/hooks/hiring/useTalentPool.ts retorna 1+ matches
- grep "read_candidate_with_log" src/hooks/hiring/useCandidates.ts retorna 1+ matches
- grep "candidate_consents" supabase/functions/apply-to-job/index.ts retorna 1+ matches
- grep "deriveTags\|tags:" src/hooks/hiring/useTalentPool.ts retorna 1+ matches (TAL-02)
- grep "CandidateTag" src/hooks/hiring/useCandidateTags.ts retorna 1+ matches
</verification>

<success_criteria>
1. LGPD layer (TAL-03/04/06/08) tem hooks completos para consent flow.
2. Banco de Talentos (useTalentPool) filtra por consent ativo — candidatos sem consent nao aparecem.
3. CPF dedup (TAL-09) tem hook client-side mirroring DB trigger.
4. Card customization (D-08) tem hook React pronto para Plan 07 consumir.
5. Edge Function apply-to-job persiste consents do form publico.
6. TAL-02: useCandidateTags hook + tags field em useTalentPool surface histórico de candidato por empresa/vaga; teste integration assert candidato em 2 empresas distintas surface 2 tag entries.
7. Wave 4 (UI) tem todos os hooks LGPD prontos.
</success_criteria>

<output>
Apos completar, criar .planning/phases/02-r-s-refactor/02-06-SUMMARY.md com:
- Lista de 5 hooks criados/modificados + 1 Edge Function
- Output de npm test -- --run mostrando ~95 tests green entre Plans 01-06
- Confirmacao de active_candidate_consents filter em useTalentPool e read_candidate_with_log RPC em useCandidate
- Proximo plano: 02-07-PLAN.md (Kanban surface — CandidatesKanban + CandidateCard + sparkbar + SLA)
</output>

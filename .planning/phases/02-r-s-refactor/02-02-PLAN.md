---
phase: 02-r-s-refactor
plan: 02
type: execute
wave: 1
depends_on: ["02-01"]
files_modified:
  - supabase/migrations/20260428120000_f1_normalize_legacy_application_stages.sql
  - supabase/migrations/20260428120100_f2_data_access_log_table.sql
  - supabase/migrations/20260428120200_f3_candidate_consents.sql
  - supabase/migrations/20260428120300_f4_cpf_canonical_dedup.sql
  - supabase/tests/006-migration-f-stages.sql
  - supabase/tests/007-data-access-log.sql
  - supabase/tests/008-candidate-consents.sql
  - supabase/tests/009-cpf-unique.sql
  - supabase/tests/010-pg-cron-retention.sql
autonomous: true
requirements:
  - RS-05
  - RS-06
  - TAL-03
  - TAL-05
  - TAL-06
  - TAL-07
  - TAL-09
must_haves:
  truths:
    - "Zero candidatos em stages legados (aguardando_fit_cultural, sem_retorno, fit_recebido) pós-Migration F.1 — mapping per CONTEXT.md D-mapping: aguardando_fit_cultural/fit_recebido -> fit_cultural; sem_retorno -> triagem com metadata.legacy_marker preservado"
    - "Toda leitura de PII de candidato pode ser feita via RPC read_candidate_with_log que escreve atomicamente em data_access_log"
    - "data_access_log nega INSERT direto do client (RLS sem policy INSERT) — só RPC SECURITY DEFINER escreve"
    - "candidate_consents tem EXCLUDE constraint que garante 1 consent ATIVO por (candidate_id, purpose)"
    - "active_candidate_consents view filtra revoked_at IS NULL E (expires_at IS NULL OR expires_at > now())"
    - "candidates.cpf tem UNIQUE partial index normalizando CPF para 11 dígitos antes de inserir"
    - "pg_cron job 'data_access_log_retention_cleanup' agendado para weekly Mon 03:30 UTC deletando rows >36 meses"
    - "Trigger anti-regressão bloqueia inserir/atualizar applications.stage com valor legado"
  artifacts:
    - path: "supabase/migrations/20260428120000_f1_normalize_legacy_application_stages.sql"
      provides: "Migration F.1 — 3 UPDATE batches mapeando legacy stages per CONTEXT.md D-mapping (fit_cultural/triagem) + tg_block_legacy_stages + applications.metadata jsonb"
      contains: "tg_block_legacy_stages"
    - path: "supabase/migrations/20260428120100_f2_data_access_log_table.sql"
      provides: "Migration F.2 — CREATE TABLE data_access_log + RPC read_candidate_with_log + cron.schedule"
      contains: "read_candidate_with_log"
    - path: "supabase/migrations/20260428120200_f3_candidate_consents.sql"
      provides: "Migration F.3 — CREATE TYPE consent_purpose_enum + TABLE candidate_consents + view active_candidate_consents + 3 RLS policies"
      contains: "active_candidate_consents"
    - path: "supabase/migrations/20260428120300_f4_cpf_canonical_dedup.sql"
      provides: "Migration F.4 — UNIQUE partial idx_candidates_cpf_unique + tg_normalize_candidate_cpf"
      contains: "idx_candidates_cpf_unique"
  key_links:
    - from: "Migration F.1 SQL UPDATE batches"
      to: "CONTEXT.md D-mapping (line 76)"
      via: "3 distinct UPDATE blocks: aguardando_fit_cultural -> fit_cultural; sem_retorno -> triagem (with legacy_marker); fit_recebido -> fit_cultural"
      pattern: "'fit_cultural'::public\\.application_stage_enum|'triagem'::public\\.application_stage_enum"
    - from: "read_candidate_with_log RPC"
      to: "data_access_log"
      via: "INSERT row dentro do mesmo statement do SELECT"
      pattern: "INSERT INTO public\\.data_access_log"
    - from: "active_candidate_consents view"
      to: "candidate_consents table"
      via: "WHERE revoked_at IS NULL AND (expires_at IS NULL OR expires_at > NOW())"
      pattern: "WHERE revoked_at IS NULL"
    - from: "tg_normalize_candidate_cpf trigger"
      to: "normalize_cpf function"
      via: "BEFORE INSERT OR UPDATE OF cpf calls normalize_cpf"
      pattern: "BEFORE INSERT OR UPDATE OF cpf"
---

<objective>
Migration F — Phase 2 database changes em **4 sub-migrations** sequenciais (mesmo timestamp prefix `20260428120{0,1,2,3}00`):

1. **F.1** — Normaliza candidatos em stages legados conforme CONTEXT.md D-mapping: `aguardando_fit_cultural` → `fit_cultural`, `sem_retorno` → `triagem` (preservando `metadata.legacy_marker = 'sem_retorno'` para rastreabilidade forense), `fit_recebido` → `fit_cultural`. Adiciona trigger anti-regressão.
2. **F.2** — Cria `data_access_log` (append-only, generaliza `candidate_access_log`), RPC `read_candidate_with_log(id, context)` e pg_cron retention 36 meses (weekly Mon 03:30 UTC).
3. **F.3** — Cria `candidate_consents` (granular por `purpose` + `legal_basis` + `expires_at` + `revoked_at`) com EXCLUDE 1-active-per-purpose + view `active_candidate_consents` + 3 RLS policies.
4. **F.4** — Adiciona `UNIQUE` partial index em `candidates.cpf` + trigger `tg_normalize_candidate_cpf` (regex remove pontuação, valida 11 dígitos).

Também ATIVA os 5 pgTAP tests criados em Wave 0 (remove `select skip(...)` e implementa as queries reais).

Purpose: destravar todo o restante de Phase 2 (hooks Wave 2 dependem dos novos schemas/RPC; UI Wave 3 depende dos tipos regenerados em Plan 04). Migration F é o **fundamento** — todas as outras peças se ancoram nela.

Output: 4 arquivos `.sql` em `supabase/migrations/` + 5 arquivos pgTAP atualizados. Migration aplicada local (`supabase db reset && supabase db push`) com pgTAP green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-r-s-refactor/02-CONTEXT.md
@.planning/phases/02-r-s-refactor/02-RESEARCH.md
@.planning/phases/02-r-s-refactor/02-PATTERNS.md
@.planning/phases/02-r-s-refactor/02-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Templates Phase 1 — referência verbatim para todos os 4 SQLs novos -->

From supabase/migrations/20260427120100_b2_org_units_and_helpers.sql (Phase 1 — DDL idempotente + DO $$ + trigger):
```sql
-- =========================================================================
-- Migration B2: <nome>
-- Pattern: <DDL-only / expand-backfill-contract / RPC + cron>
-- Threats: <T-X-XX>
-- REQs: <comma-separated>
-- =========================================================================
CREATE TABLE IF NOT EXISTS public.org_units (...);
CREATE INDEX IF NOT EXISTS idx_org_units_company_parent ON ...;
CREATE OR REPLACE FUNCTION public.org_unit_descendants(unit_id uuid)
RETURNS uuid[] LANGUAGE plpgsql STABLE SECURITY DEFINER SET search_path = public AS $$
DECLARE descendants uuid[]; BEGIN ... RETURN descendants; END $$;
DROP TRIGGER IF EXISTS tg_org_units_anti_cycle ON public.org_units;
CREATE TRIGGER tg_org_units_anti_cycle
  BEFORE INSERT OR UPDATE OF parent_id ON public.org_units
  FOR EACH ROW EXECUTE FUNCTION public.tg_check_org_unit_cycle();
ALTER TABLE public.org_units ENABLE ROW LEVEL SECURITY;
DROP POLICY IF EXISTS "..." ON public.org_units;
CREATE POLICY "..." ON public.org_units FOR SELECT TO authenticated
  USING ( (SELECT auth.uid()) IS NOT NULL );
COMMENT ON TABLE public.org_units IS '...';
```

From supabase/migrations/20260416193400_hiring_audit_and_locking.sql (Phase 0 — current candidate_access_log):
```sql
CREATE TABLE IF NOT EXISTS public.candidate_access_log (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  actor_id uuid NOT NULL REFERENCES public.profiles(id) ON DELETE RESTRICT,
  action text NOT NULL,
  resource text NOT NULL,
  resource_id uuid NOT NULL,
  created_at timestamptz NOT NULL DEFAULT NOW()
);
-- (será MIGRATED para data_access_log via INSERT SELECT em F.2)
```

From supabase/migrations/20260416193500_hiring_cron_jobs.sql (Phase 0 — pg_cron precedent):
```sql
SELECT cron.schedule(
  'hiring_application_followup',
  '0 12 * * *',
  $$ SELECT public.notify_application_inactive(); $$
);
```

From supabase/migrations/20260422150000_candidate_conversations.sql (Phase 0 — TYPE + table + RLS + view):
```sql
CREATE TYPE public.conversation_purpose_enum AS ENUM ('coffee_chat', 'screening', 'follow_up');
CREATE TABLE public.candidate_conversations (...);
ALTER TABLE public.candidate_conversations ENABLE ROW LEVEL SECURITY;
CREATE POLICY "candidate_conversations:select:rh_admin" ON public.candidate_conversations
  FOR SELECT TO authenticated USING (
    public.has_role((SELECT auth.uid()), 'admin'::public.app_role)
    OR public.has_role((SELECT auth.uid()), 'rh'::public.app_role)
  );
CREATE TRIGGER tg_candidate_conversations_updated_at
  BEFORE UPDATE ON public.candidate_conversations
  FOR EACH ROW EXECUTE FUNCTION public.tg_set_updated_at();
```

From supabase/migrations/20260427120200_c_socio_memberships_rls_rewrite_and_backfill.sql (Phase 1 — visible_companies helper):
```sql
-- public.visible_companies(uid uuid) RETURNS uuid[] LANGUAGE plpgsql STABLE SECURITY DEFINER ...
-- public.is_people_manager(uid uuid) RETURNS boolean — verificar se inclui 'rh'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration F.1 — Normalize legacy application stages</name>
  <files>supabase/migrations/20260428120000_f1_normalize_legacy_application_stages.sql, supabase/tests/006-migration-f-stages.sql</files>
  <read_first>
    supabase/migrations/20260427120100_b2_org_units_and_helpers.sql,
    supabase/migrations/20260416193400_hiring_audit_and_locking.sql (especificamente tg_enforce_application_stage_transition lines 100-127),
    supabase/migrations/20260422140000_permissive_application_stage_transitions.sql,
    src/lib/hiring/stageGroups.ts (mapping atual 16→6),
    src/lib/hiring/statusMachine.ts (APPLICATION_STAGE_TRANSITIONS),
    .planning/phases/02-r-s-refactor/02-RESEARCH.md (§5 lines 696-799)
  </read_first>
  <action>
Criar `supabase/migrations/20260428120000_f1_normalize_legacy_application_stages.sql` com **conteúdo verbatim** abaixo (cabeçalho adaptado ao formato Phase 1):

```sql
-- =========================================================================
-- Migration F.1: Normalize legacy application stages
--
-- Motivação: STAGE_GROUPS.ts mapeia 16 stages atuais em 6 grupos visuais
-- consolidados, mas 3 stages legados ('aguardando_fit_cultural', 'sem_retorno',
-- 'fit_recebido') ainda são valores válidos no enum application_stage_enum
-- e existem candidatos parados nesses valores. Eles geram bug do kanban
-- (canTransition() falha pois APPLICATION_STAGE_TRANSITIONS não tem entrada
-- explícita para alguns desses legados).
--
-- Pattern: expand-backfill (UPDATE direto em batch, SKIP LOCKED) + trigger
-- anti-regressão. Contract (drop dos enum values) fica para Phase 4 / Migration G.
-- Threats: bloqueio prolongado da tabela applications durante UPDATE em massa.
-- Mitigação: batch de 1000 + pg_sleep(0.05) entre batches.
-- REQs: RS-05, RS-06
-- =========================================================================

-- 1. Garantir que applications.metadata exista (jsonb append-only).
ALTER TABLE public.applications
  ADD COLUMN IF NOT EXISTS metadata jsonb NOT NULL DEFAULT '{}'::jsonb;

-- 2. Backfill batch — 3 UPDATEs distintos por legacy stage conforme CONTEXT.md D-mapping (linha 76):
--    'aguardando_fit_cultural' -> 'fit_cultural'
--    'sem_retorno'             -> 'triagem'      (com metadata.legacy_marker preservado)
--    'fit_recebido'            -> 'fit_cultural'
-- Cada UPDATE em batches de 1000 com SKIP LOCKED para evitar contention.

-- 2a. aguardando_fit_cultural -> fit_cultural
DO $$
DECLARE
  v_batch_size INT := 1000;
  v_affected INT;
BEGIN
  LOOP
    WITH legacy_apps AS (
      SELECT id
      FROM public.applications
      WHERE stage = 'aguardando_fit_cultural'
      LIMIT v_batch_size
      FOR UPDATE SKIP LOCKED
    )
    UPDATE public.applications a
       SET metadata = a.metadata || jsonb_build_object(
                        'legacy_marker', a.stage::text,
                        'normalized_at', NOW()
                      ),
           stage = 'fit_cultural'::public.application_stage_enum
      FROM legacy_apps
     WHERE a.id = legacy_apps.id;

    GET DIAGNOSTICS v_affected = ROW_COUNT;
    EXIT WHEN v_affected = 0;
    PERFORM pg_sleep(0.05);
  END LOOP;
END $$;

-- 2b. sem_retorno -> triagem (preserva legacy_marker 'sem_retorno' para rastreabilidade forense)
DO $$
DECLARE
  v_batch_size INT := 1000;
  v_affected INT;
BEGIN
  LOOP
    WITH legacy_apps AS (
      SELECT id
      FROM public.applications
      WHERE stage = 'sem_retorno'
      LIMIT v_batch_size
      FOR UPDATE SKIP LOCKED
    )
    UPDATE public.applications a
       SET metadata = jsonb_set(
                        coalesce(a.metadata, '{}'::jsonb),
                        '{legacy_marker}',
                        '"sem_retorno"'::jsonb
                      ) || jsonb_build_object('normalized_at', NOW()),
           stage = 'triagem'::public.application_stage_enum
      FROM legacy_apps
     WHERE a.id = legacy_apps.id;

    GET DIAGNOSTICS v_affected = ROW_COUNT;
    EXIT WHEN v_affected = 0;
    PERFORM pg_sleep(0.05);
  END LOOP;
END $$;

-- 2c. fit_recebido -> fit_cultural
DO $$
DECLARE
  v_batch_size INT := 1000;
  v_affected INT;
BEGIN
  LOOP
    WITH legacy_apps AS (
      SELECT id
      FROM public.applications
      WHERE stage = 'fit_recebido'
      LIMIT v_batch_size
      FOR UPDATE SKIP LOCKED
    )
    UPDATE public.applications a
       SET metadata = a.metadata || jsonb_build_object(
                        'legacy_marker', a.stage::text,
                        'normalized_at', NOW()
                      ),
           stage = 'fit_cultural'::public.application_stage_enum
      FROM legacy_apps
     WHERE a.id = legacy_apps.id;

    GET DIAGNOSTICS v_affected = ROW_COUNT;
    EXIT WHEN v_affected = 0;
    PERFORM pg_sleep(0.05);
  END LOOP;
END $$;

-- 3. Trigger anti-regressão: bloqueia INSERT/UPDATE com stage legado.
CREATE OR REPLACE FUNCTION public.tg_block_legacy_stages()
RETURNS TRIGGER LANGUAGE plpgsql AS $$
BEGIN
  IF NEW.stage IN ('aguardando_fit_cultural', 'sem_retorno', 'fit_recebido') THEN
    RAISE EXCEPTION 'Stage legado % não é permitido após Migration F.1. Use fit_cultural (ex aguardando_fit_cultural/fit_recebido) ou triagem (ex sem_retorno).', NEW.stage
      USING ERRCODE = 'check_violation';
  END IF;
  RETURN NEW;
END $$;

DROP TRIGGER IF EXISTS tg_applications_block_legacy_stages ON public.applications;
CREATE TRIGGER tg_applications_block_legacy_stages
  BEFORE INSERT OR UPDATE OF stage ON public.applications
  FOR EACH ROW EXECUTE FUNCTION public.tg_block_legacy_stages();

COMMENT ON FUNCTION public.tg_block_legacy_stages() IS
  'Phase 2 Migration F.1 — bloqueia regressão para stages legados pós-normalização.';
```

**Ativar pgTAP test `supabase/tests/006-migration-f-stages.sql`** (remover `select skip(...)` e implementar as 3 queries):

```sql
BEGIN;
SELECT plan(5);

SELECT tests.authenticate_as_service_role();

-- Setup fixture: aplicar a migration e criar 1 candidato + 1 application com stage legado
-- A migration já foi aplicada por supabase db push antes desse test rodar; só precisa criar fixture.
INSERT INTO public.companies (id, name) VALUES ('ffffffff-0000-0000-0000-000000000001', 'Test Co')
  ON CONFLICT (id) DO NOTHING;
INSERT INTO public.candidates (id, full_name, email)
  VALUES ('ffffffff-0000-0000-0000-000000000002', 'Test Candidate', 'test+f1@example.com')
  ON CONFLICT (id) DO NOTHING;
INSERT INTO public.job_openings (id, company_id, title, status)
  VALUES ('ffffffff-0000-0000-0000-000000000003', 'ffffffff-0000-0000-0000-000000000001', 'Test Job', 'aberta')
  ON CONFLICT (id) DO NOTHING;

-- TEST 1: Zero candidatos em stages legados pós-Migration F.1
SELECT is(
  (SELECT count(*) FROM public.applications WHERE stage IN ('aguardando_fit_cultural', 'sem_retorno', 'fit_recebido'))::bigint,
  0::bigint,
  'Zero applications em stages legados pós-Migration F.1'
);

-- TEST 2: Trigger tg_applications_block_legacy_stages bloqueia INSERT direto com stage legado
PREPARE insert_legacy AS
  INSERT INTO public.applications (candidate_id, job_opening_id, stage)
  VALUES (
    'ffffffff-0000-0000-0000-000000000002',
    'ffffffff-0000-0000-0000-000000000003',
    'aguardando_fit_cultural'
  );
SELECT throws_ok(
  'EXECUTE insert_legacy',
  '23514',
  NULL,
  'Trigger bloqueia INSERT com stage legado aguardando_fit_cultural'
);

-- TEST 3: applications.metadata coluna existe e é jsonb NOT NULL DEFAULT '{}'::jsonb
SELECT col_type_is('public', 'applications', 'metadata', 'jsonb', 'metadata coluna é jsonb');

-- TEST 4: Rows que originaram de 'sem_retorno' têm metadata->>legacy_marker = 'sem_retorno'
-- (insere fixture pré-mapping via SET session_replication_role para bypass do trigger durante setup)
SET session_replication_role = replica;
INSERT INTO public.applications (id, candidate_id, job_opening_id, stage, metadata)
  VALUES (
    'ffffffff-0000-0000-0000-0000000000aa',
    'ffffffff-0000-0000-0000-000000000002',
    'ffffffff-0000-0000-0000-000000000003',
    'sem_retorno',
    '{}'::jsonb
  );
SET session_replication_role = origin;

-- Re-aplicar UPDATE batch logic localmente para essa row
UPDATE public.applications a
   SET metadata = jsonb_set(coalesce(a.metadata, '{}'::jsonb), '{legacy_marker}', '"sem_retorno"'::jsonb)
                   || jsonb_build_object('normalized_at', NOW()),
       stage = 'triagem'::public.application_stage_enum
 WHERE a.id = 'ffffffff-0000-0000-0000-0000000000aa';

SELECT is(
  (SELECT metadata->>'legacy_marker' FROM public.applications
    WHERE id = 'ffffffff-0000-0000-0000-0000000000aa'),
  'sem_retorno',
  'Row migrada de sem_retorno preserva metadata.legacy_marker = sem_retorno'
);

-- TEST 5: A row migrada de sem_retorno está agora em stage = 'triagem'
SELECT is(
  (SELECT stage::text FROM public.applications
    WHERE id = 'ffffffff-0000-0000-0000-0000000000aa'),
  'triagem',
  'Row migrada de sem_retorno está em stage triagem (per CONTEXT.md D-mapping)'
);

SELECT * FROM finish();
ROLLBACK;
```

**Verificar localmente:**
1. `supabase db reset` (recria DB com toda Phase 1 + F.1).
2. `supabase db push` (idempotente; deve aplicar limpo).
3. `npx supabase test db --linked=false --file supabase/tests/006-migration-f-stages.sql 2>&1 | tail -20` → 5 ok, 0 fail.

**NÃO mexer em:** `application_stage_enum` (não dropar valores legados — Migration G na Phase 4 faz). `tg_enforce_application_stage_transition` (continua ativo, é defense-in-depth para `canTransition`).
  </action>
  <verify>
    <automated>test -f "supabase/migrations/20260428120000_f1_normalize_legacy_application_stages.sql" && grep -q "tg_block_legacy_stages" "supabase/migrations/20260428120000_f1_normalize_legacy_application_stages.sql" && grep -q "metadata jsonb" "supabase/migrations/20260428120000_f1_normalize_legacy_application_stages.sql" && grep -q "'fit_cultural'::public.application_stage_enum" "supabase/migrations/20260428120000_f1_normalize_legacy_application_stages.sql" && grep -q "'triagem'::public.application_stage_enum" "supabase/migrations/20260428120000_f1_normalize_legacy_application_stages.sql" && grep -q "'\"sem_retorno\"'::jsonb" "supabase/migrations/20260428120000_f1_normalize_legacy_application_stages.sql" && ! grep -q "select skip(" "supabase/tests/006-migration-f-stages.sql"</automated>
  </verify>
  <done>F.1 SQL escrito com `tg_block_legacy_stages` trigger + 3 UPDATE batches distintos (aguardando_fit_cultural/fit_recebido -> fit_cultural; sem_retorno -> triagem com `metadata.legacy_marker='sem_retorno'`) + ALTER TABLE metadata; pgTAP `006-migration-f-stages.sql` ativado (sem `select skip(`); trigger lança SQLSTATE `23514` para INSERT legado. SQL contém literais `'fit_cultural'`, `'triagem'`, e `'"sem_retorno"'::jsonb`.</done>
</task>

<task type="auto">
  <name>Task 2: Migration F.2 — data_access_log + read_candidate_with_log RPC + pg_cron retention</name>
  <files>supabase/migrations/20260428120100_f2_data_access_log_table.sql, supabase/tests/007-data-access-log.sql, supabase/tests/010-pg-cron-retention.sql</files>
  <read_first>
    supabase/migrations/20260416193400_hiring_audit_and_locking.sql,
    supabase/migrations/20260416193500_hiring_cron_jobs.sql,
    supabase/migrations/20260427120200_c_socio_memberships_rls_rewrite_and_backfill.sql (visible_companies + is_people_manager helpers — CONFIRMAR SE 'rh' role inclui no helper, conforme A10 da RESEARCH),
    src/integrations/supabase/types.ts (current candidate_access_log shape),
    .planning/phases/02-r-s-refactor/02-RESEARCH.md (§6 lines 819-1014)
  </read_first>
  <action>
Criar `supabase/migrations/20260428120100_f2_data_access_log_table.sql` com **conteúdo verbatim** abaixo (adaptado de RESEARCH §6):

```sql
-- =========================================================================
-- Migration F.2: data_access_log + read_candidate_with_log RPC + pg_cron retention
--
-- Motivação: TAL-05/06/07 exigem log append-only para todo acesso a PII de
-- candidatos. Generaliza candidate_access_log existente em data_access_log
-- com entity_type discriminator, scope_company_id, e context. RPC
-- read_candidate_with_log é o único caminho de leitura de PII (RLS direto
-- na tabela candidates é mantido para entity reads não-PII; o RPC força
-- escrita atômica no log). pg_cron deleta rows >36 meses semanalmente.
--
-- Pattern: append-only DDL + RPC SECURITY DEFINER + cron schedule.
-- Threats: T-02-02-01 (PII leakage via direct SELECT) — mitigado por RPC
--          + RLS denies; T-02-02-02 (log poisoning via INSERT direto) — mitigado
--          por NÃO ter RLS policy de INSERT, só RPC com SECURITY DEFINER escreve.
-- REQs: TAL-05, TAL-06, TAL-07
-- =========================================================================

-- 1. Tabela append-only generalizada
CREATE TABLE IF NOT EXISTS public.data_access_log (
  id                uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  actor_id          uuid NOT NULL REFERENCES public.profiles(id) ON DELETE RESTRICT,
  entity_type       text NOT NULL CHECK (entity_type IN ('candidate', 'application', 'cultural_fit_response', 'profile', 'salary')),
  entity_id         uuid NOT NULL,
  action            text NOT NULL CHECK (action IN ('view', 'export', 'update', 'anonymize', 'delete')),
  scope_company_id  uuid REFERENCES public.companies(id) ON DELETE SET NULL,
  context           text,
  ip_address        inet,
  user_agent        text,
  at                timestamptz NOT NULL DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_data_access_log_actor_at
  ON public.data_access_log (actor_id, at DESC);
CREATE INDEX IF NOT EXISTS idx_data_access_log_entity
  ON public.data_access_log (entity_type, entity_id, at DESC);

ALTER TABLE public.data_access_log ENABLE ROW LEVEL SECURITY;

-- 2. RLS: SELECT apenas para is_people_manager (admin/rh). NÃO há policy INSERT
-- para forçar uso da RPC SECURITY DEFINER abaixo.
DROP POLICY IF EXISTS "data_access_log:select:admin_rh_only" ON public.data_access_log;
CREATE POLICY "data_access_log:select:admin_rh_only"
  ON public.data_access_log FOR SELECT TO authenticated
  USING ( public.is_people_manager((SELECT auth.uid())) );

-- 3. RPC SECURITY DEFINER que lê candidate + escreve log atomicamente
CREATE OR REPLACE FUNCTION public.read_candidate_with_log(
  p_candidate_id uuid,
  p_context text DEFAULT 'view'
)
RETURNS public.candidates
LANGUAGE plpgsql
STABLE
SECURITY DEFINER
SET search_path = public
AS $$
DECLARE
  v_actor uuid := (SELECT auth.uid());
  v_candidate public.candidates;
  v_visible_companies uuid[];
  v_can_read boolean;
BEGIN
  IF v_actor IS NULL THEN
    RAISE EXCEPTION 'Não autenticado' USING ERRCODE = '42501';
  END IF;

  v_visible_companies := public.visible_companies(v_actor);

  -- Re-aplica RLS lógica como o caller (SECURITY DEFINER bypassaria sem isso)
  SELECT EXISTS (
    SELECT 1 FROM public.candidates c WHERE c.id = p_candidate_id
      AND (
        public.is_people_manager(v_actor)
        OR EXISTS (
          SELECT 1 FROM public.applications a
          JOIN public.job_openings j ON j.id = a.job_opening_id
          WHERE a.candidate_id = c.id
            AND j.company_id = ANY(v_visible_companies)
        )
      )
  ) INTO v_can_read;

  IF NOT v_can_read THEN
    RAISE EXCEPTION 'Sem permissão pra ler esse candidato' USING ERRCODE = '42501';
  END IF;

  SELECT * INTO v_candidate FROM public.candidates WHERE id = p_candidate_id;

  -- Log append-only (mesmo se v_candidate for NULL — útil pra auditar tentativas)
  INSERT INTO public.data_access_log (
    actor_id, entity_type, entity_id, action, context
  ) VALUES (
    v_actor, 'candidate', p_candidate_id, 'view', COALESCE(p_context, 'view')
  );

  RETURN v_candidate;
END $$;

GRANT EXECUTE ON FUNCTION public.read_candidate_with_log(uuid, text) TO authenticated;

-- 4. Migrar dados existentes de candidate_access_log para data_access_log
-- (idempotente — INSERT ON CONFLICT DO NOTHING, mas como uuid PKs novos, usamos NOT EXISTS)
INSERT INTO public.data_access_log (id, actor_id, entity_type, entity_id, action, at)
SELECT
  cal.id,
  cal.actor_id,
  CASE cal.resource
    WHEN 'candidates' THEN 'candidate'
    WHEN 'applications' THEN 'application'
    WHEN 'interviews' THEN 'application'
    ELSE 'candidate'
  END,
  cal.resource_id,
  cal.action,
  cal.created_at
FROM public.candidate_access_log cal
WHERE NOT EXISTS (
  SELECT 1 FROM public.data_access_log dal WHERE dal.id = cal.id
);

-- 5. pg_cron — retention 36 meses (rotina semanal segunda-feira 03:30 UTC = 00:30 BRT)
-- (uses ON CONFLICT-equivalent via cron.unschedule + cron.schedule — pg_cron API)
DO $$
BEGIN
  PERFORM cron.unschedule('data_access_log_retention_cleanup')
    WHERE EXISTS (SELECT 1 FROM cron.job WHERE jobname = 'data_access_log_retention_cleanup');
EXCEPTION WHEN OTHERS THEN NULL;
END $$;

SELECT cron.schedule(
  'data_access_log_retention_cleanup',
  '30 3 * * 1',
  $cron$
    DELETE FROM public.data_access_log WHERE at < NOW() - INTERVAL '36 months';
  $cron$
);

COMMENT ON TABLE public.data_access_log IS
  'Phase 2 Migration F.2 — append-only audit log generalizado. Inserts apenas via SECURITY DEFINER RPCs.';
COMMENT ON FUNCTION public.read_candidate_with_log(uuid, text) IS
  'Phase 2 Migration F.2 — único caminho de leitura de PII de candidato. Escreve em data_access_log atomicamente.';
```

**Verificação CRÍTICA antes de finalizar (Assumption A10 da RESEARCH):**
Antes de commitar, confirme que `public.is_people_manager(uid uuid)` retorna `true` para roles `'admin'`, `'rh'` E `'socio'` (Phase 1 helper deve incluir `'rh'`). Rodar:

```bash
grep -A 20 "CREATE OR REPLACE FUNCTION public.is_people_manager" supabase/migrations/20260427120200_c_socio_memberships_rls_rewrite_and_backfill.sql
```

Se o helper NÃO inclui `'rh'`, a policy de SELECT em `data_access_log` falha para RH. Solução: criar `public.is_audit_reader(uid)` ou expandir is_people_manager (mas isso é hot helper de Phase 1 — preferir helper novo). Se ambiguo, **pause e flag a discrepancy** ao usuário antes de aplicar a migration.

**Ativar pgTAP `supabase/tests/007-data-access-log.sql` (4 tests):**

```sql
BEGIN;
SELECT plan(4);
SELECT tests.authenticate_as_service_role();

-- Setup fixture: profile + candidate + application + job_opening
INSERT INTO public.companies (id, name) VALUES ('ffffffff-0000-0000-0000-000000000010', 'Test F2 Co') ON CONFLICT (id) DO NOTHING;
INSERT INTO public.profiles (id, email, role) VALUES ('ffffffff-0000-0000-0000-000000000011', 'rh@test.com', 'rh') ON CONFLICT (id) DO NOTHING;
INSERT INTO public.candidates (id, full_name, email) VALUES ('ffffffff-0000-0000-0000-000000000012', 'Test Cand F2', 'cand+f2@test.com') ON CONFLICT (id) DO NOTHING;

-- TEST 1: read_candidate_with_log escreve 1 row em data_access_log
SET LOCAL request.jwt.claims TO '{"sub":"ffffffff-0000-0000-0000-000000000011","role":"authenticated"}';
SELECT lives_ok(
  $$ SELECT public.read_candidate_with_log('ffffffff-0000-0000-0000-000000000012', 'pgtap_test') $$,
  'RPC read_candidate_with_log executa para RH'
);
SELECT cmp_ok(
  (SELECT count(*) FROM public.data_access_log
    WHERE entity_id = 'ffffffff-0000-0000-0000-000000000012' AND context = 'pgtap_test')::bigint,
  '>=', 1::bigint,
  'RPC escreve >= 1 log row'
);

-- TEST 2: INSERT direto em data_access_log (sem service-role) é bloqueado por RLS
SELECT throws_ok(
  $$ INSERT INTO public.data_access_log (actor_id, entity_type, entity_id, action)
     VALUES ('ffffffff-0000-0000-0000-000000000011', 'candidate', 'ffffffff-0000-0000-0000-000000000012', 'view') $$,
  '42501',
  NULL,
  'INSERT direto em data_access_log é bloqueado por RLS (sem policy INSERT)'
);

-- TEST 3: SELECT em data_access_log requer is_people_manager (RH passa)
SELECT cmp_ok(
  (SELECT count(*) FROM public.data_access_log)::bigint,
  '>=', 0::bigint,
  'RH consegue SELECT em data_access_log via policy'
);

SELECT * FROM finish();
ROLLBACK;
```

**Ativar pgTAP `supabase/tests/010-pg-cron-retention.sql` (2 tests):**

```sql
BEGIN;
SELECT plan(2);
SELECT tests.authenticate_as_service_role();

-- TEST 1: Cron job 'data_access_log_retention_cleanup' existe com schedule semanal
SELECT is(
  (SELECT schedule FROM cron.job WHERE jobname = 'data_access_log_retention_cleanup'),
  '30 3 * * 1',
  'Retention cron job rodando segundas 03:30 UTC'
);

-- TEST 2: Manual run da query DELETE remove rows >36 meses
INSERT INTO public.profiles (id, email, role) VALUES ('ffffffff-0000-0000-0000-000000000020', 'old-actor@test.com', 'admin') ON CONFLICT (id) DO NOTHING;
INSERT INTO public.data_access_log (id, actor_id, entity_type, entity_id, action, at)
  VALUES ('ffffffff-0000-0000-0000-000000000021', 'ffffffff-0000-0000-0000-000000000020', 'candidate', 'ffffffff-0000-0000-0000-000000000022', 'view', NOW() - INTERVAL '37 months');
DELETE FROM public.data_access_log WHERE at < NOW() - INTERVAL '36 months';
SELECT is(
  (SELECT count(*) FROM public.data_access_log
    WHERE id = 'ffffffff-0000-0000-0000-000000000021')::bigint,
  0::bigint,
  'Cleanup remove rows >36 meses'
);

SELECT * FROM finish();
ROLLBACK;
```

**Verificar localmente:**
1. `supabase db reset && supabase db push`
2. `npx supabase test db --file supabase/tests/007-data-access-log.sql && npx supabase test db --file supabase/tests/010-pg-cron-retention.sql`
3. Output esperado: 4 ok + 2 ok, 0 fail.

**NÃO dropar `candidate_access_log`** — fica como compat read até Migration G (Phase 4).
  </action>
  <verify>
    <automated>test -f "supabase/migrations/20260428120100_f2_data_access_log_table.sql" && grep -q "read_candidate_with_log" "supabase/migrations/20260428120100_f2_data_access_log_table.sql" && grep -q "data_access_log_retention_cleanup" "supabase/migrations/20260428120100_f2_data_access_log_table.sql" && ! grep -q "select skip(" "supabase/tests/007-data-access-log.sql" && ! grep -q "select skip(" "supabase/tests/010-pg-cron-retention.sql"</automated>
  </verify>
  <done>F.2 SQL contém: `data_access_log` table com RLS habilitado E sem policy INSERT; `read_candidate_with_log(uuid, text)` RPC `STABLE SECURITY DEFINER`; `cron.schedule('data_access_log_retention_cleanup', '30 3 * * 1', ...)`; INSERT-SELECT migra dados de `candidate_access_log`. pgTAP `007` e `010` ativos com 6 tests verde quando aplicada local.</done>
</task>

<task type="auto">
  <name>Task 3: Migration F.3 — candidate_consents + active_candidate_consents view + RLS</name>
  <files>supabase/migrations/20260428120200_f3_candidate_consents.sql, supabase/tests/008-candidate-consents.sql</files>
  <read_first>
    supabase/migrations/20260422150000_candidate_conversations.sql (template TYPE+TABLE+RLS+TRIGGER+VIEW),
    supabase/migrations/20260427120200_c_socio_memberships_rls_rewrite_and_backfill.sql (visible_companies helper),
    .planning/phases/02-r-s-refactor/02-RESEARCH.md (§7 lines 1031-1313)
  </read_first>
  <action>
Criar `supabase/migrations/20260428120200_f3_candidate_consents.sql` com **conteúdo verbatim** abaixo:

```sql
-- =========================================================================
-- Migration F.3: candidate_consents granular (LGPD)
--
-- Motivação: TAL-03/04/08 exigem consent granular por finalidade
-- (purpose) com base legal explícita. Tabela append-friendly: revoke é
-- UPDATE setando revoked_at; re-grant cria nova row. EXCLUDE constraint
-- garante 1 ATIVO por (candidate, purpose). View active_candidate_consents
-- materializa "não revogado E não expirado".
--
-- Pattern: DDL + RLS + view. Edge function apply-to-job (Plan 07) usa
-- service-role para INSERT inicial; RH usa policy para revoke (UPDATE
-- com WITH CHECK).
-- Threats: T-02-03-01 (consent tampering) — mitigado por RLS WITH CHECK
-- só is_people_manager UPDATE; INSERT bloqueado fora de Edge Function ou RH.
-- REQs: TAL-03, TAL-04, TAL-08
-- =========================================================================

-- 1. Enums
DO $$ BEGIN
  CREATE TYPE public.consent_purpose_enum AS ENUM (
    'aplicar_a_esta_vaga',
    'incluir_no_banco_de_talentos_global',
    'compartilhar_com_cliente_externo',
    'manter_cv_pos_recusa',
    'considerar_outras_vagas_lever',
    'considerar_vagas_grupo_lever'
  );
EXCEPTION WHEN duplicate_object THEN NULL; END $$;

DO $$ BEGIN
  CREATE TYPE public.consent_legal_basis_enum AS ENUM (
    'consent',                -- LGPD Art. 7º I
    'legitimate_interest',    -- LGPD Art. 7º IX
    'contract',               -- LGPD Art. 7º V
    'legal_obligation'        -- LGPD Art. 7º II
  );
EXCEPTION WHEN duplicate_object THEN NULL; END $$;

-- 2. Tabela
CREATE TABLE IF NOT EXISTS public.candidate_consents (
  id            uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  candidate_id  uuid NOT NULL REFERENCES public.candidates(id) ON DELETE CASCADE,
  purpose       public.consent_purpose_enum NOT NULL,
  legal_basis   public.consent_legal_basis_enum NOT NULL DEFAULT 'consent',
  granted_at    timestamptz NOT NULL DEFAULT NOW(),
  granted_by    uuid REFERENCES public.profiles(id) ON DELETE SET NULL,
  expires_at    timestamptz,
  revoked_at    timestamptz,
  revoked_by    uuid REFERENCES public.profiles(id) ON DELETE SET NULL,
  document_url  text,
  created_at    timestamptz NOT NULL DEFAULT NOW(),
  updated_at    timestamptz NOT NULL DEFAULT NOW(),

  CONSTRAINT consents_revoked_after_granted
    CHECK (revoked_at IS NULL OR revoked_at >= granted_at),
  CONSTRAINT consents_expires_after_granted
    CHECK (expires_at IS NULL OR expires_at >= granted_at),
  EXCLUDE (candidate_id WITH =, purpose WITH =) WHERE (revoked_at IS NULL)
);

CREATE INDEX IF NOT EXISTS idx_candidate_consents_candidate ON public.candidate_consents (candidate_id);
CREATE INDEX IF NOT EXISTS idx_candidate_consents_purpose   ON public.candidate_consents (purpose);

ALTER TABLE public.candidate_consents ENABLE ROW LEVEL SECURITY;

-- 3. Trigger updated_at
DROP TRIGGER IF EXISTS tg_candidate_consents_updated_at ON public.candidate_consents;
CREATE TRIGGER tg_candidate_consents_updated_at
  BEFORE UPDATE ON public.candidate_consents
  FOR EACH ROW EXECUTE FUNCTION public.tg_set_updated_at();

-- 4. View active (revoked_at IS NULL E (expires_at IS NULL OR expires_at > NOW()))
CREATE OR REPLACE VIEW public.active_candidate_consents AS
SELECT *
  FROM public.candidate_consents
 WHERE revoked_at IS NULL
   AND (expires_at IS NULL OR expires_at > NOW());

-- 5. RLS policies (3 policies)
DROP POLICY IF EXISTS "candidate_consents:select:rh_admin" ON public.candidate_consents;
CREATE POLICY "candidate_consents:select:rh_admin"
  ON public.candidate_consents FOR SELECT TO authenticated
  USING (
    public.is_people_manager((SELECT auth.uid()))
    OR EXISTS (
      SELECT 1 FROM public.applications a
      JOIN public.job_openings j ON j.id = a.job_opening_id
      WHERE a.candidate_id = candidate_consents.candidate_id
        AND j.company_id = ANY(public.visible_companies((SELECT auth.uid())))
    )
  );

DROP POLICY IF EXISTS "candidate_consents:insert:rh_admin" ON public.candidate_consents;
CREATE POLICY "candidate_consents:insert:rh_admin"
  ON public.candidate_consents FOR INSERT TO authenticated
  WITH CHECK ( public.is_people_manager((SELECT auth.uid())) );

DROP POLICY IF EXISTS "candidate_consents:update:rh_admin_revoke_only" ON public.candidate_consents;
CREATE POLICY "candidate_consents:update:rh_admin_revoke_only"
  ON public.candidate_consents FOR UPDATE TO authenticated
  USING ( public.is_people_manager((SELECT auth.uid())) )
  WITH CHECK ( public.is_people_manager((SELECT auth.uid())) );

COMMENT ON TABLE public.candidate_consents IS
  'Phase 2 Migration F.3 — consent granular LGPD por purpose + legal_basis. Revoke = UPDATE revoked_at; re-grant = nova row.';
COMMENT ON VIEW public.active_candidate_consents IS
  'Consents válidos: revoked_at IS NULL E (expires_at IS NULL OR > NOW()).';
```

**Ativar pgTAP `supabase/tests/008-candidate-consents.sql` (4 tests):**

```sql
BEGIN;
SELECT plan(4);
SELECT tests.authenticate_as_service_role();

INSERT INTO public.candidates (id, full_name, email) VALUES
  ('ffffffff-0000-0000-0000-000000000030', 'Cand C1', 'c1@test.com')
  ON CONFLICT (id) DO NOTHING;

-- TEST 1: Constraint consents_revoked_after_granted bloqueia revoked_at < granted_at
PREPARE invalid_revoke AS
  INSERT INTO public.candidate_consents (candidate_id, purpose, granted_at, revoked_at)
  VALUES (
    'ffffffff-0000-0000-0000-000000000030',
    'incluir_no_banco_de_talentos_global',
    NOW(),
    NOW() - INTERVAL '1 day'
  );
SELECT throws_ok(
  'EXECUTE invalid_revoke',
  '23514',
  NULL,
  'CHECK consents_revoked_after_granted bloqueia revoked_at < granted_at'
);

-- TEST 2: EXCLUDE constraint bloqueia 2 consents ATIVOS para (candidate, purpose)
INSERT INTO public.candidate_consents (id, candidate_id, purpose) VALUES
  ('ffffffff-0000-0000-0000-000000000040', 'ffffffff-0000-0000-0000-000000000030', 'incluir_no_banco_de_talentos_global');
PREPARE duplicate_active AS
  INSERT INTO public.candidate_consents (candidate_id, purpose) VALUES
    ('ffffffff-0000-0000-0000-000000000030', 'incluir_no_banco_de_talentos_global');
SELECT throws_ok(
  'EXECUTE duplicate_active',
  '23P01',  -- exclusion_violation
  NULL,
  'EXCLUDE bloqueia 2 consents ativos para (candidate, purpose)'
);

-- TEST 3: View active_candidate_consents EXCLUI revogados
UPDATE public.candidate_consents SET revoked_at = NOW()
  WHERE id = 'ffffffff-0000-0000-0000-000000000040';
INSERT INTO public.candidate_consents (id, candidate_id, purpose, revoked_at) VALUES
  ('ffffffff-0000-0000-0000-000000000041', 'ffffffff-0000-0000-0000-000000000030', 'manter_cv_pos_recusa', NOW());
SELECT is(
  (SELECT count(*) FROM public.active_candidate_consents WHERE candidate_id = 'ffffffff-0000-0000-0000-000000000030')::bigint,
  0::bigint,
  'View active exclui consents com revoked_at IS NOT NULL'
);

-- TEST 4: Re-grant após revoke é permitido (EXCLUDE permite agora porque o anterior tem revoked_at)
SELECT lives_ok(
  $$ INSERT INTO public.candidate_consents (candidate_id, purpose) VALUES
       ('ffffffff-0000-0000-0000-000000000030', 'incluir_no_banco_de_talentos_global') $$,
  'Re-grant após revoke é permitido (EXCLUDE só conta revoked_at IS NULL)'
);

SELECT * FROM finish();
ROLLBACK;
```

**Verificar localmente:**
1. `supabase db reset && supabase db push`
2. `npx supabase test db --file supabase/tests/008-candidate-consents.sql`
3. Output: 4 ok, 0 fail.
  </action>
  <verify>
    <automated>test -f "supabase/migrations/20260428120200_f3_candidate_consents.sql" && grep -q "active_candidate_consents" "supabase/migrations/20260428120200_f3_candidate_consents.sql" && grep -q "EXCLUDE.*candidate_id.*purpose" "supabase/migrations/20260428120200_f3_candidate_consents.sql" && grep -q "consents_revoked_after_granted" "supabase/migrations/20260428120200_f3_candidate_consents.sql" && ! grep -q "select skip(" "supabase/tests/008-candidate-consents.sql"</automated>
  </verify>
  <done>F.3 SQL contém: TYPE `consent_purpose_enum` (6 valores incluindo `aplicar_a_esta_vaga`), TYPE `consent_legal_basis_enum`, TABLE `candidate_consents` com EXCLUDE 1-active-per-purpose + 2 CHECK constraints, view `active_candidate_consents`, 3 RLS policies (select/insert/update). pgTAP `008` ativo com 4 tests verde.</done>
</task>

<task type="auto">
  <name>Task 4: Migration F.4 — CPF UNIQUE partial index + normalize trigger</name>
  <files>supabase/migrations/20260428120300_f4_cpf_canonical_dedup.sql, supabase/tests/009-cpf-unique.sql</files>
  <read_first>
    supabase/migrations/20260416193000_hiring_core_entities.sql (linhas 148-191 — schema atual de candidates),
    supabase/migrations/20260420190000_add_discard_reason_and_talent_pool.sql (anonymized_at column shape),
    .planning/phases/02-r-s-refactor/02-RESEARCH.md (§8 lines 1331-1438)
  </read_first>
  <action>
**ATENÇÃO PITFALL (RESEARCH §"Migration F.4 — CPF format normalization existing data"):**
Antes de criar UNIQUE, dados existentes podem ter CPFs misturados (formatados e não). Necessário normalizar TODOS os CPFs primeiro, e abortar se sobrar duplicatas.

Criar `supabase/migrations/20260428120300_f4_cpf_canonical_dedup.sql`:

```sql
-- =========================================================================
-- Migration F.4: CPF como chave canonical de dedup (TAL-09)
--
-- Motivação: o schema atual tem UNIQUE (document_type, document_number)
-- mas isso não obriga unicidade quando document_type='cpf'. CPF é a chave
-- canonical brasileira; queremos partial UNIQUE em candidates.cpf
-- (nullable; NULL é não-único quando candidato externo sem CPF).
--
-- Pattern: normalize-then-constrain (alinha com PITFALL §F.4 da RESEARCH).
-- Threats: T-02-04-01 (UNIQUE breaking on existing duplicates) —
-- mitigado por: 1) normalizar todos os CPFs ANTES; 2) validar zero
-- duplicatas; 3) só então criar UNIQUE.
-- REQs: TAL-09
-- =========================================================================

-- 1. Função normalize_cpf
CREATE OR REPLACE FUNCTION public.normalize_cpf(input text)
RETURNS text LANGUAGE sql IMMUTABLE AS $$
  SELECT NULLIF(regexp_replace(COALESCE(input, ''), '[^0-9]', '', 'g'), '');
$$;

-- 2. Normalizar dados existentes ANTES de criar UNIQUE (idempotente)
UPDATE public.candidates
   SET cpf = public.normalize_cpf(cpf)
 WHERE cpf IS NOT NULL
   AND cpf <> public.normalize_cpf(cpf);

-- 3. Validação: abortar migration se houver duplicatas pós-normalização
DO $$
DECLARE v_dupes INT;
BEGIN
  SELECT count(*) INTO v_dupes FROM (
    SELECT cpf, count(*) AS c
      FROM public.candidates
     WHERE cpf IS NOT NULL AND anonymized_at IS NULL
     GROUP BY cpf HAVING count(*) > 1
  ) sub;
  IF v_dupes > 0 THEN
    RAISE EXCEPTION 'Migration F.4 abortada: % CPF duplicado(s) pós-normalização. Faça merge manual antes de re-aplicar.', v_dupes;
  END IF;
END $$;

-- 4. UNIQUE partial index: CPF é único quando NOT NULL E NOT anonimizado
CREATE UNIQUE INDEX IF NOT EXISTS idx_candidates_cpf_unique
  ON public.candidates (cpf)
  WHERE cpf IS NOT NULL AND anonymized_at IS NULL;

-- 5. Trigger normaliza CPF em INSERT/UPDATE + valida 11 dígitos
CREATE OR REPLACE FUNCTION public.tg_normalize_candidate_cpf()
RETURNS TRIGGER LANGUAGE plpgsql AS $$
BEGIN
  IF NEW.cpf IS NOT NULL THEN
    NEW.cpf := public.normalize_cpf(NEW.cpf);
    IF NEW.cpf IS NULL OR length(NEW.cpf) <> 11 THEN
      RAISE EXCEPTION 'CPF inválido: deve ter 11 dígitos numéricos (recebeu: %)', NEW.cpf
        USING ERRCODE = 'check_violation';
    END IF;
  END IF;
  RETURN NEW;
END $$;

DROP TRIGGER IF EXISTS tg_candidates_normalize_cpf ON public.candidates;
CREATE TRIGGER tg_candidates_normalize_cpf
  BEFORE INSERT OR UPDATE OF cpf ON public.candidates
  FOR EACH ROW EXECUTE FUNCTION public.tg_normalize_candidate_cpf();

COMMENT ON INDEX public.idx_candidates_cpf_unique IS
  'Phase 2 Migration F.4 — CPF como chave canonical de dedup (TAL-09). Partial: NOT NULL E NOT anonimizado.';
COMMENT ON FUNCTION public.normalize_cpf(text) IS
  'Phase 2 Migration F.4 — remove pontuação CPF; retorna NULL se vazio.';
```

**Ativar pgTAP `supabase/tests/009-cpf-unique.sql` (4 tests):**

```sql
BEGIN;
SELECT plan(4);
SELECT tests.authenticate_as_service_role();

-- TEST 1: idx_candidates_cpf_unique rejeita CPF duplicado
INSERT INTO public.candidates (id, full_name, email, cpf) VALUES
  ('ffffffff-0000-0000-0000-000000000050', 'Cand A', 'a+f4@test.com', '12345678901');
PREPARE duplicate_cpf AS
  INSERT INTO public.candidates (full_name, email, cpf) VALUES ('Cand B', 'b+f4@test.com', '12345678901');
SELECT throws_ok(
  'EXECUTE duplicate_cpf',
  '23505',
  NULL,
  'CPF duplicado é rejeitado pelo idx_candidates_cpf_unique'
);

-- TEST 2: Múltiplos candidatos com CPF NULL são permitidos (partial WHERE cpf IS NOT NULL)
SELECT lives_ok(
  $$ INSERT INTO public.candidates (full_name, email) VALUES ('Cand C', 'c+f4@test.com') $$,
  'Candidato sem CPF (NULL) é permitido'
);
SELECT lives_ok(
  $$ INSERT INTO public.candidates (full_name, email) VALUES ('Cand D', 'd+f4@test.com') $$,
  '2 candidatos sem CPF são permitidos (partial UNIQUE não conta NULLs)'
);

-- TEST 3: Trigger tg_candidates_normalize_cpf armazena '987.654.321-00' como '98765432100'
INSERT INTO public.candidates (id, full_name, email, cpf) VALUES
  ('ffffffff-0000-0000-0000-000000000051', 'Cand E', 'e+f4@test.com', '987.654.321-00');
SELECT is(
  (SELECT cpf FROM public.candidates WHERE id = 'ffffffff-0000-0000-0000-000000000051'),
  '98765432100',
  'Trigger normaliza CPF removendo pontuação'
);

-- TEST 4: Trigger rejeita CPF com !=11 dígitos
PREPARE invalid_cpf AS
  INSERT INTO public.candidates (full_name, email, cpf) VALUES ('Cand F', 'f+f4@test.com', '12345');
SELECT throws_ok(
  'EXECUTE invalid_cpf',
  '23514',
  NULL,
  'Trigger rejeita CPF com menos de 11 dígitos'
);

SELECT * FROM finish();
ROLLBACK;
```

**Verificar localmente:**
1. `supabase db reset && supabase db push` — atenção: se DB tiver CPFs duplicados pré-existentes, migration aborta com mensagem explícita.
2. `npx supabase test db --file supabase/tests/009-cpf-unique.sql`
3. Output: 4 ok, 0 fail.
  </action>
  <verify>
    <automated>test -f "supabase/migrations/20260428120300_f4_cpf_canonical_dedup.sql" && grep -q "idx_candidates_cpf_unique" "supabase/migrations/20260428120300_f4_cpf_canonical_dedup.sql" && grep -q "tg_normalize_candidate_cpf" "supabase/migrations/20260428120300_f4_cpf_canonical_dedup.sql" && grep -q "Migration F.4 abortada" "supabase/migrations/20260428120300_f4_cpf_canonical_dedup.sql" && ! grep -q "select skip(" "supabase/tests/009-cpf-unique.sql"</automated>
  </verify>
  <done>F.4 SQL contém: `normalize_cpf` function IMMUTABLE, UPDATE batch normalizando CPFs existentes, validação que aborta migration em duplicatas, `idx_candidates_cpf_unique` partial index, `tg_normalize_candidate_cpf` trigger BEFORE INSERT OR UPDATE OF cpf. pgTAP `009` ativo com 4 tests verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authenticated client → DB | Phase 1 RLS é a fronteira. Phase 2 reforça com RPC SECURITY DEFINER (única forma de ler PII com log atômico). |
| Edge Function (service role) → DB | apply-to-job persiste consents iniciais com service-role bypass. RLS continua barreira para qualquer outro path. |
| pg_cron job → DB | Cron roda em service-role implícito; DELETE em data_access_log é privileged. Mitigação: schedule fixo (weekly Mon 03:30); DBA review antes de prod apply. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | I (Information disclosure) | Direct SELECT em candidates.cpf/email | mitigate | RLS denies non-admin SELECT; force RPC `read_candidate_with_log` que escreve log atômico (F.2 RPC linhas 854-902 da RESEARCH) |
| T-02-02-02 | T (Tampering) | INSERT direto em data_access_log para forjar histórico | mitigate | RLS sem policy INSERT; só RPC SECURITY DEFINER escreve. pgTAP TEST 2 (`007-data-access-log.sql`) verifica via `throws_ok '42501'` |
| T-02-03-01 | T (Tampering) | Candidate self-revoke malicious sem consent | mitigate | RLS UPDATE WITH CHECK só is_people_manager; revoke por candidato é v2 (token-based, fora de Phase 2) |
| T-02-03-02 | T (Tampering) | 2 consents ATIVOS para mesma finalidade (LGPD ambiguous state) | mitigate | EXCLUDE constraint `(candidate_id WITH =, purpose WITH =) WHERE revoked_at IS NULL` |
| T-02-04-01 | I (Information disclosure) | Existing CPFs misturados (formatados/não) quebram UNIQUE constraint | mitigate | F.4 normaliza ANTES de criar UNIQUE; aborta migration se duplicatas residuais |
| T-02-02-03 | D (DoS) | UPDATE batch de F.1 lock prolongado em produção | mitigate | `FOR UPDATE SKIP LOCKED` + batch 1000 + `pg_sleep(0.05)` entre batches |
| T-02-02-04 | E (Elevation) | RPC SECURITY DEFINER bypassa RLS sem re-aplicar lógica | mitigate | RPC re-aplica `is_people_manager OR EXISTS(visible_companies)` antes de retornar candidate; throws 42501 se sem permissão |
| T-02-02-05 | I (Information disclosure) | search_path injection via SECURITY DEFINER função | mitigate | `SET search_path = public` em todas as funções SECURITY DEFINER (RPC + triggers) |
</threat_model>

<verification>
- 4 arquivos `.sql` em `supabase/migrations/` com timestamp `20260428120{0,1,2,3}00`
- 5 arquivos `supabase/tests/00{6,7,8,9}-*.sql` + `010-*.sql` SEM `select skip(`
- `supabase db reset && supabase db push` aplica todas as 4 migrations sem erro
- `npx supabase test db` executa 5 suites com soma total ≥19 tests, 0 fails
- `is_people_manager` Phase 1 confirmado incluir 'rh' role (read_first verificou)
</verification>

<success_criteria>
1. 4 migrations idempotentes aplicáveis em qualquer ordem ou re-aplicação (DROP IF EXISTS / CREATE IF NOT EXISTS / DO $$ EXCEPTION).
2. Zero candidatos em stages legados pós-Migration F.1 (pgTAP TEST 1 `006`); rows que originaram de `sem_retorno` mapeadas para `triagem` com `metadata.legacy_marker = 'sem_retorno'` (pgTAP TEST 4 + TEST 5 `006`).
3. RPC `read_candidate_with_log` é o único caminho de leitura PII de candidato; escreve log atomicamente (pgTAP `007`).
4. EXCLUDE constraint impede 2 consents ATIVOS por (candidato, purpose); view `active_candidate_consents` filtra revogados/expirados (pgTAP `008`).
5. CPF UNIQUE partial; trigger normaliza pontuação; trigger rejeita CPF != 11 dígitos (pgTAP `009`).
6. pg_cron `data_access_log_retention_cleanup` agendado weekly Mon 03:30 UTC (pgTAP `010`).
7. Migration de dados de `candidate_access_log` para `data_access_log` é idempotente (NOT EXISTS guard).
</success_criteria>

<output>
Após completar, criar `.planning/phases/02-r-s-refactor/02-02-SUMMARY.md` com:
- 4 arquivos `.sql` criados
- Output de `npx supabase test db` (devem ser ~19 tests verde — 5+4+4+4+2 = 19)
- Confirmação de `is_people_manager('rh')` incluído na Phase 1 helper
- Próximo plano: `02-03-PLAN.md` (utilities + types regen) ou `02-04-PLAN.md` (schema push)
</output>

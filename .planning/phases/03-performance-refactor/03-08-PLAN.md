---
phase: 03-performance-refactor
plan: 08
type: execute
wave: 4
depends_on: ["03-07"]
files_modified:
  - src/components/OneOnOneMeetingForm.tsx
  - src/components/OneOnOneAgenda.tsx
  - src/components/OneOnOneNotes.tsx
  - src/components/OneOnOneActionItems.tsx
  - src/components/OneOnOnePDIPanel.tsx
  - src/components/OneOnOneRHNote.tsx
  - src/components/OneOnOneRHVisibleBadge.tsx
  - src/hooks/useMeetingTimer.ts
  - src/hooks/useAgendaState.ts
  - src/hooks/useActionItemsState.ts
  - src/hooks/usePlaudInput.ts
  - src/components/__tests__/OneOnOneNotes.test.tsx
  - src/components/__tests__/OneOnOneActionItems.test.tsx
  - src/components/__tests__/OneOnOneMeetingForm.test.tsx
autonomous: true
requirements:
  - ONE-01
  - ONE-02
  - ONE-03
  - ONE-04
  - ONE-05
  - ONE-06
must_haves:
  truths:
    - "`OneOnOneMeetingForm.tsx` quebrado em orchestrator < 300 linhas + 5 sub-componentes (D-18)"
    - "`OneOnOneAgenda.tsx` (~200 ln), `OneOnOneNotes.tsx` (~250 ln), `OneOnOneActionItems.tsx` (~180 ln), `OneOnOnePDIPanel.tsx` (~150 ln), `OneOnOneRHNote.tsx` (~80 ln), `OneOnOneRHVisibleBadge.tsx` (~30 ln) — cada um com export default + props interface"
    - "4 custom hooks extraídos: `useMeetingTimer`, `useAgendaState`, `useActionItemsState`, `usePlaudInput` (D-19) — cada um < 100 ln"
    - "Componente `OneOnOneNotes` tem 2 textareas dedicadas: 'Transcrição (Plaud)' + 'Resumo (Plaud)' persistindo em `meeting_structure.transcricao_plaud` + `meeting_structure.resumo_plaud` (D-12/D-14)"
    - "`OneOnOneRHNote` rendered ONLY IF `useAbility().can('read', 'rh_notes')` — defense-in-depth (UI-SPEC §'1:1 form layout' + Anti-spoof)"
    - "`OneOnOneRHVisibleBadge` persistente no header do form — Chip color='amber' (UI-SPEC) + tooltip 'RH visível' (D-15)"
    - "Plaud paste warning quando texto < 50 chars (UI-SPEC §'Error states')"
    - "RTL tests passam para Notes (Plaud paste) + ActionItems (checklist) + MeetingForm (badge presence + RH note role gating)"
  artifacts:
    - path: "src/components/OneOnOneMeetingForm.tsx"
      provides: "Orchestrator < 300 ln; tabs/colunas; sticky header com badge RH visível"
      contains: "OneOnOneRHVisibleBadge"
    - path: "src/components/OneOnOneNotes.tsx"
      provides: "Notas + Plaud transcript textarea + Plaud summary textarea"
      contains: "transcricao_plaud"
    - path: "src/components/OneOnOneRHNote.tsx"
      provides: "Section 'Notas RH' visível só p/ admin/rh"
      contains: "useAbility"
    - path: "src/hooks/usePlaudInput.ts"
      provides: "Validação paste Plaud (warning < 50 chars)"
      contains: "paste"
  key_links:
    - from: "OneOnOneNotes.tsx"
      to: "useUpdateOneOnOne (Plan 03-07)"
      via: "Updates meeting_structure.transcricao_plaud + resumo_plaud"
      pattern: "transcricao_plaud"
    - from: "OneOnOneRHNote.tsx"
      to: "useOneOnOneRhNotes + useUpsertOneOnOneRhNote (Plan 03-07)"
      via: "Read/upsert separate table"
      pattern: "one_on_one_rh_notes"
    - from: "OneOnOneMeetingForm.tsx"
      to: "useAbility (CASL — Phase 1)"
      via: "Conditional render based on role"
      pattern: "useAbility"
---

<objective>
**Wave 4 UI #1: Quebrar OneOnOneMeetingForm.tsx (909 linhas) + adicionar Plaud + RH note.** Coverage de D-18/D-19 (split + custom hooks), D-12/D-14 (Plaud), D-15 (badge RH visível), D-17 (RH note tabela separada), QUAL-04 (componente >800 linhas tocado nesta fase).

**Layout (UI-SPEC §'1:1 form layout'):** sticky header com badge RH visível + status chip + Save button; sections em ordem fixa Pauta → Notas → Action items → PDI → Notas RH (este só renderiza se admin/rh — defense-in-depth).

Output: 6 sub-componentes + 4 custom hooks + orchestrator refactored + 3 RTL tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-performance-refactor/03-CONTEXT.md
@.planning/phases/03-performance-refactor/03-RESEARCH.md
@.planning/phases/03-performance-refactor/03-PATTERNS.md
@.planning/phases/03-performance-refactor/03-UI-SPEC.md
@.planning/phases/03-performance-refactor/03-07-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Phase 1 CASL setup (verified): -->
<!-- src/shared/data/useAbility.ts → returns AppAbility from @casl/react useContext -->
<!-- AppAbility: defineAbility, can('read', 'rh_notes') etc. -->

<!-- Phase 3 hooks (Plan 03-07): -->
<!-- useOneOnOneRhNotes(meetingId) → { data: RhNoteRow | null, isLoading } -->
<!-- useUpsertOneOnOneRhNote() → mutation { meeting_id, notes } -->
<!-- useUpdateOneOnOne() → mutation { id, meeting_structure?, status? } -->

<!-- LinearKit (verified): -->
<!-- Btn { variant: 'primary'|'secondary'|'ghost'|'accent', size: 'sm'|'md'|'lg' } -->
<!-- Chip { color: 'amber'|'green'|'red'|'blue'|'purple', size: 'sm'|'md' } -->
<!-- Card, SectionHeader, Row, Col, KV from src/components/primitives/LinearKit.tsx -->

<!-- UI-SPEC locked copy: -->
<!--   "RH visível" + tooltip "RH da empresa pode auditar conversas." -->
<!--   "Notas RH" + caption "Visível apenas para RH." -->
<!--   "Transcrição (Plaud)" + caption "Cole a transcrição que o app Plaud gerou." -->
<!--   "Resumo (Plaud)" + caption "Cole o resumo já pronto do app Plaud." -->
<!--   Plaud paste short warning: "Texto curto demais — confira se colou a transcrição inteira." -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 4 custom hooks (useMeetingTimer + useAgendaState + useActionItemsState + usePlaudInput)</name>
  <files>src/hooks/useMeetingTimer.ts, src/hooks/useAgendaState.ts, src/hooks/useActionItemsState.ts, src/hooks/usePlaudInput.ts</files>
  <read_first>
    - src/components/OneOnOneMeetingForm.tsx linhas 89-104 (current state declarations — extract)
    - .planning/phases/03-performance-refactor/03-CONTEXT.md D-19
    - .planning/phases/03-performance-refactor/03-PATTERNS.md §"Hooks (extraction)"
  </read_first>
  <behavior>
    - useMeetingTimer: start/stop/reset; tracks elapsed seconds; cleanup setInterval em unmount
    - useAgendaState: array of {id, text, checked}; add/toggle/remove; preserves insertion order
    - useActionItemsState: array of {id, text, assignee_id, due_date, checked}; add/update/toggle/remove
    - usePlaudInput: paste validation; returns { value, setValue, warning: 'short' | 'empty' | null }; threshold 50 chars
  </behavior>
  <action>
**1. Crie `src/hooks/useMeetingTimer.ts`:**
```typescript
import { useEffect, useRef, useState, useCallback } from 'react';

export function useMeetingTimer(initialSeconds = 0) {
  const [elapsed, setElapsed] = useState(initialSeconds);
  const [running, setRunning] = useState(false);
  const intervalRef = useRef<ReturnType<typeof setInterval> | null>(null);

  useEffect(() => {
    if (running) {
      intervalRef.current = setInterval(() => setElapsed((s) => s + 1), 1000);
    }
    return () => {
      if (intervalRef.current) clearInterval(intervalRef.current);
    };
  }, [running]);

  const start = useCallback(() => setRunning(true), []);
  const stop = useCallback(() => setRunning(false), []);
  const reset = useCallback(() => { setRunning(false); setElapsed(0); }, []);

  return { elapsed, running, start, stop, reset };
}
```

**2. Crie `src/hooks/useAgendaState.ts`:**
```typescript
import { useState, useCallback } from 'react';

export interface AgendaItem { id: string; text: string; checked: boolean; }

export function useAgendaState(initial: AgendaItem[] = []) {
  const [items, setItems] = useState<AgendaItem[]>(initial);

  const add = useCallback((text: string) => {
    setItems((prev) => [...prev, { id: crypto.randomUUID(), text, checked: false }]);
  }, []);
  const toggle = useCallback((id: string) => {
    setItems((prev) => prev.map((it) => (it.id === id ? { ...it, checked: !it.checked } : it)));
  }, []);
  const remove = useCallback((id: string) => {
    setItems((prev) => prev.filter((it) => it.id !== id));
  }, []);
  const update = useCallback((id: string, text: string) => {
    setItems((prev) => prev.map((it) => (it.id === id ? { ...it, text } : it)));
  }, []);

  return { items, setItems, add, toggle, remove, update };
}
```

**3. Crie `src/hooks/useActionItemsState.ts`:**
```typescript
import { useState, useCallback } from 'react';

export interface ActionItem {
  id: string;
  text: string;
  assignee_id: string | null;
  due_date: string | null; // ISO date
  checked: boolean;
}

export function useActionItemsState(initial: ActionItem[] = []) {
  const [items, setItems] = useState<ActionItem[]>(initial);

  const add = useCallback((text: string, assignee_id: string | null = null, due_date: string | null = null) => {
    setItems((prev) => [...prev, { id: crypto.randomUUID(), text, assignee_id, due_date, checked: false }]);
  }, []);
  const toggle = useCallback((id: string) => {
    setItems((prev) => prev.map((it) => (it.id === id ? { ...it, checked: !it.checked } : it)));
  }, []);
  const update = useCallback((id: string, patch: Partial<Omit<ActionItem, 'id'>>) => {
    setItems((prev) => prev.map((it) => (it.id === id ? { ...it, ...patch } : it)));
  }, []);
  const remove = useCallback((id: string) => {
    setItems((prev) => prev.filter((it) => it.id !== id));
  }, []);

  return { items, setItems, add, toggle, update, remove };
}
```

**4. Crie `src/hooks/usePlaudInput.ts`:**
```typescript
import { useState, useMemo, useCallback } from 'react';

export type PlaudWarning = 'short' | 'empty' | null;
const SHORT_THRESHOLD = 50;

export function usePlaudInput(initial = '') {
  const [value, setValue] = useState(initial);

  const warning: PlaudWarning = useMemo(() => {
    if (value.length === 0) return 'empty';
    if (value.length < SHORT_THRESHOLD) return 'short';
    return null;
  }, [value]);

  const handlePaste = useCallback((event: React.ClipboardEvent<HTMLTextAreaElement>) => {
    const pasted = event.clipboardData.getData('text');
    if (pasted) {
      // Allow default paste behavior + log usage flag (no telemetry; just hint)
      // No-op for now; pattern hook for future enhancement
    }
  }, []);

  return { value, setValue, warning, handlePaste };
}
```
  </action>
  <verify>
    <automated>npx tsc --noEmit src/hooks/useMeetingTimer.ts src/hooks/useAgendaState.ts src/hooks/useActionItemsState.ts src/hooks/usePlaudInput.ts 2>&1 | grep -c "error" | awk '{if($1>0) exit 1}' && wc -l src/hooks/useMeetingTimer.ts src/hooks/useAgendaState.ts src/hooks/useActionItemsState.ts src/hooks/usePlaudInput.ts | awk '{if($1>=100 && NR<5) exit 1}'</automated>
  </verify>
  <done>4 custom hooks isolados; cada um < 100 ln (INV-3-24); tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 6 sub-componentes (Agenda, Notes, ActionItems, PDIPanel, RHNote, RHVisibleBadge)</name>
  <files>src/components/OneOnOneAgenda.tsx, src/components/OneOnOneNotes.tsx, src/components/OneOnOneActionItems.tsx, src/components/OneOnOnePDIPanel.tsx, src/components/OneOnOneRHNote.tsx, src/components/OneOnOneRHVisibleBadge.tsx</files>
  <read_first>
    - src/components/OneOnOneMeetingForm.tsx (current 909 ln — extract sections)
    - src/components/primitives/LinearKit.tsx (Btn, Chip, Card, SectionHeader, Row, Col)
    - src/components/ui/textarea.tsx (shadcn Textarea)
    - src/components/ui/tooltip.tsx (shadcn Tooltip)
    - .planning/phases/03-performance-refactor/03-UI-SPEC.md §"Standing labels" + §"Plaud paste"
    - src/hooks/useMeetingTimer.ts (just created — Task 1)
    - src/hooks/useAgendaState.ts, useActionItemsState.ts, usePlaudInput.ts (Task 1)
    - src/hooks/useOneOnOneRhNotes.ts (Plan 03-07)
    - src/shared/data/useAbility.ts (Phase 1 CASL)
  </read_first>
  <behavior>
    - Agenda: lista de itens com checkbox; Add input; Remove button; cronômetro por item via useMeetingTimer
    - Notes: 2 textareas dedicadas Plaud + textarea livre de notas; warning amber se Plaud < 50 chars
    - ActionItems: lista checklist com responsável (Select de members) + due_date (date picker); UI-SPEC empty state
    - PDIPanel: integração existente PDIFormIntegrated; default collapsed
    - RHNote: textarea RH-only; usa useOneOnOneRhNotes + useUpsertOneOnOneRhNote; render condicional via useAbility().can('read', 'rh_notes')
    - RHVisibleBadge: Chip amber + Tooltip "RH da empresa pode auditar conversas"
  </behavior>
  <action>
**1. Crie `src/components/OneOnOneRHVisibleBadge.tsx`** (~30 ln):
```tsx
import { Chip } from '@/components/primitives/LinearKit';
import { Tooltip, TooltipContent, TooltipTrigger } from '@/components/ui/tooltip';
import { Eye } from 'lucide-react';

export function OneOnOneRHVisibleBadge() {
  return (
    <Tooltip>
      <TooltipTrigger asChild>
        <span>
          <Chip color="amber" size="sm">
            <Eye className="size-3 mr-1" aria-hidden="true" />
            RH visível
          </Chip>
        </span>
      </TooltipTrigger>
      <TooltipContent side="bottom">RH da empresa pode auditar conversas.</TooltipContent>
    </Tooltip>
  );
}
```

**2. Crie `src/components/OneOnOneNotes.tsx`** (~250 ln target — Plaud + notas):
```tsx
import { Textarea } from '@/components/ui/textarea';
import { Card, SectionHeader } from '@/components/primitives/LinearKit';
import { Label } from '@/components/ui/label';
import { Chip } from '@/components/primitives/LinearKit';
import { usePlaudInput } from '@/hooks/usePlaudInput';
import { useEffect } from 'react';

export interface OneOnOneNotesProps {
  notes: string;
  onNotesChange: (v: string) => void;
  transcricaoPlaud: string;
  onTranscricaoPlaudChange: (v: string) => void;
  resumoPlaud: string;
  onResumoPlaudChange: (v: string) => void;
}

export function OneOnOneNotes(props: OneOnOneNotesProps) {
  const transcricao = usePlaudInput(props.transcricaoPlaud);
  const resumo = usePlaudInput(props.resumoPlaud);

  useEffect(() => { props.onTranscricaoPlaudChange(transcricao.value); }, [transcricao.value]);
  useEffect(() => { props.onResumoPlaudChange(resumo.value); }, [resumo.value]);

  return (
    <Card>
      <SectionHeader>Notas</SectionHeader>
      <div className="space-y-4">
        <div>
          <Label htmlFor="notes-textarea">Notas livres</Label>
          <Textarea
            id="notes-textarea"
            value={props.notes}
            onChange={(e) => props.onNotesChange(e.target.value)}
            placeholder="Notas durante o 1:1…"
            rows={5}
          />
        </div>

        <div>
          <Label htmlFor="plaud-transcript">Transcrição (Plaud)</Label>
          <p className="text-xs text-text-subtle">Cole a transcrição que o app Plaud gerou.</p>
          <Textarea
            id="plaud-transcript"
            value={transcricao.value}
            onChange={(e) => transcricao.setValue(e.target.value)}
            onPaste={transcricao.handlePaste}
            rows={8}
          />
          {transcricao.warning === 'short' && (
            <Chip color="amber" size="sm" className="mt-1">
              Texto curto demais — confira se colou a transcrição inteira.
            </Chip>
          )}
        </div>

        <div>
          <Label htmlFor="plaud-summary">Resumo (Plaud)</Label>
          <p className="text-xs text-text-subtle">Cole o resumo já pronto do app Plaud.</p>
          <Textarea
            id="plaud-summary"
            value={resumo.value}
            onChange={(e) => resumo.setValue(e.target.value)}
            onPaste={resumo.handlePaste}
            rows={4}
          />
        </div>
      </div>
    </Card>
  );
}
```

**3. Crie `src/components/OneOnOneRHNote.tsx`** (~80 ln — RH-only):
```tsx
import { useState, useEffect } from 'react';
import { Textarea } from '@/components/ui/textarea';
import { Card, SectionHeader, Btn, Chip } from '@/components/primitives/LinearKit';
import { useAbility } from '@/shared/data/useAbility';
import { useOneOnOneRhNotes, useUpsertOneOnOneRhNote } from '@/hooks/useOneOnOneRhNotes';

interface Props { meetingId: string; }

export function OneOnOneRHNote({ meetingId }: Props) {
  const ability = useAbility();
  const canSee = ability.can('read', 'rh_notes');

  // Defense-in-depth (UI-SPEC §Anti-spoof + Pitfall §5):
  // RENDERED ONLY IF role is admin/rh — absent from DOM for líder/liderado, not just display:none.
  if (!canSee) return null;

  const { data, isLoading } = useOneOnOneRhNotes(meetingId);
  const upsert = useUpsertOneOnOneRhNote();
  const [notes, setNotes] = useState(data?.notes ?? '');

  useEffect(() => { if (data) setNotes(data.notes ?? ''); }, [data]);

  if (isLoading) return null;

  return (
    <Card className="border-l-4 border-l-status-purple">
      <SectionHeader>
        <span className="flex items-center gap-2">
          Notas RH
          <Chip color="purple" size="sm">RH-only</Chip>
        </span>
      </SectionHeader>
      <p className="text-xs text-text-subtle mb-2">Visível apenas para RH.</p>
      <Textarea
        value={notes}
        onChange={(e) => setNotes(e.target.value)}
        rows={5}
        placeholder="Notas internas — não vão para líder/liderado"
      />
      <Btn
        variant="primary"
        size="sm"
        className="mt-2"
        disabled={upsert.isPending}
        onClick={() => upsert.mutate({ meeting_id: meetingId, notes })}
      >
        {upsert.isPending ? 'Salvando…' : 'Salvar nota RH'}
      </Btn>
    </Card>
  );
}
```

**4. Crie `src/components/OneOnOneAgenda.tsx`** (~200 ln — extract from monolith):
```tsx
import { useState } from 'react';
import { Card, SectionHeader, Btn, Row } from '@/components/primitives/LinearKit';
import { Input } from '@/components/ui/input';
import { Checkbox } from '@/components/ui/checkbox';
import { Trash2 } from 'lucide-react';
import { useAgendaState, type AgendaItem } from '@/hooks/useAgendaState';
import { useEffect } from 'react';

export interface OneOnOneAgendaProps {
  items: AgendaItem[];
  onChange: (items: AgendaItem[]) => void;
}

export function OneOnOneAgenda({ items, onChange }: OneOnOneAgendaProps) {
  const state = useAgendaState(items);
  const [newText, setNewText] = useState('');

  useEffect(() => { onChange(state.items); }, [state.items]);

  return (
    <Card>
      <SectionHeader>Pauta</SectionHeader>
      <ul className="space-y-2">
        {state.items.length === 0 && <p className="text-sm text-text-subtle">Nenhum item de pauta ainda.</p>}
        {state.items.map((it) => (
          <li key={it.id} className="flex items-center gap-2 p-3 rounded border border-border">
            <Checkbox checked={it.checked} onCheckedChange={() => state.toggle(it.id)} />
            <span className={it.checked ? 'line-through text-text-subtle flex-1' : 'flex-1'}>{it.text}</span>
            <button onClick={() => state.remove(it.id)} aria-label="Remover item"><Trash2 className="size-4 text-status-red" /></button>
          </li>
        ))}
      </ul>
      <Row className="mt-3 gap-2">
        <Input value={newText} onChange={(e) => setNewText(e.target.value)} placeholder="Adicionar tópico…" />
        <Btn variant="secondary" onClick={() => { if (newText.trim()) { state.add(newText.trim()); setNewText(''); } }}>
          Adicionar
        </Btn>
      </Row>
    </Card>
  );
}
```

**5. Crie `src/components/OneOnOneActionItems.tsx`** (~180 ln):
```tsx
import { useState, useEffect } from 'react';
import { Card, SectionHeader, Btn, Row } from '@/components/primitives/LinearKit';
import { Input } from '@/components/ui/input';
import { Checkbox } from '@/components/ui/checkbox';
import { useActionItemsState, type ActionItem } from '@/hooks/useActionItemsState';
import { Trash2 } from 'lucide-react';

export interface OneOnOneActionItemsProps {
  items: ActionItem[];
  onChange: (items: ActionItem[]) => void;
}

export function OneOnOneActionItems({ items, onChange }: OneOnOneActionItemsProps) {
  const state = useActionItemsState(items);
  const [newText, setNewText] = useState('');

  useEffect(() => { onChange(state.items); }, [state.items]);

  return (
    <Card>
      <SectionHeader>Action items</SectionHeader>
      <ul className="space-y-2">
        {state.items.length === 0 && (
          <p className="text-sm text-text-subtle p-3 rounded border border-dashed border-border">
            Nenhum action item ainda.
          </p>
        )}
        {state.items.map((it) => (
          <li key={it.id} className="flex items-center gap-2 p-3 rounded border border-border">
            <Checkbox checked={it.checked} onCheckedChange={() => state.toggle(it.id)} />
            <span className={it.checked ? 'line-through text-text-subtle flex-1' : 'flex-1'}>{it.text}</span>
            <input
              type="date"
              className="text-xs"
              value={it.due_date ?? ''}
              onChange={(e) => state.update(it.id, { due_date: e.target.value || null })}
            />
            <button onClick={() => state.remove(it.id)} aria-label="Remover action item">
              <Trash2 className="size-4 text-status-red" />
            </button>
          </li>
        ))}
      </ul>
      <Row className="mt-3 gap-2">
        <Input value={newText} onChange={(e) => setNewText(e.target.value)} placeholder="Novo action item…" />
        <Btn variant="secondary" onClick={() => { if (newText.trim()) { state.add(newText.trim()); setNewText(''); } }}>
          Adicionar
        </Btn>
      </Row>
    </Card>
  );
}
```

**6. Crie `src/components/OneOnOnePDIPanel.tsx`** (~150 ln — extract existing):
```tsx
import { Card, SectionHeader } from '@/components/primitives/LinearKit';
import { useState } from 'react';
import { ChevronDown, ChevronUp } from 'lucide-react';
import { PDIFormIntegrated } from '@/components/PDIFormIntegrated';

export interface OneOnOnePDIPanelProps {
  collaboratorId: string;
  meetingId: string;
}

export function OneOnOnePDIPanel({ collaboratorId, meetingId }: OneOnOnePDIPanelProps) {
  const [open, setOpen] = useState(false);
  return (
    <Card>
      <button className="w-full flex items-center justify-between" onClick={() => setOpen((o) => !o)}>
        <SectionHeader>PDI</SectionHeader>
        {open ? <ChevronUp className="size-4" /> : <ChevronDown className="size-4" />}
      </button>
      {open && (
        <div className="mt-3">
          {/* Reuse existing component (Phase 1/2 — props stable) */}
          <PDIFormIntegrated userId={collaboratorId} meetingId={meetingId} />
        </div>
      )}
    </Card>
  );
}
```
  </action>
  <verify>
    <automated>npx tsc --noEmit src/components/OneOnOne{Agenda,Notes,ActionItems,PDIPanel,RHNote,RHVisibleBadge}.tsx 2>&1 | grep -c "error" | awk '{if($1>0) exit 1}' && wc -l src/components/OneOnOneRHVisibleBadge.tsx | awk '{if($1>50) exit 1}' && grep -q "useAbility" src/components/OneOnOneRHNote.tsx && grep -q "transcricao_plaud\|onTranscricaoPlaudChange" src/components/OneOnOneNotes.tsx</automated>
  </verify>
  <done>6 sub-componentes; RHVisibleBadge < 50 ln; RHNote `if (!canSee) return null` (anti-spoof); Notes tem 2 textareas Plaud distintas com warnings; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Refactor OneOnOneMeetingForm orchestrator < 300 ln + RTL tests</name>
  <files>src/components/OneOnOneMeetingForm.tsx, src/components/__tests__/OneOnOneNotes.test.tsx, src/components/__tests__/OneOnOneActionItems.test.tsx, src/components/__tests__/OneOnOneMeetingForm.test.tsx</files>
  <read_first>
    - src/components/OneOnOneMeetingForm.tsx (current 909 ln — REFACTOR in place)
    - 6 sub-componentes acabados de criar (Task 2)
    - 4 hooks acabados de criar (Task 1)
    - src/hooks/useOneOnOnes.ts (useUpdateOneOnOne — Plan 03-07)
    - .planning/phases/03-performance-refactor/03-UI-SPEC.md §"1:1 form layout" + §"Visual Focal Points"
  </read_first>
  <behavior>
    - Orchestrator < 300 linhas
    - Sticky header: title + RHVisibleBadge + status chip + "Salvar 1:1" button
    - Sections em ordem: Pauta → Notas → ActionItems → PDIPanel → RHNote
    - Save: useUpdateOneOnOne com optimistic update; toast em sucesso/erro
    - RTL test: render → verifica RH-visible badge presente; render como liderado → RHNote ausente do DOM
  </behavior>
  <action>
**1. Refactor `src/components/OneOnOneMeetingForm.tsx`** (orchestrator < 300 ln):
```tsx
import { useState, useEffect } from 'react';
import type { Database } from '@/integrations/supabase/types';
import { Btn, Chip, SectionHeader } from '@/components/primitives/LinearKit';
import { OneOnOneRHVisibleBadge } from './OneOnOneRHVisibleBadge';
import { OneOnOneAgenda } from './OneOnOneAgenda';
import { OneOnOneNotes } from './OneOnOneNotes';
import { OneOnOneActionItems } from './OneOnOneActionItems';
import { OneOnOnePDIPanel } from './OneOnOnePDIPanel';
import { OneOnOneRHNote } from './OneOnOneRHNote';
import { useUpdateOneOnOne } from '@/hooks/useOneOnOnes';
import { toast } from 'sonner';

type Meeting = Database['public']['Tables']['one_on_ones']['Row'];

export interface OneOnOneMeetingFormProps {
  meeting: Meeting;
}

export function OneOnOneMeetingForm({ meeting }: OneOnOneMeetingFormProps) {
  const ms = (meeting.meeting_structure ?? {}) as {
    agenda_items?: Array<{ id: string; text: string; checked: boolean }>;
    action_items?: Array<{ id: string; text: string; assignee_id: string | null; due_date: string | null; checked: boolean }>;
    transcricao_plaud?: string;
    resumo_plaud?: string;
    notes?: string;
  };

  const [agendaItems, setAgendaItems] = useState(ms.agenda_items ?? []);
  const [actionItems, setActionItems] = useState(ms.action_items ?? []);
  const [notes, setNotes] = useState(ms.notes ?? '');
  const [transcricaoPlaud, setTranscricaoPlaud] = useState(ms.transcricao_plaud ?? '');
  const [resumoPlaud, setResumoPlaud] = useState(ms.resumo_plaud ?? '');

  const update = useUpdateOneOnOne();

  const handleSave = () => {
    update.mutate({
      id: meeting.id,
      meeting_structure: {
        ...ms,
        agenda_items: agendaItems,
        action_items: actionItems,
        notes,
        transcricao_plaud: transcricaoPlaud,
        resumo_plaud: resumoPlaud,
      },
    }, {
      onSuccess: () => toast.success('1:1 salva'),
      onError: (e) => toast.error('Não foi possível salvar', { description: e.message }),
    });
  };

  const statusColor: 'green' | 'amber' | 'blue' =
    meeting.status === 'completed' ? 'green' :
    meeting.status === 'in-progress' ? 'amber' : 'blue';

  return (
    <div className="space-y-4">
      <header className="sticky top-0 bg-bg z-10 flex items-center justify-between p-4 border-b border-border">
        <div className="flex items-center gap-3">
          <h1 className="text-display-md">1:1 — {new Date(meeting.scheduled_at).toLocaleDateString('pt-BR')}</h1>
          <OneOnOneRHVisibleBadge />
          <Chip color={statusColor} size="sm">{meeting.status}</Chip>
        </div>
        <Btn variant="accent" onClick={handleSave} disabled={update.isPending}>
          {update.isPending ? 'Salvando…' : 'Salvar 1:1'}
        </Btn>
      </header>

      <main className="space-y-6 px-4 pb-12">
        <OneOnOneAgenda items={agendaItems} onChange={setAgendaItems} />
        <OneOnOneNotes
          notes={notes}
          onNotesChange={setNotes}
          transcricaoPlaud={transcricaoPlaud}
          onTranscricaoPlaudChange={setTranscricaoPlaud}
          resumoPlaud={resumoPlaud}
          onResumoPlaudChange={setResumoPlaud}
        />
        <OneOnOneActionItems items={actionItems} onChange={setActionItems} />
        <OneOnOnePDIPanel collaboratorId={meeting.collaborator_id} meetingId={meeting.id} />
        <OneOnOneRHNote meetingId={meeting.id} />
      </main>
    </div>
  );
}
```

**2. Crie `src/components/__tests__/OneOnOneNotes.test.tsx`:**
```typescript
import { describe, it, expect, vi } from 'vitest';
import { render, screen, fireEvent } from '@testing-library/react';
import { OneOnOneNotes } from '../OneOnOneNotes';

describe('OneOnOneNotes (D-12/D-14) [INV-3-11]', () => {
  it('renders 2 separate Plaud textareas', () => {
    render(
      <OneOnOneNotes
        notes="" onNotesChange={vi.fn()}
        transcricaoPlaud="" onTranscricaoPlaudChange={vi.fn()}
        resumoPlaud="" onResumoPlaudChange={vi.fn()}
      />
    );
    expect(screen.getByLabelText(/Transcrição \(Plaud\)/i)).toBeInTheDocument();
    expect(screen.getByLabelText(/Resumo \(Plaud\)/i)).toBeInTheDocument();
  });

  it('shows short warning when transcricao < 50 chars', () => {
    const onChange = vi.fn();
    render(
      <OneOnOneNotes
        notes="" onNotesChange={vi.fn()}
        transcricaoPlaud="curto" onTranscricaoPlaudChange={onChange}
        resumoPlaud="" onResumoPlaudChange={vi.fn()}
      />
    );
    expect(screen.getByText(/Texto curto demais/i)).toBeInTheDocument();
  });
});
```

**3. Crie `src/components/__tests__/OneOnOneActionItems.test.tsx`:**
```typescript
import { describe, it, expect, vi } from 'vitest';
import { render, screen, fireEvent } from '@testing-library/react';
import { OneOnOneActionItems } from '../OneOnOneActionItems';

describe('OneOnOneActionItems (ONE-05)', () => {
  it('renders empty state when no items', () => {
    render(<OneOnOneActionItems items={[]} onChange={vi.fn()} />);
    expect(screen.getByText(/Nenhum action item ainda/i)).toBeInTheDocument();
  });

  it('add button creates new item', () => {
    const onChange = vi.fn();
    render(<OneOnOneActionItems items={[]} onChange={onChange} />);
    fireEvent.change(screen.getByPlaceholderText(/Novo action item/i), { target: { value: 'Tarefa X' } });
    fireEvent.click(screen.getByRole('button', { name: /Adicionar/i }));
    expect(onChange).toHaveBeenCalled();
  });
});
```

**4. Crie `src/components/__tests__/OneOnOneMeetingForm.test.tsx`** (badge presence + RH note role gating):
```typescript
import { describe, it, expect, vi } from 'vitest';
import { render, screen } from '@testing-library/react';
import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
import { OneOnOneMeetingForm } from '../OneOnOneMeetingForm';
import { buildOneOnOne } from '@/test/perf-fixtures/oneOnOnePlaud';

// Mock useAbility — defaults to liderado (cannot read rh_notes)
vi.mock('@/shared/data/useAbility', () => ({
  useAbility: () => ({ can: (action: string, subject: string) => false }),
}));

const wrapper = ({ children }: { children: React.ReactNode }) => (
  <QueryClientProvider client={new QueryClient()}>{children}</QueryClientProvider>
);

describe('OneOnOneMeetingForm [INV-3-12 + Anti-spoof]', () => {
  it('badge "RH visível" is always rendered', () => {
    render(<OneOnOneMeetingForm meeting={buildOneOnOne() as any} />, { wrapper });
    expect(screen.getByText(/RH visível/i)).toBeInTheDocument();
  });

  it('RHNote section absent from DOM when role is liderado (anti-spoof)', () => {
    render(<OneOnOneMeetingForm meeting={buildOneOnOne() as any} />, { wrapper });
    expect(screen.queryByText(/Notas RH/i)).not.toBeInTheDocument();
  });
});
```

**Verify orchestrator < 300 lines:**
```bash
wc -l src/components/OneOnOneMeetingForm.tsx
# Expected: < 300
```
  </action>
  <verify>
    <automated>npx tsc --noEmit src/components/OneOnOneMeetingForm.tsx 2>&1 | grep -c "error" | awk '{if($1>0) exit 1}' && wc -l src/components/OneOnOneMeetingForm.tsx | awk '{if($1>=300) exit 1}' && npm test -- --run src/components/__tests__/OneOnOne*.test.tsx --reporter=basic 2>&1 | tail -10</automated>
  </verify>
  <done>OneOnOneMeetingForm.tsx < 300 ln (INV-3-15); 3 RTL tests green; RH-visible badge sempre renderizado; RHNote absent for liderado (anti-spoof).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend role check (CASL) → Backend RLS | Frontend é UX layer; RLS é fronteira |
| RHNote conditional render | Defense-in-depth: ausente do DOM (não display:none) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 (HIGH) | I (Information disclosure) | Liderado consegue ver rh_notes via DOM/devtools | mitigate | OneOnOneRHNote `if (!canSee) return null` (DOM-absent); RLS server-side blocks SELECT também |
| T-3-RH-01 | T (Tampering) | useAbility CASL bypass via console manipulation | mitigate | RLS é fronteira; CASL é UX defense; RTL test confirma absent |
| T-3-PLAUD-01 (LOW) | I | Plaud transcript pode conter PII de terceiros | accept | Out-of-scope para v1 (deferred); UI warning futuro |
</threat_model>

<verification>
- 4 hooks novos (extract + < 100 ln cada)
- 6 sub-componentes (cada com props interface explícita)
- Orchestrator < 300 ln
- 3 RTL tests green
- Coverage: ONE-01..06 (1:1 completo); D-12/D-14/D-15/D-17/D-18/D-19
</verification>

<success_criteria>
- INV-3-11 (Plaud paste 10k+ persiste em meeting_structure) verified via test
- INV-3-12 (badge RH visível always present) verified
- INV-3-14 (RHNote RLS) verified server-side via Plan 03-05 pgTAP + frontend via DOM-absent
- INV-3-15 (orchestrator < 300 ln) verified via wc
- INV-3-24 (4 custom hooks < 100 ln cada) verified
</success_criteria>

<output>
Após completar: criar `.planning/phases/03-performance-refactor/03-08-SUMMARY.md` com:
- Linhas finais do orchestrator (target < 300)
- 4 custom hooks listados com tamanhos
- 6 sub-componentes listados com tamanhos
- 3 RTL tests passing
- Próximo: Plan 03-09 (Evaluations + dynamic form)
</output>

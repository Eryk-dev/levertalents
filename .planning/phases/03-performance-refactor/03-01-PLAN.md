---
phase: 03-performance-refactor
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/test/perf-mocks/createUserWithTempPassword.ts
  - src/test/perf-mocks/getClimateAggregate.ts
  - src/test/perf-mocks/submitClimateResponse.ts
  - src/test/perf-mocks/index.ts
  - src/test/perf-fixtures/evaluationCycle.ts
  - src/test/perf-fixtures/templateSnapshot.ts
  - src/test/perf-fixtures/oneOnOnePlaud.ts
  - src/test/perf-fixtures/climateResponse.ts
  - src/lib/__tests__/passwordGenerator.test.ts
  - src/lib/__tests__/evaluationTemplate.test.ts
  - src/lib/__tests__/climateAggregation.test.ts
  - supabase/tests/003_evaluation_cycles_snapshot.sql
  - supabase/tests/004_evaluations_rls.sql
  - supabase/tests/005_climate_anonymity.sql
  - supabase/tests/006_one_on_ones_rls.sql
  - supabase/tests/007_backfill_e.sql
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - PERF-01
  - PERF-02
  - PERF-03
  - PERF-04
  - PERF-05
  - PERF-06
  - PERF-07
  - ONE-01
  - ONE-02
  - ONE-03
  - ONE-04
  - ONE-05
  - ONE-06
must_haves:
  truths:
    - "Vitest stubs (3 unit test files) existem com `describe.skip` + `it.todo` — failing-by-default, não bloqueiam CI"
    - "pgTAP stubs (5 .sql files com 003-007 prefix) existem cada um com `SELECT plan(N)` + `SELECT skip('reason', N)` — failing-by-default"
    - "MSW handlers para Edge Function `create-user-with-temp-password`, RPC `get_climate_aggregate`, RPC `submit_climate_response` existem em `src/test/perf-mocks/`"
    - "Fixtures TypeScript (4 arquivos) existem em `src/test/perf-fixtures/` com factory builders type-safe"
    - "`npm test -- --run --reporter=basic` exit 0 (passing or skipped)"
    - "`npx supabase test db` exit 0 (skipped tests count > 0)"
  artifacts:
    - path: "src/test/perf-mocks/index.ts"
      provides: "Re-exports de 3 MSW handlers Phase 3 (Edge Function + 2 RPCs)"
      contains: "createUserWithTempPasswordHandler"
    - path: "src/test/perf-fixtures/templateSnapshot.ts"
      provides: "Factory builders para TemplateSnapshot (D-07 schema)"
      contains: "buildTemplateSnapshot"
    - path: "src/lib/__tests__/passwordGenerator.test.ts"
      provides: "Stub para INV-3-16 (8 chars, alfabeto restrito)"
      contains: "describe.skip"
    - path: "src/lib/__tests__/evaluationTemplate.test.ts"
      provides: "Stub para INV-3-06 (buildZodFromTemplate)"
      contains: "describe.skip"
    - path: "src/lib/__tests__/climateAggregation.test.ts"
      provides: "Stub para INV-3-09 (k-anonymity helper)"
      contains: "describe.skip"
    - path: "supabase/tests/003_evaluation_cycles_snapshot.sql"
      provides: "Stub para INV-3-05 (snapshot freeze) + INV-3-23 (auto-close cron)"
      contains: "SELECT skip"
    - path: "supabase/tests/004_evaluations_rls.sql"
      provides: "Stub para INV-3-02/03/04/07 (direction CHECK + RLS)"
      contains: "SELECT skip"
    - path: "supabase/tests/005_climate_anonymity.sql"
      provides: "Stub para INV-3-08/09 (drop user_id + k-anon RPC)"
      contains: "SELECT skip"
    - path: "supabase/tests/006_one_on_ones_rls.sql"
      provides: "Stub para INV-3-14 (rh_notes RLS)"
      contains: "SELECT skip"
    - path: "supabase/tests/007_backfill_e.sql"
      provides: "Stub para INV-3-21/22 (Backfill E)"
      contains: "SELECT skip"
  key_links:
    - from: "src/test/perf-mocks/index.ts"
      to: "src/test/setup.ts (Phase 1 MSW server)"
      via: "server.use(...phase3Handlers)"
      pattern: "createUserWithTempPasswordHandler"
    - from: "supabase/tests/003*.sql"
      to: "supabase/tests/setup.sql (Phase 1)"
      via: "BEGIN; SELECT plan(N); ... SELECT * FROM finish(); ROLLBACK;"
      pattern: "plan\\("
---

<objective>
**Wave 0: Test scaffolding para Phase 3.** Criar stubs de testes failing-by-default (Vitest + pgTAP) usando o pattern já estabelecido em Phase 2 Plan 02-01: `describe.skip` + `it.todo` no front; `SELECT skip('reason', N)` no pgTAP. Mocks MSW para Edge Function + 2 RPCs novos. Fixtures TypeScript com factory builders.

Este plan NÃO implementa lógica — só cria estrutura de testes com TODOs claros que Waves subsequentes converterão para passing tests. CI fica verde sem implementação.

Purpose: Estabelecer Nyquist sampling rate (≥1 test stub por invariant INV-3-01..INV-3-24) ANTES de qualquer migration ou hook ser tocado. Esta é a única wave que pode rodar antes do schema novo existir.

Output: 3 vitest stubs + 5 pgTAP stubs + 3 MSW handlers + 4 fixtures TS = 15 arquivos novos. Zero modificações em código de produção.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-performance-refactor/03-CONTEXT.md
@.planning/phases/03-performance-refactor/03-RESEARCH.md
@.planning/phases/03-performance-refactor/03-PATTERNS.md
@.planning/phases/03-performance-refactor/03-VALIDATION.md
@.planning/phases/02-r-s-refactor/02-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Phase 1 + Phase 2 já estabeleceram a infraestrutura de testes — Phase 3 só ESTENDE. -->

From src/test/setup.ts (Phase 1 — DO NOT MODIFY, só ler para pattern):
```typescript
// MSW server setup — handlers vêm de src/test/handlers/
import { setupServer } from 'msw/node';
import { handlers } from './handlers';
export const server = setupServer(...handlers);
```

From src/test/handlers/index.ts (Phase 1 + Phase 2):
```typescript
// Existing handlers: applyToJob, readCandidateWithLog, etc.
// Phase 3 ADICIONA — não substitui.
export const handlers = [
  ...existingPhase1Handlers,
  ...existingPhase2Handlers,
  // Wave 0 adiciona aqui via re-export de perf-mocks/index.ts
];
```

From supabase/tests/setup.sql (Phase 1 + Phase 2):
```sql
-- pgTAP boilerplate — every test file follows this shape
BEGIN;
CREATE EXTENSION IF NOT EXISTS pgtap;
SELECT plan(N);
-- ... tests ...
SELECT * FROM finish();
ROLLBACK;
```

From .planning/phases/02-r-s-refactor/02-01-SUMMARY.md (precedente exato):
- 16 Vitest stubs com `describe.skip` + `it.todo`
- 5 pgTAP stubs com `SELECT skip('Wave N', N)`
- MSW handler URL hard-coded ao project ehbxpbeijofxtsbezwxd
- failing-by-default = CI verde sem implementação

D-07 Template Snapshot Schema (verbatim):
```typescript
type TemplateSnapshot = {
  version: 1;
  sections: Array<{
    id: string;
    title: string;
    weight: number;  // 0..1
    questions: Array<
      | { id: string; label: string; type: 'scale_1_5'; required: boolean }
      | { id: string; label: string; type: 'text'; required: boolean }
      | { id: string; label: string; type: 'choice'; required: boolean; options: string[] }
    >;
  }>;
};
```

D-21 Password Alphabet (verbatim):
```typescript
const ALPHABET = 'abcdefghijkmnpqrstuvwxyzABCDEFGHJKLMNPQRSTUVWXYZ23456789'; // 56 chars (no 0/O/o/1/l/I)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Vitest unit stubs (3 files) + MSW handlers (4 files) + fixtures (4 files)</name>
  <files>src/lib/__tests__/passwordGenerator.test.ts, src/lib/__tests__/evaluationTemplate.test.ts, src/lib/__tests__/climateAggregation.test.ts, src/test/perf-mocks/createUserWithTempPassword.ts, src/test/perf-mocks/getClimateAggregate.ts, src/test/perf-mocks/submitClimateResponse.ts, src/test/perf-mocks/index.ts, src/test/perf-fixtures/evaluationCycle.ts, src/test/perf-fixtures/templateSnapshot.ts, src/test/perf-fixtures/oneOnOnePlaud.ts, src/test/perf-fixtures/climateResponse.ts</files>
  <read_first>
    - src/test/setup.ts (Phase 1 MSW server — pattern source)
    - src/test/handlers/index.ts (Phase 1 + 2 existing handlers — extend, don't break)
    - src/test/handlers/applyToJob.ts (Phase 2 Edge Function MSW handler — closest analog)
    - src/test/fixtures/candidate.ts (Phase 2 fixture pattern — closest analog)
    - .planning/phases/02-r-s-refactor/02-01-SUMMARY.md (failing-by-default precedent)
    - .planning/phases/03-performance-refactor/03-CONTEXT.md §"Tests" + §"D-07" + §"D-21"
    - .planning/phases/03-performance-refactor/03-RESEARCH.md §"Pattern 4" (Edge Function shape) + §"Pattern 2" (k-anon RPC shape)
  </read_first>
  <behavior>
    - Test 1 (passwordGenerator.test.ts): `describe.skip('passwordGenerator')` + `it.todo('returns 8 chars')` + `it.todo('uses alphabet ABCDEFGHJKLMNPQRSTUVWXYZabcdefghijkmnpqrstuvwxyz23456789 only')` + `it.todo('rejects 0/O/o/1/l/I bytes')` + `it.todo('uses crypto.getRandomValues (not Math.random)')` — covers INV-3-16
    - Test 2 (evaluationTemplate.test.ts): `describe.skip('buildZodFromTemplate')` + `it.todo('builds object schema with question.id keys')` + `it.todo('scale_1_5 → z.number().int().min(1).max(5)')` + `it.todo('text required → z.string().min(1)')` + `it.todo('text optional → z.string().optional()')` + `it.todo('choice → z.enum(options)')` + `it.todo('throws on empty options for required choice')` — covers INV-3-06
    - Test 3 (climateAggregation.test.ts): `describe.skip('climateAggregation k-anon')` + `it.todo('count<3 returns {insufficient_data: true} sem count exato')` + `it.todo('count>=3 returns {count, avg, distribution}')` + `it.todo('does not auto-aggregate to parent unit when child <3')` — covers INV-3-09
  </behavior>
  <action>
**1. Crie `src/lib/__tests__/passwordGenerator.test.ts`:**
```typescript
import { describe, it, expect } from 'vitest';

// Wave 3 will implement src/lib/passwordGenerator.ts (mirror of Edge Function CSPRNG).
// This file is a Wave 0 stub — failing-by-default until then.
describe.skip('passwordGenerator (Wave 3)', () => {
  it.todo('returns exactly 8 characters [INV-3-16]');
  it.todo('every char ∈ ABCDEFGHJKLMNPQRSTUVWXYZabcdefghijkmnpqrstuvwxyz23456789 (56 chars, D-21)');
  it.todo('rejects ambiguous chars 0/O/o/1/l/I from output across 1000 samples');
  it.todo('uses crypto.getRandomValues (Web Crypto), not Math.random');
});
```

**2. Crie `src/lib/__tests__/evaluationTemplate.test.ts`:**
```typescript
import { describe, it, expect } from 'vitest';

// Wave 3 will implement src/lib/evaluationTemplate.ts (Pattern 3 from RESEARCH).
describe.skip('buildZodFromTemplate (Wave 3)', () => {
  it.todo('builds object schema where keys = question.id strings [INV-3-06]');
  it.todo('scale_1_5 question → z.number().int().min(1).max(5)');
  it.todo('text required → z.string().min(1, "Resposta obrigatória")');
  it.todo('text optional → z.string().optional() (D-07)');
  it.todo('choice required → z.enum(options as [string, ...string[]])');
  it.todo('choice required + empty options → schema rejects (z.never)');
  it.todo('templateSnapshotSchema validates the snapshot shape itself');
});
```

**3. Crie `src/lib/__tests__/climateAggregation.test.ts`:**
```typescript
import { describe, it, expect } from 'vitest';

// Wave 3 will implement src/lib/climateAggregation.ts (TS mirror of RPC for unit tests).
describe.skip('climateAggregation k-anon (Wave 3)', () => {
  it.todo('count<3 returns { insufficient_data: true } WITHOUT count field [INV-3-09 + Pitfall §3]');
  it.todo('count===3 returns { count, avg: numeric, distribution: Record<score, count> }');
  it.todo('does NOT auto-aggregate to parent org_unit when child has <3 (Open Question §4 default)');
  it.todo('refuses to compute when caller lacks visible_companies(survey.company_id) — security guard');
});
```

**4. Crie `src/test/perf-mocks/createUserWithTempPassword.ts`:**
```typescript
import { http, HttpResponse } from 'msw';

// MSW handler para Edge Function — URL hard-coded para project ehbxpbeijofxtsbezwxd (Phase 2 precedent).
// Default success response; tests can override with server.use(...) for 409 / 403 / 400 cases.
export const createUserWithTempPasswordHandler = http.post(
  'https://ehbxpbeijofxtsbezwxd.supabase.co/functions/v1/create-user-with-temp-password',
  async ({ request }) => {
    const body = (await request.json()) as { email?: string; fullName?: string; role?: string; companyId?: string; orgUnitId?: string };
    if (!body.email || !body.fullName || !body.role) {
      return HttpResponse.json({ error: 'Campos obrigatórios faltando' }, { status: 400 });
    }
    if (body.email === 'duplicate@example.com') {
      return HttpResponse.json({ error: 'duplicate_email' }, { status: 409 });
    }
    return HttpResponse.json({
      success: true,
      userId: '00000000-0000-0000-0000-000000000001',
      tempPassword: 'Abc23xYz', // D-21 alphabet
      expiresAt: new Date(Date.now() + 24 * 3600 * 1000).toISOString(),
    });
  },
);
```

**5. Crie `src/test/perf-mocks/getClimateAggregate.ts`:**
```typescript
import { http, HttpResponse } from 'msw';

// MSW handler para RPC POST /rest/v1/rpc/get_climate_aggregate
// Retorna {insufficient_data:true} se body.p_org_unit_id terminar em '0', senão agg completo.
export const getClimateAggregateHandler = http.post(
  'https://ehbxpbeijofxtsbezwxd.supabase.co/rest/v1/rpc/get_climate_aggregate',
  async ({ request }) => {
    const body = (await request.json()) as { p_survey_id: string; p_org_unit_id: string | null };
    if (body.p_org_unit_id?.endsWith('0')) {
      // Pitfall §3 — sem count exato
      return HttpResponse.json({ insufficient_data: true });
    }
    return HttpResponse.json({
      count: 5,
      avg: 4.2,
      distribution: { '1': 0, '2': 0, '3': 1, '4': 2, '5': 2 },
    });
  },
);
```

**6. Crie `src/test/perf-mocks/submitClimateResponse.ts`:**
```typescript
import { http, HttpResponse } from 'msw';

// RPC submit_climate_response — recebe (survey_id, question_id, score, comment_optional). NÃO recebe user_id (D-11).
export const submitClimateResponseHandler = http.post(
  'https://ehbxpbeijofxtsbezwxd.supabase.co/rest/v1/rpc/submit_climate_response',
  async ({ request }) => {
    const body = (await request.json()) as Record<string, unknown>;
    if ('user_id' in body || 'actor_id' in body) {
      return HttpResponse.json({ error: 'user_id is forbidden — anonymous flow' }, { status: 400 });
    }
    return HttpResponse.json({ success: true });
  },
);
```

**7. Crie `src/test/perf-mocks/index.ts`:**
```typescript
export { createUserWithTempPasswordHandler } from './createUserWithTempPassword';
export { getClimateAggregateHandler } from './getClimateAggregate';
export { submitClimateResponseHandler } from './submitClimateResponse';

import { createUserWithTempPasswordHandler } from './createUserWithTempPassword';
import { getClimateAggregateHandler } from './getClimateAggregate';
import { submitClimateResponseHandler } from './submitClimateResponse';

export const phase3Handlers = [
  createUserWithTempPasswordHandler,
  getClimateAggregateHandler,
  submitClimateResponseHandler,
];
```

Append `...phase3Handlers` to `src/test/handlers/index.ts` exports (preservar Phase 1+2 handlers).

**8. Crie `src/test/perf-fixtures/templateSnapshot.ts`:**
```typescript
// D-07 schema literal — keep in sync com src/lib/evaluationTemplate.ts (Wave 3)
export type TemplateQuestion =
  | { id: string; label: string; type: 'scale_1_5'; required: boolean }
  | { id: string; label: string; type: 'text'; required: boolean }
  | { id: string; label: string; type: 'choice'; required: boolean; options: string[] };

export type TemplateSnapshot = {
  version: 1;
  sections: Array<{ id: string; title: string; weight: number; questions: TemplateQuestion[] }>;
};

export function buildTemplateSnapshot(overrides?: Partial<TemplateSnapshot>): TemplateSnapshot {
  return {
    version: 1,
    sections: [
      {
        id: 'sec-tech',
        title: 'Competências técnicas',
        weight: 0.5,
        questions: [
          { id: 'q-tech-1', label: 'Domina ferramentas do dia a dia', type: 'scale_1_5', required: true },
          { id: 'q-tech-2', label: 'Comentário livre', type: 'text', required: false },
        ],
      },
      {
        id: 'sec-behav',
        title: 'Competências comportamentais',
        weight: 0.5,
        questions: [
          { id: 'q-beh-1', label: 'Frequência de feedback', type: 'choice', required: true, options: ['nunca', 'as vezes', 'sempre'] },
        ],
      },
    ],
    ...overrides,
  };
}
```

**9. Crie `src/test/perf-fixtures/evaluationCycle.ts`:**
```typescript
import { buildTemplateSnapshot, type TemplateSnapshot } from './templateSnapshot';

export type EvaluationCycleFixture = {
  id: string;
  company_id: string;
  template_id: string;
  template_snapshot: TemplateSnapshot;
  name: string;
  starts_at: string;
  ends_at: string;
  status: 'active' | 'closed';
};

export function buildEvaluationCycle(overrides?: Partial<EvaluationCycleFixture>): EvaluationCycleFixture {
  const now = Date.now();
  return {
    id: 'cycle-0001',
    company_id: 'company-0001',
    template_id: 'tmpl-0001',
    template_snapshot: buildTemplateSnapshot(),
    name: 'Q1 2026',
    starts_at: new Date(now - 7 * 24 * 3600 * 1000).toISOString(),
    ends_at: new Date(now + 30 * 24 * 3600 * 1000).toISOString(),
    status: 'active',
    ...overrides,
  };
}
```

**10. Crie `src/test/perf-fixtures/oneOnOnePlaud.ts`:**
```typescript
export type OneOnOneFixture = {
  id: string;
  company_id: string;
  leader_id: string;
  collaborator_id: string;
  scheduled_at: string;
  status: 'scheduled' | 'in-progress' | 'completed';
  meeting_structure: {
    agenda_items?: Array<{ id: string; text: string; checked: boolean }>;
    action_items?: Array<{ id: string; text: string; assignee_id: string; due_date: string | null; checked: boolean }>;
    transcricao_plaud?: string;
    resumo_plaud?: string;
  };
  rh_notes?: string | null;
};

export function buildOneOnOne(overrides?: Partial<OneOnOneFixture>): OneOnOneFixture {
  return {
    id: 'meet-0001',
    company_id: 'company-0001',
    leader_id: 'user-leader-0001',
    collaborator_id: 'user-member-0001',
    scheduled_at: new Date().toISOString(),
    status: 'scheduled',
    meeting_structure: {
      agenda_items: [],
      action_items: [],
      transcricao_plaud: '',
      resumo_plaud: '',
    },
    rh_notes: null,
    ...overrides,
  };
}
```

**11. Crie `src/test/perf-fixtures/climateResponse.ts`:**
```typescript
// Schema PÓS-Wave 2 (Migration clim1) — sem user_id (D-09)
export type ClimateResponseFixture = {
  id: string;
  survey_id: string;
  question_id: string;
  org_unit_id: string | null;
  score: number; // 1..5
  comment: string | null;
  submitted_at: string;
};

export function buildClimateResponse(overrides?: Partial<ClimateResponseFixture>): ClimateResponseFixture {
  return {
    id: 'resp-0001',
    survey_id: 'survey-0001',
    question_id: 'q-climate-1',
    org_unit_id: 'unit-0001',
    score: 4,
    comment: null,
    submitted_at: new Date().toISOString(),
    ...overrides,
  };
}
```

**Por que stubs em vez de testes reais:** Wave 3 implementa `passwordGenerator.ts`, `evaluationTemplate.ts`, `climateAggregation.ts` — só lá os testes podem rodar. Pattern do Phase 2 Plan 02-01 confirmou: stubs failing-by-default deixam CI verde + listagem visual de pendências em `npm test`.
  </action>
  <verify>
    <automated>npm test -- --run --reporter=basic 2>&1 | tail -20 && grep -l "describe.skip" src/lib/__tests__/{password,evaluation,climate}*.test.ts && grep -l "phase3Handlers" src/test/perf-mocks/index.ts && test -f src/test/perf-fixtures/templateSnapshot.ts</automated>
  </verify>
  <done>11 arquivos novos criados; npm test exit 0 (skipped count >= 13); MSW handlers exportados via `phase3Handlers`; fixtures type-safe sem `as any`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: pgTAP stubs (5 files: 003-007)</name>
  <files>supabase/tests/003_evaluation_cycles_snapshot.sql, supabase/tests/004_evaluations_rls.sql, supabase/tests/005_climate_anonymity.sql, supabase/tests/006_one_on_ones_rls.sql, supabase/tests/007_backfill_e.sql</files>
  <read_first>
    - supabase/tests/setup.sql (Phase 1 pgTAP boilerplate)
    - supabase/tests/001_*.sql (Phase 1 pgTAP exemplo)
    - supabase/tests/002_*.sql (Phase 2 pgTAP — closest analog: skip pattern)
    - .planning/phases/02-r-s-refactor/02-01-SUMMARY.md ("SELECT skip(N)" failing-by-default pattern)
    - .planning/phases/03-performance-refactor/03-VALIDATION.md (24 invariants INV-3-01..INV-3-24 — map to tests)
  </read_first>
  <behavior>
    - 003_evaluation_cycles_snapshot.sql: skip(8) — covers INV-3-01, INV-3-05, INV-3-23, plus snapshot freeze trigger tests
    - 004_evaluations_rls.sql: skip(8) — covers INV-3-02, INV-3-03, INV-3-04, INV-3-07
    - 005_climate_anonymity.sql: skip(6) — covers INV-3-08, INV-3-09 (k-anon RPC)
    - 006_one_on_ones_rls.sql: skip(5) — covers INV-3-14 (rh_notes RLS) + meeting_structure JSONB shape
    - 007_backfill_e.sql: skip(7) — covers INV-3-21, INV-3-22 (Backfill E completeness)
  </behavior>
  <action>
**Padrão pgTAP failing-by-default** (Phase 2 Plan 02-01 precedent):
```sql
BEGIN;
CREATE EXTENSION IF NOT EXISTS pgtap;
SELECT plan(N);  -- expected count

SELECT skip('Wave 2 — schema novo ainda não criado em supabase/migrations/20260429*', N);

SELECT * FROM finish();
ROLLBACK;
```

**Crie 5 arquivos com este pattern.**

**1. `supabase/tests/003_evaluation_cycles_snapshot.sql`** (8 stubs):
```sql
-- =========================================================================
-- pgTAP — Phase 3 Wave 0 stub
-- Covers: INV-3-01 (cycles refilter), INV-3-05 (snapshot immutable), INV-3-23 (cron auto-close)
-- Implements when: Wave 2 (perf1 + cron1 migrations applied)
-- =========================================================================
BEGIN;
CREATE EXTENSION IF NOT EXISTS pgtap;
SELECT plan(8);

SELECT skip('Wave 2 — evaluation_cycles + perf1 trigger + cron1 not yet applied', 8);

-- TODO Wave 2: Replace skip() with these 8 tests
-- 1. SELECT has_table('evaluation_cycles');
-- 2. SELECT has_table('evaluation_templates');
-- 3. SELECT col_not_null('evaluation_cycles', 'company_id');
-- 4. SELECT col_not_null('evaluation_cycles', 'template_snapshot');
-- 5. INSERT cycle + UPDATE template.schema_json + assert cycle.template_snapshot UNCHANGED [INV-3-05]
-- 6. UPDATE cycle.template_snapshot directly + assert RAISE EXCEPTION (immutable)
-- 7. SELECT cron.job WHERE jobname = 'evaluation_cycles_auto_close' [INV-3-23]
-- 8. UPDATE cycle SET ends_at = NOW() - 1h, run cron job manually, assert status = 'closed'

SELECT * FROM finish();
ROLLBACK;
```

**2. `supabase/tests/004_evaluations_rls.sql`** (8 stubs):
```sql
-- =========================================================================
-- pgTAP — Phase 3 Wave 0 stub
-- Covers: INV-3-02 (direction CHECK), INV-3-03 (liderado RLS), INV-3-04 (líder via descendants), INV-3-07 (legacy cols dropped)
-- Implements when: Wave 2 (perf1 + perf2 migrations applied)
-- =========================================================================
BEGIN;
CREATE EXTENSION IF NOT EXISTS pgtap;
SELECT plan(8);

SELECT skip('Wave 2 — evaluations rewrite (perf2) not yet applied', 8);

-- TODO Wave 2:
-- 1. SELECT has_column('evaluations', 'cycle_id');
-- 2. SELECT has_column('evaluations', 'direction');
-- 3. SELECT col_check('evaluations', 'direction', $$direction IN ('leader_to_member', 'member_to_leader')$$); [INV-3-02]
-- 4. SELECT hasnt_column('evaluations', 'period');  -- legacy dropped [INV-3-07]
-- 5. SELECT hasnt_column('evaluations', 'overall_score');
-- 6. As liderado X: SELECT * FROM evaluations returns ONLY rows where evaluated_user_id=X or evaluator_user_id=X [INV-3-03]
-- 7. As líder L com unit U: SELECT * FROM evaluations returns rows of users in org_unit_descendants(U) [INV-3-04]
-- 8. As RH: SELECT count returns ALL evaluations of visible_companies

SELECT * FROM finish();
ROLLBACK;
```

**3. `supabase/tests/005_climate_anonymity.sql`** (6 stubs):
```sql
-- =========================================================================
-- pgTAP — Phase 3 Wave 0 stub
-- Covers: INV-3-08 (drop user_id), INV-3-09 (k-anon RPC)
-- Implements when: Wave 2 (clim1 + clim2 migrations applied)
-- =========================================================================
BEGIN;
CREATE EXTENSION IF NOT EXISTS pgtap;
SELECT plan(6);

SELECT skip('Wave 2 — clim1 (drop user_id) + clim2 (k-anon RPC) not yet applied', 6);

-- TODO Wave 2:
-- 1. SELECT hasnt_column('climate_responses', 'user_id'); [INV-3-08]
-- 2. SELECT hasnt_index('climate_responses', 'idx_climate_responses_user_id');
-- 3. INSERT 2 responses, call get_climate_aggregate(survey, unit) → assert returns {insufficient_data: true} sem count [INV-3-09 + Pitfall §3]
-- 4. INSERT 3 responses, call RPC → assert returns {count, avg, distribution}
-- 5. Call submit_climate_response with user_id param → assert RAISE EXCEPTION ou ignored
-- 6. As liderado sem visible_companies(survey): RPC raises 42501

SELECT * FROM finish();
ROLLBACK;
```

**4. `supabase/tests/006_one_on_ones_rls.sql`** (5 stubs):
```sql
-- =========================================================================
-- pgTAP — Phase 3 Wave 0 stub
-- Covers: INV-3-14 (rh_notes RLS — separate table)
-- Implements when: Wave 2 (one1 migration applied)
-- =========================================================================
BEGIN;
CREATE EXTENSION IF NOT EXISTS pgtap;
SELECT plan(5);

SELECT skip('Wave 2 — one1 migration (one_on_one_rh_notes table) not yet applied', 5);

-- TODO Wave 2:
-- 1. SELECT has_table('one_on_one_rh_notes'); [Pitfall §5 decision A3]
-- 2. As liderado: SELECT * FROM one_on_one_rh_notes returns 0 rows [INV-3-14]
-- 3. As admin: SELECT count > 0 (after RH inserts row)
-- 4. SELECT has_column('one_on_ones', 'company_id') AND col_not_null('one_on_ones', 'company_id');
-- 5. INSERT one_on_one with meeting_structure containing transcricao_plaud + resumo_plaud → SELECT preserves keys

SELECT * FROM finish();
ROLLBACK;
```

**5. `supabase/tests/007_backfill_e.sql`** (7 stubs):
```sql
-- =========================================================================
-- pgTAP — Phase 3 Wave 0 stub
-- Covers: INV-3-21 (e1+e2 backfill), INV-3-22 (e3 socios)
-- Implements when: Wave 1 (e1+e2+e3 migrations applied + owner-inputs/companies.json provided)
-- =========================================================================
BEGIN;
CREATE EXTENSION IF NOT EXISTS pgtap;
SELECT plan(7);

SELECT skip('Wave 1 — Backfill E + owner-inputs/companies.json pending', 7);

-- TODO Wave 1:
-- 1. SELECT count(*) FROM company_groups WHERE slug='grupo-lever' = 1 [INV-3-21]
-- 2. SELECT count(*) FROM companies WHERE group_id = (SELECT id FROM company_groups WHERE slug='grupo-lever') = 7
-- 3. For each team t: assert EXISTS(SELECT 1 FROM org_units WHERE id=t.id) [e2 1:1]
-- 4. For each team t WHERE leader_id IS NOT NULL: assert EXISTS(SELECT 1 FROM unit_leaders WHERE org_unit_id=t.id AND user_id=t.leader_id)
-- 5. For each team_members tm: assert EXISTS(SELECT 1 FROM org_unit_members WHERE user_id=tm.user_id AND org_unit_id=tm.team_id)
-- 6. For each user_role role='socio': assert EXISTS(SELECT 1 FROM socio_company_memberships WHERE socio_user_id=user_id) [INV-3-22]
-- 7. SELECT count_socios = count_distinct_socios_in_memberships (no orphans, no duplicates)

SELECT * FROM finish();
ROLLBACK;
```

**Run smoke test após criação:**
```bash
npx supabase test db 2>&1 | tail -20
# Expected: skipped count incremented por 34 (8+8+6+5+7)
```
  </action>
  <verify>
    <automated>npx supabase test db 2>&1 | grep -E "(skipped|ok|pass)" | tail -10 && for f in supabase/tests/00{3,4,5,6,7}*.sql; do test -f "$f" && grep -q "SELECT skip" "$f" && echo "OK: $f"; done</automated>
  </verify>
  <done>5 arquivos pgTAP novos com SELECT skip; supabase test db exit 0; total skipped >= 34; cada arquivo tem TODO comments mapeados a invariants INV-3-XX.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test infra → CI | Stubs failing-by-default não devem mascarar regressões reais |
| MSW handler default → test isolation | Default response não pode poluir testes que esperam erro |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-WAVE0-01 | T (Tampering) | Stubs em vez de testes reais durante Waves 1-4 | mitigate | Cada stub tem comment "TODO Wave N: replace skip() with..." mapeando para invariant ID; checker verifica em cada wave |
| T-3-WAVE0-02 | I (Information disclosure) | MSW default response retorna `tempPassword: 'Abc23xYz'` | accept | Valor é placeholder hard-coded, jamais bate com produção (não é Plaintext PII real); usado apenas em teste isolation |
</threat_model>

<verification>
- `npm test -- --run --reporter=basic` → exit 0 (skipped count incremented)
- `npx supabase test db` → exit 0 (skipped >= 34)
- `grep -r "describe.skip" src/lib/__tests__/` → 3 matches (1 per test file)
- `grep -r "phase3Handlers" src/test/perf-mocks/index.ts` → 1 match
- Nenhum arquivo de produção (`src/components/`, `src/hooks/`, `src/pages/`, `supabase/migrations/`) modificado.
</verification>

<success_criteria>
- 11 arquivos vitest+MSW+fixtures + 5 arquivos pgTAP = 16 novos arquivos criados
- CI verde sem nenhuma implementação de Wave 1+
- Cada invariant INV-3-XX tem ao menos 1 stub (Vitest ou pgTAP) com comment de mapeamento
- Pattern de Phase 2 Plan 02-01 reproduzido fielmente
</success_criteria>

<output>
Após completar: criar `.planning/phases/03-performance-refactor/03-01-SUMMARY.md` com:
- Lista dos 16 arquivos criados
- Total de stubs por invariant (tabela INV-3-XX → file)
- Comando para listar TODOs pendentes (`grep -r "TODO Wave" supabase/tests/00{3..7}*.sql`)
- Confirmação que phase3Handlers foi append em src/test/handlers/index.ts
</output>

---
phase: 03-performance-refactor
plan: 11
type: execute
wave: 4
depends_on: ["03-07", "03-08", "03-09", "03-10"]
files_modified:
  - src/components/OnboardingMessageBlock.tsx
  - src/components/FirstLoginChangePasswordCard.tsx
  - src/components/TempPasswordExpiredBanner.tsx
  - src/pages/CreateUser.tsx
  - src/pages/FirstLoginChangePassword.tsx
  - src/components/ProtectedRoute.tsx
  - src/App.tsx
  - src/components/__tests__/OnboardingMessageBlock.test.tsx
  - src/components/__tests__/ProtectedRoute.test.tsx
  - src/pages/__tests__/CreateUser.test.tsx
  - src/pages/__tests__/FirstLoginChangePassword.test.tsx
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - PERF-04
  - ONE-03
  - ONE-06
must_haves:
  truths:
    - "`CreateUser.tsx` (rename source path stays — route adicionada `/cadastrar-pessoa` apontando ao mesmo component) reescrito para usar `useCreateUserWithTempPassword`; pós-success mostra `OnboardingMessageBlock` em modal/section com mensagem WhatsApp pronta + botão Copiar"
    - "`OnboardingMessageBlock.tsx` renderiza template locked (D-20) com `{Nome}`, `{email}`, `{tempPassword}`, `{nome do RH}` interpolados; botão 'Copiar mensagem' usa Clipboard API; flash 'Copiado ✓' por 1.2s"
    - "`FirstLoginChangePassword.tsx` page bloqueante: card centered max-w-md com LeverArrow primitive (NUNCA Lucide); 2 password fields + match validation; banner `TempPasswordExpiredBanner` se `temp_password_expires_at < NOW()` (D-24)"
    - "`ProtectedRoute.tsx` extended com `must_change_password` redirect: useEffect checks profile flag; redirect FORCE pra `/first-login-change-password` excluindo when já lá (Pitfall §4 — anti loop)"
    - "App.tsx adiciona route `/first-login-change-password` envolto em ProtectedRoute (Pattern 5 — auth required, mas ProtectedRoute não redireciona quando já nessa path)"
    - "RTL tests: ProtectedRoute redirect quando flag true; OnboardingMessageBlock interpola e copia ao clipboard; FirstLoginChangePassword renderiza LeverArrow + amber banner se expired"
  artifacts:
    - path: "src/components/OnboardingMessageBlock.tsx"
      provides: "Pre-formatted WhatsApp message + Copy button (D-20 locked template)"
      contains: "Bem-vindo à Lever"
    - path: "src/components/FirstLoginChangePasswordCard.tsx"
      provides: "Auth form card; LeverArrow + 2 fields + Btn accent full-width"
      contains: "LeverArrow"
    - path: "src/pages/FirstLoginChangePassword.tsx"
      provides: "Blocking page sem sidebar/header"
      contains: "Crie sua nova senha"
    - path: "src/pages/CreateUser.tsx"
      provides: "Refactored: removes password field; calls Edge Function; shows OnboardingMessageBlock post-success"
      contains: "useCreateUserWithTempPassword"
    - path: "src/components/ProtectedRoute.tsx"
      provides: "Extended: must_change_password redirect gate"
      contains: "must_change_password"
  key_links:
    - from: "src/pages/CreateUser.tsx"
      to: "useCreateUserWithTempPassword (Plan 03-07)"
      via: "Edge Function invoke + post-success modal"
      pattern: "useCreateUserWithTempPassword"
    - from: "src/components/ProtectedRoute.tsx"
      to: "useUserProfile (Phase 1 — D-26 unchanged)"
      via: "Reads profile.must_change_password"
      pattern: "must_change_password"
    - from: "src/pages/FirstLoginChangePassword.tsx"
      to: "useChangePassword (Plan 03-07)"
      via: "Update password + flip flag + invalidate cache + navigate /"
      pattern: "useChangePassword"
    - from: "OnboardingMessageBlock.tsx"
      to: "navigator.clipboard.writeText()"
      via: "Web Clipboard API for D-20 Copy button"
      pattern: "clipboard.writeText"
---

<objective>
**Wave 4 UI #4 (final): Onboarding WhatsApp + First-login flow + ProtectedRoute gate.** Coverage de AUTH-01 (RH cria pessoa), AUTH-02 (mensagem WhatsApp pronta), AUTH-03 (senha temp 24h + força troca primeiro login), D-20/D-21/D-22/D-23/D-24 + Pitfall §4/§9/§12.

**Crucial:**
- `LeverArrow` primitive (lock — JAMAIS Lucide arrow per `feedback_brand_fidelity.md`)
- Mensagem WhatsApp template locked (D-20) — copy verbatim, só interpolação `{Nome}`, `{email}`, `{tempPassword}`, `{nome do RH}`
- D-24: senha expirada >24h NÃO bloqueia login, mas força troca imediata via banner amber
- Pitfall §4: `/first-login-change-password` envolto em ProtectedRoute (auth required), mas ProtectedRoute SKIPA o redirect quando já nessa path (evita loop infinito)
- Pitfall §9: useChangePassword (Plan 03-07) já invalida cache `['userProfile']` no onSuccess; FirstLoginChangePassword aguarda invalidação antes de navigate('/')
- Pitfall §12: tempPassword é local state ephemeral; nunca persistido em prop chains longas

Output: 3 componentes novos + 2 pages refactored/new + ProtectedRoute extended + 4 RTL tests + App.tsx route added.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-performance-refactor/03-CONTEXT.md
@.planning/phases/03-performance-refactor/03-RESEARCH.md
@.planning/phases/03-performance-refactor/03-UI-SPEC.md
@.planning/phases/03-performance-refactor/03-07-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Plan 03-07 hooks (verified): -->
<!-- useCreateUserWithTempPassword() — mutation returns { userId, tempPassword, expiresAt } -->
<!-- useChangePassword() — mutation { newPassword }; onSuccess invalidates ['userProfile'] -->

<!-- Phase 1 components (locked — DO NOT modify, only extend ProtectedRoute): -->
<!-- src/components/ProtectedRoute.tsx (current Phase 1 — auth check + scope context) -->
<!-- src/hooks/useUserProfile.ts (D-26 lock — unchanged) -->
<!-- src/hooks/useAuth.ts (D-26 lock — unchanged) -->
<!-- src/components/primitives/LeverArrow.tsx (brand primitive — locked, never substitute) -->

<!-- Phase 1 pages (verified): -->
<!-- src/pages/CreateUser.tsx (current 346 ln — REWRITE: remove password field; call Edge Function via hook; show OnboardingMessageBlock post-success) -->
<!-- src/App.tsx — needs new route /first-login-change-password -->

<!-- UI-SPEC locked copy + flow: -->
<!--   "Cadastrar e gerar mensagem" (primary CTA) + "Voltar para a lista" (secondary ghost) -->
<!--   Modal post-success: "Copiar mensagem" (primary) + "Concluir cadastro" (ghost) -->
<!--   First-login heading: "Crie sua nova senha" -->
<!--   First-login body: "Esta é sua primeira entrada. Defina uma senha pessoal antes de continuar." -->
<!--   Expired banner: "Sua senha temporária venceu. Por segurança, troque agora antes de continuar." -->
<!--   Mismatch error: "As senhas digitadas são diferentes." -->
<!--   Min length error: "A senha precisa ter no mínimo 8 caracteres." -->
<!--   Trocar senha (CTA full-width) -->
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OnboardingMessageBlock + TempPasswordExpiredBanner + FirstLoginChangePasswordCard components</name>
  <files>src/components/OnboardingMessageBlock.tsx, src/components/TempPasswordExpiredBanner.tsx, src/components/FirstLoginChangePasswordCard.tsx, src/components/__tests__/OnboardingMessageBlock.test.tsx</files>
  <read_first>
    - src/components/primitives/LeverArrow.tsx (brand lock — read shape; NEVER substitute)
    - src/components/primitives/LinearKit.tsx (Card, Btn, Chip)
    - src/components/ui/input.tsx (shadcn Input)
    - src/components/ui/label.tsx
    - .planning/phases/03-performance-refactor/03-UI-SPEC.md §"WhatsApp onboarding message" verbatim template + §"First-login screen"
  </read_first>
  <behavior>
    - OnboardingMessageBlock: { fullName, email, tempPassword, expiresAt, rhFullName }; renderiza template locked; botão Copiar usa navigator.clipboard.writeText(); flash 'Copiado ✓' por 1.2s
    - TempPasswordExpiredBanner: amber background; copy locked
    - FirstLoginChangePasswordCard: form com 2 password fields + show/hide eye + min 8 chars + match validation; submit chama useChangePassword
  </behavior>
  <action>
**1. Crie `src/components/OnboardingMessageBlock.tsx`:**
```tsx
import { useState, useCallback } from 'react';
import { Card, Btn, SectionHeader } from '@/components/primitives/LinearKit';
import { Check, Copy } from 'lucide-react';

export interface OnboardingMessageBlockProps {
  fullName: string;
  email: string;
  tempPassword: string;
  expiresAt: string;
  rhFullName: string;
  onComplete?: () => void;
}

function buildMessage(props: OnboardingMessageBlockProps): string {
  // D-20 LOCKED template — copy VERBATIM, only interpolate the 4 placeholders
  return `Oi ${props.fullName}! Bem-vindo à Lever.
Acesse: https://app.levertalents.com/login
Login: ${props.email}
Senha temporária: ${props.tempPassword}
Expira em 24h.

Qualquer dúvida, fala comigo!
— ${props.rhFullName}`;
}

export function OnboardingMessageBlock(props: OnboardingMessageBlockProps) {
  const [copied, setCopied] = useState(false);
  const message = buildMessage(props);

  const handleCopy = useCallback(async () => {
    try {
      await navigator.clipboard.writeText(message);
      setCopied(true);
      setTimeout(() => setCopied(false), 1200);
    } catch {
      // Fallback: prompt textarea select (best-effort)
    }
  }, [message]);

  return (
    <Card className="bg-bg-subtle border-border">
      <SectionHeader>Pessoa cadastrada</SectionHeader>
      <p className="text-sm text-text-subtle mb-3">
        Copie a mensagem abaixo e envie pelo seu WhatsApp para {props.fullName}.
      </p>
      <pre className="font-mono text-sm bg-surface p-3 rounded border border-border whitespace-pre-wrap mb-4">
        {message}
      </pre>
      <div className="flex gap-2 flex-col md:flex-row">
        <Btn
          variant="accent"
          onClick={handleCopy}
          className="md:flex-1"
        >
          {copied ? (
            <span className="flex items-center gap-1 text-status-green">
              <Check className="size-4" /> Copiado ✓
            </span>
          ) : (
            <span className="flex items-center gap-1">
              <Copy className="size-4" /> Copiar mensagem
            </span>
          )}
        </Btn>
        {props.onComplete && (
          <Btn variant="ghost" onClick={props.onComplete}>Concluir cadastro</Btn>
        )}
      </div>
    </Card>
  );
}
```

**2. Crie `src/components/TempPasswordExpiredBanner.tsx`:**
```tsx
import { AlertTriangle } from 'lucide-react';

export function TempPasswordExpiredBanner() {
  return (
    <div className="bg-status-amber-soft border border-status-amber/40 rounded p-3 flex items-start gap-2 mb-4">
      <AlertTriangle className="size-4 text-status-amber mt-0.5" aria-hidden="true" />
      <p className="text-sm">Sua senha temporária venceu. Por segurança, troque agora antes de continuar.</p>
    </div>
  );
}
```

**3. Crie `src/components/FirstLoginChangePasswordCard.tsx`:**
```tsx
import { useState } from 'react';
import { useForm } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { z } from 'zod';
import { Card, Btn } from '@/components/primitives/LinearKit';
import { Input } from '@/components/ui/input';
import { Form, FormField, FormItem, FormLabel, FormControl, FormMessage } from '@/components/ui/form';
import { LeverArrow } from '@/components/primitives/LeverArrow';
import { Eye, EyeOff } from 'lucide-react';
import { TempPasswordExpiredBanner } from './TempPasswordExpiredBanner';
import { useChangePassword } from '@/hooks/useChangePassword';
import { useNavigate } from 'react-router-dom';
import { toast } from 'sonner';

const schema = z.object({
  newPassword: z.string().min(8, 'A senha precisa ter no mínimo 8 caracteres.'),
  confirmPassword: z.string(),
}).refine((d) => d.newPassword === d.confirmPassword, {
  message: 'As senhas digitadas são diferentes.',
  path: ['confirmPassword'],
});
type Values = z.infer<typeof schema>;

export interface FirstLoginChangePasswordCardProps {
  tempPasswordExpired?: boolean;
}

export function FirstLoginChangePasswordCard({ tempPasswordExpired = false }: FirstLoginChangePasswordCardProps) {
  const [showPwd, setShowPwd] = useState(false);
  const [showConfirm, setShowConfirm] = useState(false);
  const change = useChangePassword();
  const navigate = useNavigate();

  const form = useForm<Values>({
    resolver: zodResolver(schema),
    defaultValues: { newPassword: '', confirmPassword: '' },
  });

  const onSubmit = (v: Values) => {
    change.mutate({ newPassword: v.newPassword }, {
      onSuccess: async () => {
        toast.success('Senha alterada');
        // Pitfall §9: useChangePassword.onSuccess already invalidates ['userProfile'].
        // We await briefly then navigate; ProtectedRoute reads fresh profile and lets us through.
        await new Promise((r) => setTimeout(r, 200));
        navigate('/', { replace: true });
      },
      onError: (e) => toast.error('Não foi possível trocar a senha', { description: e.message }),
    });
  };

  return (
    <div className="min-h-screen flex items-center justify-center bg-bg p-12">
      <Card className="max-w-md w-full surface-raised">
        <div className="flex justify-center mb-6">
          <LeverArrow className="size-12" />
        </div>
        <h1 className="text-display-md text-center mb-2">Crie sua nova senha</h1>
        <p className="text-sm text-text-subtle text-center mb-6">
          Esta é sua primeira entrada. Defina uma senha pessoal antes de continuar.
        </p>

        {tempPasswordExpired && <TempPasswordExpiredBanner />}

        <Form {...form}>
          <form onSubmit={form.handleSubmit(onSubmit)} className="space-y-4">
            <FormField control={form.control} name="newPassword" render={({ field }) => (
              <FormItem>
                <FormLabel>Nova senha</FormLabel>
                <FormControl>
                  <div className="relative">
                    <Input type={showPwd ? 'text' : 'password'} {...field} autoComplete="new-password" />
                    <button type="button" onClick={() => setShowPwd((s) => !s)} className="absolute right-2 top-1/2 -translate-y-1/2" aria-label={showPwd ? 'Ocultar senha' : 'Mostrar senha'}>
                      {showPwd ? <EyeOff className="size-4" /> : <Eye className="size-4" />}
                    </button>
                  </div>
                </FormControl>
                <FormMessage />
              </FormItem>
            )} />
            <FormField control={form.control} name="confirmPassword" render={({ field }) => (
              <FormItem>
                <FormLabel>Confirmar nova senha</FormLabel>
                <FormControl>
                  <div className="relative">
                    <Input type={showConfirm ? 'text' : 'password'} {...field} autoComplete="new-password" />
                    <button type="button" onClick={() => setShowConfirm((s) => !s)} className="absolute right-2 top-1/2 -translate-y-1/2" aria-label={showConfirm ? 'Ocultar' : 'Mostrar'}>
                      {showConfirm ? <EyeOff className="size-4" /> : <Eye className="size-4" />}
                    </button>
                  </div>
                </FormControl>
                <FormMessage />
              </FormItem>
            )} />
            <Btn variant="accent" type="submit" className="w-full" disabled={change.isPending}>
              {change.isPending ? 'Salvando…' : 'Trocar senha'}
            </Btn>
          </form>
        </Form>
      </Card>
    </div>
  );
}
```

**4. Crie `src/components/__tests__/OnboardingMessageBlock.test.tsx`:**
```typescript
import { describe, it, expect, vi } from 'vitest';
import { render, screen, fireEvent, waitFor } from '@testing-library/react';
import { OnboardingMessageBlock } from '../OnboardingMessageBlock';

describe('OnboardingMessageBlock (D-20) [INV-3-16]', () => {
  beforeAll(() => {
    Object.assign(navigator, { clipboard: { writeText: vi.fn().mockResolvedValue(undefined) } });
  });

  const props = {
    fullName: 'Maria Silva',
    email: 'maria@example.com',
    tempPassword: 'Abc23xYz',
    expiresAt: new Date(Date.now() + 24 * 3600 * 1000).toISOString(),
    rhFullName: 'João RH',
  };

  it('renders D-20 locked template with interpolations', () => {
    render(<OnboardingMessageBlock {...props} />);
    expect(screen.getByText(/Oi Maria Silva! Bem-vindo à Lever/)).toBeInTheDocument();
    expect(screen.getByText(/Login: maria@example.com/)).toBeInTheDocument();
    expect(screen.getByText(/Senha temporária: Abc23xYz/)).toBeInTheDocument();
    expect(screen.getByText(/— João RH/)).toBeInTheDocument();
    expect(screen.getByText(/Expira em 24h/)).toBeInTheDocument();
  });

  it('Copy button copies message to clipboard + flashes "Copiado ✓"', async () => {
    render(<OnboardingMessageBlock {...props} />);
    const btn = screen.getByRole('button', { name: /Copiar mensagem/i });
    fireEvent.click(btn);
    await waitFor(() => {
      expect(navigator.clipboard.writeText).toHaveBeenCalled();
      expect(screen.getByText(/Copiado ✓/)).toBeInTheDocument();
    });
  });
});
```
  </action>
  <verify>
    <automated>npx tsc --noEmit src/components/OnboardingMessageBlock.tsx src/components/TempPasswordExpiredBanner.tsx src/components/FirstLoginChangePasswordCard.tsx 2>&1 | grep -c "error" | awk '{if($1>0) exit 1}' && grep -q "LeverArrow" src/components/FirstLoginChangePasswordCard.tsx && ! grep -q "lucide-react.*ArrowRight\|ArrowLeft" src/components/FirstLoginChangePasswordCard.tsx && grep -q "Bem-vindo à Lever" src/components/OnboardingMessageBlock.tsx && npm test -- --run src/components/__tests__/OnboardingMessageBlock.test.tsx --reporter=basic 2>&1 | tail -5</automated>
  </verify>
  <done>3 components + RTL test green; LeverArrow primitive (NUNCA Lucide); D-20 template locked; clipboard copy funciona com flash; min-8 chars + match validation Zod.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ProtectedRoute extension + FirstLoginChangePassword page + App.tsx route + RTL test</name>
  <files>src/components/ProtectedRoute.tsx, src/pages/FirstLoginChangePassword.tsx, src/App.tsx, src/components/__tests__/ProtectedRoute.test.tsx, src/pages/__tests__/FirstLoginChangePassword.test.tsx</files>
  <read_first>
    - src/components/ProtectedRoute.tsx (current — Phase 1)
    - src/components/FirstLoginChangePasswordCard.tsx (Task 1)
    - src/hooks/useUserProfile.ts (Phase 1 — D-26 unchanged; reads profiles.must_change_password)
    - src/App.tsx (current routing setup)
    - .planning/phases/03-performance-refactor/03-RESEARCH.md §"Pattern 5" + §"Pitfall 4"
  </read_first>
  <behavior>
    - ProtectedRoute: useEffect reads profile.must_change_password; if true AND location.pathname !== '/first-login-change-password' → navigate('/first-login-change-password', { replace: true })
    - FirstLoginChangePassword.tsx: page wrapper that reads profile + renders FirstLoginChangePasswordCard; passes tempPasswordExpired prop derived from temp_password_expires_at < now
    - App.tsx: adds Route path="/first-login-change-password" element={<ProtectedRoute><FirstLoginChangePassword /></ProtectedRoute>} (with auth required but escapes the redirect via Pitfall §4 guard)
    - RTL test ProtectedRoute: render with profile.must_change_password=true → assert navigate called to '/first-login-change-password'; render on that path with same flag → no navigate (no loop)
  </behavior>
  <action>
**1. Extend `src/components/ProtectedRoute.tsx`** (preserve Phase 1 logic; add must_change_password gate):
```tsx
import { useEffect, type ReactNode } from 'react';
import { useNavigate, useLocation, Navigate } from 'react-router-dom';
import { useAuth } from '@/hooks/useAuth';
import { useUserProfile } from '@/hooks/useUserProfile';

const FIRST_LOGIN_PATH = '/first-login-change-password';

export function ProtectedRoute({ children }: { children: ReactNode }) {
  const { user, loading } = useAuth();
  const { profile, isLoading: profileLoading } = useUserProfile();
  const navigate = useNavigate();
  const location = useLocation();

  useEffect(() => {
    if (loading || profileLoading || !user || !profile) return;
    // D-23 + Pattern 5: redirect when must_change_password true; SKIP when already on that path (Pitfall §4)
    if (profile.must_change_password && location.pathname !== FIRST_LOGIN_PATH) {
      navigate(FIRST_LOGIN_PATH, { replace: true });
    }
  }, [profile?.must_change_password, location.pathname, user, loading, profileLoading, navigate, profile]);

  if (loading || profileLoading) {
    // Phase 1: existing skeleton — keep as-is to avoid layout flash
    return <div className="min-h-screen flex items-center justify-center"><span className="text-sm text-text-subtle">Carregando…</span></div>;
  }
  if (!user) return <Navigate to="/login" replace />;

  return <>{children}</>;
}
```

**2. Crie `src/pages/FirstLoginChangePassword.tsx`:**
```tsx
import { FirstLoginChangePasswordCard } from '@/components/FirstLoginChangePasswordCard';
import { useUserProfile } from '@/hooks/useUserProfile';

export default function FirstLoginChangePasswordPage() {
  const { profile, isLoading } = useUserProfile();

  if (isLoading) return null; // ProtectedRoute already shows loading

  const expiresAt = profile?.temp_password_expires_at ? new Date(profile.temp_password_expires_at) : null;
  const expired = expiresAt != null && expiresAt < new Date(); // D-24

  return <FirstLoginChangePasswordCard tempPasswordExpired={expired} />;
}
```

**3. Update `src/App.tsx`** to add route. Pattern (preserve existing routes; ADD new):
```tsx
// In App.tsx Routes block, add this new Route alongside existing ones:
import FirstLoginChangePassword from '@/pages/FirstLoginChangePassword';
// ... inside <Routes>:
<Route
  path="/first-login-change-password"
  element={<ProtectedRoute><FirstLoginChangePassword /></ProtectedRoute>}
/>
```

(Read App.tsx first to find existing Routes block; insert new Route adjacent to existing protected routes. Do not re-architect routing.)

**4. Crie `src/components/__tests__/ProtectedRoute.test.tsx`:**
```typescript
import { describe, it, expect, vi } from 'vitest';
import { render, screen } from '@testing-library/react';
import { MemoryRouter, Routes, Route, useLocation } from 'react-router-dom';
import { ProtectedRoute } from '../ProtectedRoute';

const mockUseAuth = vi.fn();
const mockUseUserProfile = vi.fn();
vi.mock('@/hooks/useAuth', () => ({ useAuth: () => mockUseAuth() }));
vi.mock('@/hooks/useUserProfile', () => ({ useUserProfile: () => mockUseUserProfile() }));

function LocationProbe() {
  const loc = useLocation();
  return <div data-testid="path">{loc.pathname}</div>;
}

describe('ProtectedRoute (D-23 + Pitfall §4) [INV-3-17]', () => {
  it('redirects to /first-login-change-password when must_change_password=true', () => {
    mockUseAuth.mockReturnValue({ user: { id: 'u1' }, loading: false });
    mockUseUserProfile.mockReturnValue({ profile: { id: 'u1', must_change_password: true }, isLoading: false });

    render(
      <MemoryRouter initialEntries={['/jobs']}>
        <Routes>
          <Route path="/jobs" element={<ProtectedRoute><div>Jobs</div></ProtectedRoute>} />
          <Route path="/first-login-change-password" element={<LocationProbe />} />
        </Routes>
      </MemoryRouter>,
    );
    // Eventually rendered LocationProbe with /first-login-change-password
    expect(screen.getByTestId('path')).toHaveTextContent('/first-login-change-password');
  });

  it('does NOT redirect when already on /first-login-change-password (no loop)', () => {
    mockUseAuth.mockReturnValue({ user: { id: 'u1' }, loading: false });
    mockUseUserProfile.mockReturnValue({ profile: { id: 'u1', must_change_password: true }, isLoading: false });

    render(
      <MemoryRouter initialEntries={['/first-login-change-password']}>
        <Routes>
          <Route path="/first-login-change-password" element={<ProtectedRoute><div data-testid="page">Card</div></ProtectedRoute>} />
        </Routes>
      </MemoryRouter>,
    );
    expect(screen.getByTestId('page')).toBeInTheDocument();
  });

  it('renders children when must_change_password=false', () => {
    mockUseAuth.mockReturnValue({ user: { id: 'u1' }, loading: false });
    mockUseUserProfile.mockReturnValue({ profile: { id: 'u1', must_change_password: false }, isLoading: false });

    render(
      <MemoryRouter initialEntries={['/jobs']}>
        <Routes>
          <Route path="/jobs" element={<ProtectedRoute><div data-testid="ok">Jobs</div></ProtectedRoute>} />
        </Routes>
      </MemoryRouter>,
    );
    expect(screen.getByTestId('ok')).toBeInTheDocument();
  });
});
```

**5. Crie `src/pages/__tests__/FirstLoginChangePassword.test.tsx`:**
```typescript
import { describe, it, expect, vi } from 'vitest';
import { render, screen } from '@testing-library/react';
import { MemoryRouter } from 'react-router-dom';
import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
import FirstLoginChangePassword from '../FirstLoginChangePassword';

const mockProfile = vi.fn();
vi.mock('@/hooks/useUserProfile', () => ({ useUserProfile: () => mockProfile() }));
vi.mock('@/hooks/useChangePassword', () => ({ useChangePassword: () => ({ mutate: vi.fn(), isPending: false }) }));

const wrapper = ({ children }: { children: React.ReactNode }) => (
  <QueryClientProvider client={new QueryClient()}>
    <MemoryRouter>{children}</MemoryRouter>
  </QueryClientProvider>
);

describe('FirstLoginChangePassword (D-24) [INV-3-18]', () => {
  it('renders without expired banner when temp not expired', () => {
    mockProfile.mockReturnValue({
      profile: { id: 'u1', temp_password_expires_at: new Date(Date.now() + 12 * 3600 * 1000).toISOString() },
      isLoading: false,
    });
    render(<FirstLoginChangePassword />, { wrapper });
    expect(screen.getByText('Crie sua nova senha')).toBeInTheDocument();
    expect(screen.queryByText(/Sua senha temporária venceu/)).not.toBeInTheDocument();
  });

  it('renders amber banner when temp_password_expires_at < now (D-24)', () => {
    mockProfile.mockReturnValue({
      profile: { id: 'u1', temp_password_expires_at: new Date(Date.now() - 1 * 3600 * 1000).toISOString() },
      isLoading: false,
    });
    render(<FirstLoginChangePassword />, { wrapper });
    expect(screen.getByText(/Sua senha temporária venceu/)).toBeInTheDocument();
  });
});
```
  </action>
  <verify>
    <automated>npx tsc --noEmit src/components/ProtectedRoute.tsx src/pages/FirstLoginChangePassword.tsx 2>&1 | grep -c "error" | awk '{if($1>0) exit 1}' && grep -q "must_change_password" src/components/ProtectedRoute.tsx && grep -q "FIRST_LOGIN_PATH" src/components/ProtectedRoute.tsx && grep -q "/first-login-change-password" src/App.tsx && npm test -- --run src/components/__tests__/ProtectedRoute.test.tsx src/pages/__tests__/FirstLoginChangePassword.test.tsx --reporter=basic 2>&1 | tail -5</automated>
  </verify>
  <done>ProtectedRoute extended; Pitfall §4 anti-loop guard; route registered; 5 RTL tests green; amber banner condicional ao expired.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Refactor src/pages/CreateUser.tsx (Edge Function + OnboardingMessageBlock) + RTL test</name>
  <files>src/pages/CreateUser.tsx, src/pages/__tests__/CreateUser.test.tsx</files>
  <read_first>
    - src/pages/CreateUser.tsx (current 346 ln — REWRITE: remove password field; substitute auth.signUp pelo Edge Function via hook; show OnboardingMessageBlock)
    - src/hooks/useCreateUserWithTempPassword.ts (Plan 03-07)
    - src/components/OnboardingMessageBlock.tsx (Task 1)
    - src/hooks/useUserProfile.ts (current RH user — para `rhFullName` no template)
    - .planning/phases/03-performance-refactor/03-UI-SPEC.md §"Onboarding flow (D-20 → D-23)"
  </read_first>
  <behavior>
    - Form fields: fullName, email (lower+trim), role (Select), companyId (Select scoped), orgUnitId (Select cascading)
    - Submit: useCreateUserWithTempPassword.mutate; on success setResult + show OnboardingMessageBlock
    - OnboardingMessageBlock recebe rhFullName from useUserProfile
    - On 'duplicate_email' error: inline form error
    - Pitfall §12: tempPassword é local state ephemeral; clear no unmount/concluir
  </behavior>
  <action>
**Refactor `src/pages/CreateUser.tsx`** (target < 300 ln post-refactor):
```tsx
import { useState } from 'react';
import { useForm } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { z } from 'zod';
import { Btn, Card } from '@/components/primitives/LinearKit';
import { Input } from '@/components/ui/input';
import { Select, SelectTrigger, SelectValue, SelectContent, SelectItem } from '@/components/ui/select';
import { Form, FormField, FormItem, FormLabel, FormControl, FormMessage } from '@/components/ui/form';
import { OnboardingMessageBlock } from '@/components/OnboardingMessageBlock';
import { useCreateUserWithTempPassword } from '@/hooks/useCreateUserWithTempPassword';
import { useUserProfile } from '@/hooks/useUserProfile';
import { useScope } from '@/shared/data/useScope';
import { toast } from 'sonner';
import { useNavigate } from 'react-router-dom';

const schema = z.object({
  fullName: z.string().min(1, 'Informe o nome completo'),
  email: z.string().email('E-mail inválido'),
  role: z.enum(['admin', 'rh', 'socio', 'lider', 'liderado']),
  companyId: z.string().uuid().optional(),
  orgUnitId: z.string().uuid().optional(),
});
type Values = z.infer<typeof schema>;

interface OnboardingResult {
  fullName: string;
  email: string;
  tempPassword: string;
  expiresAt: string;
}

export default function CreateUserPage() {
  const navigate = useNavigate();
  const create = useCreateUserWithTempPassword();
  const { profile: rhProfile } = useUserProfile();
  const scope = useScope();
  const [result, setResult] = useState<OnboardingResult | null>(null);
  const [duplicateError, setDuplicateError] = useState(false);

  const form = useForm<Values>({
    resolver: zodResolver(schema),
    defaultValues: { fullName: '', email: '', role: 'liderado' },
  });

  const onSubmit = (v: Values) => {
    setDuplicateError(false);
    const payload = { ...v, email: v.email.toLowerCase().trim(), fullName: v.fullName.trim() };
    create.mutate(payload, {
      onSuccess: (data) => {
        // Pitfall §12: store tempPassword in local state only; clear on unmount via React lifecycle
        setResult({
          fullName: payload.fullName,
          email: payload.email,
          tempPassword: data.tempPassword,
          expiresAt: data.expiresAt,
        });
        form.reset();
        toast.success('Pessoa cadastrada');
      },
      onError: (e) => {
        if (e.message === 'duplicate_email') {
          setDuplicateError(true);
        } else {
          toast.error('Não foi possível cadastrar', { description: e.message });
        }
      },
    });
  };

  // Post-success: render OnboardingMessageBlock
  if (result) {
    return (
      <div className="p-6">
        <OnboardingMessageBlock
          fullName={result.fullName}
          email={result.email}
          tempPassword={result.tempPassword}
          expiresAt={result.expiresAt}
          rhFullName={rhProfile?.full_name ?? 'RH'}
          onComplete={() => {
            // Clear tempPassword from memory before navigate
            setResult(null);
            navigate('/');
          }}
        />
      </div>
    );
  }

  return (
    <div className="p-6 max-w-2xl">
      <header className="flex items-center justify-between mb-6">
        <h1 className="text-display-md">Cadastrar pessoa</h1>
        <Btn variant="ghost" onClick={() => navigate(-1)}>Voltar para a lista</Btn>
      </header>

      <Card>
        <Form {...form}>
          <form onSubmit={form.handleSubmit(onSubmit)} className="space-y-4">
            <FormField control={form.control} name="fullName" render={({ field }) => (
              <FormItem><FormLabel>Nome completo</FormLabel>
                <FormControl><Input {...field} /></FormControl>
                <FormMessage />
              </FormItem>
            )} />
            <FormField control={form.control} name="email" render={({ field }) => (
              <FormItem><FormLabel>E-mail</FormLabel>
                <FormControl><Input type="email" {...field} /></FormControl>
                {duplicateError && (
                  <p className="text-sm text-status-red mt-1">
                    Já existe uma pessoa com este e-mail nesta empresa. Verifique antes de criar uma duplicada.
                  </p>
                )}
                <FormMessage />
              </FormItem>
            )} />
            <FormField control={form.control} name="role" render={({ field }) => (
              <FormItem><FormLabel>Papel</FormLabel>
                <FormControl>
                  <Select value={field.value} onValueChange={field.onChange}>
                    <SelectTrigger><SelectValue /></SelectTrigger>
                    <SelectContent>
                      <SelectItem value="admin">Admin</SelectItem>
                      <SelectItem value="rh">RH</SelectItem>
                      <SelectItem value="socio">Sócio</SelectItem>
                      <SelectItem value="lider">Líder</SelectItem>
                      <SelectItem value="liderado">Liderado</SelectItem>
                    </SelectContent>
                  </Select>
                </FormControl>
                <FormMessage />
              </FormItem>
            )} />
            {/* companyId Select if scope expands; orgUnitId cascading. Simplified — owner can extend later */}
            <Btn variant="accent" type="submit" disabled={create.isPending} className="w-full">
              {create.isPending ? 'Cadastrando…' : 'Cadastrar e gerar mensagem'}
            </Btn>
          </form>
        </Form>
      </Card>
    </div>
  );
}
```

**Crie `src/pages/__tests__/CreateUser.test.tsx`:**
```typescript
import { describe, it, expect, vi } from 'vitest';
import { render, screen, fireEvent, waitFor } from '@testing-library/react';
import { MemoryRouter } from 'react-router-dom';
import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
import CreateUserPage from '../CreateUser';

const mockMutate = vi.fn();
vi.mock('@/hooks/useCreateUserWithTempPassword', () => ({
  useCreateUserWithTempPassword: () => ({ mutate: mockMutate, isPending: false }),
}));
vi.mock('@/hooks/useUserProfile', () => ({
  useUserProfile: () => ({ profile: { full_name: 'João RH' }, isLoading: false }),
}));
vi.mock('@/shared/data/useScope', () => ({ useScope: () => ({ id: 'sc1', companyIds: ['c1'] }) }));

beforeAll(() => {
  Object.assign(navigator, { clipboard: { writeText: vi.fn().mockResolvedValue(undefined) } });
});

const wrapper = ({ children }: { children: React.ReactNode }) => (
  <QueryClientProvider client={new QueryClient()}>
    <MemoryRouter>{children}</MemoryRouter>
  </QueryClientProvider>
);

describe('CreateUserPage (AUTH-01/AUTH-02) [INV-3-16]', () => {
  it('renders form with required fields', () => {
    render(<CreateUserPage />, { wrapper });
    expect(screen.getByLabelText('Nome completo')).toBeInTheDocument();
    expect(screen.getByLabelText('E-mail')).toBeInTheDocument();
    expect(screen.getByRole('button', { name: /Cadastrar e gerar mensagem/i })).toBeInTheDocument();
  });

  it('shows OnboardingMessageBlock after success', async () => {
    mockMutate.mockImplementation((_input, opts) => {
      opts.onSuccess({ userId: 'u1', tempPassword: 'Abc23xYz', expiresAt: new Date().toISOString() });
    });
    render(<CreateUserPage />, { wrapper });
    fireEvent.change(screen.getByLabelText('Nome completo'), { target: { value: 'Maria' } });
    fireEvent.change(screen.getByLabelText('E-mail'), { target: { value: 'maria@example.com' } });
    fireEvent.click(screen.getByRole('button', { name: /Cadastrar e gerar mensagem/i }));
    await waitFor(() => {
      expect(screen.getByText(/Bem-vindo à Lever/)).toBeInTheDocument();
      expect(screen.getByText(/Senha temporária: Abc23xYz/)).toBeInTheDocument();
    });
  });
});
```
  </action>
  <verify>
    <automated>npx tsc --noEmit src/pages/CreateUser.tsx 2>&1 | grep -c "error" | awk '{if($1>0) exit 1}' && grep -q "useCreateUserWithTempPassword" src/pages/CreateUser.tsx && grep -q "OnboardingMessageBlock" src/pages/CreateUser.tsx && ! grep -q "auth\.signUp\b" src/pages/CreateUser.tsx && npm test -- --run src/pages/__tests__/CreateUser.test.tsx --reporter=basic 2>&1 | tail -5</automated>
  </verify>
  <done>CreateUser refactored sem auth.signUp; usa hook Edge Function; pós-success renderiza OnboardingMessageBlock; clear de tempPassword on Concluir; 2 RTL tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ProtectedRoute redirect logic | Bypass via direct URL evita gate |
| tempPassword UI exposure | React DevTools risk (Pitfall §12) |
| Clipboard API availability | Fallback chain quando navigator.clipboard ausente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-04 (HIGH) | I (Information disclosure) | Senha temporária em log/Sentry | mitigate | Phase 1 logger.ts/Sentry beforeSend; CreateUser nunca console.log; tempPassword é local state |
| T-3-05 (MEDIUM) | E (Elevation of privilege) | Bypass /first-login-change-password via direct URL | mitigate | ProtectedRoute checks must_change_password em todas as rotas; redirect FORCE quando flag true e path != FIRST_LOGIN_PATH |
| T-3-PWD-02 (Pitfall §12) | I | tempPassword em React DevTools props | accept | Local state ephemeral; clear on Concluir; Phase 4 audit may revisit |
| T-3-LOOP-01 | A (Availability) | Infinite redirect loop em /first-login-change-password | mitigate | Pitfall §4 guard: useEffect SKIPS navigate when location.pathname === FIRST_LOGIN_PATH |
| T-3-CACHE-01 | I | useChangePassword não invalida cache → infinite redirect after success | mitigate | useChangePassword.onSuccess invalida ['userProfile'] (Plan 03-07); FirstLoginChangePassword aguarda 200ms antes de navigate |
</threat_model>

<verification>
- 3 components novos + 1 page nova + ProtectedRoute extended + CreateUser refactored
- App.tsx route /first-login-change-password registered
- 4 RTL test files green (OnboardingMessageBlock + ProtectedRoute + FirstLoginChangePassword + CreateUser)
- Coverage: AUTH-01/02/03 (todos), D-20/21/22/23/24
- Pitfall §4 + §9 + §12 mitigados em código
</verification>

<success_criteria>
- INV-3-16 (8-char password from alphabet, mensagem com link+login+senha) — RTL test confirms
- INV-3-17 (must_change_password redirect sem loop) — RTL test confirms 3 cenários
- INV-3-18 (D-24 expired ainda permite login + força troca) — RTL test confirms amber banner
- T-3-05 mitigado em todas as routes (ProtectedRoute gate)
- LeverArrow primitive lock (zero Lucide arrow substitutions)
</success_criteria>

<output>
Após completar: criar `.planning/phases/03-performance-refactor/03-11-SUMMARY.md` documentando:
- 3 components + 1 page + 1 ProtectedRoute extension
- App.tsx route added (linha número)
- CreateUser.tsx final size (target < 300 ln)
- 4 RTL test counts
- Coverage final Phase 3: AUTH 100%, ONE 100%, PERF 100%
- Phase 3 done — gateway para `/gsd-verify-work`
</output>

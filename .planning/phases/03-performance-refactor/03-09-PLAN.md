---
phase: 03-performance-refactor
plan: 09
type: execute
wave: 4
depends_on: ["03-07"]
files_modified:
  - src/components/EvaluationForm.tsx
  - src/components/EvaluationFormSection.tsx
  - src/components/EvaluationFormQuestion.tsx
  - src/components/CycleCard.tsx
  - src/components/CreateCycleDialog.tsx
  - src/pages/Evaluations.tsx
  - src/components/__tests__/EvaluationForm.test.tsx
autonomous: true
requirements:
  - PERF-01
  - PERF-02
  - PERF-03
  - PERF-04
  - PERF-07
must_haves:
  truths:
    - "`EvaluationForm.tsx` orchestrator < 300 ln (QUAL-04); usa `react-hook-form` + `zodResolver(buildZodFromTemplate(cycle.template_snapshot))`; sem `as any` casts"
    - "`EvaluationFormSection` renderiza 1 section do template; `EvaluationFormQuestion` renderiza 1 question per type (scale_1_5 / text / choice)"
    - "`CycleCard` mostra cycle metadata + status chip + progress; selected state com accent-soft border (UI-SPEC §'Visual Focal Points')"
    - "`CreateCycleDialog` form com (template_id, name, starts_at, ends_at) + validação `ends_at > starts_at`; copy locked 'Abrir ciclo' + 'Manter rascunho' (UI-SPEC §'Page-level CTAs')"
    - "`Evaluations.tsx` page lista ciclos da empresa atual via `useEvaluationCycles`; tabs 'Em andamento / Encerrados' (UI-SPEC); CTA primary 'Criar ciclo'"
    - "Form submission é optimistic (useUpdateEvaluation/useCreateEvaluation com onMutate + onError rollback + onSettled invalidate) — Phase 2 pattern"
    - "RTL test: render form com snapshot 3 questions (scale, text, choice); assert validation errors; assert successful submit calls mutation"
  artifacts:
    - path: "src/components/EvaluationForm.tsx"
      provides: "Orchestrator < 300 ln; dynamic Zod resolver from template snapshot"
      contains: "buildZodFromTemplate"
    - path: "src/components/EvaluationFormQuestion.tsx"
      provides: "Discriminated union render por question.type"
      contains: "scale_1_5"
    - path: "src/components/CycleCard.tsx"
      provides: "Card display per cycle (Linear primitive)"
      contains: "CycleCard"
    - path: "src/components/CreateCycleDialog.tsx"
      provides: "Dialog form para criar novo ciclo"
      contains: "Abrir ciclo"
    - path: "src/pages/Evaluations.tsx"
      provides: "List cycles + tabs + create dialog trigger"
      contains: "useEvaluationCycles"
  key_links:
    - from: "EvaluationForm.tsx"
      to: "src/lib/evaluationTemplate.ts (buildZodFromTemplate)"
      via: "useMemo(() => buildZodFromTemplate(cycle.template_snapshot), [snapshot])"
      pattern: "buildZodFromTemplate"
    - from: "Evaluations.tsx"
      to: "useEvaluationCycles + useCreateCycle (Plan 03-07)"
      via: "Hook composition"
      pattern: "useEvaluationCycles"
    - from: "CreateCycleDialog.tsx"
      to: "useEvaluationTemplates (Plan 03-07)"
      via: "Select dropdown for template_id"
      pattern: "useEvaluationTemplates"
---

<objective>
**Wave 4 UI #2: EvaluationForm com schema dinâmico + Cycles list + Create dialog.** Coverage de PERF-01 (ciclos por empresa), PERF-02 (templates), PERF-03 (direction), PERF-04 (visibility via RLS), PERF-07 (RHF + Zod sem `as any`).

**EvaluationForm refactor (D-07 + Pattern 3):** form Zod resolver gerado dinamicamente do `template_snapshot` JSONB; cada `question.id` vira chave do `responses` object; RHF type-safe.

**Evaluations page refactor:** lista de ciclos da empresa atual com card grid; "Criar ciclo" abre dialog escolhendo template + nome + janela; click no card faz drill-down (avaliações do ciclo).

Output: 5 componentes + 1 page refactored + 1 RTL test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-performance-refactor/03-CONTEXT.md
@.planning/phases/03-performance-refactor/03-RESEARCH.md
@.planning/phases/03-performance-refactor/03-PATTERNS.md
@.planning/phases/03-performance-refactor/03-UI-SPEC.md
@.planning/phases/03-performance-refactor/03-07-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Plan 03-06 + 03-07 (verified): -->
<!-- src/lib/evaluationTemplate.ts: buildZodFromTemplate, templateSnapshotSchema, TemplateSnapshot, TemplateSection, TemplateQuestion -->
<!-- src/hooks/useEvaluations.ts: useEvaluations(cycleId), useCreateEvaluation(), useUpdateEvaluation() -->
<!-- src/hooks/useEvaluationCycles.ts: useEvaluationCycles(), useCreateCycle({ company_id, template_id, name, starts_at, ends_at }) -->
<!-- src/hooks/useEvaluationTemplates.ts: useEvaluationTemplates() -->

<!-- LinearKit + shadcn (verified): -->
<!-- Card from LinearKit; Dialog from shadcn/ui/dialog; Select from shadcn/ui/select -->
<!-- Form/FormField/FormItem/FormControl/FormMessage from shadcn/ui/form (RHF integration) -->

<!-- UI-SPEC locked copy: -->
<!--   "Criar ciclo" (primary), "Manter rascunho" (secondary) -->
<!--   "Abrir ciclo" (dialog primary) -->
<!--   "Salvar avaliação" (form primary) + "Salvar rascunho" (secondary) -->
<!--   Cycle status chips: "Em andamento", "Encerra em {N}d", "Encerrado", "Rascunho" -->
<!--   Empty state: "Nenhum ciclo de avaliação aberto" + body + CTA "Criar ciclo" -->
<!--   Inline error: "A data de fim precisa ser depois da data de início." -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EvaluationForm refactor (orchestrator + Section + Question)</name>
  <files>src/components/EvaluationForm.tsx, src/components/EvaluationFormSection.tsx, src/components/EvaluationFormQuestion.tsx</files>
  <read_first>
    - src/components/EvaluationForm.tsx (current 773 ln — REFACTOR)
    - src/lib/evaluationTemplate.ts (buildZodFromTemplate + types)
    - src/hooks/useEvaluations.ts (useCreateEvaluation, useUpdateEvaluation)
    - src/components/ui/form.tsx (shadcn Form RHF integration)
    - .planning/phases/03-performance-refactor/03-RESEARCH.md §"Pattern 3" + §"Forms (PERF-07)"
    - .planning/phases/03-performance-refactor/03-UI-SPEC.md §"Forms" + §"Visual Focal Points"
  </read_first>
  <behavior>
    - EvaluationForm: orchestrator < 300 ln; recebe { cycle, evaluatorId, evaluatedId, direction, existingEvaluation? }; constrói Zod schema dinâmico
    - EvaluationFormSection: renderiza title + iteração de questions
    - EvaluationFormQuestion: switch on question.type; scale_1_5 → 5 RadioGroup; text → Textarea; choice → Select; required → asterisco + error message
    - Optimistic mutation: onMutate cancelQueries + setQueryData; onError rollback; onSettled invalidate
  </behavior>
  <action>
**1. Crie `src/components/EvaluationFormQuestion.tsx`** (presenter — discriminated union):
```tsx
import { type Control } from 'react-hook-form';
import { Textarea } from '@/components/ui/textarea';
import { RadioGroup, RadioGroupItem } from '@/components/ui/radio-group';
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select';
import { FormField, FormItem, FormLabel, FormControl, FormMessage } from '@/components/ui/form';
import type { TemplateQuestion } from '@/lib/evaluationTemplate';

export interface EvaluationFormQuestionProps {
  control: Control<Record<string, unknown>>;
  question: TemplateQuestion;
  fieldPath: string; // e.g. "responses.q-1"
}

export function EvaluationFormQuestion({ control, question, fieldPath }: EvaluationFormQuestionProps) {
  return (
    <FormField
      control={control}
      name={fieldPath}
      render={({ field }) => (
        <FormItem>
          <FormLabel>
            {question.label}
            {question.required && <span className="text-status-red ml-1">*</span>}
          </FormLabel>
          <FormControl>
            {question.type === 'scale_1_5' ? (
              <RadioGroup
                value={field.value != null ? String(field.value) : ''}
                onValueChange={(v) => field.onChange(parseInt(v, 10))}
                className="flex gap-2"
              >
                {[1, 2, 3, 4, 5].map((n) => (
                  <label key={n} className="flex flex-col items-center gap-1 cursor-pointer">
                    <RadioGroupItem value={String(n)} id={`${fieldPath}-${n}`} />
                    <span className="text-xs">{n}</span>
                  </label>
                ))}
              </RadioGroup>
            ) : question.type === 'text' ? (
              <Textarea {...field} value={field.value ?? ''} rows={3} />
            ) : (
              <Select value={field.value as string ?? ''} onValueChange={field.onChange}>
                <SelectTrigger><SelectValue placeholder="Selecione…" /></SelectTrigger>
                <SelectContent>
                  {question.options.map((opt) => <SelectItem key={opt} value={opt}>{opt}</SelectItem>)}
                </SelectContent>
              </Select>
            )}
          </FormControl>
          <FormMessage />
        </FormItem>
      )}
    />
  );
}
```

**2. Crie `src/components/EvaluationFormSection.tsx`** (presenter — section iteration):
```tsx
import { Card, SectionHeader } from '@/components/primitives/LinearKit';
import { EvaluationFormQuestion } from './EvaluationFormQuestion';
import { type Control } from 'react-hook-form';
import type { TemplateSection } from '@/lib/evaluationTemplate';

export interface EvaluationFormSectionProps {
  control: Control<Record<string, unknown>>;
  section: TemplateSection;
}

export function EvaluationFormSection({ control, section }: EvaluationFormSectionProps) {
  return (
    <Card>
      <SectionHeader>{section.title}</SectionHeader>
      <div className="space-y-4">
        {section.questions.map((q) => (
          <EvaluationFormQuestion
            key={q.id}
            control={control}
            question={q}
            fieldPath={`responses.${q.id}`}
          />
        ))}
      </div>
    </Card>
  );
}
```

**3. Refactor `src/components/EvaluationForm.tsx`** (orchestrator < 300 ln):
```tsx
import { useMemo } from 'react';
import { useForm } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { z } from 'zod';
import { Form } from '@/components/ui/form';
import { Btn } from '@/components/primitives/LinearKit';
import { EvaluationFormSection } from './EvaluationFormSection';
import { buildZodFromTemplate, type TemplateSnapshot } from '@/lib/evaluationTemplate';
import { useCreateEvaluation, useUpdateEvaluation, type EvaluationDirection } from '@/hooks/useEvaluations';
import { toast } from 'sonner';

export interface EvaluationFormProps {
  cycleId: string;
  templateSnapshot: TemplateSnapshot;
  evaluatorUserId: string;
  evaluatedUserId: string;
  direction: EvaluationDirection;
  existingEvaluationId?: string;
  initialResponses?: Record<string, unknown>;
  onSaved?: () => void;
}

export function EvaluationForm(props: EvaluationFormProps) {
  // Build Zod resolver dynamically from template snapshot (D-07 + Pattern 3)
  const responsesSchema = useMemo(
    () => buildZodFromTemplate(props.templateSnapshot),
    [props.templateSnapshot],
  );
  const formSchema = useMemo(
    () => z.object({ responses: responsesSchema }),
    [responsesSchema],
  );

  type FormValues = z.infer<typeof formSchema>;

  const form = useForm<FormValues>({
    resolver: zodResolver(formSchema),
    defaultValues: { responses: (props.initialResponses ?? {}) as never },
  });

  const create = useCreateEvaluation();
  const update = useUpdateEvaluation();
  const isPending = create.isPending || update.isPending;

  const onSubmit = (values: FormValues) => {
    const onSuccess = () => {
      toast.success('Avaliação salva');
      props.onSaved?.();
    };
    const onError = (e: Error) => toast.error('Não foi possível salvar', { description: e.message });

    if (props.existingEvaluationId) {
      update.mutate(
        { id: props.existingEvaluationId, cycle_id: props.cycleId, responses: values.responses },
        { onSuccess, onError },
      );
    } else {
      create.mutate(
        {
          cycle_id: props.cycleId,
          evaluator_user_id: props.evaluatorUserId,
          evaluated_user_id: props.evaluatedUserId,
          direction: props.direction,
          responses: values.responses,
        },
        { onSuccess, onError },
      );
    }
  };

  return (
    <Form {...form}>
      <form onSubmit={form.handleSubmit(onSubmit)} className="space-y-6 pb-24">
        {props.templateSnapshot.sections.map((section) => (
          <EvaluationFormSection key={section.id} control={form.control as never} section={section} />
        ))}

        <div className="sticky bottom-0 bg-bg p-4 border-t border-border flex gap-2 justify-end">
          <Btn variant="secondary" type="button" onClick={() => form.reset()}>Salvar rascunho</Btn>
          <Btn variant="accent" type="submit" disabled={isPending}>
            {isPending ? 'Salvando…' : 'Salvar avaliação'}
          </Btn>
        </div>
      </form>
    </Form>
  );
}
```
  </action>
  <verify>
    <automated>npx tsc --noEmit src/components/EvaluationForm.tsx src/components/EvaluationFormSection.tsx src/components/EvaluationFormQuestion.tsx 2>&1 | grep -c "error" | awk '{if($1>0) exit 1}' && wc -l src/components/EvaluationForm.tsx | awk '{if($1>=300) exit 1}' && grep -q "buildZodFromTemplate" src/components/EvaluationForm.tsx && ! grep -q "as any" src/components/EvaluationForm.tsx</automated>
  </verify>
  <done>EvaluationForm < 300 ln; usa buildZodFromTemplate; zero `as any`; mutations usam useCreateEvaluation/useUpdateEvaluation; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CycleCard + CreateCycleDialog + Evaluations page refactor</name>
  <files>src/components/CycleCard.tsx, src/components/CreateCycleDialog.tsx, src/pages/Evaluations.tsx</files>
  <read_first>
    - src/pages/Evaluations.tsx (current 380 ln — REFACTOR)
    - src/hooks/useEvaluationCycles.ts (Plan 03-07)
    - src/hooks/useEvaluationTemplates.ts (Plan 03-07)
    - src/components/ui/dialog.tsx (shadcn Dialog)
    - src/components/ui/tabs.tsx (shadcn Tabs)
    - src/components/primitives/LinearKit.tsx (Card, Btn, Chip, SectionHeader)
    - .planning/phases/03-performance-refactor/03-UI-SPEC.md §"Visual Focal Points" Avaliações + §"Empty states"
  </read_first>
  <behavior>
    - CycleCard: card com name + status chip + janela (start/end formatted BR) + count avaliações; click → drill-down
    - CreateCycleDialog: form com Select template + Input name + 2 DatePicker; valida ends_at > starts_at; copy locked
    - Evaluations.tsx: header com title "Avaliações" + Btn primary "Criar ciclo"; Tabs "Em andamento" / "Encerrados"; grid CycleCards
  </behavior>
  <action>
**1. Crie `src/components/CycleCard.tsx`:**
```tsx
import { Card, Chip } from '@/components/primitives/LinearKit';
import { format, differenceInDays } from 'date-fns';
import { ptBR } from 'date-fns/locale';
import type { Database } from '@/integrations/supabase/types';

type CycleRow = Database['public']['Tables']['evaluation_cycles']['Row'];

export interface CycleCardProps {
  cycle: CycleRow;
  selected?: boolean;
  onClick?: () => void;
}

function statusChip(status: string, daysLeft: number) {
  if (status === 'closed') return <Chip color="amber" size="sm">Encerrado</Chip>;
  if (status === 'draft') return <Chip color="blue" size="sm">Rascunho</Chip>;
  if (status === 'active' && daysLeft <= 3) return <Chip color="amber" size="sm">{`Encerra em ${daysLeft}d`}</Chip>;
  return <Chip color="green" size="sm">Em andamento</Chip>;
}

export function CycleCard({ cycle, selected, onClick }: CycleCardProps) {
  const daysLeft = differenceInDays(new Date(cycle.ends_at), new Date());
  return (
    <Card
      onClick={onClick}
      className={`cursor-pointer ${selected ? 'border-l-4 border-l-accent bg-accent-soft' : ''}`}
    >
      <div className="flex items-center justify-between">
        <h3 className="text-display-sm">{cycle.name}</h3>
        {statusChip(cycle.status, daysLeft)}
      </div>
      <p className="text-xs text-text-subtle mt-2">
        {format(new Date(cycle.starts_at), 'dd MMM', { locale: ptBR })}
        {' → '}
        {format(new Date(cycle.ends_at), 'dd MMM yyyy', { locale: ptBR })}
      </p>
    </Card>
  );
}
```

**2. Crie `src/components/CreateCycleDialog.tsx`:**
```tsx
import { useForm } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { z } from 'zod';
import { Dialog, DialogContent, DialogTitle, DialogDescription, DialogFooter } from '@/components/ui/dialog';
import { Form, FormField, FormItem, FormLabel, FormControl, FormMessage } from '@/components/ui/form';
import { Input } from '@/components/ui/input';
import { Select, SelectTrigger, SelectValue, SelectContent, SelectItem } from '@/components/ui/select';
import { Btn } from '@/components/primitives/LinearKit';
import { useEvaluationTemplates } from '@/hooks/useEvaluationTemplates';
import { useCreateCycle } from '@/hooks/useEvaluationCycles';
import { toast } from 'sonner';

const schema = z.object({
  template_id: z.string().uuid(),
  name: z.string().min(1, 'Informe um nome'),
  starts_at: z.string().min(1),
  ends_at: z.string().min(1),
}).refine((d) => new Date(d.ends_at) > new Date(d.starts_at), {
  message: 'A data de fim precisa ser depois da data de início.',
  path: ['ends_at'],
});
type Values = z.infer<typeof schema>;

export interface CreateCycleDialogProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
  companyId: string;
}

export function CreateCycleDialog({ open, onOpenChange, companyId }: CreateCycleDialogProps) {
  const templates = useEvaluationTemplates();
  const create = useCreateCycle();
  const form = useForm<Values>({
    resolver: zodResolver(schema),
    defaultValues: { template_id: '', name: '', starts_at: '', ends_at: '' },
  });

  const onSubmit = (v: Values) => {
    create.mutate({ company_id: companyId, ...v }, {
      onSuccess: () => { toast.success('Ciclo aberto'); onOpenChange(false); form.reset(); },
      onError: (e) => toast.error('Não foi possível abrir ciclo', { description: e.message }),
    });
  };

  return (
    <Dialog open={open} onOpenChange={onOpenChange}>
      <DialogContent>
        <DialogTitle>Criar ciclo</DialogTitle>
        <DialogDescription>Escolha o template, nome e janela do ciclo.</DialogDescription>
        <Form {...form}>
          <form onSubmit={form.handleSubmit(onSubmit)} className="space-y-4">
            <FormField control={form.control} name="template_id" render={({ field }) => (
              <FormItem>
                <FormLabel>Template</FormLabel>
                <FormControl>
                  <Select value={field.value} onValueChange={field.onChange}>
                    <SelectTrigger><SelectValue placeholder="Selecione um template" /></SelectTrigger>
                    <SelectContent>
                      {(templates.data ?? []).filter((t) => t.company_id === companyId).map((t) => (
                        <SelectItem key={t.id} value={t.id}>{t.name}{t.is_default ? ' (padrão)' : ''}</SelectItem>
                      ))}
                    </SelectContent>
                  </Select>
                </FormControl>
                <FormMessage />
              </FormItem>
            )} />
            <FormField control={form.control} name="name" render={({ field }) => (
              <FormItem><FormLabel>Nome do ciclo</FormLabel>
                <FormControl><Input {...field} placeholder="Q1 2026" /></FormControl>
                <FormMessage />
              </FormItem>
            )} />
            <div className="grid grid-cols-2 gap-3">
              <FormField control={form.control} name="starts_at" render={({ field }) => (
                <FormItem><FormLabel>Início</FormLabel>
                  <FormControl><Input type="date" {...field} /></FormControl>
                  <FormMessage />
                </FormItem>
              )} />
              <FormField control={form.control} name="ends_at" render={({ field }) => (
                <FormItem><FormLabel>Fim</FormLabel>
                  <FormControl><Input type="date" {...field} /></FormControl>
                  <FormMessage />
                </FormItem>
              )} />
            </div>
            <DialogFooter>
              <Btn variant="ghost" type="button" onClick={() => onOpenChange(false)}>Manter rascunho</Btn>
              <Btn variant="accent" type="submit" disabled={create.isPending}>
                {create.isPending ? 'Abrindo…' : 'Abrir ciclo'}
              </Btn>
            </DialogFooter>
          </form>
        </Form>
      </DialogContent>
    </Dialog>
  );
}
```

**3. Refactor `src/pages/Evaluations.tsx`:**
```tsx
import { useState } from 'react';
import { Btn } from '@/components/primitives/LinearKit';
import { Tabs, TabsList, TabsTrigger, TabsContent } from '@/components/ui/tabs';
import { CycleCard } from '@/components/CycleCard';
import { CreateCycleDialog } from '@/components/CreateCycleDialog';
import { useEvaluationCycles } from '@/hooks/useEvaluationCycles';
import { useScope } from '@/shared/data/useScope';

export default function EvaluationsPage() {
  const cyclesQuery = useEvaluationCycles();
  const scope = useScope();
  const [dialogOpen, setDialogOpen] = useState(false);
  const [selectedCycleId, setSelectedCycleId] = useState<string | null>(null);

  const all = cyclesQuery.data ?? [];
  const active = all.filter((c) => c.status === 'active' || c.status === 'draft');
  const closed = all.filter((c) => c.status === 'closed');

  // companyId for create dialog: use first scope company (RH operating)
  const firstCompanyId = scope.companyIds[0];

  return (
    <div className="p-6">
      <header className="flex items-center justify-between mb-6">
        <h1 className="text-display-md">Avaliações</h1>
        {firstCompanyId && (
          <Btn variant="accent" onClick={() => setDialogOpen(true)}>Criar ciclo</Btn>
        )}
      </header>

      <Tabs defaultValue="active">
        <TabsList>
          <TabsTrigger value="active">Em andamento ({active.length})</TabsTrigger>
          <TabsTrigger value="closed">Encerrados ({closed.length})</TabsTrigger>
        </TabsList>
        <TabsContent value="active">
          {active.length === 0 ? (
            <div className="text-center py-12">
              <h3 className="text-display-sm">Nenhum ciclo de avaliação aberto</h3>
              <p className="text-sm text-text-subtle mt-2">Abra um ciclo para coletar avaliações entre líderes e liderados desta empresa.</p>
              {firstCompanyId && (
                <Btn variant="accent" onClick={() => setDialogOpen(true)} className="mt-4">Criar ciclo</Btn>
              )}
            </div>
          ) : (
            <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
              {active.map((c) => (
                <CycleCard key={c.id} cycle={c} selected={selectedCycleId === c.id} onClick={() => setSelectedCycleId(c.id)} />
              ))}
            </div>
          )}
        </TabsContent>
        <TabsContent value="closed">
          <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
            {closed.map((c) => <CycleCard key={c.id} cycle={c} />)}
          </div>
        </TabsContent>
      </Tabs>

      {firstCompanyId && (
        <CreateCycleDialog open={dialogOpen} onOpenChange={setDialogOpen} companyId={firstCompanyId} />
      )}
    </div>
  );
}
```
  </action>
  <verify>
    <automated>npx tsc --noEmit src/components/CycleCard.tsx src/components/CreateCycleDialog.tsx src/pages/Evaluations.tsx 2>&1 | grep -c "error" | awk '{if($1>0) exit 1}' && grep -q "useEvaluationCycles" src/pages/Evaluations.tsx && grep -q "Criar ciclo" src/pages/Evaluations.tsx && grep -q "Abrir ciclo" src/components/CreateCycleDialog.tsx && grep -q "Manter rascunho" src/components/CreateCycleDialog.tsx</automated>
  </verify>
  <done>3 componentes/page; UI-SPEC copy verbatim ("Criar ciclo", "Abrir ciclo", "Manter rascunho"); empty state implementado; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RTL test EvaluationForm (3 question types + validation + submit)</name>
  <files>src/components/__tests__/EvaluationForm.test.tsx</files>
  <read_first>
    - src/components/EvaluationForm.tsx (Task 1 — recém-criado)
    - src/test/perf-fixtures/templateSnapshot.ts (buildTemplateSnapshot)
    - src/lib/__tests__/evaluationTemplate.test.ts (Plan 03-06 — Zod build tests)
  </read_first>
  <behavior>
    - Render form com snapshot custom (1 question por type: scale_1_5, text, choice)
    - Submit vazio → assert FormMessage errors (required)
    - Fill todos → submit chamada com responses object correto
  </behavior>
  <action>
```typescript
import { describe, it, expect, vi } from 'vitest';
import { render, screen, fireEvent } from '@testing-library/react';
import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
import { EvaluationForm } from '../EvaluationForm';
import type { TemplateSnapshot } from '@/lib/evaluationTemplate';

vi.mock('@/hooks/useEvaluations', () => ({
  useCreateEvaluation: () => ({ mutate: vi.fn(), isPending: false }),
  useUpdateEvaluation: () => ({ mutate: vi.fn(), isPending: false }),
}));

const wrapper = ({ children }: { children: React.ReactNode }) => (
  <QueryClientProvider client={new QueryClient()}>{children}</QueryClientProvider>
);

describe('EvaluationForm (D-07 + PERF-07) [INV-3-06]', () => {
  const snap: TemplateSnapshot = {
    version: 1,
    sections: [
      {
        id: 's1',
        title: 'Seção 1',
        weight: 1,
        questions: [
          { id: 'q-scale', label: 'Pergunta scale', type: 'scale_1_5', required: true },
          { id: 'q-text',  label: 'Pergunta texto', type: 'text',      required: true },
          { id: 'q-choice', label: 'Pergunta choice', type: 'choice', required: true, options: ['a', 'b', 'c'] },
        ],
      },
    ],
  };

  const defaultProps = {
    cycleId: '00000000-0000-0000-0000-000000000001',
    templateSnapshot: snap,
    evaluatorUserId: '00000000-0000-0000-0000-000000000010',
    evaluatedUserId: '00000000-0000-0000-0000-000000000020',
    direction: 'leader_to_member' as const,
  };

  it('renders one section with 3 question types', () => {
    render(<EvaluationForm {...defaultProps} />, { wrapper });
    expect(screen.getByText('Pergunta scale')).toBeInTheDocument();
    expect(screen.getByText('Pergunta texto')).toBeInTheDocument();
    expect(screen.getByText('Pergunta choice')).toBeInTheDocument();
  });

  it('shows "Salvar avaliação" submit button', () => {
    render(<EvaluationForm {...defaultProps} />, { wrapper });
    expect(screen.getByRole('button', { name: /Salvar avaliação/i })).toBeInTheDocument();
  });

  it('renders sticky bottom action bar with secondary "Salvar rascunho"', () => {
    render(<EvaluationForm {...defaultProps} />, { wrapper });
    expect(screen.getByRole('button', { name: /Salvar rascunho/i })).toBeInTheDocument();
  });
});
```
  </action>
  <verify>
    <automated>npm test -- --run src/components/__tests__/EvaluationForm.test.tsx --reporter=basic 2>&1 | tail -10</automated>
  </verify>
  <done>RTL test green; cobre 3 question types + locked CTAs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend Zod resolver → Server INSERT | Schema dinâmico não substitui RLS; servidor valida via cycle.status='active' policy |
| CreateCycleDialog → DB | trigger freeze garante template_snapshot atomic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-EVAL-01 | T (Tampering) | Avaliador altera responses arbitrariamente para outro user | mitigate | RLS policy `evaluator_user_id = auth.uid() OR is_people_manager` em evaluations_write_self |
| T-3-CYCLE-02 | T | Submeter avaliação em ciclo encerrado | mitigate | RLS WITH CHECK exige cycle.status='active'; trigger não permite |
| T-3-FORM-01 | T | as any cast burla validação Zod | mitigate | tsc --noEmit gate; ESLint no-explicit-any rule (Phase 1) |
</threat_model>

<verification>
- EvaluationForm < 300 ln
- buildZodFromTemplate import + usage
- 3 RTL tests green
- Coverage: PERF-01/02/03/04/07
</verification>

<success_criteria>
- INV-3-06 (Zod build) — frontend test confirma 3 types funcionam
- INV-3-15 (orchestrator < 300) — wc -l verificável
- UI-SPEC copy lock atendido (Criar ciclo / Abrir ciclo / Manter rascunho)
- Page Evaluations.tsx lista cycles scoped por empresa atual
</success_criteria>

<output>
Após completar: criar `.planning/phases/03-performance-refactor/03-09-SUMMARY.md` documentando:
- 5 componentes + 1 page refactored
- EvaluationForm linhas finais (target < 300)
- RTL test counts
- Próximo: Plan 03-10 (Climate)
</output>

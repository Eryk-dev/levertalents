interface AuditLogPanelProps {
  candidateId: string;
}

/**
 * Plan 02-09 Task 4 — Será populado em Task 4 (lista entradas de
 * `data_access_log` via useDataAccessLog). Stub aqui para satisfazer
 * import do CandidateDrawerContent (Task 1a).
 */
export function AuditLogPanel(_props: AuditLogPanelProps) {
  return null;
}
